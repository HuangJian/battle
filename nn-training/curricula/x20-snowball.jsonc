// 课程 x20-snowball —— 20 敌 1 命的**中盘激励腿**（x20-rebirth 的后继腿）
//
// 状态：**就绪，待用户手工开训**（it0 探针待跑；bc 已填实；剂量见 §2）。
//   agent 不得启动训练。
//
// 立项（2026-09-19，用户假说驱动）：x20 池外结算显示通关局拾取率 1.22/千tick
//   vs 死亡局 0.64、早期死亡局 0.20（`tmp/x20-settle/pool-it30-it96.jsonl`，存活
//   时间归一化后仍差 2 倍）—— 赢家玩的是"拾取滚雪球"，输家开局几乎不捡
//   （0.12 个/局），55% 的对局止步 5 杀前、中位死亡 tick 1229。凶手四类坦克
//   全有（power 289 / basic 241 / armor 167 / fast 135），无单一克星 ⇒ 全面
//   中盘续航赤字。线性 wKill（第 2 杀与第 19 杀同价）在中盘（5–12 杀）形成
//   回报洼地 ⇒ 本腿 thesis（单变量）：**里程碑 bonus 把梯度拉进中盘**。
//   残局技术现成（it30 池外 60 局通关），瓶颈是到达中盘，不是打完残局。
//
// 刻意**不动**的（证据或用户指令，见 §7）：
//   · PPO 位移包/schedule/batch/gamma 全与 x20 逐字相同（对照）。peak it30
//     恰好落在 it25 降温**之后** ⇒ "降温冻住进展"证伪，不 re-warm。
//     plateau 65 轮在 ±0.5 量程噪声之上清晰可见 ⇒ batch 不是瓶颈，不加量。
//   · 网络容量不动（用户指令）。value-loss 不可观测（remote PPO 只回 kl/
//     entropy）⇒ gamma/lam 不盲调；value 观测性列为 infra 缺口（§7跟进项）。
//   · c06 回测门删除（用户指令：不考虑兼容 c06）。c07 回测门保留。
//   · 中盘开局（god 前缀）**暂缓**：诊断指向开局拾取缺口（早期死亡局 0.12
//     个/局），prefix 跳过开局 = 药下错地方（学生永远学不到"如何到达中盘"）。
//     fallback ① 改为 powered-opening 变体（现成 --player-level，零引擎改动）
//     或 wPickup 剂量腿；god-prefix 仅当"到达后转化率"被证实为瓶颈时再议。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（与 x20 逐字相同：ladder-c20-lives1，4 变体，20 敌，12900 tick，lives 1，hard）
// ══════════════════════════════════════════════════════════════════════════
//   4 关 ID 2000-2003（abcd/bcda/cdab/dabc 轮转，成分恒 5/5/5/5）；
//   max_ticks 12900；seed_rotate 150（600/4）；per-stage 6000 samples。
//   任务侧 = §0.2 的 1 命定义（D11：命数 tier 未变 ⇒ 不触发立案；reward 语义
//   变化 ⇒ 按 §15.5 新实验：fresh out/traj + DECISIONS 条目，见 §7）。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 起点与配方
// ══════════════════════════════════════════════════════════════════════════
// bc = x20-rebirth.it30（池外段三选一胜者：7.5%/6.10杀；it96 互不可区分落选；
//   c06 回测超限与本腿无关 —— 用户已豁免 c06）。
//   `nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json`
//   （379KB、`nn-weights-json`）。`kickstart_ref = true` + `warmup_iters = 0`。
//
// 奖励 = x20 dense-only 七项（数值逐字沿用）＋ 里程碑三项：
//   `+ wMS8*where(kills>=8, 1, 0) + wMS12*where(kills>=12, 1, 0) + wMS16*where(kills>=16, 1, 0)`
//   语义：Φ 行求值 + diff 派生 ⇒ 台阶 EDGE 触发，**各付一次**（与
//   `wWin*where(clearTick>=0,1,0)` 同构，ladder_factory 先例）。
//   剂量 wMS8/wMS12/wMS16 = **6.0/9.0/12.0**（合计 27 ≈ 9 个击杀 ≈ 中盘 5→12 杀
//   的线性价值翻倍以上；相对通关总击杀价值 60 为 +45% 上限）。
//   EV 排序论证：坐（sit）到 8 杀概率为 0 ⇒ 坐 EV 不动；走/莽谁先到达谁拿，
//   margin 向"到达者"倾斜正是本意 ⇒ 走 > 莽 > 坐 排序不变（x20 §2 三档逻辑延续）。
//   公式总长 <1024 字符 ✓；`where` + `>=` 有 ladder_factory/c6-bonus 先例 ✓。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 探针（待跑，开训前置）
// ══════════════════════════════════════════════════════════════════════════
// 口径 R（rollout 采样，4 关各 10 局 seed 0–9，共 40 局）：重算 est（x20 取 147；
//   里程碑可能拉长对局，trailing 5 轮纠偏）＋ EV 三档重验（wMS 项只加不减，到达
//   概率走>莽>坐 ⇒ 排序不变是预测，实测为准；若翻转 ⇒ 降剂量，不改腿定义）。
// 口径 G（贪心 4 关各 50 局 seed0 860001，共 200 局）：里程碑分母锚点。
//   预测分母 ≈ it30 锚点（4.53杀/4.8% 级别；x20 it0 是 3.63/2.0%）：bc 换成 it30
//   ＋配方加 bonus ⇒ 起步应高于 x20 it0。若 G 分母 kills <5.0 ⇒ 先查 bc 载入（M3）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 判据（预注册；主读数 kills/局，门 pass —— 量程纪律延续 x20 §4）
// ══════════════════════════════════════════════════════════════════════════
// **主读数 = kills/局**（贪心）：分母 = it30 池外 **6.10** ⇒ 里程碑 **8 → 12 → 16 → 20**
//   （第一档 8 = +31%，中盘突破线；20 = 通关线，机械恒等）。
// **门 = pass**（pooled）：分母 7.5% ⇒ 升级门 **≥30%**（与 x20 同线，不移动球门）。
// **约束**：① dmg/局（分母池外 246.9）：+30%（=321）判换血漂移；
//   ② ticks：通关局均值参考（x20 it0 1461，长局方差大，不判死）；
//   ③ 超时熔断：`timeout_frac > 5%` ⇒ 立即停（dense-only 反悔键延续；x20 全程 0%）；
//   ④ 掉队（分关，相对 it30 池外同关）：
//      abcd 5.0%/5.97 ｜ bcda 8.0%/6.29 ｜ cdab 10.0%/6.25 ｜ dabc 7.0%/5.87；
//      任一关 pass 退步 >5pp 即掉队（kills 口径 ±0.5 内不算退步）。
// 判线：**达成** = pass ≥30% 且 kills ≥14；**部分达成** = kills 到 8+ 但 pass 未过门；
//   **判负** = it40 主检点未过（见 §5）或超时熔断触发。
// **最优归档保护 + M2**：每 eval 点记 kills，最优另存；终点经池外段三选一
//  （kills 最优 / pass 最优 / 末 it；互不可区分取 kills 高者）。
// **★ 回测门**：终点候选在 c07 段（seed0 410000，4 关各 200）回测不退步
//   （pass ≤5pp / dmg ≤+10%，配对段；c06 回测按用户指令删除）。
//   超限 ⇒ 不得为终点（顺延）；全超限 ⇒ 判语加"旧能力丢失"。
// **统计口径（写死）**：
//   · 判决只在**池外固定段**：`--seed0 413000`（**800 局 = 4 关各 200**，
//     seeds 413000–413199；2026-09-19 查 413000–413999 全空；412000 是 x20 结算段，禁复用）。
//     **seed 台账增补**：| 413000–413999 | x20-snowball 判决段（预注册） |
//     **中途不得改段**（L3）。
//   · 配套锚点 200 局同种子配对（seed0 860001）；pd 灰度带不变。
//   · **日常 200 局只做趋势，绝不判决**；日常第一读数 kills。
// **对照**：**it0 vs 终点**。god 参照：15.0% / kills 7.55（x20 §2，4 变体 pooled）。
// **诊断项**：命中率、killerKinds＋exposure（power 是否仍居首）、firstHitKind、
//   pu/stars（**滚雪球读数**：pu/千tick 是否向通关组 1.22 靠拢）、timeout_frac。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 止损
// ══════════════════════════════════════════════════════════════════════════
//   · **it20 只记录读数，不判决**（x20 §5 惯例延续）。
//   · **主检点 it40**：贪心 kills 相对分母（6.10）增幅 <+15%（即 <7.0）⇒ 停腿，
//     判「里程碑 bonus 在 20 敌 1 命下 belie 不足」→ 按 §7 开 prefix 腿（分支），不加轮数。
//   · **超时熔断**：`timeout_frac > 5%` ⇒ 立即停（bonus 诱发苟 ⇒ 命题死，加回重开）。
//   · **换血熔断**：dmg↑30%+ 且 kills 不升 ⇒ 学错了，改奖励结构，不加轮数。
//   · **机械横盘**：kills 连续 4 个 eval 点无改善（|Δ|<10%）⇒ 停腿；宽限一次 +10 轮。
//   · **分支四（自锁）**：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3）。
//   · ⚠ **优先级：超时熔断 / it40 主检点 > 横盘 > 一切诊断**。
//   · pre-registered fallback（判负时按序，不临场发明）：① powered-opening
//     变体（--player-level 起步，测"带电开局能否滚雪球"）或 wPickup 剂量腿；
//     ② batch 24000→48000（降噪＋中盘暴露翻倍）；③ PPO re-warm（证据弱，最后）。
//     god-prefix 中盘开局已否决（§7：跳过开局不对症），重提需新证据。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 预算与采样规模
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（与 x20 逐字相同；4 关均分 6000/关）。
// · `est_samples_per_game` = **147**（x20 真值沿用；里程碑拉长对局的方向由
//   trailing 5 轮纠偏；首轮 ≈163 局 ≈40 局/关）。
// · `seed_rotate` = **150**；`iters 0`（无限轮，由用户拍板，M1）＋ `max_hours 12`。
// · 单 run（L4）。`workers 8` / `ent_break 0.25` 全链一致。
// · **位移包不动**（`lr 0.0005 / epochs 6 / kl 0.02→0.01 / kl_cap 0.2 双段 / until 25`，
//   与 x20 §6 逐字相同 —— peak-it30-在-降温后 ⇒ 不 re-warm 的证据，见 §7）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 已知局限 + 程序事项
// ══════════════════════════════════════════════════════════════════════════
// 1. reward 语义变化 ⇒ §15.5 新实验：fresh out/traj（`tmp/x20-snowball/`）＋
//    DECISIONS 条目（§2026-09-19-x20-snowball）。命数 tier 未变 ⇒ D11 不触发。
//    `ladder-c20` / x20 毕业账不混入（x20 无合格终点，更无账可混）。
// 2. 单变量纪律：本腿唯一训练变量 = 里程碑 bonus；schedule/batch/gamma/容量/
//    开局分布全是控制。fallback 另开腿，不在本腿加变量。
// 3. 量程纪律延续 x20 §7.3：低技能段看 kills；M2 不变。
// 4. god 不是天花板（§0.2）：god kills 7.55 只是参照，里程碑 8 的第一目标就是超教师。
// 5. 结算必填（对照 §3 逐格填）：池外 kills/pass/分关 ＋ 超时 ＋ 换血双轨 ＋
//    c07 回测 ＋ killerKinds/命中率/pu 率 ＋ 横盘 ＋ M2 三选一。

{
  "version": 5,
  "name": "x20-snowball",
  "mode": "per-tick",
  "level": "ladder-c20-lives1",

  // ── 奖励：x20 dense-only 七项逐字沿用 ＋ 里程碑三项（EDGE 一次性，见 §1）───
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist) + wMS8*where(kills>=8, 1, 0) + wMS12*where(kills>=12, 1, 0) + wMS16*where(kills>=16, 1, 0)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      "wApproach": 0.008,
      "wMS8": 6.0,
      "wMS12": 9.0,
      "wMS16": 12.0
    },
    "terminal": {
    },
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点：x20 池外三选一胜者 it30 ────────────────────────────────────
  "bc": "nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §5；iters 由用户拍板，默认 0）────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：与 x20 逐字相同（对照，不 re-warm）─────────────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 }
  ],

  // ── 采集：总量 24000/轮；4 关均分 ─────────────────────────────────────
  "target_transitions": 24000,
  "est_samples_per_game": 147,
  "seed_rotate": 150,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x20-snowball/weights.json",
  "traj": "tmp/x20-snowball",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x20-snowball",
  "backup_prefix": "x20-snowball",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 4 关：ID 2000-2003。日常 50/关（4×50=200 局/次，只做趋势，第一读数 kills）；
  // 判决走池外段（§3：413000，4 关各 200，seeds 413000-413199）。
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc（已存在，379KB；缺失会响亮报错）。
// ② **it0 探针（待跑，§2 三件套）** → 里程碑分母 ＋ est ＋ EV 三档。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`。
// ⑥ 确认 a95/a97 在线。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 起跑线 / 边际 / EV 表：`tmp/x20-rebirth-probe.py` 改配方重跑（wMS 三项加入 EV）
// · 贪心：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c20-lives1.jsonc \
//     --games 200 --seed0 860001`
// · 判决（池外固定段）：`... --games 800 --seed0 413000`
// · 回测：`... --course nn-training/levels/ladder-c07.jsonc --games 800 --seed0 410000`
// · 走势：`tmp/x1-trend.py`（改路径；看 kills 第一，看 pu/千tick 第二）
//
// ── 结算节（跑完回填）────────────────────────────────────────────────────
// 必填（对照 §3 逐格填）：池外 kills 里程碑（分母 6.10）＋ pass 门（≥30%）＋ 分关表
//   ＋ 超时熔断读数 ＋ 换血双轨 ＋ c07 回测 ＋ killerKinds/命中率/pu 率 ＋ 横盘 ＋ M2。
