// 课程 x1-rebirth —— 纯从零 RL 臂（不蒸馏 God-AI），从随机初始化起步
//
// 状态：**工程就绪，待用户手工开训**（agent 不得启动训练）。
//
// ── 立项 ─────────────────────────────────────────────────────────────────
// 缘起（2026-09-16 的实测，勿重证）：
//   ① 教师 God-AI 自己也只有 66.25%（arena2-acbc，400 局）；NN（it333）64.75%，
//      x2 线训练 30/80 轮后 64.50 / 64.00 —— **训练量在该关集上零增益**。
//   ② BC 把教师的「先打 power」行为弄丢了：首命中 power 教师 33.3% → NN 0.5%，
//      且训练后进一步掉到 0.0%（x2-acbc.it30）。
//   ③ 七条腿的 per-iter KL 全锁在 0.0018–0.0027（x2-start 80 轮亦然），
//      策略被 kl_coef=0.2 钉在 BC 附近 ⇒ 改奖励 / 加轮数 / 松缰都测不出东西。
//   ⇒ 结论：**从零学是唯一能离开「教师吸引域」的路径**。
//      奖励只能在已有行为之间重新分配权重，而 NN 的策略分布里压根没有
//      「首打 power」这个模式 —— 它需要不受教师先验约束的策略空间去自己发现。
//
// ── ⚠ 历史先例的正确读法（2026-09-17 用户订正，务必读清）────────────────
// `docs/goal-nn.progress.md §10` 的 S1「17 iters 过门（25%→97%）」**不能**
// 当作「从零可行」的证据：那一版 **player 是 3 命 1 星** ⇒ 策略学到的其实是
// 「莽」——靠换血赢，**到 x10 就冲不上去了**。
// ⇒ 两点后果：
//   (a) 本腿 **不与任何既有课程做对照**（不要求与 ladder-c01 同构：它没训练过，
//       且从头开始本来就没有可比基线）。本腿只跟自己的起跑线比。
//   (b) **★ 「莽」必须被压制——「0 伤杀敌」是硬要求**（2026-09-17 用户订正）：
//       我此前按**单关**算术得出「莽划算」（+3 − 1 = +2 > 苟着 0），**结论是错的**，
//       错在把「一关的局部选择」当成了「策略的行为模式」：
//         ① 策略是**跨关共享**的——莽不是某一关的选择，是一种行为方式；
//         ② ladder 是**递进**的（a→b→c→d，升档后敌人更多），换血在小关能赢、
//            到难关必崩 ⇒ **用 1 命换来的那点分，代价是「永远升不了档」**；
//         ③ 目标是**一个能打所有关的策略**，不是单关最优。
//       ⇒ 设计上按 **0 伤** 走：承伤要**有价**（可能致命 + 会累积到后面崩）。
//         但**「有价」不等于「重罚」**——罚狠了会学出「不交火就不会挨打」的保守陷阱，
//         那是「恐惧」不是「干净」。剂量标定见下方 reward 块。
//       ⚠ **现状缺口（本腿奖励里没有承伤项）**：当前 formula 只有
//         `wKill*kills + wHit*enemyHits + wWin*…`，死亡仅 `lives_exhausted=-1.0`
//         ⇒ **挨打完全免费**，学不出「0 伤」。
//       ⚠ **且历史 `wDmg` 一直挂在死项上**：`combat.ts:599` 里非致命命中→
//         `playerDamageTaken`、致命命中→`playerHits`（1 命下恒=死亡指示），
//         所以 `-wDmg*playerHits` 与 `lives_exhausted` 重复，是个**伪装的死项**。
//         **唯一真正的承伤信号是 `playerDamageTaken`**。
//       ⇒ 压制莽的正确杠杆 = **`-wDmg*playerDamageTaken`**，而它至今没有被任何
//         课程正确使用过（历史上挂错了列）。**本腿已挂上它**——见下方 reward 块。
//   另注：§586 明写「纯从零臂从未跑过 ⇒ 教师是否构成天花板仍无答案」，
//   本腿补的就是这个空白（且是在真实关卡上，当年只在玩具竞技场验证过）。
//
// ── 初始化：必须用 scripts/init_scratch_weights.py，禁止手搓随机 ──────────
//   朴素 kaiming 在 8 层残差 ConvMixer 上复合放大（输入 0..255 无归一）⇒
//   策略头 logits 随机即 ±2000 ⇒ 采样 one-hot、熵≈0 无探索；首个 PPO 更新把策略
//   打成确定性（实测 entropy 0.41→0.009、kl=11930 后恒 0 **自锁**）。
//   该脚本按正齐次性缩放并自带断言：trunk×0.1 / move+fire 头×0.01 / value×0.1。
//   本腿 bc 自检实测：|move_logits|max=0.0000 |fire_logits|max=0.0000 |value|=0.0074。
//
// ── 派生（非训练变量，语义必需）：kickstart_ref = false ──────────────────
//   kickstart 是「相对 `bc` 参考策略」的额外 KL 锚（kickstart_kl 1.0，0.5^n 衰减，
//   policy_iter≥31 精确归零）。热启动腿里它温和地把策略拽向 BC；
//   **本腿的 bc 就是随机初始化** ⇒ 开着等于「把策略钉在随机策略上」，
//   且恰好在 it1–it30 这个最需要探索的窗口最猛 ⇒ **必须关**。
//
// ── 起跑线实测（2026-09-17，200 局 / seed0 860001 / ladder-c01）────────────
// | arm | pass | kills | dmg | avgTicks |
// |---|---|---|---|---|
// | **scratch init（本腿起点）** | **8/200 = 4.0%** | 8 | 18508 | 1203 |
// | God AI（教师） | 190/200 = **95.0%** | 190 | 11088 | 439 |
// | it333（x3 线 BC） | 200/200 = **100.0%** | 200 | **754** | 360 |
// ⇒ 起点极低（随机策略开了 4046 枪只命中 146 次）是**正常的**，别据此判失败。
// ⇒ x1 对 NN 已被打穿（it333 满分、承伤仅教师 1/15）⇒ **wr 会撞天花板**；
//    且撞天花板时「莽」与「会打」都显示 100% ⇒ 必须靠承伤/速度拆开。
//
// ── 判据（避开 wr 天花板；连续量优先；专门防「莽」）─────────────────────
//   ① rollout wr 曲线：4% → 是否明显上升（上升 ≠ 成功，见 ②③）。
//   ② **★ 承伤 dmg/局 —— 头号读数（0 伤＝任务定义，已进奖励 `-wDmg*…`）**：
//      基准：God 11088 → it333 754（后者是**含承伤惩罚之前**的旧基准，仅作参照）。
//      看它是否**朝 0 走**（是要趋势，不是要求立刻到 0）。
//      ⚠ 但**不要用「加剂量」来硬压这个数**：承伤高也可能是「敢交火」的正常代价，
//        一味加 wDmg 会把策略推向「不挨打也不杀敌」的保守陷阱。剂量调整要同时看
//        wr 与 kills —— 出现「dmg↓ 但 kills 也↓」立刻回退剂量。
//   ③ avgTicks / **零承伤通关占比**：it333 是 360 tick 且大量零承伤；
//      「莽」的典型特征是击杀不低但承伤高、局时长。
//   ④ ~~首命中 power~~ —— **在 x1 上无判别力，本腿不适用**（2026-09-17 用户指出）：
//      ladder-c01 **每关只有 1 个敌人**（c 关更是纯 power 关，forces="ccc…"）
//      ⇒ 任何命中都只能命中 power ⇒ `firstHitKind` 恒为 'power'，读不出「先打谁」。
//      该读数只在**多敌关**才有判别力（acbc 的 basic+power 同时在场的 33.3% vs 0.5%
//      正是多敌场景）。⇒ **power 行为的验证留到升档**；本腿改看 **c 关（纯 power 关）
//      的通关率 / 承伤 / avgTicks**——它测的是「能不能解决 power」，不是「先打谁」。
//   ⑤ entropy 轨迹：缓降 = 健康；**一步坍缩到 ≈0 = 坏 init 自锁**，立即停。
//   ⑥ KL：本腿无 BC 锚 ⇒ per-iter KL 与累积位移应**远大于**旧地板 0.002。
//
// ── 预算与止损 ───────────────────────────────────────────────────────────
// iters=60；**it20 做一次中途检点**（看 wr / dmg / entropy 三者的组合形状）：
//   · 会打（wr↑ 且 dmg↓）⇒ 续跑；
//   · （**莽不在本腿判据内**：1 敌关没有换血空间。升档到多敌关后若出现
//     wr↑ 但 dmg 不降，再按「改奖励结构（承伤惩罚）而非加轮数」处理。）
//   · 自锁（entropy→0）⇒ 停，回查 bc 缩放；
//   · 无趋势 ⇒ 停，判「真实关卡从零不成立」，回落「BC 补 power 语料」方案。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//   开工前：hub 可达 + eval 节点 epoch 对齐（`plan/eval-outdated.plan.md` 检查单）。
//
// ── 关于「单变量」：本腿**不受其约束**（用户 2026-09-17 拍板）────────────────
// 从零起步、且不与任何既有课程比较 ⇒ 不存在「哪个差异是训练变量」的归因问题，
// 也不需要与 ladder-c01 同构。因此本腿自由设计：**scratch init ＋ 含承伤惩罚的奖励
// 一起上是有意的**——它们共同构成「从零 ＋ 0 伤」这个**新基准**，而不是一个 A/B 差异。
// 唯一需要的对照是本腿自己的起跑线：随机策略 4.0% / dmg 18508。
//
// 非目标：改关卡（任务侧不可动）/ 松缰（另一条腿的事）/ 与任何课程做对照。
{
  "version": 5,
  "name": "x1-rebirth",
  "mode": "per-tick",
  "level": "ladder-c01",

  // ── 奖励：含承伤项（「0 伤」＝任务定义，用户 2026-09-17 拍板）───────────
  // `-wDmg*playerDamageTaken` 是**唯一正确的承伤信号**：`combat.ts:599` 里非致命命中
  // 记 `playerDamageTaken`、致命命中记 `playerHits`（1 命下恒＝死亡指示）。
  // 历史 `wDmg` 一直挂在 `playerHits` 上 ⇒ 与 terminal.lives_exhausted 重复，
  // 是个**伪装的死项**，从未真正惩罚过「挨打」——这就是「莽」一直没被治的原因。
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots",
    "params": {
      "wKill": 3.0,
      "wHit": 0.3,
      "wWin": 2.0,
      // 剂量：**鼓励 0 伤，不是强制 0 伤**（2026-09-17 用户订正）。
      // 为什么不能重罚：挨打常常是**敌人子弹不可预测**的结果，罚得太狠 ⇒ 策略发现
      // 「不交火就不会挨打」⇒ 掉进**保守陷阱**（苟着不杀敌，同样赢不了）。
      // 那是把「恐惧」当成了「干净」。⇒ 定价原则是让「0 伤通关」**自然最优**，
      // 而不是把承伤罚成灾难。
      // 标定：取 wDmg = wHit = 0.3 ⇒ **一次承伤与一次命中敌同价**（对称、好解释）。
      //   整局典型承伤（it333 3.77/局）≈ 扣 1.13，占通关收益（wKill+wWin=5.0）的 23%
      //   ⇒ 0 伤通关(5.35) 明显优于换血通关(4.22)，但换血仍是**正收益**（不会吓得不敢打）。
      "wDmg": 0.3,
      // ★ 反「原地狂射」项（2026-09-17 用户预判「随机权重很可能学会苟」）。
      // 为什么需要：从零的随机策略里「不动 + 一直开火」是**低方差正收益**行为
      // （开火无成本、偶尔命中 +0.3），而交火是高方差（偶尔 +5、经常 -1.0 死亡）
      // ⇒ PPO 梯度**先抓住低方差那个**。实测证据：随机策略 **20.2 次开火/局**，
      // 而学会后（it333）只要 **5.26 次/局** ⇒ 狂射正是随机态的默认行为。
      // 剂量 `0.05` 的标定（关键是能区分「狂射」与「必要射击」）：
      //   · 随机态命中率 3.6% ⇒ 5 次开火换 0.18 命中 = 收益 0.054，成本 0.25 ⇒ **净负**（抑制）
      //   · 学会后命中率 ~52% ⇒ 1 次开火 = 0.52 命中 = 收益 0.156，成本 0.05 ⇒ **净正**（不抑制）
      // ⇒ 同一单价同时惩罚瞎打、奖励打得准。**无需 min/max 包裹**：
      //   `playerShots` 只在开火时 +1，φ 差分天然是「每次开火扣一次」，本就有界
      //   （不像 v2 的 `-wTick*ticks` 那样每 tick 无界累积、污染价值头）。
      "wShot": 0.05,
    },
    "terminal": {
      // ★ 终局惩罚梯度（2026-09-17 用户拍板一并处理）——实测值见下（`tmp/verify-x1-reward.py`）：
      //   0伤通关 +5.35 > 换血通关 +4.22 > 冒险(杀1个再死) −2.25
      //   > 苟到超时 −3.0 > 苟(超时狂射20发) −3.79 > 死亡 −5.75
      //   ⇒ 单调、且**死亡永远最坏**、**冒险优于苟**。
      //   · `lives_exhausted = -5.0`：调前是 **−1.0，比一次承伤（−1.13）还便宜** ⇒
      //     「宁可死也别挨打」的荒谬信号。取 −5.0 = **一次通关的全部收益**
      //     （wKill+wWin），死亡即抵消掉一整局成果 ⇒ 任何情况下死亡都是最坏结果。
      //   · `timeout = -3.0`（**原为 0，未列出的 outcome 一律 0**）：这是「苟」的收益来源——
      //     不杀敌也不挨打的稳定 0。给成 −3.0 后，「什么都不做」是明确的负收益，
      //     而「冒险去杀但死了」≈ **−2.25** ⇒ **冒险优于苟**（差 0.75）。
      //     ⚠ 剂量下限由梯度反推（实测标定，`tmp/verify-x1-reward.py`）：
      //     冒险局 ≈ +wKill(3.0) + 命中(0.6) − 承伤(0.6) − 开火(0.25) − 死亡(5.0) = −2.25
      //     ⇒ 要让它高于「苟」，必须 |timeout| > 2.25 ⇒ 取 **−3.0**（留 0.75 余量）。
      //     取 −2.0 时的实测排序是「苟(−2.0) > 冒险(−2.25)」——**保守仍占优**，会重新掉进陷阱。
      //     ⚠ 但不得再逼近死亡( −5.0 )：苟必须仍优于死，死亡永远最坏。
      "lives_exhausted": -5.0,
      "timeout": -3.0,
    },
    "scheme": "toy",
  },
  "dodge": "",

  // ── ★ 从零起步（本腿的本体）──────────────────────────────────────────
  // 由 `scripts/init_scratch_weights.py --out nn-training/weights/x1-rebirth/scratch-init.json --seed 7`
  // 生成。**禁止替换为 BC 权重**，否则本腿失去意义。
  "bc": "nn-training/weights/x1-rebirth/scratch-init.json",
  // ★ 必须 false：bc 是随机 init，开着 kickstart 会把策略钉在随机策略上（见文件头）。
  "kickstart_ref": false,
  // 必须为 0（kickstart_ref 的硬要求；改 1 启动失败）。
  "warmup_iters": 0,

  "iters": 60,
  "max_hours": 12,
  "workers": 8,

  // 顶层 lr 与 ppo_schedule 第一段保持一致（运行时以 schedule 为准；两处同值防误读）。
  "lr": 0.0003,
  "epochs": 4,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  // ── 位移旋钮：lr 与 kl_coef 一起调（2026-09-17，用户要求「一起调好」）────────
  // 目标：把**轮内位移**拉回 S1 的量级（KL ≈ 0.03），而不是现在 BC 起点的 0.002。
  //
  // 测算依据（`docs/goal-nn.progress.md §7/§10` + 本项目 7 条腿实测）：
  //   · S1 —— 唯一成功的「从零」先例 —— 训练期 **KL ≈ 0.03**（§10：KL<0.03、熵缓降 1.77→1.21）。
  //   · 本项目 BC 起点（x3 系 7 条腿，kl_coef=0.2）**KL 恒 ≈ 0.002**（0.0018–0.0027），是 S1 的 1/15。
  //   · 从零 vs BC 的差别在哪：advantage 经 `normalize_adv` 归一后**尺度被拉平**，
  //     但**方向一致性**不同 —— 随机态下样本几乎同向（都指向「改进」），
  //     BC 态下方向杂乱（已在局部最优附近，梯度互相抵消）⇒ **从零的有效梯度更大**
  //     ⇒ 同一个 kl_coef 下 KL 会更大。⚠ 幅度**不可静态精确预测**（是动力学量），
  //     所以下面取保守值 + 首轮观察规则，而不是一次到位。
  //   · 粗反推：若 KL ∝ 1/kl_coef，达 0.03 需 ≈ **0.013**；但该比例是在 BC 起点测的，
  //     从零起点同值会更大 ⇒ **取 0.05**（松 4 倍，留安全余量）。
  //   ⚠ `kl_cap` 保持 0.2 —— 它在 **remote/serial 路径未接线**（DECISIONS
  //     §2026-09-15-goalnn-kl-cap-unwired：`ppo_update` 无形参、`worker.py` 只读 `kl_coef`，
  //     消费者只有 `rl/stream.py`），**那是对的**；真正有效的旋钮只有 `kl_coef` / `lr` / KL_BREAK。
  //     【2026-09-17 订正】原文补的那句「**恒**不生效」把话说满了：**stream 路径会读它** ——
  //     `args._kl_cap = sch.get("kl_cap")` 每轮**整个重赋**（`rl/loop_steps.py:771`）
  //     ⇒ **段内缺席即 None ⇒ 回落 `policy.streamKlCap`**（`rl/stream.py:163-166`），
  //     本仓 rl-config 给的是 **0.18**（0.06 只是键缺失时的字面默认）。
  //     默认为什么碰不到：`rl-config.json` 的 `rl.stream = 0` 压过 `cli.py` 的 argparse 兜底
  //     （已实测 `args.stream = 0`）⇒ 本路径不生效。
  //     本腿已跑完 ⇒ **配置不动**（改了会与它的语料/归档脱节），仅订正说法；
  //     新腿一律**两段显式写值**（见 `x2-rebirth.jsonc`）。
  //   ⚠ `lr` 与 `kl_coef` 同向（都增大位移），故可一起调；但**若结果异常，回退时分开试**
  //     （先只回 kl_coef，再看 lr）才能归因。
  //
  // 首轮观察规则（it1 出 KL 即可判，不必等 20 轮）：
  //   KL < 0.005        ⇒ 缰绳仍太紧，下一步降到 0.02
  //   KL 0.01 – 0.03    ⇒ 命中目标区间（S1 量级），保持
  //   KL > 0.05         ⇒ 过松，回退 0.1
  //   熵一步坍缩 / KL 暴涨 ⇒ 立即回退 0.2 再排查（scratch init 已控尺度，预期不会）
  //
  // ★ lr 1.5e-4 → **3e-4**（对齐当年从零线的配套标定）：
  //   §7 的设计点是「grad clip=1.0、**lr=3e-4**；trunk 按正齐次性缩放后每 grad step 的
  //   |Δlogp| ≈ 0.03 纳特 ⇒ **更新温和**」⇒ 3e-4 是**与 scratch init 配方配套标定**的值，
  //   不是激进值。本腿原先的 1.5e-4 抄自 ladder-c01（BC 热启动），与从零前提不匹配。
  "ppo_schedule": [
    { "until_iter": 40, "kl_coef": 0.05, "lr": 0.0003, "kl_cap": 0.2 },
    { "kl_coef": 0.03, "lr": 5e-5 },
  ],

  // ── 采集配额：按 **样本量**，不按局数（用户 2026-09-17 订正）───────────
  // 为什么不能拿 seed_rotate 当配额：它控的是「局数」，但**局时长随训练剧烈变化**——
  //   随机策略磨到超时（avgTicks 1203 ≈ 121 samples/局），
  //   学会速通后一局只要 ~360 tick（≈36 samples/局）。
  //   ⇒ 固定局数会让每轮样本量漂移 3× 以上：早期采太多、后期采太少，
  //      学习曲线被采样量本身污染（后期梯度噪声变大，真趋势看不出来）。
  // `target_transitions`（单位 = **samples**，即 jsonl 的 nSamples 之和，**不是 ticks**——
  //   旧键 est_ticks_per_game 就是 10× 量纲错，T9 已改名）：
  //   轮中按「已结算 transitions」**逐关补波**直到全轮达标 ⇒ **每轮样本量恒定**。
  //   24000 与 x3 系同量级（实测 ~2.33 万 samples/轮）。**进身份**（语料变更）。
  "target_transitions": 24000,
  // 首轮兜底的局均 samples 估计，之后由最近 5 轮实测 trailing 均值自动覆盖（不进身份）。
  // 取值依据：随机策略 avgTicks 1203 × (samples/ticks≈0.1007) ≈ 121。
  "est_samples_per_game": 120,
  // 每关局数硬顶：**刻意不设**（省略 ⇒ 用 volume_waves 的 `default_game_cap(G0) = G0 × 4`）。
  // 理由：显式固定值在后期会变成瓶颈——局变短后 G0 变大，固定硬顶可能触发 game_cap
  // 截断配额（「配额未满也停」）。自适应值随 G0 放大，始终保留 4 倍补波空间。
  // 仅作**种子轮转**用（决定抽哪些种子）；开启动态采集后局数由 target_transitions 决定。
  "seed_rotate": 600,

  // bc 语义变了（热启动 → 从零）⇒ 必须新目录，禁续跑（§15.5）。
  "out": "tmp/x1-rebirth/weights.json",
  "traj": "tmp/x1-rebirth",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x1-rebirth",
  "backup_prefix": "x1-rebirth",

  "eval_stages": "2000-2003",
  "eval_games_per_stage": 200,
  "eval_every": 5,
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc 权重：`nn-training/weights/x1-rebirth/scratch-init.json`（已生成，seed 7）。
// ② 关卡 `ladder-c01`（4 关：a=basic / b=fast / c=power / d=armor，各 1 敌轮换、
//    队列 20、max_ticks 1500、lives 1、hard）。
// ③ transport：显式 `--remote-transport pull`；hub 可达、eval 节点 epoch 对齐。
// ④ 动态采集的两条硬约束（`rl/loop_core.py:_volume_stages`）：
//    · 与 `--curriculum-stages` / `--rotate-stages` 不兼容（会 SystemExit）；
//    · **必须能拿到显式 --stages**（launch 期由课程 stages 派生），否则 SystemExit。
// ⑤ 期望起点极低（4%）⇒ **前几轮 rollout wr 难看是正常的**；看斜率不是绝对值。
//
// ── 结算节（跑完回填）────────────────────────────────────────────────────
// 终点权重：<it60 归档路径>
// 曲线：rollout wr + **dmg/局** + avgTicks + 零承伤通关占比 + entropy + per-iter KL
// ★ 承伤（0 伤是硬要求）：dmg/局 是否向 0 收敛；「零承伤通关」局占比。
//   若 dmg 长期停在数百量级 ⇒ 策略在换血 ⇒ **即使 wr 好看也判「学错了」**，
//   因为这种策略在难关（敌人更多）上必崩，升不了档。
// ★ power 专项：看 c 关（纯 power 关）的通关率 / 承伤 / avgTicks —— 测「能不能解决
//   power」。**不用「首命中 power」**（1 敌关该读数恒为 power，无判别力，见判据④）。
// ★ 每轮样本量：**训练侧已严格截断到 per_stage=6000 × 4 关 = 24000**（`ppo/common.
//   trim_shard_arrays`，逐关、截断在 GAE 之前）。采集侧仍按补波过冲到 ≥24000，
//   超出部分被丢弃（不切半局进新关）⇒ 结算记录 wasted = collected − 24000 看浪费率。
//   好处：n 恒定 ⇒ `n % mb` 恒定 ⇒ 所有 minibatch shape 恒定（TPU 侧无抖动）。
// 判决：会打 ⇒ 从零路线成立，升档到 acbc / ladder-c03 看能否超越教师（66% / 70%）；
//        莽 或 自锁 或 无趋势 ⇒ 停，回落「BC 补 power 语料」方案。
