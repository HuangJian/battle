// 课程 x5-approach —— 拾取趋近 shaping 腿（ladder-c05 单关，plan/pickup-shaping.plan.md）
//
// 状态：**待开训**（课程文件建成；用户手工开训，agent 不得启动训练）。
// 本文档 = pickup-shaping.plan.md 的判据原文搬家（§5 判决规格整段搬入，禁改）；
// plan 文件已退役（归档 plan/archive/）。
//
// 一句话：让「朝道具走」这个行为维度**存在**（供给允许范围内 pu/局显著涨），
// 同时承伤不崩。不追求 pu 数字本身 —— pu 是机制读数，主端点仍是承伤（L12 同构）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（levels/ladder-c05.jsonc 实读，勿信记忆；与 x5 逐字同构）
// ══════════════════════════════════════════════════════════════════════════
//   单关：ladder-c05，forces `abcdabcdabcdabcdabcd`（20 长），count 5 ⇒ 队列 20 取前 5
//   = a+b+c+d+a（basic 双份 + fast/power/armor 各一）。max_ticks 3900、lives 1、hard、
//   空场 13×13。自定义关 ID 2000-2000。任务侧一个字不动（AGENTS §0.2）；单 run 声明
//   （L4）。seed_rotate 600、per-stage 配额 = 总量（单关结构后果，同 x5）。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 背景（勿重证，直接引用 x5-rebirth.jsonc 结算⑧–⑮）
// ══════════════════════════════════════════════════════════════════════════
// · x5（wPickup 3.0，63 轮）：pu 0.71→0.69 纹丝不动，判部分达成；其余项（dmg 同种子
//   −39%）动了 ⇒ **拾取特异性不动**，不是全局梯度死亡（⑧）。
// · 意愿半径 cliff（⑩①）：出生距离 0–4 格拾取率 74%，5 格+ ≈5%；过期 tracks 生前最近
//   距离中位 2.5 格 ⇒ **零绕路**，「朝道具走」行为不存在（⑩②）。
// · Star 是供给问题（⑨）：spawn 0.085/局、拾取率 ~82%，不是需求问题。
// · 截胡假说（⑩④）：撞见步 = 战斗最优步 ⇒ +3.0 加固战斗不加固拾取（⑮④干预实验与本腿
//   并行不互斥；若干预证伪截胡，本腿剂量方法不变，只换 dui 变量）。
// · 已排除：管线（metric 进 shard）、可观测（obs ch12 位置+类型+剩余时间）。
// · 设计失误认领（⑪）：约束③（2×弱起点 = 1.42）超供给上限（~1.2–1.6/局），不可能条款；
//   stars 半句删除（⑨）；BONUS 窗口（~600 tick）与战前不分账（⑩⑤，metric 限制）。
// · 附带修复（同 Phase 0，不进训练变量）：`puGot*` 全零输出 bug（⑩⑤，根因 = fence 等
//   类型无输出桶）→ 新增残差桶 `puGotOther`（metrics v7 idx39）；见 x4/x3 不适用。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 实测（2026-09-18；bc = x5-rebirth.it35.best；metrics v7 语料
//    `tmp/x5-approach0`，40 局 seed 0–39，rollout 口径）
// ══════════════════════════════════════════════════════════════════════════
//   胜率 60%（24/40 过关）｜ dmg **153.1/局** ｜ cells 67.4 ｜ pu **0.525/局**
//   ｜ spawn **1.225/局** ｜ kill+win 贡献均值 **12.68**（wKill 3.0×kills + wWin 2.0×cleared）
//   ｜ 局均决策步 **153.4**（est_samples_per_game 取 153）
//   pickupDist 读数（v7 新列）：
//    · 存活拾取步占比 35.3%（局均 live 步 54/153）⇒ 有拾取可趋近的时间窗有限；
//    · 每决策步均距（含哨兵归零）3.58 格；趋近步/远离步 = 15.0/18.0（**比 <1 ⇒
//      「朝道具走」步比「离开」还少** —— 本腿的干预靶点，不是诊断意外）；
//    · telescoped（Σr_approach = d0−dN，d0 恒 0 因开局无存活拾取）：均值 −5.1，
//      无正贡献局 ⇒ 势能法不变式：每局趋近项净 ≤0，全清场归零。**势能塑形不改最优
//      策略**（Ng et al.）—— 这正是本腿敢上的前提；「来回刷分净零」同源。
//   **口径澄清（2026-09-18 评审提问，留档）**：本 it0 胜率 **24/40 = 60%**，与
//   x5 §2 口径 R 记的 x4-best「40/40」**不是同一任务口径** —— x5 探针脚本
//   （tmp/x5-rebirth-probe.py）**没传 `--lives-override`/`--player-level`** ⇒ 跑的是
//   hard 默认 **3 命 + 出生 1 星**；训练/判据口径是课程 **1 命 + 0 星**。已用 v7 代码
//   现场钉死：同一 x4-best 权重、同种子 0–39、不带覆写 ⇒ **40/40 = 100%**（与 x5 记录
//   逐字一致，证 v7 只读改动对 sim 零行为影响）；带课程覆写 ⇒ 25/40。x5-it35.best
//   带课程覆写 ⇒ 24/40 —— 60% 是**课程口径的诚实读数**，不是权重退化、也不是虚好；
//   达标线 0.8×it0 在同一课程口径内比较，方向无偏。
//   供给可行均值 = spawn 1.225 × 0.9 = **1.103**（拾取率 90% 即满分假设；§5 约束②分母
//   **执行时以新腿 it0 重测** —— 判据分母 = 新腿 it0，段间差异照 x5（池外 bc 161.2 vs
//   日常 it0 154.8，差 6.4）先例处理）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 奖励：x5 终点配置（a2-v4 + wPickup 3.0）**+ 唯一新项**：稠密趋近势能
// ══════════════════════════════════════════════════════════════════════════
//   `wKill 3.0 / wHit 0.6 / wWin 2.0 / wDmg 0.03 / wShot 0.05 / wExplore 0.2 /
//    wPickup 3.0` ＋ **`wApproach 0.008`** ＋ terminal `−5.0 / −2.0`，scheme `toy`。
//   公式（与 x5 逐字同构 + 尾部追加一项）：
//     ... + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)
//   · 原理（势能法）：diff 后该项恰为「每步趋近收益」—— 朝道具走一步即时到账，不依赖
//     150 步后的稀疏 +3.0；趋近后远离净还回去（来回刷分净零，势能差的性质）；
//     no-alive-pickup ⇒ 哨兵 -1 ⇒ where 归零 ⇒ 无方向可 hack（公式侧统一处理）。
//   · **剂量冻结（Phase 1，2026-09-18 按 plan §3 方法标定；开训后改剂量 = 新腿）**：
//     ① 边际算术（a2 口径）：sit n=10 dmg 204.2 / wander n=9 dmg 108.0（cells 38.4 vs 99.8）
//        ⇒ **−1.57 dmg/格**（x5 原语料 −0.56/格；LSQ −1.69 交叉核对）—— 同一符号，
//        c05 上「会打」态多走动反而少承伤 ⇒ 趋近的 dmg 成本**非正**，「谁出钱谁定价」
//        不构成约束（趋近不额外流血）。
//     ② 门（喧宾夺主线，x5 ②b 同款）：Σwhere0(pickupDist)/局 均值 **753.8**，
//        kill+win 均值 **12.68** ⇒ wApproach ≤ 0.5×12.68/753.8 = **0.0084**。
//        ⇒ 冻结 **wApproach = 0.008**（比门限留 ~5% 裕量；每决策步趋近信度 ≈
//        3.58 格×0.008 = 0.029，与 wHit 的 0.047/步同量级 —— 可见但不出头）。
//     ③ 天花板（观测项，不设数字）：converged 行为不得出现「放着敌人不管追道具」
//        （日常 killerKinds / zero-kill-frac 盯；熔断见 §6）。
//   · 禁止事项（plan §3 原样）：不动 wPickup（3.0 已是天花板，P2）、不动 wDmg（P5 红线）、
//     不按道具差别定价（doctrine，fence 残差接受）、**禁动观测/模型/encoder**。
//   · **EV 表（走/坐/莽/捡/不捡 五档；w=0.008，§6 DoD 的落库件）**：
//     ┌──────────────┬────┬────────┬───────┬──────────┬───────┬────────┬──────┬───────┐
//     │ 档            │ n  │approach│ kill+win│ pickup  │ dmg   │ term   │ EV   │       │
//     ├──────────────┼────┼────────┼───────┼──────────┼───────┼────────┼──────┼───────┤
//     │ 走  (A≥p75)   │ 10 │ +17.01 │ +17.00 │ +1.50    │ −3.65 │ 0.00   │+38.48│ ← 最高│
//     │ 坐  (cells≤p25)│ 10 │  +0.03 │  +7.30 │ +1.20    │ −6.13 │ −4.00  │ +2.11│      │
//     │ 莽  (dmg>p75) │  9 │  +4.83 │ +11.22 │ +2.00    │ −7.27 │ −2.78  │+12.82│      │
//     │ 捡  (pu≥1)    │ 19 │  +5.66 │ +15.74 │ +3.32    │ −4.05 │ −0.79  │+26.23│      │
//     │ 不捡 (pu=0)   │ 21 │  +6.36 │  +9.90 │ 0.00     │ −5.09 │ −3.10  │+12.52│      │
//     │ 全体          │ 40 │  +6.03 │ +12.68 │ +1.58    │ −4.59 │ −2.00  │+19.03│      │
//    └──────────────┴────┴────────┴───────┴──────────┴───────┴────────┴──────┴───────┘
//   ⚠ **量纲 caveat（2026-09-18 评审必改①）**：本表 approach 列 = **势能水平和**
//     （w×Σwhere0(pickupDist)，undiffed），**不是**训练实际吃到的信用 —— realized credit
//     是 **telescoped diff**（Σr_approach = d0−dN，均值 −5.1 ⇒ 每局 realized ≈ **−0.04** @
//     w=0.008，见 §2 telescoped 读数；calib-report 同口径）。per-step 真实机制 =
//     ±wApproach×Δdist（≈ ±0.02/决策步，与 wHit 的 ~0.047/步同量级）。门 ② 用「水平和」
//     保的是**保守上限**（方向偏小，不拦车）：EV 表里 approach 列的数字只用于横向比较
//     组间趋近活跃度，不用于预测回报。
//   · 读数（2026-09-18 评审必改②，按 EV 正排序）：走 38.48 > 捡 26.23 > 莽 12.82 >
//     不捡 12.52 > 坐 2.11 —— 方向正确：趋近活跃组与收集组都压在坐/莽之上。
//     ⚠ **反直觉观察（不藏，反写）**：approach 列 **不捡 +6.36 > 捡 +5.66** —— 趋近活跃
//     **≠** 吃到：本语料里「散步多」的局走近的道具也多，但没站上采集点 ⇒ 恰是 x5 ⑩④
//     「偶遇」假说的同款证据（撞见步 = 战斗最优步，+3.0 加固的从来不是拾取）。这是本腿
//     **必须动 wApproach** 的理由之一，也是 §5 拾取约束看 **pu 总量**（不看趋近步数）的原因。
//     w=0.004 时「走」approach +8.51、w=0.016 时 +34.03 —— 剂量翻倍敏感，0.008 居中。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 起点与缰绳（与 x5 逐字同构；bc 换成 x5 的择优终点 it35）
// ══════════════════════════════════════════════════════════════════════════
// `bc = x5-rebirth 的择优终点` = `nn-training/weights/x5-rebirth-best/x5-rebirth.it35.best.json`
//   （已存在：379KB、`schema_major 3`、42 键，2026-09-18 已核；x5 池外三选一的终点，
//   见 x5-rebirth.jsonc 结算节 —— **旧 -best 当 bc 可用**，作废声明不涉及）。
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求，全链一致；M3：预期贡献 ≈0，
//   热启 = 权重初始化，首轮 KL 暴涨先查 bc 载入）。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 判据（plan §5 原文搬家，冻结，执行时禁改）
// ══════════════════════════════════════════════════════════════════════════
// · 池外段：`--seed0 403000`（800 局单关；执行时先 `grep` 确认 403xxx 全空，
//   被占则顺延并更新台账——中途不得改段，L3）。
//   `bun tools/sim/eval-course-ckpt.ts --course nn-training/levels/ladder-c05.jsonc
//   --weights <归档> --games 800 --seed0 403000 --workers 8 --out tmp/<tag>.jsonl`
// · 主端点 dmg/局（分母 = 新腿 it0，执行时重测；达标线 ≤0.8×it0 且 CI 不重叠）＋
//   耗时约束（≤1.1×it0，x3 CI 口径）＋ 掉队护栏（单关相对 it0 −5pp）。
// · **拾取约束（重写版，供给先行）**：终点 pu/局 ≥ min（1.5×it0，供给可行均值×0.8），
//   其中「供给可行均值」= it0 语料 spawn/局 × 0.9（拾取率 90% 即满分假设；
//   两项取小 —— 弱起点倍数与供给上限双封顶，⑪ 纪律）。
//   stars 半句删除；战前/窗口不分账（metric 限制，接受并声明）。
//   预登记数字（执行时以新腿 it0 重测替换）：it0 语料 pu 0.525 ⇒ 1.5× = 0.79；
//   供给可行均值 1.103 ⇒ ×0.8 = 0.88；取小 ⇒ **终点 pu/局 ≥ 0.79（预登记）**。
// · 终点三选一（日常最优 / pass 最优 / 末 it；互不可区分取 pass 高者；M2）。
// · 熔断：shots/kills 沿 x5 口径 ＋ **趋近 hack 熔断**：`cellsVisited` 暴涨而 kills 不升
//   （绕道具打转形态）或 zero-kill-frac 翻倍 ⇒ 立即停（防 shaping 特有 hack）。
// · 日常 200 局只做趋势；单 run 声明（L4）；未达标措辞三选一（L3）；
//   **不得写「拾取无效」**（判负只可能是配方/梯度/执行）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 止损（与 x4/x5 同格；+ 趋近熔断）
// ══════════════════════════════════════════════════════════════════════════
// · 机械横盘：连续 20 轮（= 4 个 eval 点）承伤无改善（|Δ|<5%）或漂移形态 ⇒ 停腿；
//   宽限一次 +10 轮（§68 先例；到期必停）。
// · **it35 检点（第 7 个 eval 点； shaping 腿第一次验证，不给第二次机会）**：
//   pu 必须动（方向 + CI），否则按 §5 熔断停腿 —— it35 无改善 = 机制证伪。
//   entropy 升只能证伪「坍缩」，不能证明「在学」（x5⑦ 纪律），判据看 pu/承伤。
// · 熔断轴：shots/kills 熔断与 x5 同口径（分母换 it0）＋ pu 熔断（日常 pu/局 < 0.5×it0
//   ⇒ 立即停）＋ **趋近 hack 熔断**（§5：cellsVisited 暴涨而 kills 不升 / zero-kill-frac
//   翻倍 ⇒ 立即停）。
// · 分支二（换血）：dmg↑ 而 wr 不降 ⇒ 学错了，改奖励结构，不加轮数。
// · 分支四（自锁）：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3）。
// · 优先级：横盘 / it35 > 拾取约束 > 一切诊断。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 预算与采样规模（与 x5 逐字同构；est 换新语料实测）
// ══════════════════════════════════════════════════════════════════════════
// · target_transitions 24000/轮（总量不动，PPO batch 守恒；单关 per-stage = 24000）。
// · est_samples_per_game = **153**（it0 语料实测 153.4 向下取整；trailing 5 轮纠偏）。
// · seed_rotate 600 / iters 0（无限轮，**由用户拍板**，M1）/ max_hours 12 / workers 8。
// · 位移包不动（lr 0.0005 / epochs 6 / kl 0.02→0.01 / kl_cap 0.2 双段 / until 25；
//   有限轮 N 则 until 取 min(25, 40%×N)）。ent_break 0.25。
//
// ══════════════════════════════════════════════════════════════════════════
// 8. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. **metrics v7 作废声明**：旧 shard（traj/，v6 及更早）**不可续跑**（列序已变，
//   任何 shape[0] 下游用 version 判别会响亮报错）；旧 `-best` 当 bc 可用；
//   旧 metric 数值与新数值**禁直接比较** —— 终点三选项限于新版本内。
// 2. 单 run（<2pp 非效应）。
// 3. 战前拾取与 BONUS 窗口不分账（metric 限制，接受并声明；§5 约束只看 pu 总量）。
// 4. d0 恒 0 ⇒ 趋近项每局净 ≤0（telescoping）—— 势能塑形原理所致，不改变最优策略；
//    若 it35 pu 不动，合法措辞 = 配方/梯度/执行问题（L3），不得写「拾取无效」。
// 4b. 备案（评审顺带记录，二阶效应，量级可接受，不处理）：diff 语义下道具**出现**的
//    那步会扣 −wApproach×d（不可控噪声，≤0.4/事件，约 episode 2%），道具**过期**退
//    回 +wApproach×d —— round-trip 净零已由 tests/pickup-dist-metric 与公式回归
//    test_pickup_shaping_metrics_v7_formula 钉死，无 hack 矢量。
// 5. est 只是首轮兜底（trailing 5 轮覆盖）。

{
  "version": 5,
  "name": "x5-approach",
  // 每一决策步一条样本（非 intent/goal 窗口），与当前 PPO 管线匹配。
  "mode": "per-tick",
  // 环境唯一来源 = levels/ladder-c05.jsonc（单关，5 敌 a+b+c+d+a、队列 20 取前 5、
  // max_ticks 3900、lives 1、hard）。课程侧禁止重写
  // stages/difficulty/max_ticks/player（任务侧不可动，AGENTS §0.2）。
  "level": "ladder-c05",

  // ── 奖励：x5 终点配置 + 唯一新项 wApproach（见 §3；其余逐字不动）──────────
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      // P5 红线：一个数不许动。
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,   // x5 定稿剂量，不做第二个变量。
      "wApproach": 0.008 // ★ 唯一训练变量（§3 标定冻结；改剂量 = 新腿）。
    },
    "terminal": {
      "lives_exhausted": -5.0,
      "timeout": -2.0
    },
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点：x5-rebirth 的择优终点 it35（见 §4；42 键已核）───────────────────
  "bc": "nn-training/weights/x5-rebirth-best/x5-rebirth.it35.best.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §7；iters 由用户拍板，默认 0）────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：全链一致不动（见 §7）─────────────────────────────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段 it1–25（iters=0 时；有限轮 N 则 until 取 min(25, 40%×N)。kl_cap 两段显式。
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段 it26+：kl 放松 + lr 砍半。
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 }
  ],

  // ── 采集：总量 24000/轮；单关 per-stage = 24000 ───────────────────────────
  "target_transitions": 24000,
  // 153 = it0 语料实测 153.4 向下取整（§2；之后 trailing 5 轮覆盖）。
  "est_samples_per_game": 153,
  // 单关回退值；volume 模式不参与。
  "seed_rotate": 600,

  // ── 路径：新目录、禁续跑（§15.5 / 作废声明）──────────────────────────────
  "out": "tmp/x5-approach/weights.json",
  "traj": "tmp/x5-approach",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x5-approach",
  "backup_prefix": "x5-approach",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 单关 2000。日常单关 200 局/次（只做趋势）；判决走池外段（§5：403000，800 局）。
  "eval_stages": "2000-2000",
  "eval_games_per_stage": 200,
  "eval_every": 5
}

// ── 开工前置（Phase 0/1 已完成，可直接开训）──────────────────────────────
// ① **Phase 0 metric 工程全绿**（metrics v7，2026-09-18）：
//    TS 落盘端 pickupDist/puGotOther 两列 + METRICS_DIM 41 / METRICS_VERSION 7；
//    Python METRICS 缀尾 + 范围 + 公式解析回归；golden 重生成（64 cases）；
//    codec 独立重实现 + 炸弹局/多拾取局/BONUS 窗口局等价用例（tests/pickup-dist-metric）；
//    `bun run check` 绿 + nn pytest 396 用例绿。
// ② **Phase 1 剂量已冻结**（§3：wApproach = 0.008，门 ② 限 0.0084，余 ~5%）。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）——**旧 v6 shard（tmp/x5-*）不可续跑**。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`（x3 前置⑤事故）。
// ⑥ 确认 a95/a97 在线。
//
// 启动（**用户手工；agent 不得启动训练**）：
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x5-approach \
//     --remote-transport pull
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 剂量标定语料/报表：`tmp/x5-approach0/`（40 局 v7 shards）+ `tmp/x5-approach-calib.py`
//   （③ EV 表 / ② 门 / ① 边际 / ④ 供给推导；rows.json 在 out 目录）。
// · 贪心 + god：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c05.jsonc \
//     --games 200 --seed0 860001`（+ `--policy god` 即参照臂）
// · 判决（池外固定段）：`... --games 800 --seed0 403000`
// · 走势 / 动作分布 / 时间账：`tmp/x1-trend.py` / `tmp/x1-acthist2.py` / `tmp/a2-timing.py`