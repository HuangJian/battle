// 课程 x3-rebirth —— 3 敌关（ladder-c03）的**热启迁移**腿（rebirth 阶梯第三级）
//
// 状态：**已结课（2026-09-18，it153，用户人工停）—— 判决 = 达成**。
//   池外固定段（seed0 400800，800 局）：承伤 103.5 → 56.4（**−45.5%，CI 不重叠**），
//   通关 ticks 1342 → 1227（**改善 8.6%，约束守住**），pass 73.6% → 92.4%，
//   四关相对 it0 全部提升（含 acd 70.0% → 87.5%），无掉队。终点 = it153（末 it），
//   已另存 `nn-training/weights/x3-rebirth-best/x3-rebirth.it153.best.json`。
//   详见文件尾「结算节（2026-09-18 结课回填）」。agent 不得启动训练。
//
// 前置假设：x2-rebirth（ladder-c02 / 2 敌）已达成其达成口径（承伤 + 耗时双降、无掉队关）。
// 本腿把它的终点权重热启到 ladder-c03（3 敌 / 4 关），回答三个问题：
//   ① 迁移链（c01 → c02 → c03）能不能继续守住「承伤 + 耗时」？
//   ② 3 敌关首次出现 **power+armor 双肉组合**（acd），这是 x3-start 被钉死 59–60 的
//      跨 count 稳定病灶 ⇒ 从零长出来的策略能不能啃动它？
//   ③ 每条腿都要回答的：多敌关到底需不需要一个真正的「朝敌」信号（a2 open limitation #2）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（levels/ladder-c03.jsonc 实读，勿信记忆）
// ══════════════════════════════════════════════════════════════════════════
//   4 关：abc / abd / acd / bcd（a=basic b=fast c=power d=armor），**每关 3 敌**，
//   队列 20 取前 3、max_ticks **2700**、lives 1、hard、空场 13×13。
//   自定义关 ID **2000-2003**（`rl/ladder_factory.py:228 eval_stages_for`）。
//   任务侧一个字不动（AGENTS §0.2）—— 只动训练侧。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. ★ x3 系列教训（本节的每一条都对应一次真实亏损，逐条在本腿落地）
// ══════════════════════════════════════════════════════════════════════════
// · **L1（x3-start，最贵的一条）**：零样本 53.8% → it10 67.5%（+14pp）→ **之后 320 轮
//   横盘 0.65–0.72**（三锚点 69.4/67.8/69.5，带宽 1.7pp），acd 被钉死 59–60。
//   结论：**plateau 是信号配方到顶，不是训练事故**；同配方加轮/加密度**不搬运偏科关**。
//   ⇒ 本腿：判据只看**连续量**（承伤/耗时）不看胜率；并加**机械横盘检测**（§7）。
// · **L2（x3-start）**：`iters: 0` + 文件里写「60 轮证伪条款」⇒ 条款触发了但**没人停**，
//   一直跑到 333 轮靠用户一句话才收。
//   ⇒ 本腿：**iters 有限（60）**，且证伪/横盘条款写成**会自己停车的判据**（§7），不靠人。
// · **L3（x3-power，判负且方向显著为负 −3.35σ）**：① 日常 50/关 **SE≈6.65pp，
//   分辨不了 +5pp** ⇒ 日常**只做趋势、绝不判决**；② **改 seed 段 = 换判决条件 = 事后重开判决**
//   （禁止）；③ 基线必须**先冻结**再开腿；④ KL~0.002 时的合法结论是
//   「梯度无方向 / 执行瓶颈」，**不得**写成「机制无效」。
//   ⇒ 本腿：判决只在**池外固定段**（§6），段号写死且不得中途改；日常只画趋势。
// · **L4（x3-credit-p6 / -r2，双 run 一致性纪律）**：关键结论必须 **≥2 个独立 run**；
//   **pd 5–10% 是灰度带，不单独判决**；主端点要有**判线数值**（Δ≥+0.7≈4σ 且配对单侧 p<0.05）；
//   判线要**预注册**（门禁 ≥616/800 且 McNemar 单侧 p<0.05）。
//   ⇒ 本腿：主端点带数值判线 + 灰度带规则（§6）。⚠ 本腿**不承诺**双 run（GPU 配额），
//   因此结算文字里必须写明「单 run，幅度未过 run 本底」，不得断言效应。
// · **L5（x3-step 评审）**：① 机制别拍脑袋 —— 先拿历史数据/冻结 shard 做**离线判决**
//   （30 分钟 vs 4 小时腿）；② 判据必须可证伪：`Δ>0` 不是判据（800 局纯噪声 |Δ|
//   95% 带宽 ±3.7pp）、**必须补中间带**（实测最可能落在中间）、**统计量要写死**
//   （哪个窗口的 mean/max）、**必须有零成本对照**。
//   ⇒ 本腿：开训前先跑 it0 探针（§开工前置①，~3 分钟），主判据带中间带（§6）。
// · **L6（x3-step 评审 §4-C，两次事故的同一形态）**：`--remote-transport auto` 会
//   **回落推全局 gpu_push 节点**，不是 hub/pull ⇒ 启动命令**必须显式写
//   `--remote-transport pull`**（与 x2-rebirth 同）。
// · **L7（x3-credit-p6）**：metrics v6 的击杀双口径（`SimulationPowerUps.ts:391` 的
//   bomb 清屏不 push `tank_destroyed` ⇒ `sum(killsByKind) < kills`，实测缺 11.6%）。
//   x3-credit-p6 用「分列 + 残差桶」绕；**本腿直接用标量 `kills`，天然免疫**。
//   ⚠ **禁止**为「更细的兵种信用」把本腿公式改成分列 —— 那会静默丢掉炸弹击杀信用。
// · **L8（x3-chip，wChip 剂量三臂 k0/k05/k10 判阴）**：c01–c03 不上 wChip（roadmap N3），
//   且剂量腿已定案为阴 ⇒ **本腿不上 wChip，也不再开 wChip 剂量腿**。
// · **L9（量纲）**：`transitions = samples`（≈ 局均 ticks / 10），不是 ticks。
//   历史多处 10× 错（x3-start / c4-dodge 都踩过）⇒ 本腿所有配额按 samples 记。
// · **L10（x2-rebirth，最新最贵的一条）**：**有效训练量可能只有前几轮** —— 承伤在
//   **it5 就见底**（106.4 → 33.3），之后 20 轮零改善、it20/it25 还回退（46.1 / 41.6），
//   且漂移方向是「**更快但更脏**」。（数字与结课判决出处：**`docs/nn.progress.md §68`**，
//   本腿立项依据即由此闭环。）
//   ⇒ 本腿三处改：① **`iters` 由 60 砍到 40**（跑满没有意义）；
//     ⚠ **本行已作废**：开训时用户拍板改为 **`iters 0`（无限轮）+ `eval_every 5`**
//     （去睡觉没人盯；见 §6 与评估块）。保留原文只为记录"改了两次"的过程。
//     ② **`eval_every` 由 5 改到 3**（早期密集采样，才抓得住最优点、也才能早点触发横盘停腿）；
//     ③ 每个 eval 点执行 §4 的**最优归档保护**（最好的 it 立刻另存）。
// · **L11（x2-rebirth）**：**掉队要相对 it0 看** —— it5 的 bc 关 64% 单看不刺眼，
//   但 it0 的 bc 是 74% ⇒ **训练后反而退步 10pp**，这才是掉队。绝对值和"最弱关"都会骗人。
// · **L12（x2-rebirth）**：**power 盲区很可能修不好，别把它当判据** —— x2 的 ac 关
//   pass 从 30% 打到 96%，但 `firstHitKind` 仍是 100% basic（**先打 power 永远 0.0%**）
//   ⇒ 它是靠"别的能力"赢的。⇒ 本腿**不把「先打 power」当主端点**（主端点仍是承伤），
//   `firstHitKind` 只作诊断；**acd（basic+power+armor）预先点名**为最可能解不开的关。
// · **L13（x2-rebirth）**：远端节点要先弄活 —— x2 全程 a95/a97 只承担 **3.8%**，
//   本机白扛；这是唯一零代码 2×，开训前必须做（写在开工前置①）。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. 奖励：与 x1-rebirth-a2 / x2-rebirth 逐字一致（整条 rebirth 线不换配方）
// ══════════════════════════════════════════════════════════════════════════
//   `wKill 3.0 / wHit 0.6 / wWin 2.0 / wDmg 0.03 / wShot 0.05 / wExplore 0.2`
//   terminal `lives_exhausted −5.0 / timeout −2.0`，scheme `toy`。
// 为什么一字不改（三条理由）：
//   ① **线上内一致**：a2（1 敌）与 x2-rebirth（2 敌）用同一配方都成立 ⇒ 换配方就等于
//      在「环境升级」之外再引入第二个变量，归因直接报废（x3-credit 的单变量纪律）。
//   ② **剂量不随敌数重标**：a2 已证杠杆是「每格边际收益/成本之比」而非终局项；实测该
//      比值**随分布翻号**（随机态 +3.08 dmg/格 →「会打」的起点 −0.95）⇒ 敌数不是标尺。
//   ③ **x3 线换配方的下场**：x3-power（改 kill/hit 比值）判负且方向为负、x3-credit-p6
//      （metrics v6 分列）双 run 判负 ⇒ 在 3 敌关靠改奖励撬行为，本仓**零成功先例**。
// ⚠ 承伤项 `-wDmg*playerDamageTaken` 是「0 伤」的载体（`combat.ts:599`：非致命命中 →
//   playerDamageTaken，致命 → playerHits；挂在 playerHits 上是伪装的死项）。
// ⚠ **本腿不启用 wChip / wPickup / wTick**（L8 + roadmap N3）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 起点与缰绳
// ══════════════════════════════════════════════════════════════════════════
// `bc = x2-rebirth 的**择优终点**（2026-09-18 已填实）
//   = `nn-training/weights/x2-rebirth-best/x2-rebirth.it15.best.json`。
//   ⚠ **选取规则（唯一）**：**按 §4 的「最优归档保护」选出的 `-best` 权重**，
//   **不是「it 最大的归档」** —— x2-rebirth 的血泪正是：承伤在 it5 见底、it20/it25 回退，
//   「取最大 it」会拿到显著更差的那个（it25 承伤 Δ=+7.7，SE 3.6，z≈2.1）。
//   择优过程与配对证据见 `docs/nn.progress.md §68`（与 x3-credit 系拿 `x3-start.it333`
//   当 bc 是同一套「继承上一条腿的成果」先例，但**继承的是择优结果，不是最后一个 it**）。
//   若 x2-rebirth 之后重跑/换了终点 ⇒ 以 §68 的 `-best` 为准，勿回退到「取最大 it」。
//   开训前必须核：格式 `nn-weights-json` / `schema_major 3` / 参数键集与 `scratch-init.json` 同集。
// `kickstart_ref = true`（与 x2-rebirth 同，也与 x3 系全部腿一致）：锚 = `1.0 × 0.5^(it−1)`
//   与 `kl_coef` **并列相加** ⇒ it1 总锚 1.02、it5 0.08、it10 0.02 ⇒ **实质只锁 it1–7**
//   （护住上一条腿学到的「不挨打」行为），之后交给位移包。`warmup_iters = 0`（硬要求）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 判据（预注册；吸取 L3/L4/L5：**可证伪 + 有阈值 + 有中间带 + 统计量写死**）
// ══════════════════════════════════════════════════════════════════════════
// **主判据 = 承伤（主端点）+ 耗时（约束）** —— 2026-09-18 用户裁定，取代「双主端点双降」。
// 依据是 x2-rebirth 的实测（c02，anchor 300 局，bootstrap 95% CI）：
//   承伤 106.4 [97.3,115.8] → 46.1 [38.0,54.4]（**−56.7%，CI 不重叠 = 显著**）；
//   通关 ticks 928.7 [886,972] → 972.0 [930,1013]（+4.7%，**CI 大幅重叠 = 没动**）。
//   ⇒ 承伤是能被训练推动的量；耗时在 2 敌关**既推不动也没被牺牲**。
//   要求两者同时下降 ⇒ 大概率把一条本来该判「达成」的腿误判成负。
// 主端点：**`dmg/局`**（局末 `playerDamageTaken`，池外段全体均值）。
// 约束项：通关局 `avgTicks` —— **不得恶化 >10%**（不要求下降）。
//   **口径写死（不得临场发明）**：以池外段（seed0 400800，800 局）**均值的点估计**计；
//   **若 95% CI 下界已超过 +10% ⇒ 提前判负**（不必等点估计越线）。
//   反向同理：只有 CI 上界仍 > +10% 时才算「未定」，否则按约束守住处理。
//   ⚠ 写这么死的理由就是 x2-rebirth 的教训：我按点估计判「耗时反向 +9%」，
//   加 CI 后**完全在噪声内、结论不成立**（`docs/nn.progress.md §68`）。
//   ⚠ 设这条约束的理由（x2-rebirth 的后半段）：it15→it20 出现 dmg 33.9→46.1 而
//   ticks 1002→972 —— 策略在往「**更快但更脏**」漂。**没有这条约束，那种漂移会被判成改善。**
// **判线（写死，不得临场发明）**：
//   | 结果 | 判决 |
//   | 承伤 **↓≥20% 且 CI 不与 it0 重叠** ＋ 耗时约束守住（≤+10%） | **达成** |
//   | 承伤 ↓5–20%（中间带），耗时守住 | **部分达成**（有效但不充分；不停腿也不算达成） |
//   | 承伤改善 ≤5% 或反向 | **判负** |
//   | **耗时恶化 >10%**（不管承伤；按上面的 CI 口径判定） | **判负**（用「等/绕」换干净，代价不可接受） |
// **★ 统计量纪律（x2-rebirth 的血泪）**：连续量**必须带 bootstrap 95% CI**（n=300 局）
//   再下结论 —— 我曾按点估计判「耗时反向 +9%」，加 CI 后**完全在噪声内、结论不成立**
//   （与 x3 线「Δ>0 不是判据」是同一类错误）。⇒ **任何不带 CI 的连续量结论都不写进结算节。**
// **★ 最优归档保护（x2-rebirth 的第二条血泪）**：承伤**可能在很早就见底**
//   （x2-rebirth 在 **it5** 就到最优 33.3，之后 15 轮零改善、末点回退到 46.1）。
//   ⇒ 每个 eval 点记一次承伤，**最好的那个 it 立刻单独拷到
//   `nn-training/weights/<课>-best/`**，别指望 `keep_iters` 保留它。
//   中间带必须有 —— x3-step 评审的 P0-3 就是「三支判据缺中间带 ⇒ 执行时临场发明口径」。
// **统计口径（写死）**：
//   · 判决只在**池外固定段**：`--seed0 400800`（800 局，4 关各 200）。
//     **seed 台账**（2026-09-18 实测；出处 `docs/nn.progress.md §68`）：
//       | 400000–400199 | x3-power（含冻结基线）+ x3-chip-k0/k05/k10 |
//       | 400200–400399 | 已用（x3-credit-p6 文件头注明）             |
//       | 400600–400799 | x3-credit-p6 / -r2                          |
//       | **400800–400999** | **空闲 ⇒ 本腿预注册**                  |
//     **中途不得改段**（改段 = 换判决条件，L3）。复验一行：
//       `grep -rn "seed0 40[0-9][0-9][0-9][0-9]" nn-training/curricula/*.jsonc docs/*.md`
//   · 配套**锚点 200 局同种子配对**（seed0 860001）用于看位移，与判决互锁（x3-credit 体例）。
//   · `pd`（配对差异率）**5–10% = 灰度带，不单独判决**（L4）。
//   · **日常 50/关只做趋势，绝不判决**（SE≈6.65pp，L3）。
// **对照（零成本，判决时必须一起报）**：**it0 vs 终点**，同环境、同种子、同权重族，
//   唯一差异 = 训练轮数 ⇒ 这是本腿唯一合法的对照（x3-rebirth 换了环境+起点，
//   **不与 x3-start / x3-power 直接对照**，那两条是 BC 线、另一起点另一配方）。
// **掉队规则（L11）**：分关 pass **必须与 it0 同关对比** —— 任一关相对 it0 退步
//   >5pp 即判「掉队」，该 it 不得作为终点（it5 就是这样被排除的：bc 64% vs it0 74%）。
//   绝对值与「谁是最弱关」都不作判据，只看相对退步。
// **诊断项（不作判据）**：分关 pass（只用于掉队探测）、`firstHitKind` / `killerKinds`
//   （power 行为；L12：**不指望先打 power**，acd 预先点名为最可能解不开的关）、
//   零伤通关占比、entropy 轨迹、a_move 归一化熵（行为是否出峰）。
//   ⚠ **不要用「KL 是否进某个区间」判据**（a2 已证 0.01–0.03 那条带是拍出来的错的）。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 止损（**自执行** —— 吸取 L1/L2：x3-start 横盘 320 轮没人停）
// ══════════════════════════════════════════════════════════════════════════
//   · **机械横盘检测**：连续 **15 轮（= 5 个 eval 点，`eval_every=3`）** 承伤**无改善**
//     （回归斜率 |Δ|<5%），或出现「承伤回退 + ticks 变快」的漂移形态 ⇒ **立即停腿**
//     并写 GATE verdict「配方到顶」。这一条取代 x3-start 那句没人执行的条款（L2）。
//     （15 轮而非 9 轮：回归斜率在 5 点上比 3 点稳；且 40 轮刚好装得下「一次检测 + 一次宽限 10 轮」。）
//   · **宽限只给一次，上限 +10 轮**（先例出处：**`docs/nn.progress.md §68`**，
//     2026-09-18 x2-rebirth 结课条）：x2-rebirth 在 **it20** 触发本条，用户裁定宽限到
//     it25/it30 再看一次 —— 这是**允许的**，但**只允许一次、只多 10 轮**；
//     宽限到期无论结果如何**必须停**，不得二次宽限。
//     ⚠ 不设这个上限，本条就会退化成 x3-start 那种「再等等」跑到 333 轮（L2）。
//   · **宽限期的判读口径**：宽限期内若出现「承伤回到 ≤ 最优水位 +5% 且耗时守住」⇒ 取该点；
//     否则取历史最优 it（按 §4 的最优归档保护留的那份）。
//   · **it21 检点**（= 第 7 个 eval 点；eval 网格是 3/6/…/18/21，**it20 采不到数**）：
//     承伤相对 it0 **无改善（|Δ|<5% 且 pd<5%）** ⇒ 停腿，
//     判「热启 + 位移包在 3 敌关不足以改写行为」，下一手是**加朝向/距离 metric**（独立工程
//     —— 要动 TS 导出 + bump `metrics_version` ⇒ 历史语料与归档权重作废）或停在这一级。
//   · **熔断轴（s-dodge 教训，x3-power 沿用）**：`shots` 相对 it0 崩（< 0.9×）或
//     `kills` 不升反降 ⇒ 奖励把开火吓没了 ⇒ **立即停**，回退剂量。
//   · **分支二（换血）**：dmg↑ 而 wr 不降 ⇒ 即使通过率好看也判「学错了」（ladder 递进，
//     换血必崩、升不了档）⇒ 改奖励结构，**不加轮数**。
//   · **分支四（自锁）**：entropy 一步坍缩 / KL 暴涨 ⇒ 先查 bc 载入与 kickstart 量级。
//   · ⚠ **优先级：横盘检测 / it21 检点 > 一切诊断项**（含 power）—— 与 x2-rebirth 同规。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 预算
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（**启用**动态采集，与 a2 / x2-rebirth 同每轮样本量）
//   ⇒ 位移包力度不变。4 关均分 = **6000 samples/关**（正好回到 a2 的 per-stage 配额）。
//   `est_samples_per_game` = **开训前必须实测**（§开工前置②），单位 **samples ≈ ticks/10**（L9）。
// · `iters 0`（**无限轮**，2026-09-18 用户拍板：去睡觉让它一直跑）＋ `max_hours 12` 兜底。
//   ⚠ **这正是 L2（x3-start）的形态**：x3-start 就是 `iters: 0` + 人肉「60 轮证伪条款」，
//   结果条款触发了却一路跑到 333 轮。本腿的差别是：**刹车写在 §5 且带上限**，
//   但**没有任何一条是代码自执行的**（`ent_break` 只管熵崩，`max_hours` 只管 12 小时）。
//   ⇒ **人回来第一件事**就是按 §4 的「最优归档保护」把承伤最好的 it 另存，
//     再看 §5 的横盘条款有没有触发。按 x2 的经验（有效窗口 5 轮、每轮 ≈2 分钟），
//     12 小时 ≈ 300+ 轮，**99% 的轮数是无效甚至有害的**（会往「更快但更脏」漂）。
//   ⇒ 这不是推荐配置，是「没人盯」时的妥协；有人盯的时候应该 `iters` 设 20–40。
// · 不开双 run（GPU 配额）⇒ 结算文字必须写「单 run，幅度未过 run 本底」（L4）。
// · ⚠ **`until_iter` 已改 25**（不是 40）：否则 `iters=0` 时早段覆盖 1–40、尾段仍在
//   it41+ 才开始 —— 那倒不会死，但会浪费 15 轮「大位移」窗口。25 是评审查出的真 bug 的修法
//   （原 `iters 40 / until 40` ⇒ 尾段**永不可达的死代码**）。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. **依赖 x2-rebirth 达成** —— 它若判负，本腿的前提（迁移链成立）不成立，应先结案而不是开腿。
// 2. **单 run** ⇒ 任何 <2pp 的移动都不是效应（T6 实测 run 本底 ≈2pp）。
// 3. **acd 是预先点名的硬关**（power+armor 双肉，x3-start 跨 count 稳定病灶）⇒ 若本腿
//    只在 acd 上失败而其余三关改善，判决写成「**部分达成（acd 例外）**」，不要整体判负。
// 4. `cellsVisited` 仍是半解 —— 3 敌关密度更高，半解可能反而更够用；若 it20 分布不出峰，
//    下一手才是朝向 metric（独立工程）。
// 5. `est_samples_per_game` 只是首轮兜底，之后由最近 5 轮 trailing 均值覆盖。
// 6. 本腿**不回答**「从零在 3 敌关能不能学」—— 那是另一条腿（rebirth 线是热启链）。

{
  "version": 5,
  "name": "x3-rebirth",
  // 每一决策步一条样本（非 intent/goal 窗口），与当前 PPO 管线匹配。
  "mode": "per-tick",
  // 环境唯一来源 = levels/ladder-c03.jsonc（4 关 abc/abd/acd/bcd，每关 3 敌、
  // 队列 20 取前 3、max_ticks 2700、lives 1、hard）。课程侧禁止重写
  // stages/difficulty/max_ticks/player（任务侧不可动，AGENTS §0.2）。
  "level": "ladder-c03",

  // ── 奖励：与 a2 / x2-rebirth 逐字一致（见文件头 §2）────────────────────────
  "reward": {
    // ⚠ 用**标量 kills**，不用分列 killsByKind —— metrics v6 的双口径缺口
    // （bomb 清屏不进事件流，缺 11.6%）会让分列公式静默丢掉炸弹击杀信用（L7）。
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
    },
    "terminal": {
      "lives_exhausted": -5.0,
      "timeout": -2.0,
    },
    "scheme": "toy",
  },
  "dodge": "",

  // ── 起点：x2-rebirth 的**择优终点**（不是最后一个 it！）───────────────────
  // 由 x2-rebirth 结课时的同种子配对选出：承伤与 it5 并列最优（Δ=+0.6，配对 SE 3.9，
  // 变差/变好局数 41/41 完全对称）、通过率显著更高（p=0.039）、**无掉队关**；
  // it25 的承伤显著更差（Δ=+7.7，SE 3.6，z≈2.1）⇒ 不用最后一个 it。
  "bc": "nn-training/weights/x2-rebirth-best/x2-rebirth.it15.best.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算 ───────────────────────────────────────────────────────────────
  // 见文件头 §6（iters 0 无限轮 + max_hours 12，用户 2026-09-18 拍板：去睡觉一直跑）。
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：与 a2 / x2-rebirth 逐字一致（本线要外推的正是它）──────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段 it1–25（**`until_iter` 已改 25**：`iters=0` 时若还写 40，早段会拖到 it40、
    //   尾段 it41+ 才开始 —— 浪费 15 轮「大位移」窗口。25 是评审查出的真 bug 的修法。）
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段 it26+：kl 继续放松 + lr 砍半做小步精修（无限轮腿里它会一直跑到停腿为止）。
    // ⚠ `kl_cap` 两段**都显式写**：它在 per-tick 的 serial/remote 路径**不接线**
    // （DECISIONS §2026-09-15-goalnn-kl-cap-unwired，`ppo_update` 无形参），
    // 但 stream 路径会读；段内缺席即 None ⇒ 回落 `policy.streamKlCap`（本仓 0.18）
    // ⇒ 会与 kl_coef 的放松方向打架。**别把它当护栏，也别留缺席。**
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 },
  ],

  // ── 采集：严格样本量配额（与 a2 / x2-rebirth 同每轮样本量）────────────────
  "target_transitions": 24000,
  // 首轮兜底的局均 **samples**（不是 ticks，L9）：**2026-09-18 实测 128.7**
  // （it15 权重在 c03 上 4 关 × 10 局，`tmp/x3-rebirth-probe.py` → report-it15.txt）。
  // 取整 130（向上）。⚠ 不要用 x2 的 99.2 或占位 120 —— 3 敌关局更长（avgTicks 1270，
  // samples = ticks/10 ≈ 128.7），估小了首波会超采被 trim 丢弃（白烧 rollout）。
  "est_samples_per_game": 130,
  // 仅种子轮转用；volume 模式下**不参与**（种子流由 (rotate_seed, it, stage, wave) 键控）。
  // 老模式回退：多关课按关数分摊 600/4 = 150。
  "seed_rotate": 150,

  // ── 路径：bc 语义变了 ⇒ 必须新目录、禁续跑（§15.5）──────────────────────
  "out": "tmp/x3-rebirth/weights.json",
  "traj": "tmp/x3-rebirth",
  // 熔断线：它是「探索崩掉」的护栏，不是「正常下降」的阈值。
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x3-rebirth",
  "backup_prefix": "x3-rebirth",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 4 关：ID 2000-2003（abc/abd/acd/bcd）。日常 50/关（4×50=200 局/次，**只做趋势**）；
  // 判决走池外固定段（§4）。
  // L10：**eval_every 5**（用户 2026-09-18 拍板：去睡觉没人盯，5 一采省开销）。
  //   ⚠ 与 L10 的「早期密集采样」矛盾 —— 那是「有人盯」时的最优；无人盯时 5 的代价是
  //   可能漏掉早期最优点（x2 的承伤就在 it5 见底）。**靠 §4 的最优归档保护 + 人回来第一件事
  //   捞最优 it 来兜**。判决走池外固定段（§4），日常 50/关只做趋势。
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5,
}

// **启用 `target_transitions`（动态采集）** —— 与 x3 系列各腿不同（它们全部不启用），
//   本腿跟随 rebirth 线：局时长随训练剧烈变化，固定局数会让每轮样本量漂移。

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① **bc 已填实** = `nn-training/weights/x2-rebirth-best/x2-rebirth.it15.best.json`
//    —— x2-rebirth **按 §4 最优归档保护选出的择优终点**（证据与配对统计见
//    `docs/nn.progress.md §68`）。⚠ **不要改成「取 it 最大的归档」**：那正是 L10 要消灭的
//    思维（x2 的 it25 承伤比 it15 显著更差，Δ=+7.7，SE 3.6，z≈2.1）。
//    开训前核：`schema_major 3`、参数键集与 `scratch-init.json` 同集（42 键）、文件存在。
// ② **it0 探针（已跑，2026-09-18）** —— 结果见 `tmp/x3-rebirth-probe/`：
//    a) 贪心 4 关 × 50 局（`it15-c03-eval.jsonl`）：**pass 144/200 = 72.0%**、
//       dmg 115.1/局、avgTicks 1112（gameover 57）。
//    b) rollout 语料 4 关 × 10 局（`rollout-it15` / `report-it15.txt`）：
//       **全 40 局通关（sampled 口径）**、dmg 107.7/局、**est_samples_per_game 128.7**（已写入配置）、
//       **每格边际承伤 +0.64 dmg/格**（⚠ 是**正的** —— 3 敌关走得多挨得多，与 x2 起点的 −0.95 相反；
//       EV 表仍是 走 > 坐 > 莽，剂量沿用 a2 不改）。
//    → 判据分母（it0 基线）= 上面的贪心 200 局数；开训前若有变化以 `it15-c03-eval.jsonl` 为准。
// ③ transport：**必须显式 `--remote-transport pull`**（L6 —— auto 会推全局 gpu_push 节点）。
// ④ 新 out/traj 目录（bc 语义变了 ⇒ 禁续跑，§15.5）。
// ⑤ 若与其它课并行：先确认 `rl-config.json` 的 `courses` 块总和不超裸机容量
//    （`dashboard/src/core/slots.ts:checkCapacity`）；**并给每条腿登记互不重复的
//    `courses.<课>.slot`（0–3）** —— 未登记的课全部落 slot 0、共享 8787 的 hub，
//    而控制台「hub-server 已在运行」是幂等成功**不报错** ⇒ 第二条腿会静默挂在第一条腿的 hub 上。
// ⑥ rollout 节点：先把 a95/a97 弄活（a2 实测本机承担 98%），零代码 2×。
//
// 启动（**用户手工；agent 不得启动训练**）：
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x3-rebirth \
//     --remote-transport pull
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 起跑线 / 边际 / EV 表：`tmp/x3-rebirth-probe.py`（照 x2 版改 4 关 + max_ticks 2700）
// · 分关 + power 读数：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c03.jsonc`
//   （行内含 `firstHitKind` / `killerKinds` / `killsByKind`）
// · 判决（池外固定段）：`bun tools/sim/eval-course-ckpt.ts ... --games 800 --seed0 400800`
// · 走势 / 动作分布 / 时间账：`tmp/x1-trend.py` / `tmp/x1-acthist2.py` / `tmp/a2-timing.py`
//   （改路径；⚠ shard 每轮被清理，动作分布诊断要及时做）
//
// ── 结算节（2026-09-18 结课回填；it153，用户人工停）────────────────────────
// **判决 = 达成**（承伤主端点达标 + 耗时约束守住 + 无掉队关；单 run，幅度远超 run 本底）。
// 运行实际：00:45–05:05，153 轮 / ~4.3h（`iters 0` 无限轮 + 人工停，用户拍板）；
//   `eval_every 5`（日常 31 次）、`est 130`（探针 128.7 取整）、`stream 0`、`pull`；
//   rollout 节点 a97 全程在线（每轮 5–6 局）+ mac ~105–115 局 ⇒ 远端分担约 28%
//   （x2 的 3.8% ⇒ L13 部分落地，但本机仍扛 ~72%）。
//
// ① **it0 vs 终点 it153**（池外固定段 seed0 400800，800 局 = 4 关各 200，同种子配对）：
//   | | it0（bc） | it153 | Δ |
//   | pass | 589/800 = 73.62% | 739/800 = **92.37%** | McNemar b01=187/b10=37，pd=**+18.75pp**，exact p<0.0001 |
//   | dmg/局 | 103.5 [97.5,109.5] | **56.4 [51.1,61.6]** | 配对 Δ=**−47.2**（SE 3.8，z=−12.4；变好 386 / 变差 134 / 持平 280），**−45.5%，CI 不重叠** ⇒ 主端点达标（判线 ↓≥20%） |
//   | 通关 ticks | 1342 [1309,1375] | **1227 [1202,1253]** | **−8.6%，CI 不重叠且方向是改善** ⇒ 约束（≤+10%）守住，且有余量 |
//   CI = bootstrap 95%（B=10000）。耗时约束按 §4 的 CI 口径：点估计 −8.6% < +10%，
//   CI 上界也不碰线 ⇒ 「守住」，无需动用提前判负条款。
// ② **锚点配对 pd**：判决段内同种子配对 pd=+18.75pp（b01=187/b10=37），
//   远超 5–10% 灰度带 ⇒ 不在灰度内，无需"不单独判决"。日常锚点（860001 段）只做趋势，未入判决。
// ③ **分关 pass**（it0 → it153；括号内日常终点 it150 作交叉核对）：
//   abc 63.0% → **93.5%**（日常 88%）｜ abd 86.0% → **99.0%**（97%）｜
//   **acd 70.0% → 87.5%**（85%；预先点名的硬关，x3-start 被钉死 59–60 的病灶被拔掉）｜
//   bcd 75.5% → **89.5%**（91%）。四关相对 it0 全部提升（+13 ~ +30pp），**无掉队关**
//   （掉队线 = 相对 it0 退步 >5pp，无一触发）。
// ④ **诊断项**：零伤通关占比（通关局中 dmg=0）48.6% → **64.7%**；
//   `firstHitKind`（800 局）：power 先打 bc 8（1.0%）→ it153 **4（0.5%**，basic 597/fast 199）
//   ⇒ **power 盲区未修**（L12 预言正确：不指望，确实没修）；
//   `killerKinds` 死于 power：bc 166 局 → it153 **60 局** ⇒ 靠"别的能力"赢的，与 x2 同构
//   （x2：ac 30%→96% 但先打 power 恒 0%）。acd 能解不是因为学会了先打 power。
// ⑤ **横盘检测**：日常锚点 it135–150 dmg 回归斜率 **+1.21%/轮**（|Δ|<5% ⇒ 无改善，
//   但也未出现「承伤回退 + ticks 变快」的漂移形态；ticks 同期还在变快 1195–1295）；
//   且该漂移在池外段**未复现**：it153 vs 日常最优 it120，dmg Δ=+1.0（SE 3.2，z=+0.32），
//   pass pd=+1.38pp（p=0.29）⇒ **不可区分**，属锚点特异波动。横盘停腿条款与宽限条款均未触发
//   （终点即末 it，无需宽限）。
// ⑥ **样本量**：`transitions_collected` 153 轮均值 **25079** vs 配额 24000 ⇒ **+4.5% 超采**
//   （首轮兜底 130 vs 实测 trailing 均值差异；浪费可接受）。entropy 0.921 → 0.606
//   （−34%，缓降健康）；KL max **0.0217**（a2 mean 0.0059 的 ~3.7 倍，位移杠杆在本腿继续生效）；
//   全程无熔断（`ent_break`/`kl_break` 均未触发）。
//
// **终点选取**：候选 it120（dmg 55.4 [50.0,60.8]，pass 91.00%）、it130（57.0，90.38%）、
//   it153（56.4，92.37%）三者 dmg 互比 |z|<0.6、pass 互比 p>0.1 ⇒ **互相不可区分**；
//   按 x2-rebirth §68 的先例（dmg 并列 ⇒ 取 pass 高者），**终点 = it153**
//   （pass 点估计最高 + 末 it + 无掉队）。已另存
//   `nn-training/weights/x3-rebirth-best/x3-rebirth.it153.best.json`
//   （379KB、`nn-weights-json`、`schema_major 3`、42 键，已核验）。
//   判决语料：`tmp/x3-rebirth-judge/eval.jsonl`（3200 行 = 4 候选 × 800 局）+ `judge.py` / `pick.py` / `misc.py`。
//
// **回答立项三问**：① 迁移链 c01→c02→c03 **全线贯通**（承伤 −45.5%、耗时 −8.6%、无掉队）；
//   ② acd 双肉关 70.0% → 87.5%，**从零长出来的策略啃动了**（但靠的不是先打 power）；
//   ③ 朝向信号：**仍未证伪半解够用** —— 3 敌关 `cellsVisited` 照样把承伤砍半，
//   a2 open limitation #2 留给下一级（c04+）再撞。
// ⚠ 单 run ⇒ 以上幅度虽远超 run 本底（≈2pp），结算仍按 L4 写明「单 run」。
// ⚠ 运行参数与立项版差异（用户 2026-09-18 拍板，非事故）：`iters 0`（L10 原写 40）、
//   `eval_every 5`（原写 3）、`est 130`（探针实测 128.7，前置②已执行）、`stream 0`。
//   kickstart 日志值全程 ≈0（缰绳实际没勒上，迁移靠权重初始化本身）—— 结果好，如实记录，不追。
