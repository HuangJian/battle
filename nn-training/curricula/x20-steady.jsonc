// 课程 x20-steady —— **降方差腿**（B 方案）
//
// 状态：**已收官（2026-09-20 停腿）** —— 175 轮，终点 it175；判决段 low% 42.9→35.5
//   （−7.4pp，门 −7.9pp，差 0.5pp/4 局，字面未过，差距在噪声内），p10 1→2 ✅，
//   mean 6.71→7.84 ✅，回测门通过。结论：有效、强改善、门边未过（详见本文件结算节）。
//
// 与 x20-floor（A 方案）的关系：
//   A 腿跑了 115 轮 ⇒ low%(kills≤3) 从 44.0% → 41.2%（**−2.8pp**），而门是 −7.9pp；
//   p10 恒 1；pooled 在 6.05–7.25 之间**震荡、无趋势**。
//   用户判断（2026-09-20）：「1v20 每一滴血都是宝贵的，换参改不了什么」⇒ 转向 B。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 一句话
// ══════════════════════════════════════════════════════════════════════════
// **只改"怎么学"（降学习信号的方差），不改"学什么"（奖励回到 x20-rebirth 原公式）。**
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 立项依据
// ══════════════════════════════════════════════════════════════════════════
// · **为什么放弃"改奖励"**：本关血是**环境常数**（hp 263、敌人单发 100~128 ⇒ 一局约能挨 2.6 发）。
//   P0-1 已证明**被命中次数 NN 与 God 完全相同（2.60 vs 2.60）**，差别只在"挨打的速度"
//   （God 把额度摊在 2117t、NN 只用 525t）。⇒ 这不是"敢不敢打"的取舍，**怎么定价都改不了 2.6 发**
//   —— 罚重了只会更保守（A 腿实测：A 项把"走"的收益也砍了 18 分，最终只有震荡、没有趋势）。
// · **为什么怀疑"方差"**：A 腿 115 轮的曲线是 6.05→7.25→6.05→…**无趋势的来回**，
//   且同一策略在同一批 seed 上命运两极（10 局 20 杀 / 10 局 1 杀）⇒ 典型的"学习信号被噪声主导"。
// · **配额已恢复**（2026-09-20）：B 需要更大采样，现在付得起。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. 唯一变量：一次看更多数据再改
// ══════════════════════════════════════════════════════════════════════════
// 大白话：教练每看几十局就改一次打法，但每批局的**运气差别很大**（有的局敌人特别凶、有的特别菜）
//   ⇒ 教练被运气牵着走 ⇒ 改来改去在原地晃。
//   B = **一次看更多局再改**，让运气在更大的样本里互相抵消。
//
// 具体两个旋钮（同一语义，一起动）：
//   ① `target_transitions` 24000 → **48000**（每轮采样量翻倍：41 → 82 局/关，4 关共 328 局/轮）
//   ② `mb`（PPO minibatch）512 → **1024**（每次梯度更新的样本翻倍 ⇒ 梯度更稳）
//
// **不动**：lr（3e-4）、epochs（4）、gamma（0.998）、lam（0.95）、kl_coef（0.05）、
//   位移包（回基线，沿用 x20-floor）、**奖励公式（移除 A 项，回到 x20-rebirth 原版）**。
//
// ⚠ 诚实记录"单变量纯度"：① 和 ② 是两个旋钮，但**语义同一**（都是"一次看更多数据"），
//   分开动反而测不出东西 ⇒ 视为一个复合变量。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 判据（与 x20-floor 完全一致，保证可比）
// ══════════════════════════════════════════════════════════════════════════
// **主读数 = 「kills ≤ 3 的局占比」**（贪心，池外判决段 414000，800 局）：it0 = **42.9%**。
// **辅助读数 = p10 kills**：it0 = **1**（⚠ 只由 ~13 个局决定 ⇒ 不稳健，只作辅助）。
// **日常趋势口径**（每 5 轮，860001 段 400 局）：it0 = **mean 6.88 / low% 44.0% / pass 12.5%**。
// **门**（相对 it0 的改善）：低分局占比 **≤35%（≈ −7.9pp）** 且 `p10 ≥ 2`。
// **约束（熔断优先级最高）**：
//   ① 上限熔断：日常 mean 跌破 **6.19**（it0 6.88 的 −10%）⇒ 立即停；
//   ② 分关（4 关）任一处退步 >5pp ⇒ 掉队；
//   ③ 超时熔断：`timeout_frac > 5%` ⇒ 立即停。
// **回测门**：终点候选必须在 `--seed0 415000`（诊断段）上不劣于 it0（同段同种子配对）。
// **判据一律不得由教师（God）表现反推**（AGENTS §0.2）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 止损
// ══════════════════════════════════════════════════════════════════════════
//   · **主检点 it40**：low% 未从 44.0%（日常）明显下移（≥ −3pp）⇒ 停腿，判「方差不是主因」。
//     **【事后：未执行，且执行会误杀】** it40 日常 low% = 44.2（+0.2pp），但转折在 it90
//     才来；it40 只占终身数据量的 23%（见轮数核算），对 B 而言设得太早。
//   · **上限熔断**：日常 mean < 6.19 ⇒ 立即停（优先级高于一切）。
//   · **机械横盘**：low% 连续 4 个 eval 点无改善 ⇒ 停腿；宽限一次 +10 轮。
//   · **超时熔断**：`timeout_frac > 5%` ⇒ 立即停。
//   · ⚠ **轮数核算**：本腿每轮耗时约为 x20-floor 的 **2 倍** ⇒ 12 小时约跑 **60 轮**
//     （x20-floor 是 118 轮）。it40 主检点仍在预算内；若到 it40 时已用 >9 小时 ⇒ 直接判横盘停。
//     **【事后：估算错约 3 倍】** 实际 ~3.6min/轮，10.5 小时跑出 **175 轮**（8.4M transitions）。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 起点与缰绳（与 x20-floor 同起点 ⇒ 两条腿可直接对照）
// ══════════════════════════════════════════════════════════════════════════
// bc = `nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json`
//   （与 x20-floor 同一个起点 ⇒ A/B 两腿结果可直接对照）
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求）。
// 位移包 = **回基线**（lr 3e-4、epochs 4、kl_coef 0.05、单段），与 x20-floor 一致。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. **每轮 2 倍耗时** ⇒ 同样 12 小时只有约一半轮数；若真实瓶颈是"需要更多轮"而非
//    "每轮更稳"，B 会更差 —— 这正是本腿要证伪的。
// 2. **单变量是复合的**（采样量 + batch 一起动），若生效，不能分清是哪一个的作用。
// 3. **A 项已移除** ⇒ 本腿不检验"奖励定价"那条路（x20-floor 已给出阴性结果）。
// 4. 若 B 也无效 ⇒ 下一步应考虑 **C（难局加权）**，或接受"下限由环境决定"这一结论
//    （P0-3 已显示：God 在 LOW 组那 10 个 seed 上 p10 也是 0）。

{
  "version": 5,
  "name": "x20-steady",
  "mode": "per-tick",
  // 环境不变：ladder-c20-lives1（4 关 2000-2003、20 敌、max_ticks 12900、lives 1、hard）。
  // 任务侧一个字不动（AGENTS §0.2）。
  "level": "ladder-c20-lives1",

  // ── 奖励：**回到 x20-rebirth 原公式（移除 A 项）** ──────────────────────
  // 本腿只改"怎么学"，不改"学什么" ⇒ wDmgLate / dmgKnee 全部撤掉。
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      "wApproach": 0.008
    },
    // dense-only（与父腿一致）：terminal 空表合法。
    "terminal": {},
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点：与 x20-floor 同一个 it30 ⇒ 两腿可直接对照 ─────────────────────
  "bc": "nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算 ───────────────────────────────────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：回基线（与 x20-floor 一致，不是本腿变量）─────────────────────
  "lr": 0.0003,
  "epochs": 4,
  // ★ 唯一变量 ②：PPO minibatch 512 → 1024（梯度更稳）
  "mb": 1024,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    { "kl_coef": 0.05, "lr": 0.0003, "kl_cap": 0.2 }
  ],

  // ── 采集 ★ 唯一变量 ①：每轮采样量 24000 → 48000（41 → 82 局/关）─────────
  "target_transitions": 48000,
  "est_samples_per_game": 147,
  "seed_rotate": 150,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x20-steady/weights.json",
  "traj": "tmp/x20-steady",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x20-steady",
  "backup_prefix": "x20-steady",

  // ── 评估 ───────────────────────────────────────────────────────────────
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc = `nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json`（已存在）。
// ② **配额已恢复**（用户 2026-09-20 确认）⇒ 本腿每轮采样翻倍，需确认节点容量登记了 slot。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`；确认本机 self（127.0.0.1:8443）在线。
// ⑥ 确认 `remote_hubs["x20-steady"]` 已写入 rl-config（若沿用默认值要人工核对）；
//    ⚠ 上次 x20-floor 就出现过「run_rl 冻结的 hub ≠ rl-config 里课程隧道」的情况。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//
// 开训后 30 秒自检：`grep -h "hub=" tmp/x20-steady/training-loop.log | tail -1`
//   必须等于 rl-config.json 里**当时活着**的那个 hub URL（否则全天 530 且不自愈，只能重启）。
//   ⚠ 统计 530 要用带边界的模式（如 `HTTP 530`），裸 `530` 会命中 seed 号造成误报。
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 日常趋势：`... --games 400 --seed0 860001`（日常口径，只做趋势）
// · 判决/回测：`--seed0 414000`（判决段，800 局）与 `--seed0 415000`（回测段，800 局）
// · 监控脚本：`tmp/x20-floor-watch.py`（改路径即可复用）→ 按 eval 点出 low%/p10/mean + 熔断提示
// · 若再出现"区分不开"：`docs/nn-diagnosis-methodology.md` + `docs/nn-diagnosis-toolkit.md`
//
// ── 结算节（2026-09-20 回填，终点 = it175 `x20-steady.it175.20260920-215502.json`）──
// ① low% 与 p10（判决段 414000，800 局，`--no-dist` 与 it0 基线同口径）：
//    low% 42.9 → **35.5（95CI 32.3–38.9，−7.4pp）**，门 ≤35（−7.9pp）——**差 0.5pp/4 局，
//    字面未过，差距在噪声地板（~2pp）之内**；p10 1 → **2** ✅。不重跑刷门（见 §110）。
//    回测段（415000，800 局）：low% 42.0 → **38.8（it135）/ 38.9（it175）**，mean
//    6.34 → **7.53 / 7.76**，pass 7.6% → **14.2% / 15.4%** —— 回测门通过。
// ② pooled 均杀：判决段 6.71 → **7.84（+1.13）**，约束"不得下降"通过；日常 6.88 → 终点区
//    7.7–8.0（it170 8.04 全程最高）；pass 10.2% → **15.1%**。上限熔断线 6.19 从未接近。
// ③ 分关表（终点，判决段 414000，每关 200 局）：s2000 7.30/40.0%/p10 1 ·
//    s2001 6.76/41.5%/p10 1（最弱，mean 全场最低）· s2002 8.44/29.5%/p10 2 ·
//    s2003 8.85/31.0%/p10 2。日常 it0 分关（52/46/38/40）对照：四关全改善，无掉队（约束②通过）。
// ④ 与 x20-floor（A）对照：A 115 轮 −2.8pp（2.76M transitions）/ 无趋势；B 175 轮判决 −7.4pp、
//    日常 −10pp（8.4M）。B 明显更优，**但转折（it90）累计 4.32M = 1.6× A 终身数据量** ⇒
//    分不清"每轮更稳"还是"总量更多"（总量混杂；课程自查的单变量纯度只管住采样+batch 复合，
//    漏了总量）。震荡幅度未收敛（日常 low% 终身在 33–48 带走）。"方差是主因"**没有被证明**。
// ⑤ 超时/换血：终点判决 800 局结局只有 gameover/stage_clear，**超时 0**（约束③通过）；
//    换血 dmg/局 = 248（≈ hp 263 的 94%，仍是一局 ~2.6 发的老额度），被命中 1.00 次/局 ——
//    血额度纹丝不动（与立项依据一致：定价改不了 2.6 发，B 也没动它）。
// ⑥ 实际轮数与耗时：it1–175（11:26→21:55，~10.5h），**~3.6min/轮** —— "2 倍耗时→60 轮"
//    的估算错约 3 倍；it40 主检点只占终身数据 23%。另：it76 停机重启无损（§107），
//    `--no-dist` 评估已封存、默认走分布式（DECISIONS §2026-09-20-eval-prefers-dist）。
// 终局判语：**有效、强改善、门边未过**。下一步：C（难局加权，先啃 s2000/s2001 的 LOW seed）
//   或接受环境下限（P0-3：God 在 LOW 组 p10 也是 0）。全程记录：docs/nn.progress.md §107–110。
