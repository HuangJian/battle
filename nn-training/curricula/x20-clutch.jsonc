// 课程 x20-clutch —— **C 腿加权臂**（与 x20-clutch-null 配对并行）
//
// 状态：**已起草，待用户手工开训**（agent 不得启动训练；与 C-0 两臂同时开）。
//
// 与 B 腿（x20-steady）的关系：
//   B 终点判决 low% 42.9→35.5（−7.4pp，门差 0.5pp/4 局），p10 1→2，mean 6.71→7.84。
//   §112：NN 均值追平教师（16.08 vs 16.31，两命）、下限反超，唯一缺口是通关转化
//   （pass −8.6pp）。§113：败局 = 开局事故 275（不可学）/ 中段 272 / 残局惜败 123
//   （89% 可转化，81 局教师同 seed 能清）。
//   ⇒ 本腿只治"残局转化"，不动采样、不动超参、不动起点配对（C-0 逐字相同）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 一句话
// ══════════════════════════════════════════════════════════════════════════
// **给"差一口气"的败局发安慰奖**：10+ 杀仍命丧的局 +6，其余一字不动。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 立项依据
// ══════════════════════════════════════════════════════════════════════════
// · **瓶颈不在下限在转化**（§112）：两命下 NN 均值/下限已平超教师，pass 差 8.6pp ⇒
//   败局里空刷杀、转不成通关。一命判决里 123 个 10–19 杀惜败局就是这批"差一口气"的局。
// · **靶子可学**（§113）：惜败局 89% 可转化（至少一方两命能转），66% 教师同 seed 通关；
//   死亡中位 3002t（打了 2/3 才死），不是开局事故。275 开局事故局本腿**不碰**（采样不动）。
// · **零代码**：项下每一条都是现成机制（见 §2），配平方可开训，不欠任何前置实现。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. 唯一变量：惜败奖（纯配置，2026-09-20 已编译+行为实测）
// ══════════════════════════════════════════════════════════════════════════
// 公式 = B 原公式 + 一项：
//   `+ wClutch * is_lives_exhausted * where(kills >= 10, kills - 9, 0)`，`wClutch = 1.0`。
// （2026-09-20 评审修正：初版阶跃 `where(kills>=10,1,0)` diff 后只剩第 10 杀一个尖峰，
//   12 杀与 19 杀惜败拿同样的钱、残局段零梯度 —— 打不到靶子。斜坡版把梯度铺到残局：
//   10 杀之后每多杀一份，19 杀惜败拿 10 倍于 10 杀惜败。）
//
// · **机制现成**：outcome 虚拟符号（`rl/reward_library.py:134`，`is_lives_exhausted`
//   0/1 逐局注入）+ `where`/`>=`/`*` 全在公式白名单。训练结局只有
//   lives_exhausted/stage_clear（it175 实测 196+35，无其他）⇒ 该项覆盖**全部**败局。
// · **行为实测**（venv 实跑合成数据，文件内公式原样）：19 杀敗局 +10.0（10 步各 +1.0，
//   落在第 10–19 杀）；12 杀 +3.0；10 杀 +1.0；9 杀及以下 +0；通关局 +0
//  （`is_lives_exhausted=0` 杀死整项）。残局每杀价值 3.0→4.0（+33%），梯度铺满残局。
// · **为什么是斜坡不是阶跃**：阶跃版（初稿 `where(kills>=10,1,0)`）diff 后只剩第 10 杀
//   一个 +6 尖峰 —— 12 杀与 19 杀惜败拿同样的钱，残局段零梯度，实际教的是"别在第 10 杀
//   前死"（中局生存），打不到"残局转化"的靶子，且第 10 杀边际价值 3 倍尖峰（非单调塑形）。
//   斜坡版 19 杀拿 10 倍于 10 杀，转化行为逐杀计价（2026-09-20 评审修正，已改）。
// · **为什么没有自杀动机**：奖逐杀入账、通关项恒 0 —— 19 杀时自杀不多拿一分，继续打每杀
//   至少 +3.0；想拿大头（通关 dense ~60+）必须真通关。
// · **为什么阈值=10、定价=1.0**：10 = clutch 定义下沿（§113），mid（4–9）/ low 原封不动；
//   1.0 = 残局杀 +33%，小量塑形。单阈值保单变量纯度（最优值若存在，下一轮再扫）。
//
// **备选与否决**：
// - **复盘过采样**（clutch/mid seed 列表每轮加权复采）——否：要动 seed 调度代码 +
//   固定 395 seed 每轮重复 = 记忆化风险（§15），而评估段是池外的，背下来也白背；
//   惜败奖泛化到一切未见 seed，零代码，语义更干净。
// - **奖励乘子**（惜败局整局奖励 ×N）——否：乘子同时放大"杀"的 dense，稀释阈值语义；
//   且乘子是"学什么"的大改，与 A 腿奖励路纠缠。阈值奖是加法小项，可逆可度量。
// - **通关 terminal 大奖**——否：1 命通关本就 dense ~60+，再加 terminal 是给已会的行为
//   加码，对"差一口气"没有边际信息；且会抬高"苟到通关"的收益，方向反了。
// - **把 275 开局事故也纳入加权/塑形**——否（§113：不可学；权重倒进去是稀释，塑形是教认命）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 判据（主读数 = 配对差，不是绝对门）
// ══════════════════════════════════════════════════════════════════════════
// **主读数 = 终点判决段配对差 + McNemar 配对检验**（414000，800 局，两臂**各自同 seed**跑）：
//   `C0 low% − Cw low%`（正 = 加权好；配对相减消掉段噪声）+ McNemar（同 seed 配对的 low 指示
//   变量，只看 discordant 对 —— 配对比较的正确检验，不是双比例 z）。
// **门**：差值 **≥ +2pp 且 McNemar p < 0.05**；Cw p10 ≥ C0 p10（同段比）。
// **|差| < 2pp = 「无结论」，不是「无效」**（800 局 SE ~1.7pp，零效应也常晃到 ±2pp）。
// **配对失败回退**：若开腿核对发现两臂 rotateSeed 不等（§5），verdict 自动退回非配对双比例
//   z 检验，门提到差值 ≥ +5pp（非配对差 SE ~2.4pp，+2pp 就是噪声）。回退分支也要响亮记录。
// **约束**：Cw mean ≥ C0 mean − 0.3（上限不丢）· 超时 ≤5% · 分关（vs C0 终点分关）无 >5pp 退步。
// **回测门**：415000 上 Cw 不劣于 C0（各自 800 局；防"判决好看、难局更崩"）。
// **日常**（860001，每 5 轮）：只看**差值趋势**，不判决（§108：日常偏乐观 ~6pp）。
// **判据一律不得由教师表现反推**（AGENTS §0.2；教师只在 B_gap 9 seeds 作参照，不进门）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 止损（含 6h 自适应：输的臂杀掉还容量）
// ══════════════════════════════════════════════════════════════════════════
//   · **6h 中点看日常差值**（400 局口径，阈值放宽）：**连续 2 个 eval 点差值 < −3pp**
//     ⇒ **杀 Cw**，容量还给 C-0（本腿证伪，C-0 自动转正为"B 续命臂"）；否则双臂跑满。
//     （单点 ±2pp 误杀率约一半，不做杀臂判据；|差|小 = 无结论，继续跑。）
//   · **上限熔断**：任一臂日常 mean < 6.19（B 的 it0−10% 线，两臂起点 ~7.6，触发即真崩）
//     ⇒ 停该臂（优先级高于一切）。
//   · **超时熔断**：任一臂 `timeout_frac > 5%` ⇒ 停该臂。
//   · ⚠ **容量现实**：两臂共享集群各约半速；B 全速 175 轮/10.5h ⇒ 每臂 12h 约 85–90 轮。
//     中点 verdict 前移就是为此准备的 —— 比较不需要等到过门。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 起点与缰绳（与 C-0 配对 ⇒ 除公式外逐字相同）
// ══════════════════════════════════════════════════════════════════════════
// bc = `nn-training/weights/x20-steady/x20-steady.it175.20260920-215502.json`
//   （B 终点；两臂同起点 ⇒ 配对差干净）
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求）。
// 位移包 = 回基线（lr 3e-4、epochs 4、kl_coef 0.05、单段），采样 48000 / mb 1024，
//   gamma 0.998 / lam 0.95 —— 全与 B 一致（C-0 亦然）。
// **配对要求**：两臂同 `--seed` **且同 `--rotate-seed <V>`** 起跑（V 任取，如 20260920；
//   seed draw keyed by `(rotateSeed, it)` ⇒ 轮轮同数据）+ 同 `seed_rotate = 150`。
//   旧教训：全新跑 rotateSeed 带时刻抖动（`loop_core` 旧逻辑），同 `--seed` 不同秒起 ⇒
//   种子流完全不同 ⇒ §5 那句"轮轮同数据"是错的（2026-09-20 评审抓获，已修：显式覆盖）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. **单阈值/单定价未扫参**（10 / 6.0 一次拍板）：若差值≈0，分不清"方向错"还是
//    "定价错" —— 中点先看差值**符号**，定价问题留待下一轮（符号对了再扫参）。
// 2. 只奖 lives_exhausted（1 命下 = 全部败局，见 §2，现状无妨；若将来出现
//    base_destroyed 高杀局，该项会漏奖 —— 届时再议，不预支复杂度）。
// 3. 惜败奖 lands 在第 10 杀那一步（dense 化）：严格说是"撑过 10 杀奖"而非"残局奖" ——
//    但 10–19 杀惜败局的残局行为正在跨过 10 杀之后，塑形位置正确。
// 4. 两臂共享集群：若一方抢占容量导致另一方挨饿，中点差值会混入容量噪声 ——
//    接受（差值 verdict 本来就比绝对门糙一档；真到差 0.5pp 这种边线再个案分析）。
// 5. **半速风险**（2026-09-20 评审）：B 的转折在 it90（累计 4.3M）；双臂各半速 12h 约
//    85–90 轮 ⇒ 很可能跑完两条都没信号，得到「无结论」而不是证伪。接受（差值 verdict 比
//    绝对门需要的数据少，部分对冲；中点规则防误杀，不防无结论 —— 无结论也是答案）。
// 6. **配对是代码保证的**：`--rotate-seed` 同值 → `rl/course.py::resolve_rotate_seed` 三级
//    优先级（显式 > 继承 > 抖动），单测 `tests/test_rotate_seed_override.py`。
//    同 `--seed` 不同 `--rotate-seed`（或不传）= 没配对，verdict 自动走 §3 回退分支。
//
{
  "version": 5,
  "name": "x20-clutch",
  "mode": "per-tick",
  // 环境不变：ladder-c20-lives1（4 关 2000-2003、20 敌、max_ticks 12900、lives 1、hard）。
  // 任务侧一个字不动（AGENTS §0.2）。
  "level": "ladder-c20-lives1",

  // ── 奖励：B 原公式 + 唯一变量惜败奖 ─────────────────────────────────────
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist) + wClutch * is_lives_exhausted * where(kills >= 10, kills - 9, 0)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      "wApproach": 0.008,
      "wClutch": 1.0
    },
    // dense-only（与父腿一致）：terminal 空表合法。
    "terminal": {},
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点：B 终点 it175（与 C-0 同起点 ⇒ 配对）────────────────────────────
  "bc": "nn-training/weights/x20-steady/x20-steady.it175.20260920-215502.json",
  "kickstart_ref": true,

  // ── 配对（plan/accident.plan.md §2，2026-09-21）────────────────────────────
  // 与 x20-clutch-null **同一把 V**（机器按「同 V = 配对」核对两臂账本的 run_start.rotateSeed）。
  // 20260921 = 2026-09-21 C 双腿实际起跑用的那把值（`tmp/x20-clutch{,-null}/training_log.jsonl`
  // 的 run_start.rotateSeed，两臂一致）——写进文件 = 复现那次配对，且不再靠人手传。
  // ⚠ 改它 = `corpus_identity_fp` 变 ⇒ 本地一次全量重采（用户 2026-09-21 已裁决接受）。
  "paired_rotate_seed": 20260921,
  "warmup_iters": 0,

  // ── 预算（两臂共享集群，各约半速）────────────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：回基线（与 B 一致，不是本腿变量）─────────────────────────────
  "lr": 0.0003,
  "epochs": 4,
  "mb": 1024,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    { "kl_coef": 0.05, "lr": 0.0003, "kl_cap": 0.2 }
  ],

  // ── 采集：与 B 一致（本腿不动采样）────────────────────────────────────────
  "target_transitions": 48000,
  "est_samples_per_game": 147,
  "seed_rotate": 150,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x20-clutch/weights.json",
  "traj": "tmp/x20-clutch",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x20-clutch",
  "backup_prefix": "x20-clutch",

  // ── 评估 ───────────────────────────────────────────────────────────────
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc = `nn-training/weights/x20-steady/x20-steady.it175.20260920-215502.json`（已存在）。
// ② 两臂 hub 解析同源：`remote_hubs` 有条目用条目，无条目则确认两臂回落到**同一** hub 与
//    默认（对称回落可比，非对称不可开；tunnel URL 高频变、rl-config 是 git-ignored 的 live
//    配置，**不手改**，走控制台流程登记）。确认节点容量登记了**两课并行**的 slot。
//    传输不再是课程/启动参数：`rl.hub_push`（缺省开）+ 登记节点 + hub 队列裁决
//    （2026-09-19 起课程与 worker 节点正交；旧 `--remote-transport` 旗标仅历史记录）。
// ③ **配对 rotateSeed 写在课程文件里**（`paired_rotate_seed`，见下面配置块）：两臂各自的
//    文件写**同一把 V** ⇒ 起跑即配对，**不再经手指传**（2026-09-21 §2 事故：手工传漏/传错
//    1789926833 vs 1789926915 ⇒ 配对失败返工）。`--rotate-seed` 仅作调试后门。
//    同 V 即“配对”的机器含义：训练侧启动自检与控制台开课回执都按它核对两臂账本的
//    `run_start.rotateSeed`（不等 ⇒ 响亮；本课声明与实际不符 ⇒ 拒启）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 公式编译+行为已验证（本文件 §2，2026-09-20 venv 实测），开训无需代码前置。
//
// 启动：**控制台开课**（`bun run dashboard` → http://127.0.0.1:8900，课程选择器选本课 → 开课；
//   两臂各按一次）。裸 CLI 启动已下线（漏开课标记/暂停解禁/hub 模式三件套，plan/accident.plan.md §1）：
//   任何训练操作经控制台；agent 不得启动训练。V 与 hub 解析都不再是启动参数。
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 日常差值：两臂同 it 的日常 low% 相减（`... --games 400 --seed0 860001` 各自口径，只做趋势）
// · 判决/回测：两臂**各自** `--seed0 414000`（800 局）与 `--seed0 415000`（800 局），走分布式
//   （DECISIONS §2026-09-20-eval-prefers-dist · 全文 → docs/nn/experiments.md §33；`--no-dist` 已封存）
// · 监控脚本：`tmp/x20-floor-watch.py`（改路径即可复用）→ 按 eval 点出两臂差值 + 熔断提示
//
// ── 结算节（跑完回填）────────────────────────────────────────────────────
// 必填：① 判决段配对差（C0−Cw，带各自 CI）与 McNemar p ② 两臂各自 vs it0 / B 终点的绝对对照
//   ③ 分关差表（Cw−C0，定位加权在哪关生效/失效）④ PPO 健康对照（kl/ent 两臂并排）
//   ⑤ 超时/换血读数 ⑥ 6h 中点决策记录（继续/杀臂）+ 实际轮数与耗时
//   ⑦ 预注册机制读数：两臂各自"≥10 杀败局的死亡 tick 中位数" —— 直接读假设（加权是否把残局
//   打深了）；即使赢了也要看是不是这个项干的，否则差值无法归因。
