// 课程 x1-rebirth-a2 —— 放大位移腿（x1-rebirth 的继任者）
//
// 状态：**已结课（2026-09-17，it46 停，用户手动）**。
//
// ══════════════════════════════════════════════════════════════════════════
// 结课报告（2026-09-17）
// ══════════════════════════════════════════════════════════════════════════
//
// **一句话**：从**随机初始化**出发、不靠任何蒸馏，46 轮把 ladder-c01 打到
// **eval argmax 1.0000 / 训练内 rollout 连续 5 轮 1.0000**，同时把**每局承伤压到 0.877**
// （起点 81.3）—— **满分与近零承伤同时达成**。
// 主要意义不在分数，而在**验证了一条归因**：前身 x1-rebirth 的 42 轮衰退是
// **「每轮位移太小」**，不是「奖励权重失衡」也不是「缺朝向信号」。
//
// ── 1. 结果 ──────────────────────────────────────────────────────────────
// | 口径 | scratch 起点 | **a2 最终** | God AI | it333(BC) |
// | eval argmax（部署口径） | 0.05 | **1.0000**（it45） | 0.95 | 1.00 |
// | 训练内 rollout wr | 0.096 | **1.0000**（it42–46 连续 5 轮） | — | — |
//
// 行为质量（每局）：**dmg 81.3 → 0.877**（`phits=0.0000`）｜ ticks 1432 → **227** ｜
// shots 22.8 → **3.18**。**承伤比 God AI（55.4）低 60 倍以上**
// ⇒ 不是「追平规则教师」，是**在教师做不到的维度上超过它**（1 敌关范围内）。
//
// wr 曲线（拐点 it26–32）：
//   it21 0.0745 → it26 0.2513 → it28 0.3158 → it31 0.3750 → **it32 0.5023** →
//   it34 0.5696 → **it35 0.7778** → it37 0.9054 → it40 0.9677 → it41 0.9956 → **it42–46 1.0000**
//   窗口均值：0.1278（it1-13）→ 0.1666（it14-27）→ 0.6570（it28-41）→ 满分。
//
// eval 两口径：
//   it0 argmax 0.05 ｜ it5–20 **全 0**（timeout_frac ~0.90）｜ it25 0.0225 ｜ it30 0.0100 ｜
//   it35 0.3225（timeout 0.4325）｜ it40 **0.9600**（timeout 0.0200）｜ it45 **1.0000 / 0.0000**
//
// ── 2. 它验证了什么：位移是瓶颈（三条同向证据）──────────────────────────
// | 指标 | x1 | **a2** |
// | KL（轮内位移） | mean 0.0029 | **mean 0.0059**（max 0.00919） |
// | entropy 降幅 | 5% / 42 轮 | **44.65% / 46 轮** |
// | 移动分布归一化熵 | 0.9852（19轮，几近未动） | **0.919–0.946**（明显出峰） |
//
// ⚠ **反向警示**：KL **自始至终没进过 0.01–0.03 那条「目标带」（46 轮 0 次）**。
//   ⇒ **那条带是拍出来的、错误的**；0.005 量级 + 行为分布成形就已足够。
//   **判据应改用「行为分布是否出峰」，不要用「KL 是否进某个区间」。**
//
// ── 3. 奖励的边际比是杠杆（不是终局项）──────────────────────────────────
// 关键一步是**实测边际成本**：「每多走一格」承伤 ≈ **+2.39 dmg/格**（wander 122.2 − sit 77.8 ÷ 18.6）。
// | 版本 | 每格边际 | EV 排序（scratch 技能 / tmp/x2-ev-table.py） |
// | v3（wExplore 0.1 / wDmg 0.05） | +0.1 收益 vs **−0.12** 成本 ⇒ **净亏 0.02** | **坐 −1.56 > 走 −3.12**（方向反了） |
// | **v4（wExplore 0.2 / wDmg 0.03）** | +0.2 vs −0.072 ⇒ **净赚 0.13** | **走 +3.98 > 坐 +2.80 > 莽 −0.89** ✓ |
// ⚠ 期间曾误以为「调 `timeout`」能翻转 —— **不能**，timeout 只是常数项，改不动边际比。
//
// ── 4. 成本账（46 轮）────────────────────────────────────────────────────
// 总墙钟 **6731s = 112 分钟**：rollout 2830s（**42.0%**）｜ ppo 3608s（53.6%）｜ 其它 4.4%。
// 后期 rollout 单轮 **89.8s**（前期 48.9s）——根因是**局数** 200 → **1043**（局变短 ⇒ samples/局降）。
// ⚠ **GPU 是常开 notebook** ⇒ 这 42% 计入配额。优化见 `plan/rollout-io-overhead.plan.md`
//   （实测：obs 压缩比 **326×**、**6/16 通道恒零**）。
// ⚠ 推论：**减 `target_transitions` 不省总时间**（总墙钟 ∝ 总样本量）；
//   唯一有效方向是**提高并行度**或**降低每局开销**。
//
// ── 5. 局限（勿外推）────────────────────────────────────────────────────
// ① **本关是 1 敌关**（ladder-c01，每关 1 敌、max_ticks 1500）⇒「100% / 0 伤」
//    **不可外推到多敌关**。
// ② **`cellsVisited` 是半解**：奖励「走到没去过的格子」，**不奖励「朝敌人走」**
//    （39 个 metric 里没有距离/朝向类）。本腿能成，是因为 1 敌关里「到处走」就足以碰到敌人。
//    **多敌关是否需要真正的朝向信号，尚未验证。**
// ③ a2 vs x1 同时改了「位移包」与「奖励包」，**归因只能到「包 vs 包」**
//    （用户按「GPU 配额珍贵、高置信度就一起上」拍板；v2/v3 已在真实语料上验证方向）。
//
// ── 6. 方法论沉淀（比结果更值钱）────────────────────────────────────────
// ① **「排序正确」≠「行为会照排序走」**：x1 的梯度实测单调正确
//    （0伤通关 +5.35 > 换血 +4.22 > 冒险 −2.25 > 苟 −3.0 > 死亡 −5.75），策略仍学会苟——
//    因为按它**当时的技能**，冒险 EV ≈ `0.05×5.35 + 0.95×(−5.0) = −4.48` **差于**确定的苟 −3.0。
//    ⇒ 策略走的是「技能条件下的期望」，不是设计者的收益排序。
// ② **权重必须用真实语料标定**：初版 `wDmg=0.3` 按 `dmg≈3.77`（BC 的数）标，
//    真实语料 **99.7** ⇒ 承伤项放大到 −29.9（正项之和的 22 倍），压平全部正信号。
// ③ **改配置前先对齐口径**（本腿踩两次）：`metrics_stats.jsonl` 的 `mean` 是 **per-step**，
//    与 eval jsonl / shard 的**局末累计**不可比。曾因此误判「cells 下降」「`wShot` 生效」各一次。
//
// ── 7. 复现 ─────────────────────────────────────────────────────────────
//   python scripts/init_scratch_weights.py \
//     --out nn-training/weights/x1-rebirth/scratch-init.json --seed 7
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x1-rebirth-a2 --remote-transport pull
// 诊断：`tmp/a2-report.py`（走势/eval/直方图）｜`tmp/a2-timing.py`（时间账）｜
//       `tmp/x2-ev-table.py`（三策略 EV，EV_ROOT 指定语料）｜`tmp/obs-compress-probe.py`（obs 压缩比）
//
// ══════════════════════════════════════════════════════════════════════════
// 以下为立项时的设计说明（保留作历史；其中「待开训」的语气是当时的，勿误读）
// ══════════════════════════════════════════════════════════════════════════
//
// ══════════════════════════════════════════════════════════════════════════
// 第一部分：x1-rebirth 的实测结论（2026-09-17，本腿的立项依据）
// ══════════════════════════════════════════════════════════════════════════
//
// ── 1. 这条腿怎么死的：跑 42 轮后衰退回起点 ──────────────────────────────
// | it | 6 | 19 | 26 | 30 | 40 | 42 |
// | wr | 0.1685 | **0.1676（峰值）** | 0.0632 | 0.1200 | 0.0734 | **0.0629** |
// 起点是 0.040 ⇒ **后 20 轮持续下滑，正在退回起点**。
// eval（it40，800 局）同步恶化：`winRate 0.0`（it15 曾 8%）、`timeout_frac 0.6538→0.9363`、
// `gameover 277→51`、`phits_mean 0.3463→0.0638`
// ⇒ **策略退化成「不动」：不死，但也不打**。
//
// ── 2. ★ 第一条教训：「排序正确」≠「行为会照排序走」────────────────────
// 当时的奖励梯度是**实测正确的**（build_reward_fn 真跑，6 行单调、死亡最坏、冒险优于苟）：
//   0伤通关 +5.35 > 换血通关 +4.22 > 冒险 −2.25 > 苟(超时) −3.0 > 苟(狂射) −3.79 > 死亡 −5.75
// **但策略还是学会了苟**。因为：
//   苟到超时 = **确定 −3.0**
//   冒险     = `0.05×(+5.35) + 0.95×(−5.0) ≈ −4.48`   ← 按它当时的通关率
// ⇒ **确定 −3.0 > 期望 −4.48 ⇒ 选苟是「对」的**。
// **教训：策略按「自己的技能 + 风险」走，不按设计者的收益排序走。**
// 奖励排序必须结合**当前技能水平**评估，否则会把策略推向「确定的坏」而非「波动的更好」。
// （用户 07:45 的预判「随机权重很可能学会苟」由此应验。）
//
// ── 3. ★ 第二条教训：静态推理出来的权重标定，会在真实语料上崩 ────────────
// 初版把 `wDmg=0.3` 按「一次承伤 ≈ 一次命中（wHit=0.3）」标定，
// **前提假设是 `dmg≈3.77/局`**（那是 it333/BC 的数）。
// 用 it19 权重跑 40 局真实语料实测：**`dmg ≈ 99.7/局`**（莽策略，是 BC 的 26 倍）
// ⇒ `wDmg` 项被放大到 **−29.9**，是**所有正项之和（1.35）的 22 倍** ⇒ 全部正信号被压平。
// **⇒ 任何权重标定都必须先跑一批真实语料看实际量级，不能从别的策略的数外推。**
//
// ── 4. 三档试完的收敛结论（奖励权重路线已到天花板）──────────────────────
// 用同一批 40 局语料（`tmp/x2-check/rollout`）复算，逐档改进：
// | 档 | 改动 | 分项平衡 | clear 局奖励 | 「动>不动」差 |
// | v1 | wExplore=0.05 | 承伤 −29.9 压倒 | −12.78 | **−0.25（反了）** |
// | v2 | wDmg 0.3→0.05, wExplore→0.1 | 承伤 −4.99 / 探索 +3.65 ✓ | **+4.46** | **+0.204** |
// | v3 | wHit 0.3→0.6 | 命中 +0.71 | +5.18 | **+0.056** |
// ⇒ v2 修好了平衡与方向；**v3 没有进一步改善核心问题**（差距反而缩小）。
// **为什么抬 wHit 无效（算术）**：这批语料 `dmg ≈ 99.7/局` 而 `hits ≈ 1.2/局`
// ⇒ **挨打量级是命中的 80 倍**，提高单次命中单价（+0.35 均值）在 −4.99 面前微不足道。
// ⇒ 症结不是「命中不够值钱」，而是**命中次数本身太少**（策略在挨打，但打不中）。
//
// ── 5. ★★ 第三条教训（最关键）：训练 19 轮几乎没改变任何行为 ─────────────
// 用 **scratch（未训练）** 与 **it19（训练 19 轮）** 各跑 40 局、同 seed 同关做 A/B：
// | 量（局末累计，同口径） | scratch | it19 | 变化 |
// |---|---|---|---|
// | **shots** | **22.75** | **22.85** | **几乎为零** |
// | cells | 38.58 | 36.52 | ↓5% |
// | hits | 0.93 | 1.18 | ↑27% |
// | kills | 0.12 | 0.20 | ↑ |
// | clear | 5/40 | 8/40 | ↑ |
// | **a_move 归一化熵** | **0.9978** | **0.9852** | **只降 1.3%** |
// ⇒ **移动分布 19 轮只从 0.998 挪到 0.985；开火次数完全没变。**
// **修正后的因果链**：不是「开火学会了、移动没学会」，
// 而是 **训练整体几乎没有推进任何行为维度**。这与 `KL ≈ 0.0029`（每轮位移极小）**完全一致**。
// ⇒ **第一杠杆应是「位移」（kl_coef / lr / epochs），而不是「加新 metric」。**
//    在放大位移之前谈「缺朝向信号」是早了 —— 现在连已有的信号都没走完。
//
// ── 6. ⚠ 一条方法论纠正（口径混淆，务必记住）────────────────────────────
// 我在中途报告里说过「开火从 20.2 → 10.7，wShot 生效」——**这是错的**。
// 那个 `10.7` 来自 `metrics_stats.jsonl` 的 `playerShots.mean`，它是
// **按 decision step 平均**；而 `20.2` 是 eval jsonl 的**每局**值。两者不可比。
// 同口径（局末累计）下 scratch 22.75 vs it19 22.85 ⇒ **wShot 根本没生效**。
// **⇒ 跨数据源比数字前必须先对齐口径。`metrics_stats` 的 mean 是 per-step，不是 per-game。**
//
// ══════════════════════════════════════════════════════════════════════════
// 第二部分：本腿的设计（唯一假设 = 放大位移）
// ══════════════════════════════════════════════════════════════════════════
//
// **假设**：x1-rebirth 之所以 42 轮几乎没动，主因是**每轮位移太小**
// （KL 0.0029，entropy 1.8177→1.7279 只降 5%），而不是「奖励信号不存在」。
// ⇒ 本腿把三个**同向**的位移旋钮一起放大，其余尽量沿用 x1-rebirth：
//   · `kl_coef 0.05 → 0.02`（松开 KL 惩罚；⚠ 历史上 x3-step 用 0.03 时 KL 仍只有 0.00267，
//     所以这一项**单独可能无效**，故与下面两项同向叠加）
//   · `lr 3e-4 → 5e-4`（提高单步幅度；scratch init 的配套标定值是 3e-4，5e-4 是 1.67×，仍克制）
//   · `epochs 4 → 6`（每轮多更新 50%；这是**不改变单步尺度**的位移放大，最安全的一项）
//
// **奖励用「边际已转正」的那组**（v4；v2/v3 在真实语料上验过量级，v4 再修方向）：
//   `wKill 3.0 / wHit 0.6 / wWin 2.0 / wDmg 0.03 / wShot 0.05 / wExplore 0.2`
//   terminal `lives_exhausted -5.0 / timeout -2.0`
//   ⇒ v4 EV 表：**走 +3.98 > 坐 +2.80 > 莽 −0.89**，clear +7.54 ✓
//   （v3 的 wExplore 0.1 / wDmg 0.05 方向是**反的**：sit −1.56 > wander −3.12
//     ⇒ 那时的奖励指向「坐」。修法见 §4 与 reward 块：杠杆是「每格边际收益/成本之比」。）
//
// **起点仍用 scratch-init**（不从 it19 热启动）：it19 是**莽策略**（dmg 99.7），
// 从它开始会继承莽；且从 scratch 才能与 x1-rebirth 做**同起点对照**，把差异归到位移旋钮上。
//
// **判据（核心是看「位移是否真的变大了」）**：
//   ① **KL 曲线（本腿第一读数）**：是否稳定落在 **0.01–0.03**（x1 是 0.0029）。
//      若仍在 0.005 以下 ⇒ 位移杠杆无效，须换方向（那时再谈新 metric）。
//   ② **entropy 下降速率**：x1 是 42 轮降 5%；本腿应明显更快（这是位移的另一个读数）。
//   ③ **a_move 归一化熵**：x1 是 0.9978→0.9852（19 轮降 1.3%）。**要看到它开始掉**，
//      才说明移动策略真的在变。（诊断脚本：`tmp/x1-acthist2.py`，自动选最新有 shard 的轮。）
//   ④ **shots / cells 与 scratch 的差**：x1 的 shots 是 22.75→22.85（零变化）。
//   ⑤ wr / clear / dmg：常规读数，但**不是本腿的主要判据**。
//   ⑥ **★ cells 是否上涨（本腿新增奖励包的直接读数）**：v4 表说「走」最优，
//      但那是**在 scratch 的行为分布上分组算的**，不等于「策略真会去走」。
//      ⇒ 若 cells 明显高于 scratch 的 38.6 ⇒ 奖励包生效；若不动 ⇒ 见局限 3 的新止损分支。
//
// **止损**：若 it20 时 KL 仍 <0.005 且 entropy 降幅 <3% ⇒ 判「位移杠杆无效」，停腿。
//
// 启动（用户手工；agent 禁止）：
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x1-rebirth-a2 \
//     --remote-transport pull
//
// ── ⚠ 已知局限（2026-09-17 评审记录，务必读）──────────────────────────────
// 1. **本腿同时改了两个包，对照只能归因到「包 vs 包」**：
//    a2 vs x1 的差异 = **(kl_coef / lr / epochs) + (wHit / wDmg / wExplore / timeout)**。
//    原稿只承认前者是「位移包」，**漏了后者**。
//    后果：若 it20 出现「KL 进带但行为不动」，止损分支「瓶颈在别处」
//    **无法区分**是「位移仍不够」还是「新奖励错了」。
//    打包可辩护（§4 已在真实语料上验证 v2/v3 的方向），但**归因能力确实被牺牲**。
// 2. **奖励权重是二阶杠杆**——这是 §2 教训的直接推论：既然「排序 ≠ 行为」，
//    关键在「策略能否走到那条路径」（位移/探索），而不是「路径值多少分」。
//    ⇒ 更干净的设计是「**先只做位移、奖励不动**」；本腿没那么做，是为省一轮。
// 3. ~~本腿的奖励仍指向「坐」~~ —— **2026-09-17 18:05 已修**：
//    把 `wExplore 0.1→0.2` + `wDmg 0.05→0.03` 后，v4 EV 表变成
//    **走(+3.98) > 坐(+2.80) > 莽(−0.89)** ⇒ 奖励已指向「走」，本条局限**解除**
//    （记录保留，说明当时是怎么发现并修的：边际收益/成本之比是杠杆，不是 timeout）。
//    ⇒ **新的止损分支**：若 it20 出现「KL 进带 + 行为变确定，但仍不涨 cells/shots」
//      ⇒ 说明这个半解（`cellsVisited` 只奖励「乱走」，不奖励「朝敌走」）不够用，
//      那时才需要**新增距离/朝向 metric**（独立工程）。
//
// 非目标：加新 metric（那要动 TS 导出 + metrics_version，是独立工程）/ 改关卡 / 与其它课程对照。
{
  "version": 5,
  "name": "x1-rebirth-a2",
  "mode": "per-tick",
  "level": "ladder-c01",

  // ── 奖励：v2/v3 已在真实语料上证过量级平衡（见文件头 §3/§4）────────────
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      // wDmg=0.05→**0.03**（2026-09-17 18:05，与 wExplore 同批上调）：
      // 依据是**边际算术**——实测「每多走一格」的承伤成本 ≈ +2.39 dmg/格，
      // wDmg=0.05 时 = −0.12/格，**高于** wExplore=0.1 给的 +0.1/格 ⇒ 每格净亏 0.02
      // ⇒ 策略的最优是「坐」（v3 EV 表：sit −1.56 > wander −3.12）。降到 0.03 后
      // 边际成本 ≈ −0.072/格。⚠ 不要再往 0 靠：承伤仍是「0 伤」这条任务定义的载体。
      "wDmg": 0.03,
      // wShot=0.05（数字按**本腿的 wHit=0.6** 重算，2026-09-17 评审修正；
      // 原稿误用了 x1-rebirth 的 wHit=0.3，结论不变但数字是错的）：
      //   随机态命中率 3.6% ⇒ 5 发换 0.18 命中，收益 0.18×0.6 = **0.108** < 成本 0.25 ⇒ 净负（抑制瞎打）；
      //   学会后命中率 ~52% ⇒ 1 发 = 0.52 命中，收益 0.52×0.6 = **0.312** > 成本 0.05 ⇒ 净正（不抑制）。
      "wShot": 0.05,
      // wExplore=0.1→**0.2**（2026-09-17 18:05）：这是让「走」成为最优的那一半。
      // v4 实测 EV 表（scratch 技能 / 40 局 / `tmp/x2-ev-table.py`，token 同 v4）：
      //   | 策略 | explore | hit | kill+win | dmg | shot | term | **EV** |
      //   | sit   (cells≤p25) | +5.60 | +0.48 | +1.50 | −2.33 | −1.05 | −1.40 | **+2.80** |
      //   | wander(cells>p75) | +9.31 | +0.67 | +0.56 | −3.67 | −1.11 | −1.78 | **+3.98** ← 最优 |
      //   | rush  (dmg  >p75) | +8.15 | +0.83 | +0.63 | −7.64 | −1.11 | −1.75 | **−0.89** |
      //   | clear 局          | +6.16 | +1.44 | +5.00 | −4.30 | −0.76 | 0.00 | **+7.54** |
      // ⇒ **走 > 坐 > 莽**，且通关远高于一切 —— 这才是本腿想要的早期动力学。
      // ⚠ 它仍是**半解**：`cellsVisited` 只奖励「走到没去过的格子」，不奖励「朝敌人走」
      //   （39 个 metric 里没有距离/朝向类）。且新格子会越走越少（边际递减）。
      "wExplore": 0.2
    },
    "terminal": {
      // ⚠ 本块 rationale **2026-09-17 评审后重写**。原稿写「保持苟 < 冒险的实测梯度」**是错的**：
      //   (a) 那条六行梯度是**旧权重**下的实测；新权重下那批「冒险局」（−2.25）会被「苟」（−2.0）**反超**
      //       ⇒ 引用旧梯度论证「保持」无效，必须在新权重下重算；
      //   (b) 更根本：按 §2 的逻辑，`timeout −3.0 → −2.0` 让「坐」的**确定收益更高**
      //       ⇒ 这个改动**拉大**了「坐」相对「冒险」的优势，正是 §2 指认的致死机制，不是解药。
      //
      // **真正的早期动力学判据是实测 EV 表**（scratch 技能 / 40 局 / `tmp/x2-ev-table.py`）：
      //   ▸ v3 权重（wExplore 0.1 / wDmg 0.05）——**暴露问题的那一版**：
      //     | sit −1.56 | wander −3.12 | rush −10.06 | clear +1.60 |
      //     ⇒ **「坐」最优**。机制：走得多 ⇒ 挨打多（wander dmg 122 vs sit 78），
      //       探索边际收益 (+0.1/格) **低于**承伤边际成本 (2.39 dmg/格 × 0.05 = −0.12/格)
      //       ⇒ 每多走一格净亏 0.02。
      //   ▸ v4 权重（wExplore 0.2 / wDmg 0.03）——**本腿采用的版本**：
      //     | sit +2.80 | **wander +3.98** | rush −0.89 | clear +7.54 |
      //     ⇒ **走 > 坐 > 莽**，通关远高于一切 ⇒ 这才是本腿要的早期动力学。
      // **不要把 timeout 当作翻转「坐 vs 走」的杠杆** —— 真正的杠杆是「每格的边际收益/成本」，
      // 即 `wExplore` 与 `wDmg` 的**比值**。timeout 取 −2.0 的理由**仅**是不把探索一起重罚。
      "lives_exhausted": -5.0,
      "timeout": -2.0
    },
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点与 x1-rebirth 相同（同起点才能把差异归到位移旋钮）──────────────
  "bc": "nn-training/weights/x1-rebirth/scratch-init.json",
  "kickstart_ref": false,
  "warmup_iters": 0,

  "iters": 60,
  "max_hours": 12,
  "workers": 8,

  // ── ★ 本腿的唯一假设：放大位移（三个同向旋钮）─────────────────────────
  // 见文件头「第二部分」。读数看 KL 是否进 0.01–0.03、entropy 是否降得更快。
  "lr": 0.0005, // 3e-4 → 5e-4（1.67×，scratch init 配套标定是 3e-4，仍克制）
  "epochs": 6, // 4 → 6（每轮多更新 50%；不改变单步尺度，最安全的位移放大）
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段：松开 KL 惩罚（0.05→0.02）+ lr/epochs 放大位移。
    // ⚠ 历史证据：x3-step 用 kl_coef=0.03 时 KL 仍只有 0.00267 ⇒ 这一项**单独可能无效**，
    //   所以与 lr/epochs 同向叠加，本腿测的是「位移包」而不是单项。
    { "until_iter": 40, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段 rationale（2026-09-17 评审补；原稿无理由，且与 x1 反向，像手滑）：
    // · **kl 继续放松（0.02→0.01）**：本腿要验的正是「位移不足」假设。若前 40 轮证明
    //   位移有效、行为开始成形，尾段继续给空间让它走到更远处，而不是提前收敛在
    //   一个可能仍不达标的位置。（x1 尾段是**收紧** 0.05→0.03 以收敛——两者方向不同
    //   是**有意的**：x1 假设「已到解附近」，本腿假设「还差得远」。）
    // · **lr 砍半（5e-4→2.5e-4）**：尾段改成「小步精修 + 靠 kl 放松维持空间」，
    //   与早段的「大步探索」形成有意的不对称。
    // · **kl_cap 尾段缺席**：它在 serial/remote 路径**不接线、恒不生效**（见 x1-rebirth 的说明），
    //   写上只为记录意图。两段不对称是无意的，但**无害**。
    { "kl_coef": 0.01, "lr": 0.00025 }
  ],

  // ── 采集：沿用 x1-rebirth 的严格样本量配额（已实测稳定）────────────────
  "target_transitions": 24000,
  "est_samples_per_game": 120, // 首轮兜底；之后由最近 5 轮 trailing 均值自动覆盖
  "seed_rotate": 600, // 仅种子轮转；局数由 target_transitions 决定

  "out": "tmp/x1-rebirth-a2/weights.json",
  "traj": "tmp/x1-rebirth-a2",
  // ent_break=0.25：熔断线（entropy 跌破即触发 breaker）。
  // ⚠ 2026-09-17 评审确认过这个值是否该随「预期 entropy 掉得更快」而下调 ⇒ **维持 0.25**：
  //   它是「**探索崩掉**」的护栏，不是「正常下降」的阈值。
  //   x1 实测 entropy 从 1.8177 只降到 1.7279（42 轮），离 0.25 极远；本腿即使掉得快
  //   也不该在几十轮内接近它。真要接近了那是**坍缩**（该熔断），不是「学得快」。
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x1-rebirth-a2",
  "backup_prefix": "x1-rebirth-a2",

  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5,
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc：`nn-training/weights/x1-rebirth/scratch-init.json`（seed 7，已存在）。
//    若缺失：`python scripts/init_scratch_weights.py --out nn-training/weights/x1-rebirth/scratch-init.json --seed 7`
// ② 关卡 `ladder-c01`（4 关 a/b/c/d，每关 1 敌、队列 20、max_ticks 1500、lives 1、hard）。
// ③ transport：显式 `--remote-transport pull`；hub 可达、eval 节点 epoch 对齐。
// ④ **新 out/traj 目录**（bc 与奖励公式都变了 ⇒ 禁续跑，§15.5）。
//
// ── 诊断工具（本腿会用到）────────────────────────────────────────────────
// · 训练走势：`tmp/x1-trend.py`（改 P 指向本腿 training_log.jsonl）
// · 动作直方图 / 移动熵：`tmp/x1-acthist2.py`（自动选最新仍有 shard 的轮）
//   ⚠ **shard 每轮会被清理**，诊断要及时做。
// · 奖励结构复算：`tmp/x2-reward-check.py`、两批对比 `tmp/x2-compare.py`
//   （改 root 指向本腿，或复用 x2-check 的语料做纵向对照）
//
// ── 结算节（2026-09-17 结课回填；it46 停，**满分结课**）────────────────────
// **结果**：eval argmax **1.0000**（it45）/ 训练内 rollout **it42–46 连续 5 轮 1.0000**；
//          **dmg 0.877/局、phits 0.0000**（起点 81.3）⇒ 「0 伤」达成；
//          ticks 227（起点 1432）/ shots 3.18（起点 22.8）。
// 对照：God AI 0.95（贪心）/ it333(BC) 1.00；承伤 God 55.4 ⇒ **本腿低 60 倍以上**。
//
// ★ 第一读数（KL）：**自始至终没进 0.01–0.03 带（0/46）**，mean 0.0059 / max 0.00919。
//   ⇒ **那条带是拍出来的、错误的**；0.005 量级 + 行为分布成形就已足够。
//   **判据应改用「行为分布是否出峰」，不要用「KL 是否进某个区间」。**
// ★ entropy 降幅：**44.65% / 46 轮**（1.8129→1.0034）；x1 是 5% / 42 轮 ⇒ 快约 9 倍。
// ★ a_move 归一化熵：scratch 0.9978 → x1(19轮) 0.9852 → **a2 0.919–0.946** ⇒ 明显出峰。
// ★ cells/shots 相对 scratch：cells 38.6→**12.5**（局变短，见下）、shots 22.75→**3.18**。
//   注：cells 下降**不是倒退**——ticks 从 1432 缩到 227，通关局短得多。
// 判决：**「位移是瓶颈」成立**（x1 的失败归因被验证）。
//   ⇒ 三条方法论已写入 `docs/nn.progress.md §56`：① 排序≠行为（按技能算 EV）；
//     ② 权重必须用真实语料标定；③ 杠杆是「边际收益/成本之比」而非终局项。
// ⚠ 局限：本关是 **1 敌关**，「100% / 0 伤」不可外推到多敌关；
//   `cellsVisited` 是半解（不奖励「朝敌走」），多敌关是否需要真朝向信号尚未验证。
