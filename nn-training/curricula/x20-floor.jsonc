// 课程 x20-floor —— **抬下限腿**（两极化收敛）
//
// 状态：**就绪，待用户手工开训**（Phase 0 探针已跑完，杠杆已定；bc 已填实）。
//   agent 不得启动训练。
//
// 本腿回答一个问题：**能不能在保住 20 杀上限的同时，把"只杀 1 个"的那一半局抬起来？**
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 一句话
// ══════════════════════════════════════════════════════════════════════════
// x20 的上限早就够（能 20 杀通关），缺的是**下限**。
// 本腿的目标函数是 **p10 kills / 低分局占比**，不是 pooled 均值 —— 后者会把两极化抹平。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 立项依据（Phase 0 探针实测，详见 plan/x20-floor.plan.md §1/§4）
// ══════════════════════════════════════════════════════════════════════════
// · **两极化**（it30 白板 415000 段 800 局）：pooled 6.34 杀，但 **p10=1 / p50=4 / p90=16**；
//   20 局配对样本 = 10 局 20 杀通关 + 10 局 1 杀 ⇒ 6.34 ≈ (10×20 + 10×1)/20。
// · **上限高于教师**（同 seed 配对）：NN 通关的 10 个 seed 上教师只 5.70 杀 / 1/10 通关
//   ⇒ God 不是天花板（§0.2 得数据支持）。⚠ 教师只作参照，不作门。
// · **P0-1 同 seed 配对（无幸存者偏差）锁定了差异**：
//     - 被命中次数 `hit_n`：God **2.60** vs NN **2.60**（**完全相同**）⇒ 不是"躲"的差异；
//     - `hp_max` 双方恒 263 ⇒ 都没升级 ⇒ 不是"血量池"；
//     - `dmg_total` 229.8 vs 205.4（边缘）⇒ 不是"伤害量"；
//     - **首挨 t**：God 442 vs NN 297；**承伤速率** 0.109 vs 0.390 hp/tick（**3.6 倍**）；
//     - God 的行为：move 57% / aim 46% / appr 26% vs NN 87% / 81% / 66%（**God 全部更低**）。
//   ⇒ 差异在**挨打的时间分布**：hp=263 ⇒ 一局"挨打额度"≈2.6 次命中（环境常数）；
//     NN 用 525t 用完（换 0.9 杀），God 用 2117t 用完（换 6.70 杀）⇒ 每额度产出差 7.4 倍。
// · **P0-2 三臂**：scratch 20/20 局 **0 杀**（hitRate≈0）；NN 0.90/20.00；God 6.70/5.70
//   ⇒ NN 能力全来自训练；且 **「活得久」≠「杀得多」**（scratch 活 ~790t 却 0 杀，不主动交火）。
// · **P0-3 可动空间**：God 在 LOW 组 kills=[0,1,1,1,2,3,5,14,20,20] ⇒ p10=0
//   ⇒ 这些 seed 的下限本身就低 ⇒ 门**不设硬线**，用相对 it0 的改善；且不得由教师反推。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. 唯一变量（A 方案）：承伤项改「分段线性」—— 血薄阶段额外加罚
// ══════════════════════════════════════════════════════════════════════════
// 假设：**策略缺少「态势相关的激进度调节」** —— 它以同一模式对待所有局面，
//       在困难 seed 上过早用完挨打额度。
//
// 实现（**避开势函数跳变**）：reward 是势函数差分（`r = np.diff(phi)`），
//   所以**不能**写「权重随 playerLevel 切换」（升级瞬间 Φ 会对累计承伤换权重 ⇒ 产生 +10 级伪奖励）。
//   改用**连续、边际递增**的形式：
//     `- wDmg*playerDamageTaken  -  wDmgLate*max(playerDamageTaken - dmgKnee, 0)`
//   ⇒ Φ 连续（无跳变），`dmg > dmgKnee` 之后边际惩罚从 0.03 升到 0.13。
//   ⚠ metric 里**没有 hp**（只有累计的 playerDamageTaken）⇒ 只能用承伤量间接表达"血薄"。
//
// 剂量标定（`tmp/x20-floor-dose.py`，语料 = x20-rebirth 起跑线探针 40 局）：
//   | 配置 | EV(sit) | EV(wander) | EV(rush) | 判定 |
//   | 现状（late=0） | −0.90 | 68.57 | 41.74 | 走>坐 ✓ |
//   | **knee=150 late=0.10** | **−11.82** | **50.26** | **19.59** | **走>坐 ✓，莽被压** |
//   | knee=150 late=0.20 | −22.75 | 31.95 | −2.57 | 走>坐 ✓（但 wander 也被削狠） |
//   ⇒ 选 **knee=150 / late=0.10**（rush 仍 >0 但显著低于 wander；late=0.20 会把 wander 打得太低）。
//   ⚠ 诚实记录：A 项**同时惩罚 wander**（wander 档 dmg 也高：−18.3 vs sit −10.9），
//     即它压的是"挨打多的局"而非"只有莽" ⇒ 排序未翻转但强度被削弱（69.5 → 62.1）。
//   ⚠ 40 局样本偏小 ⇒ **it10 观察哨**用首轮真实语料复标（只调 knee/late，不动判据）。
//   ⚠ **程序预注册（否则 it10 现场犯难）**：调 knee/late = 改 reward 语义，与 §15.5
//     （新实验须 fresh out/traj、禁续跑）**冲突**。裁决：**it10 的 knee/late 调整视为
//     同一条腿内的一次性剂量修正、豁免 §15.5**（语义仍是"血薄阶段加罚"、判据不动），
//     但**必须记录调整前后参数与依据**，且**只允许一次**；
//     若需要改的是**公式结构**（不是这两个标定参数）⇒ 那是新实验 ⇒ 另开腿、新 out/traj。
//   ⚠ 复标时**必须把"通关局的 late 项负担"列入检查项**：metric 无 hp，A 项用累计承伤代理
//     "血薄"，但吃到升级/修复的局（结束 hp 可到 315）会被一并课税 —— 目前有界
//     （≤0.1×(263−150)≈11.3 ≈ 3.8 杀）且有 pooled −10% 熔断兜底，复标时确认。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 判据（预注册；**读数必须换 —— 这是本腿最确定的一条**）
// ══════════════════════════════════════════════════════════════════════════
// **主读数 = 「kills ≤ 3 的局占比」**（贪心，池外固定段 414000）：it0 = **42.9%**（343/800，已实测）。
// **辅助读数 = p10 kills**：it0 = **1** —— ⚠ p10 是分位数，只由"0–1 杀"那 ~13 个局的位置决定，
//   **极不稳健**（13/800 的局就能翻转它）⇒ **降为辅助**（早期版本把它当主读数，已改）。
// **门 = 相对 it0 的改善**：判线 = 低分局占比 **≤35%（≈ −7.9pp）** 且 `p10 ≥ 2`，
//   且 **pooled 均杀不得下降**（it0 = **6.71**，判决段实测）。
//   ⚠ 早期版本写"低分局 ≤18%" —— 那是按**错误基线 25%** 算的（实测 42.9%）⇒ 相当于要求 −24pp，
//     难度是设计意图的 3 倍 ⇒ **已重标**。门一律不得由教师表现反推（§0.2）。
// **判决段 it0 全量基线**（414000，800 局，本机 `--no-dist`，与分布式逐字等价）：
//   `mean 6.71 / p10=1 / p25=2 / p50=4 / p75=9 / p90=20 / kills≤3 42.9% / pass 10.2%`
//   （对照 415000 诊断段 `6.34 / 42.0% / 7.6%` ⇒ 两段同分布，段间差在噪声内）。
// **约束**：
//   ① 上限熔断：pooled 均杀跌破 it0 的 **−10%** ⇒ 立即停；
//   ② 分关（4 关）任一处退步 >5pp ⇒ 掉队；
//   ③ 超时熔断：`timeout_frac > 5%` ⇒ 立即停；
//   ④ **`cellsVisited/局` 与 `move%` 不得显著下降**（防保守陷阱，见 §2 末与 §7）。
// **统计口径（写死）**：判决走池外固定段 `--seed0 414000`（800 局 = 4 关各 200，一次性）；
//   日常 200 局（seed0 860001）只做趋势、绝不判决；噪声地板 ≈2pp。
// **最优归档保护**：每 eval 点记 **p10 + kills 分布**（不是只有均值），最优另存。
// **★ 回测门**：终点候选必须在 `--seed0 415000`（本会话的诊断段）上**不劣于 it0**
//   （同段同种子配对；p10 与低分局占比双向看）—— 防止"在 414000 上好看、在已知难局上更崩"。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 止损
// ══════════════════════════════════════════════════════════════════════════
//   · **主检点 it40**：p10 仍为 1（未动）⇒ 停腿，判「下限不可动，A 方案无效」。
//   · **上限熔断**（§3①）：pooled 跌破 −10% ⇒ 立即停。
//   · **机械横盘**：p10 连续 4 个 eval 点无改善 ⇒ 停腿；宽限一次 +10 轮。
//   · **超时熔断**：`timeout_frac > 5%` ⇒ 立即停。
//   · **分支四（自锁）**：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入。
//   · ⚠ 优先级：上限熔断 > it40 主检点 > 横盘 > 一切诊断。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 起点与缰绳
// ══════════════════════════════════════════════════════════════════════════
// bc = `nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json`
//   （x20-rebirth 的白板最优：7.6% / 6.34 杀 / p10=1；亦为 x20-powered 的起点，可对照）
// `kickstart_ref = true`（bc 是学到手的策略；x2/x3 先例）+ `warmup_iters = 0`（硬要求）。
// **位移包回基线**（用户 2026-09-19 拍板"移除位移包放大"）：lr 5e-4→**3e-4**、epochs 6→**4**、
//   kl_coef 0.02→**0.05**（更紧）。依据：x20-powered 从**已成形**的 it30 出发 + 放大位移
//   ⇒ entropy 0.583→0.766、94 轮净零；a2 那条"位移是杠杆"只成立于 scratch（未成形）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 预算与采样规模
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（与 x20 系列同量；4 关均分 6000/关）。
// · `est_samples_per_game` = **147**（同环境同起点，沿用 x20-rebirth 的 1 命真值 146.8 取整）。
// · `seed_rotate` = 150（600/4）；`iters 0`（无限轮，用户拍板）+ `max_hours 12`。
// · 单 run（L4）。`workers 8` / `ent_break 0.25` 全链一致。
// · **两段/单段都显式写 `kl_cap`**（本路径未接线；不写会让 cap 依赖 rl-config + 运行路径）。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. **A 项同时压 wander**（§2 标定）⇒ 若它把"走"的收益削得太狠，可能推向"坐"。
//    监控项：`cellsVisited/局` 与 `move%` —— 若显著下降而 p10 没涨 ⇒ 立即停（那是保守陷阱）。
// 2. **本腿不改进攻能力**：只调"挨打的代价回收"。若 p10 不动，下一步应在算法侧（B/C），
//    而不是继续加剂量。
// 3. 40 局标定样本偏小 ⇒ it10 观察哨复标（只调 knee/late）。
// 4. **不得用教师表现**定门/难度/可学性（§0.2）。
// 5. `plan/x20-floor.plan.md` 的 §4.2 列的 B/C 手段本腿**不做**（保持单变量）。
// 6. ★ **单变量纯度名义上破了（必须记住，否则归因会错）**：相对父腿 x20-rebirth，本腿
//    **同时改了两处** —— ① reward 加 A 项；② **位移包回基线**（lr 5e-4→3e-4、epochs 6→4、
//    kl_coef 0.02→0.05、双段→单段）。后者有用户拍板 + x20-powered 的 94 轮净零证据支撑，
//    但**若本腿失败，"A 项无效"与"位移包回退的影响"不可分** ⇒ 结算时**不得把结果单独归因给 A 项**。
// 7. **标定语料与起点不完全匹配（已知）**：标定用的是 x20-rebirth 起跑线探针（bc=x6-it45、
//    均杀 3.88）的 40 局，而本腿起点是 rebirth.it30（均杀 6.34、承伤分布更靠右）。
//    ⇒ knee=150 在新起点分布下**落入高罚区的比例更高** ⇒ 这正是 it10 复标要解决的问题。

{
  "version": 5,
  "name": "x20-floor",
  "mode": "per-tick",
  // 环境 = ladder-c20-lives1（4 关 2000-2003，20 敌、max_ticks 12900、lives 1、hard）。
  // 任务侧一个字不动（AGENTS §0.2）。
  "level": "ladder-c20-lives1",

  // ── 奖励：x20-rebirth 逐字 + A 项（分段线性承伤）───────────────────────
  "reward": {
    // A 项：`- wDmgLate*max(playerDamageTaken - dmgKnee, 0)` —— Φ 连续，无跳变。
    "formula": "wKill*kills + wHit*enemyHits - wDmg*playerDamageTaken - wDmgLate*max(playerDamageTaken - dmgKnee, 0) - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wDmg": 0.03,
      // ── A 方案（本腿唯一变量；标定见文件头 §2）──
      "wDmgLate": 0.1,
      "dmgKnee": 150.0,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      "wApproach": 0.008
    },
    // dense-only（与 x20-rebirth 一致）：terminal 空表合法（reward_library `.get(outcome,0.0)`）。
    "terminal": {},
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点：x20-rebirth 的白板最优 it30（p10=1 的崩局起点）─────────────────
  "bc": "nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见文件头 §6）──────────────────────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：**回基线**（用户拍板移除放大；见文件头 §5）───────────────────
  "lr": 0.0003,
  "epochs": 4,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    { "kl_coef": 0.05, "lr": 0.0003, "kl_cap": 0.2 }
  ],

  // ── 采集：总量 24000/轮 ─────────────────────────────────────────────────
  "target_transitions": 24000,
  "est_samples_per_game": 147,
  "seed_rotate": 150,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x20-floor/weights.json",
  "traj": "tmp/x20-floor",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x20-floor",
  "backup_prefix": "x20-floor",

  // ── 评估 ───────────────────────────────────────────────────────────────
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc = `nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json`（已存在）。
// ② **Phase 0 已跑完**（plan/x20-floor.plan.md §4）：三臂齐（NN / God / scratch）、
//    同 seed 配对因子表、剂量标定（knee=150 / late=0.10，EV 排序 走>坐>莽 未翻转）。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`。
// ⑥ 确认 a95/a97 **以及本机 self（127.0.0.1:8443 的 agent）** 在线
//    —— 本机 agent 不在会静默被 ping 门排除（0.05s 秒败），800 局墙钟差 4 倍。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//
// 开训后 30 秒自检：`grep -h "hub=" tmp/x20-floor/training-loop.log | tail -1`
//   必须等于 rl-config.json 里当时活着的 hub URL（否则整天 HTTP 530 且永不自愈，只能重启）。
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 主读数（p10 / 低分局占比）：`... --games 800 --seed0 414000`（池外判决段）
// · 回测段：`... --games 800 --seed0 415000`（本会话诊断段；见 §3 回测门）
// · 逐帧（若又出现"区分不开"）：`docs/nn-diagnosis-methodology.md` +
//   `docs/nn-diagnosis-toolkit.md`（replay-dump + 同 seed 换策略）
//
// ── 结课结算（2026-09-20 回填；终点 = it118 归档 ckpt）───────────────────
// 评估：判决段 414000 与回测段 415000，各 800 局，与 it0（x20-rebirth.it30）**同段同种子**对照。
// 数据：`tmp/x20-floor-settle/settle-report.txt`。
//
// ① 主读数 —— **未达门**
//    low%（kills≤3 局占比）判决段：it0 42.9% [39.5,46.3] → 终点 **39.5% [36.2,42.9]**（−3.4pp）
//                          回测段：it0 42.0% [38.6,45.5] → 终点 **41.0% [37.6,44.4]**（−1.0pp）
//    p10：两段**恒为 1**（未动）⇒ **下限未抬起** ⇒ 按 §4 主检点判据 = **阴性结果**。
//    （门：low% ≤35% ≈ −7.9pp 且 p10≥2 ⇒ 实差 4.5~6.9pp；low% 的 CI 与 it0 重叠
//      ⇒ **不能声称显著改善**，只能说方向对、幅度不足。）
//
// ② pooled 均杀（上限是否守住）—— **守住**
//    判决段 6.71 [6.31,7.14] → 6.68 [6.29,7.12]（−0.03）
//    回测段 6.34 [5.96,6.74] → 6.62 [6.21,7.03]（+0.28）
//    ⇒ 未破 −10% 熔断线。**A 项没有把策略推向保守陷阱**（本腿最大风险项，未发生）。
//
// ③ 分关表（终点，判决段 414000，每关 200 局）
//    s2000  6.54 / p10 1 / low 39.0% / pass 10.0%
//    s2001  6.72 / p10 1 / low 38.0% / pass  7.5%
//    s2002  7.25 / p10 2 / low 35.0% / pass 10.5%   ← 最好（唯一 p10=2 的关）
//    s2003  6.21 / p10 1 / low 46.0% / pass 10.0%   ← 短板
//
// ④ 监控项（防保守陷阱）：dmg 242.7（it0 238.4，+4.3）。
//    ⚠ **cellsVisited 缺失**（该 jsonl 无此列）⇒ 保守陷阱只能用 dmg / pass 间接判断：
//    pass 回测段 **+2.3pp**（7.6→9.9）⇒ **没有变得更保守**。
//
// ⑤ 超时/换血：timeout_frac ≈ 0（与 it0 一致，未见超时）。
//
// ⑥ 回测段（415000）结论：mean +0.28、pass +2.3pp、low% −1.0pp ⇒ **不劣于 it0（回测门通过）**。
//
// ⑦ 总评：**未达判据 ⇒ A 方案阴性**。但**上限守住 + 回测不劣** ⇒
//    下一步不必担心"改奖励定价会毁掉上限"，可放心转向 B / C（x20-steady 已开 B）。
//
// ⑧ 训练中断（非故障）：04:40 起 PPO 作业在 hub 上 30 分钟未回
//    （`wait_job 超时 >1800s`）—— 系**云机配额用尽**，预料之中 ⇒ it118 后停止，非课程或代码问题。
