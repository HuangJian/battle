// 课程 x2-rebirth —— 2 敌关（ladder-c02 / 6 种类型对）的**热启迁移**腿
//
// 状态：**2026-09-18 已结课（it25 停，用户手动）**。终点权重 = **it15**（择优理由见 §结 2）。

// ══════════════════════════════════════════════════════════════════════════
// 结课报告（2026-09-18；判据 = 承伤主端点 + 耗时约束）
// ══════════════════════════════════════════════════════════════════════════
// **一句话**：把 a2 的 1 敌关满分权重热启到 2 敌关 —— **承伤 106.4 → 33.9（−68%，
// 配对 CI 不重叠 = 显著）**，零伤通关 37.0% → 77.5%，pass 81.0% → 95.0%；
// 代价是**通关耗时 +7.9%（约束 ≤+10% 内）**。**迁移链成立**。
// 但承伤**在 it5 就见底**，之后 20 轮零改善且 it20/it25 回退 ⇒ **有效训练量只有前 5 轮**。
//
// ── 结 1. 判据走势（锚点半区 300 局，seed0 860001；bootstrap 95% CI）────────
//   | it | dmg/局 [95%CI] | 通关 ticks [CI] | pass | 零伤通关 |
//   |  0 | 106.4 [ 97.2,115.8] |  928.7 [885, 971] | 81.0% | 37.0% |
//   |  5 | **33.3 [ 26.7, 40.7]** |  909.0 [868, 951] | 92.3% | **80.9%** |
//   | 10 |  37.7 [ 30.2, 45.5] |  963.0 [923,1006] | 93.7% | 77.2% |
//   | **15** | **33.9 [ 27.2, 41.7]** | 1002.1 [959,1045] | **95.0%** | 77.5% |
//   | 20 |  46.1 [ 38.2, 54.9] |  972.0 [930,1012] | 96.0% | 70.5% |
//   | 25 |  41.6 [ 34.2, 49.7] | 1000.4 [957,1042] | 96.0% | 71.5% |
//   ⇒ **承伤**：it0 → it15 **−68%，CI 不重叠 = 显著**（判线 ↓≥20% **达标**）。
//   ⇒ **耗时**：it0 → it15 **+7.9%，CI 大幅重叠 = 无显著变化**（约束 ≤+10% **守住**）。
//   ⚠ 我 2026-09-17 23:50 曾按**点估计**判「耗时反向 +9%」—— **加 CI 后不成立**。
//     点估计会骗人：本腿据此补了「连续量结论必须带 CI」的纪律（已写进 x3-rebirth）。
//
// ── 结 2. 为什么终点取 it15（同种子配对，n=300）────────────────────────
//   · **it5 vs it15**：承伤 Δ=+0.6（配对 SE 3.9；**变差 41 局 / 变好 41 局，完全对称**）
//     ⇒ 承伤**不可区分**；pass 92.3% → 95.0%（b01=10 / b10=2，pd 4.00%，
//     McNemar 双侧 **p=0.039**）⇒ **it15 的通过率优势是显著的**。
//   · **it15 vs it25**：承伤 Δ=+7.7（SE 3.6 ⇒ **z≈2.1，it25 显著更差**）；
//     pass 95.0% → 96.0%（pd 4.33%，p 不显著）⇒ **it25 是拿承伤换 1pp 通过率，不划算**。
//   · **掉队探测**：it5 的 **bc 关 64% < it0 的 74%**（唯一相对 it0 退步的关）⇒ 掉队；
//     it15 六关全部 ≥ it0（ab 98/94、ac 96/30、ad 100/90、bc 76/74、bd 100/100、cd 100/98）
//     ⇒ **无掉队关**。
//   ⇒ **it15 = 主端点并列最优 + 约束守住 + 无掉队 + 通过率显著更高**。
//   已另存 `nn-training/weights/x2-rebirth-best/x2-rebirth.it15.best.json`（防 `keep_iters` 清理）。
//
// ── 结 3. 分关（it0 → it15）───────────────────────────────────────────
//   | 关 | pass | dmg/局 | 零伤通关 |
//   | ab |  94.0 →  98.0% | 105.0 → 50.6 | 19.1 → 55.1% |
//   | ac | **30.0 → 96.0%** | 125.4 → 29.6 | 60.0 → 83.3% |
//   | ad |  90.0 → 100.0%  | 139.0 → 22.6 | 15.6 → 78.0% |
//   | bc |  74.0 →  76.0%  |  69.5 → 47.4 | 73.0 → 89.5% |
//   | bd | 100.0 → 100.0%  |  83.4 → 48.2 | 50.0 → 68.0% |
//   | cd |  98.0 → 100.0%  | **116.0 →  5.2** | 26.5 → 94.0% |
//   ⇒ **it0 的崩点 ac（basic+power）30% → 96%**；最弱仍是 **bc（fast+power）76%**。
//
// ── 结 4. power（诊断，不作判据）──────────────────────────────────────
//   · 死因 `killerKinds`：**power 47 → 11**（it20 离线，−77%）。
//   · 但 **ac 关 `firstHitKind` 仍是 100% basic（先打 power = 0.0%）**，bc 仅 6.1%。
//   ⇒ **power 盲区没有被修好**：ac 是靠「别的能力」（躲避/清场顺序）拿下的，
//     不是靠「先打 power」。这条对 x3-rebirth 是**预先点名的未解问题**
//     （x3-rebirth 的 acd = basic+power+armor，正是 x3-start 被钉死 59–60 的那一关）。
//
// ── 结 5. 机器 ────────────────────────────────────────────────────────
//   25 轮，无 ABORT / ERROR / 降级；entropy 1.12 → ~0.99（缓降，熔断 0.25 极远）；
//   KL 0.0073 → **it15/16 峰值 0.0142**（a2 全程 mean 0.0059 / max 0.00919）
//   ⇒ **本腿位移是 a2 的 1.5–2.4 倍**（a2 的「位移是杠杆」归因再次成立）；
//   samples/轮 25–27k（target 24000，波次略超）；每轮 ≈2 分钟。
//   ⚠ rollout 节点：self 316 / mac 157 / local 104 / **a95 17 / a97 6** ⇒ 远端只承担
//   **3.8%** —— 开训前置①那条「零代码 2×」**始终没兑现**。
//
// ── 结 6. 判决 ────────────────────────────────────────────────────────
//   承伤达标（−68%，显著）+ 耗时守住（+7.9% ≤ +10%）+ 无掉队关 ⇒ **达成**。
//   ⚠ **单 run** ⇒ 未过 run 本底（≈2pp）的幅度不作断言（承伤 −68% 远超本底，不受影响）。
//   ⚠ **「达成」不等于「跑得值」**：承伤在 **it5** 就见底，之后 20 轮零改善、末段回退
//     ⇒ 有效训练量 = 前 5 轮（≈12 分钟），后面 20 轮（≈50 分钟）是纯浪费，
//     而且 it20/it25 还在往「更快但更脏」漂。这是本腿**最大的教训**，已写进 x3-rebirth。
//
// ══════════════════════════════════════════════════════════════════════════
// 以下为立项时的设计说明（保留作历史）
// ══════════════════════════════════════════════════════════════════════════
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 一句话
// ══════════════════════════════════════════════════════════════════════════
// 把 x1-rebirth-a2 在 ladder-c01（1 敌关）打出来的「满分 + 近零承伤」策略，
// 热启到 ladder-c02（2 敌关），回答三个问题：
//   ① 迁移能不能在 2 敌关**守住承伤**？（it0 实测 dmg 106.4/局 ⇒ 差 a2 的 0.877 两个数量级）
//   ② wr 已撞天花板时，「**承伤 + 耗时**」还能不能继续改善？
//   ③ a2 留下的 open limitation #2 正面撞一次：多敌关是否需要一个真正的「朝敌」信号。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 立项依据（两条前序腿的交接）
// ══════════════════════════════════════════════════════════════════════════
// · **x1-rebirth-a2（提供起点）**：从 scratch 起 46 轮把 ladder-c01 打到
//   eval argmax **1.0000**、训练内 rollout **连续 5 轮 1.0000**，同时把每局承伤
//   从 **81.3 压到 0.877**、ticks 1432 → 227（**满分与近零承伤同时达成**）。
//   三条方法论（`docs/nn.progress.md §56`）：① **「排序正确」≠「行为会照排序走」**
//   （策略按自己的技能算期望，要按**当前技能**估 EV）② **权重必须用真实语料标定**
//   ③ **杠杆是「边际收益/成本之比」，不是终局项**。
//   两条**未验证的局限**：a) 「100% / 0 伤」是 **1 敌关**的成绩，**不可外推**；
//   b) `cellsVisited` 是**半解** —— 奖励「走到没去过的格子」，不奖励「朝敌人走」
//   （39 个 metric 里没有距离/朝向类）。
// · **x2-start（提供环境定义）**：同一个 ladder-c02 的 BC 热启腿，起点 35.5%、
//   80 轮后在 arena2-acbc 口径上仍 64.0% ⇒ **训练量在该关集上零增益**。
//   本腿**不复用它的奖励**（它不含承伤项，`-wDmg*playerDamageTaken` 从未被挂对过），
//   只共用环境。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 实测（2026-09-17，本腿的起跑线；同关同种子 seed0 860001）
// ══════════════════════════════════════════════════════════════════════════
// 口径 A —— **训练内 rollout**（每关 10 seeds × 6 关 = 60 局；
//   `tmp/x2-rebirth-probe/rollout-a2`，报表 `report-a2.txt`）：
//     a2-it46 **60/60 = 100% 通关** ｜ dmg **122.6/局** ｜ ticks 976 ｜ shots 17.8
//     ｜ cells 48.6 ｜ 局均决策步 **99.2** ⇒ `est_samples_per_game` 的标定来源。
// 口径 B —— **贪心 argmax**（6 关 × 50 局 = 300 局；`tmp/x2-rebirth-probe/a2-eval.jsonl`）：
//   | 关 | ab | ac | ad | bc | bd | cd | **pooled** |
//   | pass | 94.0% | **30.0%** | 90.0% | 74.0% | 100.0% | 98.0% | **81.0%** |
//   | dmg/局 | 105.0 | 125.4 | 139.0 | 69.5 | 83.4 | 116.0 | **106.4** |
//   | avgTicks | 916 | 513 | 969 | 702 | 1015 | 799 | **819** |
//   | 零伤通关占比 | 18% | 18% | 14% | **54%** | **50%** | 26% | — |
// 参照臂（同口径、同 300 局、同种子；**只作参照，不作门** —— AGENTS §0.2）：
//   | 臂 | pooled pass | dmg/局 | avgTicks |
//   | **scratch**（随机初始化 = 本环境的「从零」） | 0.0% | 208.3 | 981 |
//   | **a2-it46**（本腿起点） | **81.0%** | **106.4** | 819 |
//   | God AI（规则教师） | 73.7% | 132.6 | 712 |
// ⇒ **三条读数**：
//   ① **迁移成立**：1 敌关的满分权重直接搬到 2 敌关就有 81.0%（贪心）/ 100%（采样），
//      **已高于教师 73.7%**，承伤也低于教师（106.4 vs 132.6）⇒ 「1 敌学的行为可迁移」。
//   ② **两个口径在 ac 关上分歧 70pp**（贪心 30.0% vs rollout 100%）⇒ **ac 的读数必须
//      两口径并列**，单口径不足以判（本项尚未解释清楚，见 §7 局限 4）。
//   ③ **承伤与耗时都远没到底**：dmg 106.4（a2 在 1 敌关是 **0.877** ⇒ 差 120 倍）、
//      avgTicks 819（比教师慢 15%）⇒ **本腿的空间在这两项上，不在 wr**。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. ★ 承伤专项：迁移过来的策略在 2 敌关「从不先打 power」
// ══════════════════════════════════════════════════════════════════════════
// `firstHitKind`（贪心口径 300 局；1 敌关该读数恒为 power，**无判别力**，
//   2 敌关首次让它有意义 —— 见 x1-rebirth 的判据④）：
//   · ac(basic+power)：50/50 **全是 basic**  · bc(fast+power)：50/50 **全是 fast**
//   · cd(power+armor)：50/50 全是 power（本关只有这两种兵种，**无判别力**）
//   ⇒ **含 power 且另有一位非 power 的关里，a2 的策略 100% 从不先命中 power** ——
//     与 BC 线「教师 33.3% vs NN 0.5%」（arena2-acbc 口径）是**同一个盲区**。
// 死因 `killerKinds`：ac 关 50 局里 **34 局死于 power**（basic 仅 1 局）；bc 关 13 局死于 power。
//   ⇒ power 全程存活到最后处刑，与 §50 段的记载一致。
// **本腿对 power 的立场**：只作**诊断**，不作判据（用户 2026-09-17 指定判据 = 承伤 + 耗时）。
//   它回答的是「热启能不能改掉 a2 继承来的盲区」；答案若在 it20 仍是否 ⇒ 见 §6 止损分支三。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 奖励：与 x1-rebirth-a2 逐字一致（用户 2026-09-17 拍板）
// ══════════════════════════════════════════════════════════════════════════
// 公式 / 参数 / terminal **全部照抄 a2**，一个数不改：
//   `wKill 3.0 / wHit 0.6 / wWin 2.0 / wDmg 0.03 / wShot 0.05 / wExplore 0.2`
//   terminal `lives_exhausted −5.0 / timeout −2.0`
//
// **为什么不做「按 2 敌重标」**（本腿立项时准备做，实测后放弃）：实测「每多走一格」的
//   承伤在**两个分布上符号相反** ——
//     · 随机态（scratch，60 局）：**+3.08 dmg/格**（比 a2 的 1 敌关 +2.39 高 29%）
//       `tmp/x2-rebirth-probe/report.txt`
//     · **起点分布（a2-it46，60 局）：−0.95 dmg/格**（wander dmg 73.6 < sit 116.3）
//       `tmp/x2-rebirth-probe/report-a2.txt`
//   ⇒ **「多走 = 多挨打」只在「还不会打」的随机态成立**；会打之后，走得多反而挨得少
//     （敌人被先清掉）。本腿的起点已经是「会打」⇒ **a2 的剂量在这里不过松也不过紧，
//     没有重标依据**，改它只会牺牲归因（起点分布上的 EV 表见下，排序与 a2 同向）。
// 起点分布上的 EV 表（a2 剂量，n=60，60 局**全部通关** ⇒ kill+win 恒 +8.00）：
//   | 档 | explore | hit | kill+win | dmg | shot | **EV** |
//   | sit   (cells≤p25) | +5.05 | +2.48 | +8.00 | −3.49 | −0.44 | **+11.60** |
//   | wander(cells>p75) | +14.00 | +3.60 | +8.00 | −2.21 | −1.29 | **+22.10** ← 最优 |
//   | rush  (dmg>p75)  | +9.75 | +2.92 | +8.00 | −9.01 | −0.85 | **+10.81** |
//   ⇒ **走 > 坐 > 莽**，与 a2 同向。
// ⚠ **但 rush 的 EV 是正的（+10.81），这条必须写清、结算时别误读**：起点分布上 60 局
//   全通关、kill+win 恒 +8.00，承伤项（−3.49 ~ −9.01）压不到零以下。要让 rush 转负需要
//   `wDmg > 0.066`，而 a2 明写「**别用加剂量硬压承伤**」（会把策略推向「不挨打也不杀敌」
//   的保守陷阱）。⇒ **本腿的「0 伤」靠的是排序（走/坐 优于 莽），不是靠把莽罚成负数**；
//   结算只看 dmg 是否向 0 收敛，**不看单项符号**。
// ⚠ **这张表是「起点条件分布」下的量，不是全局不变量**（2026-09-17 评审补）：
//   60 局全通关 ⇒ `kill+win` 恒 +8.00，才把 rush 托在 +10.81。若训练早期 wr 退化
//   （局限 2 预告的「先降后升」），`kill+win` 方差回归、终局项开始分化，**「走>坐>莽」可能翻转**
//   ⇒ 那时**别拿这张表去判「学错了」**；判换血的唯一入口是分支二（**dmg↑ 而 wr 不降**）。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 起点与缰绳（本腿与 a2 唯一的语义差）
// ══════════════════════════════════════════════════════════════════════════
// `bc = x1-rebirth-a2.it46`（a2 的终点权重，**结课归档原件**，不复制不改名）。
//   已核验可作热启源：`format nn-weights-json` / `schema_major 3` / 42 个参数键与
//   `scratch-init.json` **同集**（含 value 头），与 `x3-start` 拿 `x2-start.it50`
//   当 bc 是同一套先例。
// `kickstart_ref = true`（用户 2026-09-17 拍板）—— **a2 是关着的，本腿开**：
//   · a2 关它有一个**只对随机初始化成立**的理由：那个 bc 是随机权重，「开着等于把策略
//     钉在随机策略上」。本腿的 bc 是**一条学到手的策略**，钉住它恰恰是要的。
//   · 与两个热启先例一致：`x2-start`（BC 热启）、`x3-start`（PPO 热启 it50）都是 true。
//   · 量级（per-tick 的 kickstart 系数 = `kickstart_kl 1.0 × 0.5^(it−1)`，
//     `run_rl.update_kwargs`；与本腿 `kl_coef` 0.02 **并列相加**）：
//     it1 总锚 **1.02** ｜ it3 0.27 ｜ it5 0.08 ｜ it10 0.02 ｜ ~it36 归零
//     ⇒ **实质只锁 it1–7**，即「别把 a2 的 0 伤行为在第一步冲掉」那个窗口；
//     之后完全交给位移包。a2 的爬升发生在 it26–32，**在锚的窗口之外**，两者不冲突。
// `warmup_iters = 0`：kickstart 的硬要求（非 0 会让首轮系数归零，config 层直接拦）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 判据与止损（判据由用户 2026-09-17 指定）
// ══════════════════════════════════════════════════════════════════════════
// **主判据 = 承伤下降 + 耗时下降**（两条都要，缺一不算过关）：
//   ① **头号：dmg/局** —— 起点 106.4（贪心）/ 122.6（rollout）⇒ 看它**向 0 收敛**。
//      配套读 **零伤通关占比**（起点分关 14/18/18/26/50/54%）。
//      ⚠ 承伤高也可能是「敢交火」的正常代价：剂量调整必须同时看 wr 与 kills ——
//      出现「dmg↓ 但 kills 也↓」立刻回退（那是恐惧，不是干净）。
//   ② **第二：avgTicks/局** —— 起点 819（贪心）/ 976（rollout）⇒ 看它下降。
//      参照：教师 712（**只作参照**，不作门）。
//   ③ **掉队探测**：分关 pass 只用来发现「有没有哪一关被牺牲」（x2-acbc 的
//      「拆东墙补西墙」就是这种失败），**不作门** —— it0 在 rollout 口径已是 100%。
//   ④ **诊断项（不作判据）**：entropy 轨迹（缓降=健康；**一步坍缩到 ≈0 = 坏**）、
//      **行为分布是否出峰**（a_move 归一化熵；⭐ **不要用「KL 是否进某个区间」当判据**
//      —— a2 已证那条 0.01–0.03 的带是拍出来的错的，0.005 量级 + 分布出峰就已足够）、
//      power 读数（`firstHitKind` / `killerKinds` / 分关 pass）。
//   ⑤ **两个口径都记**：贪心 vs rollout 在 ac 上分歧 70pp（30.0% vs 100%），
//      单口径不足以判任何一关。
//
// **不走「毕业门」**：roadmap D4 的 ≥80% pooled 门在 **开训前就已满足**
//   （rollout 100% / 贪心 81.0%）⇒ 拿它当门形同虚设。本腿的达成口径 =
//   **「dmg/局 与 avgTicks/局 双双显著下降，且没有任何一关掉队」**；
//   **不预设数值阈值**（§0.2：DoD 不得由教师表现或历史腿的数字反推），
//   结算时对照 it0 两口径给出。
//
// **结算统计口径（预注册 —— 免掉事后挑口径；2026-09-17 评审加）**：
//   · 以**同种子配对差值**为主，**pooled 与分关并列**给出；dmg / avgTicks 报 **均值 ± CI**
//     （n = 6 关 × 50 局 = 300；bootstrap 或正态近似皆可）。
//   · **ac 关必须两口径并列**（贪心 / rollout）—— 已知它们分歧 70pp（见局限 4）。
//   · 必须写明**噪声地板**：同配置两个独立 run 在池外 800 局口径下可差 **2.000pp**（T6 实测）
//     ⇒ **单臂 <2pp 的差异不是效应证据**。本腿不为此加 run（无 GPU 预算），但结算文字里
//     不得把 1pp 级的移动说成改进。
//   · 本协议**只规定怎么算，不含阈值** ⇒ 不违反 §0.2。
//
// **止损（it20 一次中途检点）**：
//   · **分支一（主）**：dmg 与 avgTicks **双双不动或反向**，且行为分布**不出峰**
//     ⇒ 停腿，判「热启 + 位移包在 2 敌关不足以改写行为」；下一手是**加朝向/距离 metric**
//     （独立工程）或回到 scratch 臂，**不是**加轮数。
//   · **分支二**：dmg↑ 而 wr 不降 ⇒ 策略在**换血** ⇒ **即使通过率好看也判「学错了」**
//     （递进 ladder 上换血必崩，升不了档）⇒ 调奖励结构，**不加轮数**。
//   · **分支三（power）**：含 power 的三关（ac/bc/cd）pass 与 `firstHitKind` 在 20 轮内
//     完全不向 power 移动 ⇒ 记「热启继承的盲区改不掉」，作为「多敌关是否需要朝向信号」
//     的正面证据；**不因此停腿**（power 不是本腿判据）。
//   · **分支四（自锁）**：entropy 一步坍缩 / KL 暴涨 ⇒ 先查 bc 载入与 kickstart 量级
//     （本腿 it1 的锚是 1.02，与 a2 的档不同），再决定是否回退。
//   ⚠ **优先级：分支一 > 分支三**（2026-09-17 评审补）。it20 若「dmg/ticks 双不动 +
//     行为分布不出峰」与「power 也不动」**同时命中** ⇒ **按分支一停腿**，
//     power 那条只作停腿报告里的一条证据，不得用它把腿续下去。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 已知局限（勿外推）
// ══════════════════════════════════════════════════════════════════════════
// 1. **起点是热启** ⇒ 「环境」与「起点」两个变量同时动。本腿**不与 x2-start 直接对照**
//    （它 BC 热启、另一起点、另奖励），也**不回答**「从零在 2 敌关能不能学」——那是另一条腿。
// 2. **起点 rollout 100% 通关** ⇒ wr 无判别力。若训练早期有退化，wr 会呈**先降后升**，
//    别误读成「学错了」；反之也别把 100% 当成「没事」。
// 3. `cellsVisited` 是**半解**：本腿第一次真正撞上它（a2 的 open limitation #2）。
//    it20 若行为分布不出峰 ⇒ 才轮到「加朝向/距离 metric」（要动 TS 导出 + bump
//    `metrics_version` ⇒ 全部历史语料与归档权重作废，是独立工程）。
// 4. **贪心 vs rollout 在 ac 上分歧 70pp 未解释清**（30.0% vs 100%）⇒ 结算须两口径并列，
//    且不要把该分歧的扩大当作策略改进。
// 5. `est_samples_per_game` 只是**首轮兜底**（之后由最近 5 轮 trailing 均值覆盖）；
//    它是**起点分布**的值（99.2），若策略退化导致局变长，第二轮起会自动跟上。
//
// ══════════════════════════════════════════════════════════════════════════
// 8. 预算（用户 2026-09-17 拍板 iters=80）
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮` = **与 a2 同一每轮样本量** ⇒ 位移包的力度不变
//   （a2 实测每轮 KL 0.0059、46 轮把行为分布推成形）。6 关均分 = 4000 samples/关
//   = **40 局/关 × 6 关 = 240 局/轮**（`G0 = ceil(4000 / 100) = 40`；est 取整方向：
//   实测 99.2 → 写 **100 向上取整** ⇒ 首轮欠采 ~0.8%，第二轮起由 trailing 均值 + 补波自动纠偏）。
// · `iters 80`（a2 是 60、实际 46 轮满分结课）⇒ 给 1.33× 余量；收敛早了自己手动停。
//   `max_hours 12` 兜底（a2 实测 46 轮 = 112 分钟，其中 rollout 42% / ppo 54%）。
//   ⚠ 本腿单轮比 a2 便宜（局更短），但**别把 80 轮当成「必须跑满」**：a2 的 c6-bonus 式盲跑
//   教训就是磨到墙钟上限才停。

{
  "version": 5,
  "name": "x2-rebirth",
  // 每一决策步一条样本（非 intent/goal 窗口），与当前 PPO 管线匹配。
  "mode": "per-tick",
  // 环境唯一来源 = levels/ladder-c02.jsonc（6 关 = 6 种类型对 ab/ac/ad/bc/bd/cd，
  // 每关 2 敌、队列 20 取前 2、max_ticks 2100、lives 1、hard）。课程侧禁止再写
  // stages/difficulty/max_ticks/player（§2.4 数据优先 / 任务侧不可动）。
  "level": "ladder-c02",

  // ── 奖励：与 x1-rebirth-a2 逐字一致（见文件头 §4）────────────────────────
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      // 承伤项是「0 伤」这条任务定义的载体（`combat.ts:599`：非致命命中 →
      // playerDamageTaken；致命 → playerHits，1 命下恒 = 死亡指示 ⇒ 历史挂在
      // playerHits 上的 wDmg 是个伪装的死项，a2 才第一次把它挂对）。
      // 不按 2 敌重标的理由见文件头 §4（起点分布上「每格承伤」已翻号成 −0.95）。
      "wDmg": 0.03,
      "wShot": 0.05,
      // 半解：只奖励「走到没去过的格子」，不奖励「朝敌人走」（39 个 metric 无距离/朝向类）。
      // 本腿起点分布上仍给「走 > 坐」（+22.10 vs +11.60）；it20 若分布不出峰见 §6 分支一。
      "wExplore": 0.2,
    },
    "terminal": {
      // a2 的梯度：0伤通关 +5.35 > 换血通关 +4.22 > 冒险 −2.25 > 苟 −3.0 > 死亡 −5.75；
      // timeout 只是常数项，**改不动「坐 vs 走」的边际比**（杠杆是 wExplore:wDmg）。
      "lives_exhausted": -5.0,
      "timeout": -2.0,
    },
    "scheme": "toy",
  },
  "dodge": "",

  // ── 起点：a2 的结课归档（热启，不是蒸馏；见文件头 §5）────────────────────
  "bc": "nn-training/weights/x1-rebirth-a2/x1-rebirth-a2.it46.20260917-215130.json",
  // 与两个热启先例一致；a2 关它是因为它的 bc 是随机初始化，理由不适用于本腿。
  "kickstart_ref": true,
  // kickstart 的硬要求：非 0 会让首轮系数归零（config 层直接拦）。
  "warmup_iters": 0,

  // ── 预算 ───────────────────────────────────────────────────────────────
  "iters": 80,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：与 a2 逐字一致（本腿要外推的正是它）─────────────────────────
  // a2 的结课结论：x1-rebirth 的 42 轮衰退归因于**每轮位移太小**（KL 0.0029、
  // entropy 42 轮只降 5%），不是奖励权重失衡。放大位移后 entropy 降 44.65%、
  // a_move 归一化熵 0.9978 → 0.919–0.946（明显出峰）。
  // ⚠ a2 的反向警示：**KL 自始至终没进过 0.01–0.03 那条带（0/46）** ⇒ 那条带是错的，
  //   判据应改用「行为分布是否出峰」。
  "lr": 0.0005, // 3e-4 → 5e-4（1.67×；scratch init 的配套标定是 3e-4）
  "epochs": 6, // 4 → 6（每轮多更新 50%；不改变单步尺度，最安全的位移放大）
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段：松开 KL 惩罚（0.05 → 0.02）+ lr/epochs 放大位移。
    // ⚠ 本段与 kickstart 的锚**并列相加**：it1 总锚 1.02、it5 0.08、it10 0.02（见 §5）。
    { "until_iter": 40, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段：kl 继续放松（0.02 → 0.01，本腿假设「还差得远」，与 x1 的收紧方向相反是有意的）；
    // lr 砍半做小步精修。
    // ⚠ **kl_cap 两段都显式写 0.2**（2026-09-17 评审订正）。本文件初稿从 a2 抄了
    //   「尾段缺席无害 / 在 serial/remote 路径不接线、恒不生效」——**前半对，后半把话说满了**：
    //   · **对的部分**：per-tick 的 **remote/serial 路径确实不接线** —— `ppo_update` 无形参、
    //     `remote/worker.py` 只读 `kl_coef`，全仓消费者只有 `rl/stream.py`（+ manifest 打包）
    //     ⇒ DECISIONS §2026-09-15-goalnn-kl-cap-unwired 已把口径钉死，并**明令不得**把它写成
    //     生效硬顶/「咬合」。本腿默认就走这条路径：`rl-config.json` 的 `rl.stream = 0` 压过
    //     `cli.py:_d("stream", 1)` 的 argparse 兜底（**已实测 `args.stream = 0`**），
    //     `--ppo remote` 还会在 `rl/config.py:207` 再强制置 0
    //     ⇒ **默认路径下，写不写这个键都不影响训练**。
    //   · **话说满的部分**：「**恒**不生效」漏了 **stream 路径** —— 那里读 `args._kl_cap`，
    //     而 `args._kl_cap = sch.get("kl_cap")` 是**每轮整个重赋**（`rl/loop_steps.py:771`）
    //     ⇒ **段内缺席即 None ⇒ 回落 `policy.streamKlCap`**（`rl/stream.py:163-166`）：
    //     **本仓 rl-config.json 给的是 0.18**（0.06 只是代码里键缺失时的字面默认）。
    //     ⇒ 缺席会在 it40 造出一次与 `kl_coef` 放松**方向相反**的不连续（cap 0.2 → 0.18）。
    //   ⇒ 两段都写 0.2：**cap 在默认路径下是惰性护栏，不是调度旋钮**；写显式值只为了消掉
    //     「依赖 rl-config + 依赖运行路径」的隐式回落，**不是**宣称它在本腿生效。
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 },
  ],

  // ── 采集：严格样本量配额（a2 同剂量；见文件头 §8）────────────────────────
  "target_transitions": 24000,
  // 首轮兜底的局均 **samples**（不是 ticks）：起点分布实测 99.2（6 关 × 10 局）。
  // 之后由最近 5 轮 trailing 均值自动覆盖 ⇒ 不进身份。
  "est_samples_per_game": 100,
  // 仅种子轮转用（本腿走 volume 模式，种子流由 (rotate_seed, it, stage, wave) 键控，
  // rotate_seed 来自 --seed ⇒ 此键在 volume 模式下**不参与**；保留只为与 a2 对齐
  // 与老模式回退）。多关课必须除以关数：a2 单关是 600 ⇒ 本腿 600/6 = 100。
  "seed_rotate": 100,

  // ── 路径：bc 语义变了 ⇒ 必须新目录、禁续跑（§15.5）──────────────────────
  "out": "tmp/x2-rebirth/weights.json",
  "traj": "tmp/x2-rebirth",
  // 熔断线：它是「**探索崩掉**」的护栏，不是「正常下降」的阈值 —— 别因为「学得快、
  // entropy 掉得猛」就去下调它（真接近了那是坍缩，该熔断）。
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x2-rebirth",
  "backup_prefix": "x2-rebirth",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 6 种类型对：ID 2000-2005（ab/ac/ad/bc/bd/cd；`rl/ladder_factory.py:228` 同口径）。
  // 日常 50/关（6×50 = 300 局/次，双轨 A-eval 锚点段 + 轮转段 = 每关 100 局）；
  // 正式门/正式对照才上 200/关。
  "eval_stages": "2000-2005",
  "eval_games_per_stage": 50,
  "eval_every": 5,
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① **rollout 节点先弄活**（零代码 2×，优先级最高的前置）：a2 实测本机承担 98%
//    （self 399 / mac 200 / **a95 6 / a97 6**）⇒ 把 a95/a97 修好是唯一真正省墙钟的动作。
//    本腿规模：80 轮 × 240 局；按 a2 单轮 2.4 分钟外推（2 敌 sim 略贵）约 3–4 小时，
//    `max_hours 12` 兜得住 —— **所以这不是「跑不完」的问题，是「白烧配额」的问题**。
// ② bc：`nn-training/weights/x1-rebirth-a2/x1-rebirth-a2.it46.20260917-215130.json`
//    （已核验：379KB、`schema_major 3`、42 个参数键与 scratch-init 同集含 value 头）。
//    `kickstart_ref: true` 要求该文件存在，缺失会响亮报错（"要求课程 bc 权重存在"）。
// ③ 环境：`ladder-c02`（6 关 ab/ac/ad/bc/bd/cd，各 2 敌、队列 20 取前 2、max_ticks 2100、
//    lives 1、hard）。**任务侧一个字不动**（AGENTS §0.2）。
// ④ transport：显式 `--remote-transport pull`；hub 可达 + eval 节点 epoch 对齐
//    （`plan/eval-outdated.plan.md` 检查单）。
// ⑤ **新 out/traj 目录**：bc 与奖励语义都换了起点 ⇒ 禁续跑（§15.5）。
// ⑥ 并发配额：若与其它课并行，先确认 `rl-config.json` 的 `courses` 块总和不超裸机容量
//    （`dashboard/src/core/slots.ts:checkCapacity`：Σmax(workers, local_slots) ≤ max(rl.workers, rl.local_slots)）。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//
// ── 诊断工具（本腿会用到）────────────────────────────────────────────────
// · 起跑线 / 边际算术 / EV 表（两套语料已落盘）：
//     `bash tools/githook/nn-py-safe.sh tmp/x2-rebirth-probe.py`      # scratch 分布
//     `bash tools/githook/nn-py-safe.sh tmp/x2-rebirth-probe-a2.py`   # a2 起点分布
//   语料：`tmp/x2-rebirth-probe/rollout-scratch`（60 局）/ `rollout-a2`（60 局）；
//   报表 `report.txt` / `report-a2.txt`（中文报表落 UTF-8 文件，控制台是 GBK）。
// · 剂量网格（换成别的 wDmg/wExplore 会怎样）：`tmp/x2-rebirth-dose.py`
// · 贪心分关 + power 读数（`firstHitKind` / `killerKinds`）：
//     `bun tools/sim/eval-course-ckpt.ts --course nn-training/levels/ladder-c02.jsonc \
//        --weights <归档权重> --games 300 --seed0 860001 --workers 8 --out <jsonl>`
//    参照臂加 `--policy god`（无 --weights）。
// · 训练走势 `tmp/x1-trend.py`｜动作直方图 / 移动熵 `tmp/x1-acthist2.py`
//   ｜时间账 `tmp/a2-timing.py`（三者把路径改指向本腿即可；⚠ shard 每轮会被清理，
//   动作分布诊断要及时做）。
//
// ── 结算节（2026-09-18 已回填 → 见文件头「结课报告」）──────────────────────
// 已填：判据走势（6 个 eval 点，含 bootstrap CI）、终点择优（同种子配对 + 掉队探测）、
//   分关 it0→it15、power 读数、机器健康、判决。终点权重 = **it15**
//   （另存 `nn-training/weights/x2-rebirth-best/x2-rebirth.it15.best.json`）。
// 未填（本腿没做，留给后腿）：`a_move` 归一化熵（行为分布出峰的直接读数）、
//   每轮样本量浪费率（collected vs 24000）。
// ⚠ 判据口径已在结算时**修订**（2026-09-18）：由「承伤 + 耗时双主端点双降」改为
//   「**承伤主端点 + 耗时约束（≤+10%）**」—— 实测承伤能降 68%，而耗时根本没动
//   （CI 重叠）。x3-rebirth 已按新口径写死。
