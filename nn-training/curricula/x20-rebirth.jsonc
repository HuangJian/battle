// 课程 x20-rebirth —— 20 敌 1 命（ladder-c20-lives1）的**总攻腿**
//
// 状态：**已停腿（用户手动，2026-09-19，trainer 在 it97 publish 前意外退出；
//   最后归档 it96），终点结算已跑（M2 + 池外段 + 双回测，见结算节）。无合格终点。
//
// 本腿是 20 敌中长期目标的总攻（§0.2 的 1 命任务定义落地；D1/D2 的阶梯命数在此让路 ——
// 程序事项见 §7）。bc = x6-rebirth.it45.best（c20-1命零样本对决胜者：
// 11.0%/6.96 vs x7best 8.4%/6.13，同段 411000 配对，方向全优）。
// 回答三个问题：
//   ① 4v1 技巧 + 续航能不能拼出 20 杀？同场最多 4 敌（MAX_ENEMIES_ALIVE），c04–c07
//      已把 4v1 打满 —— 剩的是续航（5000+ tick 不死）+ 队列深度（20 个），不是新战术。
//   ② dense-only 配方行不行？本腿是用户命题的正面实验：去 wWin、去双 terminal，
//      只留持续项（§3）。1 命本身即终局（死了整局作废，机会成本高达 +60），
//      20 敌围歼下苟均衡不存在（待验证，不断言 —— 超时熔断是保险，见 §5）。
//   ③ kills 里程碑能不能当进展读数？it0 贪心 3.63 / rollout 3.88 ——
//      pass 11% 在地板上（读它全是噪声），kills 有整段梯度（7→12→16→20）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（nn-training/levels/ladder-c20-lives1.jsonc；4 出场变体，见 §1 立项④）
// ══════════════════════════════════════════════════════════════════════════
//   **4 关**：ladder-c20-lives1-{abcd,bcda,cdab,dabc}，forces 为 `abcd` 循环的 4 个轮转
//   （成分恒 5/5/5/5；`count 20` ⇒ 队列取满；各关先出类型不同）。
//   同场上限 4 敌（与 c04–c07 同并发）；max_ticks **12900**（D7 立案式，原样继承）；
//   lives **1**、hard、空场。自定义关 ID **2000-2003**（4 变体；c07 体例推广）。
//   任务侧 = §0.2 的 1 命定义（D11 原 3 命口径不混入；见 §7.1）。
//   结构后果：真·分关掉队（见 §4）；seed_rotate 150（600/4）；per-stage = 6000。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 立项依据（2026-09-18；c20 transfer probes + 用户三问）
// ══════════════════════════════════════════════════════════════════════════
// · c20-3命 83%（x6best 零样本）vs c20-1命 9.9% —— 命数即难度，§0.2 vs D1/D2 矛盾爆发。
//   本腿按 §0.2（用户亲定）打 1 命。
// · c20-1命零样本对决（800 局 seed0 411000，同种子；`tmp/xfer-c20l1/`）：
//   x6best 11.0% / 6.96杀 / 命中率51.9% vs x7best 8.4% / 6.13杀 / 51.1% ——
//   方向全优 x6 ⇒ **bc = x6-rebirth.it45.best**（x6 = ladder-c06 终点，x7 才是 c07
//   （其 bc 即 x6.it45）；c06 通用技巧 transfer —— 与"同场4敌"机制一致）。
// · **④ 出场变体（用户拍板；c07 顺序决定论的直接推广）**：单变体下先打谁 = 谁先出
//   （c07 干预 200/200 铁证）⇒ ladder-c20-lives1 扩成 4 轮转变体（配置级改动，
//   零引擎改动；D11 合规见 §7.1 —— 命数 tier 变化触发立案，四项已逐项打勾）。
//   firstHit 从此退出读数（均匀即正常）。
// · 起步形状警告（诚实写在前面）：pooled 贪心 **2.0%**（分关 6/0/2/0%）——
//   全链最低（x5 的 27% → 部分达成；x1 的 4% → 衰退）。2% 是 x1 形状，不是 a2 形状。
//   敢开的三个理由：① a2 位移包（低起步唯一成功先例）；② dense-only（无苟地板，
//   坐的 EV 没有托底）；③ 20 敌围歼坐不住（待验证，超时熔断是保险）。
//   若 it40 kills 不到 5.5（+50%），按 §5 停腿 —— x1-repeat 的熔断写在前面。
// · 4v1 论证保留（用户提出，已核）：`MAX_ENEMIES_ALIVE = 4` ⇒ c20 的每一秒战斗
//   都是打过的仗；剩的是续航 + 队列深度。坐不住的实证：rollout shots 90.7/局。
// · dense-only 立项（用户命题；跨关迁移数据支持一半）：x6-it45 回测 kills ±1-2%
//   vs pass −3pp/dmg +4%（`tmp/xfer-check.py`）—— dense 跨关，结局逐关。
//   wWin 可删（杀光=通关，+2.0 只是 top-up；离线 EV 排序不变）；双 terminal 可删
//   **仅因为** 1 命20敌（机会成本 +60 淹没 −5；围歼下苟均衡不存在待验证）。
//   ⚠ 这是全链第一次删终局项 —— 超时熔断（§5）是预注册的反悔键，不是装饰。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 实测（2026-09-19，bc = x6-rebirth.it45.best；god 同种子参照；
//    单变体旧数已作废，以下全是 4 变体新数）
// ══════════════════════════════════════════════════════════════════════════
// 口径 R —— **rollout**（采样，4 关各 10 局 seed 0–9，共 40 局；`rollout-x6-v4-lives1b`；
//     ⚠ 2026-09-19 命数事故订正：首版探针漏传 `--lives-override`，静默跑成 3 命
//     （47.5%/715.5/478.8 —— 全是 3 命数，已作废）；以下为补跑的 **1 命真值**
//     （manifest `startLives: 1` 已验；根因已修：export-rl-rollout 无 flag 即响亮失败）：
//     1/40 = 2.5% 通关 ｜ dmg **281.8/局** ｜ ticks 1455 ｜ steps **146.8**
//     ｜ kills **3.88** ｜ hits 9.70 ｜ shots 27.5 ｜ cells 59.1 ｜ 命中率 35.3%
//     ｜ pu 0.78/局 ｜ stars 0.07 ｜ spawn 1.30/局
//     分关（胜/杀）：abcd 10%/6.2 ｜ bcda 0%/2.9 ｜ cdab 0%/3.3 ｜ dabc 0%/3.1
//     ｜ 边际 **+0.90 dmg/格**（sit 259.6 → wander 324.2；LSQ +1.00 交叉确认 ——
//       符号从 3 命版的 −2.71 翻正：1 命下走路要付费，与 a2 的 scratch 态同构，
//       但 EV 排序不变，剂量不动）
//     ｜ EV 走 +45.6 > 莽 +29.4 > 坐 **−2.03**（坐是负数 —— 1 命下坐=等死，
//       dense-only 重算（去终局项，1 命真值语料 n=40）走 +57.7 > 莽 +40.0 > 坐 +3.3，
//       排序不变 ✓；单变体旧数 +50.1/+33.9/+3.0 作废，方向一致）
//     ⇒ `est_samples_per_game` 取 **147**（146.8 向下取整；见 §6；3 命旧 478 作废）。
//     首轮 ≈ 24000/147 ≈ **163 局**（≈40 局/关；1 命局短，局数比 3 命版多 3 倍 ——
//     est 高估的方向是安全的，trailing 5 轮纠偏）。
// 口径 G —— **贪心**（4 关各 50 局 seed0 860001，共 200 局；`it0-v4.jsonl`，里程碑分母）：
//     pass **4/200 = 2.0%** ｜ dmg **227.5** ｜ ticks_win **1461**（仅 4 局，方差极大）
//     ｜ kills **3.63** ｜ shots 25.5 ｜ pu 0.70 ｜ gameover 196
//     分关 pass：abcd 6% / bcda 0% / cdab 2% / dabc 0%（两关挂零 ——
//     掉队规则 floor 注记：0% 起步退无可退，规则只看 abcd/cdab 是否退步，见 §4）。
//     ｜ firstHitKind basic 51 / fast 51 / power 50 / armor 48（**均匀**，200/200）。
//     ｜ killerKinds pooled：power 51 / basic 48 / armor 38 / fast 30（power 仍居首）。
// 参照臂 —— **god**（同种子 200 局；只作参照）：pass **30/200 = 15.0%**
//     （18/20/10/12%）｜ dmg 234.4 ｜ kills 7.55 ｜ pu 2.44 ｜
//     firstHit fast 65/power 59/armor 41/basic 32（教师也被顺序带着走）。
//     ⇒ 起步 pooled 2.0% vs 教师 15.0%（−13pp）；单变体旧数（11.5%/7.49杀）作废。
// 采样/贪心鸿沟（1 命真值）：rollout 2.5%/3.88杀 vs 贪心 2.0%/3.63杀 ——
//   pass 差 0.5pp，kills 差 0.25，**双双贴地板，无鸿沟**。
//   ⚠ 旧 71pp/45pp 鸿沟已证伪：那是用 3 命 rollout 对 1 命贪心比出来的（命数都没对齐），
//   不是 mode-collapse。⇒ "能力在分布内" thesis 在 c20-1命**不成立**（能力哪哪都不在）。
//   双口径保留（方法上便宜，M2），但不再声称它能捕捉隐藏能力 —— 判决看 kills 里程碑 + pass 门。
// 剂量复核（结论：**dense-only 开腿**）：
//   · 公式：`wKill*kills + wHit*enemyHits - wDmg*playerDamageTaken - wShot*playerShots
//     + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)`
//     （去 wWin、去双 terminal；其余七项数值逐字沿用 x6）。
//   · wShot：rollout 35.3% ⇒ +0.212 ✓（1 命真值；3 命旧 45.8% 作废）；wDmg/wExplore：边际 +0.90
//     （符号从 −2.71/−0.67 翻正：1 命下走路付费，但 +0.90×0.03=0.027/格 << +0.2/格探索收益，
//     净 +0.17/格，EV 走最优 ✓ —— 剂量不动，边际符号翻转不影响排序，见 §2）；
//     wPickup 3.0：1 命真值 pu 0.78/局（3 命旧 4.88 作废）⇒ +2.34 vs kill+win
//     ≈11.6（kills 3.88×3；wWin 去掉后无 top-up）≈ 20% < 50% 线 ✓；
//     wApproach 0.008沿用（c20 门待首轮语料复算，见 §6 观察哨）；
//   · EV 三档重算（去终局项）：走 > 莽 > 坐（数字见上；与"走 > 坐 > 莽"口径差一档，
//     以数字为准 —— 莽/坐差 26 点，远小于"走最优"的 margin，不影响剂量结论）⇒
//     配方切换不翻转梯度。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 起点与缰绳
// ══════════════════════════════════════════════════════════════════════════
// `bc = x6-rebirth 的择优终点` = `nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json`
//   （已存在：379KB、`nn-weights-json`、`schema_major 3`、42 键；c20-1命对决胜者）。
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求；M3：预期贡献 ≈0）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 判据（预注册；**kills 里程碑是进展读数，pass 是门** —— 量程纪律）
// ══════════════════════════════════════════════════════════════════════════
// **主读数 = kills/局**（贪心）：分母 **3.63** ⇒ 里程碑 **8 → 12 → 16 → 20**
//   （20 = 通关线；kills 到顶即通关，机械恒等式；分母从单变体的 7.49 重标 ——
//   变体混合后贪心 kills 减半，里程碑跟着下来，不算移动球门：球门一直是"杀光"。）
// **门 = pass**（pooled）：分母 2.0% ⇒ 升级门 **≥30%**（阶段门，不及 D4 的 80% ——
//   2% 起步，30% 是"脱离地板"线，执行时不得临场改）。
// **约束**：
//   ① dmg/局（分母 227.5）：相对 it0 恶化 >+30% 即判换血漂移（1 命关 dmg 天然高，
//      线放宽；看的是"越打越脏"，不是绝对值）。
//   ② ticks：通关局均值（分母 1461，**仅 4 局，方差极大** —— 约束名存实亡，
//      如实写明；长局天然波动大，不判死）。
//   ③ 超时熔断（dense-only 的反悔键）：`timeout_frac > 5%` ⇒ **立即停**，
//      回滚决策：加回 `timeout −2.0` terminal 另开一腿（"围歼下无苟均衡"证伪 ⇒ 命题死）。
//   ④ 掉队（**真·分关**）：分关 pass 相对 it0 同关比较 —— abcd 6% / bcda 0% /
//      cdab 2% / dabc 0%；bcda/dabc 在地板（0%），退无可退；abcd/cdab 退步 >5pp 即掉队。
// 判线：**达成** = pass ≥30% 且 kills ≥14；**部分达成** = kills 里程碑到 8+ 但 pass 未过门；
//   **判负** = kills 在 5 下晃 40 轮（梯度无方向）或超时熔断触发。
//   统计量纪律不变（CI，否则不进结算）。
// **最优归档保护 + M2**：每 eval 点记 kills（不是 dmg！—— 量程纪律：低技能段看 kills），
//   最优另存；终点经池外段三选一（kills 最优 / pass 最优 / 末 it；互不可区分取 kills 高者）。
// **★ 回测门**：终点候选在 c07 段（seed0 410000，4 关各 200）＋ c06 段（405000，800 局）
//   回测不退步（pass ≤5pp / dmg ≤+10%，配对段；c07 是 4 变体段，规则适用 pooled＋注明分关）。
//   超限 ⇒ 不得为终点（顺延）；全超限 ⇒ 判语加"旧能力丢失"。
// **统计口径（写死）**：
//   · 判决只在**池外固定段**：`--seed0 412000`（**800 局 = 4 关各 200**，seeds 412000–412199；
//     2026-09-18 查 412xxx 全空；411000 是 shootout 段，禁复用为判决段）。
//     **seed 台账增补**：| 411000–411799 | c20-1命 shootout（x6/x7 对决，非判决） |
//     | **412000–412999** | **空闲 ⇒ 本腿预注册** |
//     **中途不得改段**（L3）。
//   · 配套锚点 200 局同种子配对（seed0 860001；日常 eval 即此口径）；pd 灰度带不变。
//   · **日常 200 局只做趋势，绝不判决**；日常第一读数是 kills（不是 pass —— 量程纪律）。
// **对照**：**it0 vs 终点**。god 参照：15.0% / 234.4 / kills 7.55（4 变体 pooled；单变体旧 236.6 作废）。
// **诊断项**：命中率（分母 rollout 35.3% ；3 命旧 45.8% 作废）、killerKinds＋exposure、
//   firstHitKind（power 是否还是 0 —— 第十二条腿）、pu/stars、timeout_frac（熔断读数）。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 止损
// ══════════════════════════════════════════════════════════════════════════
//   · **it20 只记录读数，不判决**（kills/dmg/pu 三轨照抄进日志；里程碑判决从 it40 起 ——
//     评审建议：c20 噪声大，it20 的任何"达标/判负"都是幻象；横盘条款（it20 可触发的那一半）保留）。
//   · **主检点 it40**（kills 里程碑制：第 8 个 eval 点）：贪心 kills 相对 it0（3.63）
//     增幅 <+50%（即 <5.4）⇒ 停腿，判「dense-only 在 20 敌 1 命下梯度无方向」。
//     （it35 是旧刻度；c20 局长、噪声大，检点后移到 it40 —— parametrize 变了，
//     条款跟着变，写明不算移动球门：球门是"kills 里程碑"，刻度是执行细节。）
//   · **超时熔断**：`timeout_frac > 5%` ⇒ 立即停（见 §4③；苟均衡存在的证据）。
//   · **换血熔断**：dmg↑30%+ 且 kills 不升 ⇒ 学错了，改奖励结构，不加轮数（分支二）。
//   · **机械横盘**：kills 连续 4 个 eval 点无改善（|Δ|<10%，kills 量程噪声大，线放宽）
//     ⇒ 停腿；宽限一次 +10 轮（到期必停）。
//   · **分支四（自锁）**：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3）。
//   · ⚠ **优先级：超时熔断 / it40 主检点 > 横盘 > 一切诊断**。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 预算与采样规模
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（总量不动；4 关均分 = 6000 samples/关）。
// · `est_samples_per_game` = **147**（§2 口径 R 1 命真值 146.8，向下取整；trailing 5 轮纠偏）。
//   首轮 ≈ 24000/147 ≈ **163 局**（≈40 局/关；3 命旧 478 作废，高估方向安全，见 §2 注记）。
// · `seed_rotate` = **150**（600/4，多关分摊；volume 不参与）。
// · `iters 0`（无限轮；**由用户拍板**，M1）＋ `max_hours 12`。
//   wall-clock 估算：163 局/轮 × 局均 1455 ticks ⇒ 单轮 ~2 分钟，12 小时 ≈ 300 轮。
// · 不开双 run（单 run 声明，L4）。`workers 8` / `ent_break 0.25` 其余全链一致。
// · **位移包不动**（`lr 0.0005 / epochs 6 / kl 0.02→0.01 / kl_cap 0.2 双段 / until 25`）。
// · **观察哨（it10）**：用首轮 v7 shard 重算 Σwhere0 → 复核 wApproach 门
//   （c20 未测，先跑后算；超限 ⇒ 按门调剂量**并记录** —— 这是 Phase-1 方法在本腿的
//   延续执行，不是临场改配方：改的是执行细节，判据（趋近行为存在）不变）。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 已知局限 + 程序事项
// ══════════════════════════════════════════════════════════════════════════
// 1. **lives1 关卡晋升 + D11 合规（2026-09-19 评审纠正：旧写法"D11 认关名"是错的）**：
//    D11 原文（roadmap:37）= "**2 命/3 命 tier = 新任务线**：DECISIONS 立案 +
//    fresh out/traj + reward 重验算 + bc 零样本重测" —— 触发条件是**命数 tier 变化**，
//    不是关名。本腿把 c20 从 3 命改 1 命 ⇒ **D11 触发**，四项逐项打勾：
//    ① DECISIONS 立案（§2026-09-19-x20-dense-only：去终局项 + lives1 晋升 + 4 变体）；② fresh out/traj（`tmp/x20-rebirth/`，§15.5）；
//    ③ reward 重验算（§2 剂量复核 + EV 三档）；④ bc 零样本重测（§2 it0 三件套）。
//    D11 的 c20 毕业口径原按 3 命 —— 本腿是 1 命任务分支，不混入 ladder-c20 的毕业账
//    （ledger 不记本腿；毕业另议）。§0.2 vs D1/D2 矛盾本身未裁决（用户保留）。
// 2. **dense-only 是全链第一次**：wWin/双 terminal 全删；terminal 块留空 dict
//    （loader 若要求非空则填占位并注记 —— 校验说了算，见下）。
//    超时熔断是反悔键（§4③）；若触发，命题死，加回重开。
// 3. 量程纪律：低技能段（pass <40%）看 kills 不看 pass；pass 过 40% 后双口径并列。
//    M2（日常/池外）不变，只是"日常第一读数"换成 kills。
// 4. 单 run（<2pp 非效应；kills 量程±0.5）。
// 5. `est` 只是首轮兜底（trailing 5 轮覆盖；首轮 163 局，1 命短局方差反而小）。
// 6. §0.2 vs D1/D2 矛盾：本腿执行 A 面（1 命）；B 面（3 命 83%）已测，账在 transfer probe，
//    不开腿。矛盾本身未裁决（用户保留）—— 本腿不替用户裁，只是选边跑。

{
  "version": 5,
  "name": "x20-rebirth",
  "mode": "per-tick",
  // 环境 = ladder-c20-lives1（4 变体，20 敌、队列 20 取满、max_ticks 12900、lives 1、hard）。
  // 任务侧 = §0.2 的 1 命定义（D11 原 3 命口径不混入；见 §7.1）。
  "level": "ladder-c20-lives1",

  // ── 奖励：dense-only（见 §2；去 wWin、去双 terminal；其余数值逐字沿用 x6）───
  "reward": {
    // ⚠ 用**标量 kills**（L7）。
    "formula": "wKill*kills + wHit*enemyHits - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      "wApproach": 0.008,
    },
    "terminal": {},
    "scheme": "toy",
  },
  "dodge": "",

  // ── 起点：x6-rebirth 的择优终点 it45（c20-1命对决胜者；42 键已核）────────────
  "bc": "nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §6；iters 由用户拍板，默认 0）────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：全链一致不动 ─────────────────────────────────────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 },
  ],

  // ── 采集：总量 24000/轮；4 关均分 = 6000 samples/关 ───────────────────────
  "target_transitions": 24000,
  // 147 = 口径 R 1 命真值 146.8 向下取整（§2；首轮 ≈163 局）。
  "est_samples_per_game": 147,
  // 600/4 多关分摊；volume 模式不参与，老模式回退。
  "seed_rotate": 150,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x20-rebirth/weights.json",
  "traj": "tmp/x20-rebirth",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x20-rebirth",
  "backup_prefix": "x20-rebirth",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 4 关：ID 2000-2003。日常 50/关（4×50=200 局/次，只做趋势，第一读数 kills）；
  // 判决走池外段（§4：412000，4 关各 200，seeds 412000-412199）。
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5,
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc = `nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json`（已存在，
//    379KB、`schema_major 3`、42 键）。缺失会响亮报错。
// ② **it0 探针（已跑，2026-09-19，结果见 §2 与 `tmp/x20-rebirth-probe/`）**：
//    rollout 4 关各 10 局（est 146.8、边际 +0.90、EV 走>莽>坐 margin +54；1 命真值语料）＋
//    贪心/god 各 4 关各 50 局 seed0 860001（pooled 2.0%/3.63杀 vs 15.0%/7.55杀 —— 落后教师 13pp；
//    采样/贪心双双贴地板（2.5%/2.0%），无鸿沟可言 —— 双口径照记（方法），不再声称隐藏能力）。
//    → 里程碑分母 = 贪心 kills 3.63；est 已覆盖（147）；剂量复核通过（§2，EV 排序干净）；
//    wPickup 边际检验（本轮补测）：捡组 EV +48.77 vs 不捡组 +8.38（差 +40.4，未加拾取项），
//    +3.0 再加 +8.5（占比 ~18%），无反转 —— 剂量通过；wApproach 门实测（Σwhere0 444.3，
//    上限 0.0131，0.008 余 39% —— 评审估算的 ≥5.6× 余量用了 3 命分子，已纠正）。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`。
// ⑥ 确认 a95/a97 在线。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 起跑线 / 边际 / EV 表：`tmp/x20-rebirth-probe.py`（已跑）
// · 贪心 + god：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c20-lives1.jsonc \
//     --games 200 --seed0 860001`（+ `--policy god` 即参照臂）
// · 判决（池外固定段）：`... --games 800 --seed0 412000`
// · 回测：`... --course nn-training/levels/ladder-c07.jsonc --games 800 --seed0 410000` /
//   `... --course nn-training/levels/ladder-c06.jsonc --games 800 --seed0 405000`
// · 走势：`tmp/x1-trend.py`（改路径；看 kills 第一）
//
// ── 结算节（已跑，2026-09-19；行源 tmp/x20-settle/*.jsonl）────────────────
// 池外段 412000（800 局 = 4 关各 200，配对种子；CI: pass SE≈1.3%，kills 量程±0.5）：
//   bc零样本 3.3%/3.85杀/dmg235.4 ｜ it30 **7.5%/6.10**/dmg246.9 ｜ it96 8.0%/5.94/dmg242.9
//   it30 vs it96：Δpass −0.5pp（−0.37σ）Δkills +0.15 —— 互不可区分 ⇒ 取 kills 高者 it30 为终点候选。
//   相对 bc：pass +4.2pp / kills +2.2（c20 能力真涨了）；超时全 0；dmg +5%（未超 +30% 线）。
// 回测 c07（410000，800 局；门：pass ≤5pp / dmg ≤+10%）：
//   it30 39.1%/4.28/dmg183 vs bc 30.4%/3.53/dmg193 —— pass **+8.7pp（+3.7σ）**，dmg −5% ⇒ 通过。
//   附带：it30 分关 36–44% 拉平，bc 偏科（s2000 51% vs s2001/s2003 19.5%）—— 4 变体训练治好了偏科。
//   it96 36.4%/4.13/dmg190 ⇒ 同样通过（+6pp vs bc）。
// 回测 c06（405000，800 局单关；bc 主场，x6 即在此训出）：
//   it30 52.9%/4.24/dmg167 vs bc 64.1%/4.70/dmg147 —— pass **−11.2pp（−4.6σ）**，
//   dmg **+13.4%**（CI 167±6 vs 147±6，不重叠）⇒ 双双超限 ⇒ it30 不得为终点，顺延 it96。
//   it96 51.5%/4.20/dmg173 —— pass −12.6pp（−5.2σ），dmg +17.4% ⇒ 同样超限。
// 判决：**全超限 ⇒ 无合格终点，判语加"旧能力丢失"**（c06 主场 −12pp，dmg +13~17%）。
//   §4 格：达成（pass≥30%&kills≥14）否；部分达成（kills 8+）否（池外 6.10）；
//   超时熔断否（全程 0%）；换血否；横盘字面否（it95 +18.5% 打破计数）事实 plateau 65 轮。
//   三问：① 20 杀否（plateau ~6–7 杀）；② dense-only **是**（零超时零苟化）；③ kills 读数是。
