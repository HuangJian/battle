// 课程 x6-rebirth —— 6 敌关（ladder-c06）的**热启迁移**腿（rebirth 阶梯第六级）
//
// 状态：**已结课（2026-09-18，68 轮，用户人工停）—— 判决 = 部分达成**。
//   池外固定段（seed0 405000，800 局）：承伤 163.2 → 147.3（−9.8%，CI 不重叠）；
//   pass 56.25% → 64.12%（+7.9pp，p=0.0007）；pu 1.077 → 1.294（+20%，
//   但约束线 1.80 没过 ⇒ 封顶部分达成）。回测门双双通过（c04 −2.5pp/+4.2%，
//   c05 −3.1pp/+1.5%，全不显著）。终点 = it45，
//   已另存 `nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json`。
//   详见文件尾「结算节（2026-09-18 结课回填）」。agent 不得启动训练。
//   agent 不得启动训练。
//
// 前置假设：x5-approach 已结课（判负主端点／机制端点达成，终点 it155）。
// 本腿把它的终点权重热启到 ladder-c06（6 敌 / 单关），回答三个问题：
//   ① 迁移链（…c04 → c05 → c06）能不能在供给打开后继续？c06 的 spawn/局是 c05 的
//      **2 倍**（2.50 vs 1.16–1.23，见 §2）—— x5 在 c05 被供给卡死的东西，在 c06
//      第一次有条件兑现（约束③本腿 feasible，见 §5）。
//   ② shaping 留下的 steering（c05 pu +30%）在新关是资产还是负债？c04 回测证明
//      它在旧关是负债（pass −18pp）；c06 零样本它反而是正向（§2 bc 二选一）。
//   ③ 朝向信号问题（a2 open limitation #2）—— 继续后排，c06 密度下半解若还够用，
//      它就再往后排一级。
// ⚠ 本腿**不换配方**（环境是唯一变量；x5 的双变量教训：归因到包。本腿回到 x2/x3/x4
//   的干净热启形状）。it0-vs-终点对照合法；与 x5-approach 直接对照禁止（另一起点）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（levels/ladder-c06.jsonc 实读，勿信记忆）
// ══════════════════════════════════════════════════════════════════════════
//   **单关**：ladder-c06，forces `abcdabcdabcdabcdabcd`（20 长），`count 6` ⇒
//   队列 20 取前 6 = **a+b+c+d+a+b**（basic×2/fast×2/power/armor 各一）。
//   max_ticks **4500**、lives 1、hard、空场 13×13。
//   自定义关 ID **2000-2000**（单关沿用，`rl/ladder_factory.py:228`）。
//   任务侧一个字不动（AGENTS §0.2）—— 只动训练侧。
//   单关结构后果与 x4/x5 同（无分关掉队 / seed_rotate 600 / per-stage = 总量）。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 立项依据（x5-approach 结算 + c06 零样本二选一，2026-09-18）
// ══════════════════════════════════════════════════════════════════════════
// · x5-approach 结课：判负主端点（dmg −2.8%~−5.6% CI 重叠）＋ 机制端点达成
//   （pu +30%，≥供给线 0.88 ✓）；终点 it155（与末 it201 互不可区分，取 pass 高者）。
// · **bc 二选一**（c06 零样本，800 局 seed0 404000，同种子配对；`tmp/xfer-c06/`）：
//   x4best 48.63% / 172.5 / pu 0.81 ｜ x5best 56.75% / 162.4 / pu 1.00 ｜
//   **x5a155 60.75% / 154.6 / pu 1.10**，ticks 1948（最快）。
//   x5a155 vs x5best：dmg −7.8（SE 4.1，z=−1.92）、pass +4.0pp（p=0.11）、
//   方向全优 ⇒ **bc = x5-approach.it155.best**（shaping 的 steering 跨关是资产）。
//   （x5best 备选落选；x4best 出局。）
// · c04 回测警示（`tmp/x5-approach-judge/xfer-c04.jsonl`）：x5a155 在 c04 段
//   pass −18.1pp、dmg +19.8（paired，双显著）—— **灾难性遗忘实锤**（迁移链第一次回测）。
//   ⇒ 本腿带**回测门**（§5/§6，第一次）：终点资格含旧关不退步条款。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 实测（2026-09-18，bc = x5-approach.it155.best； god 同种子 200 局参照）
// ══════════════════════════════════════════════════════════════════════════
// 口径 R —— **rollout**（采样，40 局 seed 0–39；`tmp/x6-rebirth-probe/rollout-it155`）：
//     40/40 = 100% 通关 ｜ dmg **174.3/局** ｜ ticks 1799 ｜ steps **181.5**
//     ｜ kills 6.00 ｜ hits 13.35 ｜ shots 33.5 ｜ cells 78.9 ｜ 命中率 39.8%
//     ｜ pu **1.73/局** ｜ stars 0.10 ｜ spawn **2.50/局**
//     ｜ 边际 **+0.57 dmg/格**（sit 178.2 → wander 205.3；LSQ +0.18 交叉确认）
//     ｜ EV 走 +40.5 > 莽 +33.5 > 坐 +32.1（莽/坐微反转 +1.4，n≈10 噪声，不动作）
//     ⇒ `est_samples_per_game` 取 **181**（181.5 向下取整；见 §7）。
// 口径 G —— **贪心**（200 局 seed0 860001；`it0.jsonl`，判据分母）：
//     pass **123/200 = 61.5%** ｜ dmg **156.3** ｜ ticks_win **1922**
//     ｜ kills 4.47 ｜ shots 26.1 ｜ pu **1.27** ｜ 零伤 27.6% ｜ gameover 77
//     ｜ firstHitKind **basic 200**（power 恒 0，第七条腿）
//     ｜ killerKinds fast 27 / basic 25 / power 22 / armor 10（死因分散；
//        exposure 校准见 c06 零样本分析 —— basic 居首是基数效应，单兵 power 仍最致命）。
// 参照臂 —— **god**（同种子 200 局；只作参照）：pass 44.0% ｜ dmg 186.6 ｜
//     ticks 1836 ｜ kills 3.60 ｜ pu 1.20 ｜ firstHit power 1（≈0）。
//     ⇒ **起步三项全超教师**（+17.5pp / −30.3 / pu +0.07）—— 全链第一次起步三杀教师
//     （x2 只超两项）。c06 起步形状 ≈ c04 起步（52.5% → 80.4% 的前例），预期可爬。
// 剂量复核（§前置②b 同款门；结论：**不动**）：
//   · Σwhere0(pickupDist)/局 = **832.2** ⇒ wApproach 上限 0.5×20.0/832.2 = **0.012**，
//     当前 0.008 在限内（余 33%）⇒ shaping 不动。
//   · wPickup 3.0：pu 1.73/局（+5.2 vs kill+win 20.0，比例健康）；边际 +0.57 下走仍最优 ⇒ 不动。
//   · wShot：贪心命中率待算？rollout 39.8% ⇒ 单发 +0.239 > 0.05 ✓ 不动。
//   · 趋近/远离步比 0.91（与 c05 it0 的 0.83–0.95 同档 —— 趋近行为尚未出现，符合预期）。
//   ⇒ **奖励逐字沿用 x5-approach**（a2-v4 + wPickup 3.0 + shaping 0.008），
//   wDmg 等六项 + terminal 照旧；不上 wChip/wTick/wStuck/分列（P2/P5/L7/L8 全沿用）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 起点与缰绳
// ══════════════════════════════════════════════════════════════════════════
// `bc = x5-approach 的择优终点` = `nn-training/weights/x5-approach-best/x5-approach.it155.best.json`
//   （已存在：379KB、`nn-weights-json`、`schema_major 3`、42 键，2026-09-18 已核；
//   c06 零样本二选一的胜者，见 §1）。
//   选取规则沿用唯一规则：最优归档，不是最大 it（§68 血泪）。
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求；M3：预期贡献 ≈0，
//   首轮 KL 暴涨先查 bc 载入）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 判据（预注册；分母 = §2 贪心 it0）
// ══════════════════════════════════════════════════════════════════════════
// **主判据 = 承伤（主端点）+ 耗时（约束）+ 拾取（升级资格约束）**。
// 主端点：**`dmg/局`** —— 分母 **156.3** ⇒ 达标线 **≤125.0（−20%）且 CI 不重叠**。
// 约束①（耗时）：通关 ticks 分母 **1922** ⇒ **≤2114（+10%）**，x3 CI 口径。
// 约束②（掉队，单关版）：终点 pass 相对 it0（**61.5%**）退步 >5pp 即掉队（护栏）。
// 约束③（拾取升级资格，供给先行 —— 本腿第一次 feasible）：
//   终点贪心 pu/局 ≥ min（2×it0 = 2.54，供给可行 2.50×0.9×0.8 = **1.80**）
//   ⇒ **≥1.80**（需 ~72% 拾取率，难但可能；x5 的 1.42 不可能事件不再重演）。
//   stars 半句删除（供给 0.10/局）；战前/窗口不分账（接受并声明）。
// 判线（与 x5-approach 同表）：达成（↓≥20% + 三约束全守住）/ 部分达成 /
//   判负（≤5% 或反向 / 耗时恶化）。统计量纪律不变（CI，否则不进结算）。
// **最优归档保护 + M2**：每 eval 点记 dmg，最优另存；终点经池外段三选一
//   （日常最优 / pass 最优 / 末 it；互不可区分取 pass 高者）。
// **★ 回测门（第一次；c04 遗忘的教训）**：终点候选必须在旧段回测不退步 ——
//   c04 段（seed0 401000，800 局）：相对 x4-it45（80.37% / 101.1）pass 退步 ≤5pp
//   且 dmg 恶化 ≤15%（c04 是两级之前，线放宽于单级护栏；dmg 用 +15% 不用 +10%，
//   跨两级的分布漂移要留余量）；c05 段（seed0 402000，800 局）：相对**直接前任
//   x5a-it155**（68.75% / 141.1）pass 退步 ≤5pp 且 dmg 恶化 ≤10%。
//   （2026-09-18 对账注：另一 agent 按文件旧字面用了 x5-it35（63.12%/141.9）作参照，
//   得 +2.5pp/+0.9%；本位用直接前任得 −3.1pp/+1.5% —— **两读数下门都过**，
//   参照取直接前任（bc 即它），x5-it35 作双参照保留。文件以此为准。）
//   任一超限 ⇒ 该候选**不得为终点**（往次优顺延）；全部超限 ⇒ 判语加"旧能力丢失"，
//   最高部分达成（迁移链的完整性是任务的一部分；§0.2 的延伸：升不了档的通关不算升级）。
//   回测只跑终点候选（2–3 个权重 × 2 段 × 800 局 ≈ 30 分钟），日常不跑。
// **统计口径（写死）**：
//   · 判决只在**池外固定段**：`--seed0 405000`（**800 局单关**，405000–405799；
//     2026-09-18 查 405xxx 全空；404000–404799 是 transfer probe 段，禁复用为判决段）。
//     **seed 台账增补**：| 403000–403999 | x5-approach（池外判决段） |
//     | 404000–404799 | c06 transfer probe（x4/x5/x5a 三臂零样本，非判决） |
//     | **405000–405999** | **空闲 ⇒ 本腿预注册** |
//     **中途不得改段**（L3）。
//   · 配套锚点 200 局同种子配对（seed0 860001；日常 eval 即此口径）；pd 灰度带不变。
//   · **日常单关 200 局只做趋势，绝不判决**。
// **对照**：**it0 vs 终点**（同环境、同种子、同权重族）。god 参照：44.0% / 186.6 / 1836。
// **诊断项**：零伤占比（分母 27.6%）、killerKinds＋exposure 双列（分母纪律：raw counts 必除
//   exposure；c06 单兵 power 最致命，见零样本分析）、entropy、a_move 熵、
//   趋近/远离步比（shaping 机制读数；v7 列天然有）。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 止损（与 x5-approach 同格；+ 回测 Fallback）
// ══════════════════════════════════════════════════════════════════════════
//   · **机械横盘**：连续 20 轮（= 4 个 eval 点）承伤无改善（|Δ|<5%，读法 C 优先：
//     从未比 it0 好 5%+ 即触发）或漂移形态 ⇒ 停腿；宽限一次 +10 轮（到期必停）。
//   · **it35 检点**（第 7 个 eval 点）：承伤无改善（|Δ|<5% 且 pd<5%）⇒ 停腿，
//     判「热启 + 位移包 + shaping 在 6 敌关不足以改写行为」；
//     **pu 检点独立**：pu 相对 it0（1.27）不升 ⇒ 即使承伤动了也只给宽限不给过
//     （c05 的教训：承伤和拾取可以脱钩，两个都要看）。
//     下一手：朝向 metric（独立工程）/ 意图层（F3）/ 停在 c05。
//   · **熔断轴**：shots/kills/pu 熔断与 x5-approach 同口径（分母换 it0：shots<0.9×26.1=23.5
//     或 kills<4.47 不升反降 ⇒ 立即停）＋ 趋近 hack 熔断（cells 暴涨而 kills 不升 /
//     zero-kill-frac 翻倍 ⇒ 立即停）。
//   · **分支二（换血）**：dmg↑ 而 wr 不降 ⇒ 学错了，改奖励结构，不加轮数。
//   · **分支四（自锁）**：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3）。
//   · ⚠ **优先级：横盘 / it35 > 拾取约束 > 回测门（结算时） > 一切诊断**。
//     （回测门只决定"谁当终点"，不中途停腿 —— 训练中途停只看横盘/检点/熔断。）
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 预算与采样规模
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（总量不动，PPO batch 守恒；单关 per-stage = 24000）。
// · `est_samples_per_game` = **181**（§2 口径 R 实测 181.5，向下取整；trailing 5 轮纠偏）。
//   首轮 ≈ 24000/181 ≈ **133 局**。
// · `seed_rotate` = **600**（单关回退值；volume 不参与）。
// · `iters 0`（无限轮；**由用户拍板**，M1）＋ `max_hours 12`。
//   wall-clock 估算：133 局/轮 × 局均 1799 ticks ≈ x4 量级 ⇒ 单轮 ~2 分钟，
//   12 小时 ≈ 300+ 轮。刹车靠 §5 + 人工停。
// · 不开双 run（单 run 声明，L4）。`workers 8` / `ent_break 0.25` 其余全链一致不动。
// · **位移包不动**（`lr 0.0005 / epochs 6 / kl 0.02→0.01 / kl_cap 0.2 双段 / until 25`；
//   有限轮 N 则 until 取 min(25, 40%×N））。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. 起步三杀教师 —— 若终点仍超不过教师线太多，别误读成"失败"（判据是相对 it0，§0.2）。
// 2. 单 run（<2pp 非效应）。
// 3. 回测门首次启用：阈值（5pp / +15% / +10%）是拍脑袋定的（首版，执行后按数据修订）。
//    c04 用 +15%、c05 用 +10% 的不对称是故意的（跨两级漂移余量），不是笔误。
// 4. `est` 只是首轮兜底（trailing 5 轮覆盖）。
// 5. 莽/坐 EV 微反转（+1.4，n≈10 噪声）：剂量不动的理由是"走"明显最优（+40.5），
//    若训练中出现"莽策略"形态（dmg↑ 而 wr 不降 = 分支二），回头再议。
// 6. bc 在 c06 的 ticks（1922/2130）比教师快得不多 —— 耗时约束分母就是 it0，无妨。

{
  "version": 5,
  "name": "x6-rebirth",
  // 每一决策步一条样本（非 intent/goal 窗口），与当前 PPO 管线匹配。
  "mode": "per-tick",
  // 环境唯一来源 = levels/ladder-c06.jsonc（单关，6 敌 a+b+c+d+a+b、队列 20 取前 6、
  // max_ticks 4500、lives 1、hard）。课程侧禁止重写
  // stages/difficulty/max_ticks/player（任务侧不可动，AGENTS §0.2）。
  "level": "ladder-c06",

  // ── 奖励：x5-approach 终点配置逐字沿用（见 §2 剂量复核；唯一变量 = 环境）───
  "reward": {
    // ⚠ 用**标量 kills**（L7）。
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      // 上限 0.012（Σwhere0 832.2），冻结 0.008（余 33%）；改剂量 = 新腿。
      "wApproach": 0.008,
    },
    "terminal": {
      "lives_exhausted": -5.0,
      "timeout": -2.0,
    },
    "scheme": "toy",
  },
  "dodge": "",

  // ── 起点：x5-approach 的择优终点 it155（§1 二选一胜者；42 键已核）───────────
  "bc": "nn-training/weights/x5-approach-best/x5-approach.it155.best.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §6；iters 由用户拍板，默认 0）────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：全链一致不动 ─────────────────────────────────────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段 it1–25（iters=0 时；有限轮 N 则 until 取 min(25, 40%×N)。kl_cap 两段显式。
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段 it26+：kl 放松 + lr 砍半。
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 },
  ],

  // ── 采集：总量 24000/轮；单关 per-stage = 24000 ───────────────────────────
  "target_transitions": 24000,
  // 181 = 口径 R 实测 181.5 向下取整（§2；之后 trailing 5 轮覆盖）。
  "est_samples_per_game": 181,
  // 单关回退值；volume 模式不参与。
  "seed_rotate": 600,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x6-rebirth/weights.json",
  "traj": "tmp/x6-rebirth",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x6-rebirth",
  "backup_prefix": "x6-rebirth",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 单关 2000。日常单关 200 局/次（只做趋势）；判决走池外段（§4：405000，800 局）。
  "eval_stages": "2000-2000",
  "eval_games_per_stage": 200,
  "eval_every": 5,
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc = `nn-training/weights/x5-approach-best/x5-approach.it155.best.json`（已存在，
//    2026-09-18 已核：379KB、`schema_major 3`、42 键）。缺失会响亮报错。
// ② **it0 探针（已跑，2026-09-18，结果见 §2 与 `tmp/x6-rebirth-probe/`）**：
//    rollout 40 局（est 181.5 已写入配置、边际 +0.57、EV 走最优）＋
//    贪心/god 各 200 局 seed0 860001（61.5%/156.3/1922 vs 44.0%/186.6/1836 —— 起步三杀教师）。
//    → 判据分母 = 贪心数；est 已覆盖；剂量复核通过（§2）。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`（x3 前置⑤事故）。
// ⑥ 确认 a95/a97 在线。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 起跑线 / 边际 / EV 表：`tmp/x6-rebirth-probe.py`（已跑）
// · 贪心 + god：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c06.jsonc \
//     --games 200 --seed0 860001`（+ `--policy god` 即参照臂）
// · 判决（池外固定段）：`... --games 800 --seed0 405000`
// · 回测（终点候选）：`... --course nn-training/levels/ladder-c04.jsonc --games 800 --seed0 401000` /
//   `... --course nn-training/levels/ladder-c05.jsonc --games 800 --seed0 402000`
// · 走势 / 动作分布 / 时间账：`tmp/x1-trend.py` / `tmp/x1-acthist2.py` / `tmp/a2-timing.py`
//
// ── 结算节（2026-09-18 结课回填；68 轮，用户人工停）───────────────────────
// **判决 = 部分达成**（承伤中间带 −9.8% + 约束①②守住 + 回测门通过，但**约束③失败 ⇒
// 封顶**；单 run）。合法措辞：主端点侧 = 梯度无方向（L3）；不得写「拾取/shaping 无效」
// （pu +20%，机制动了；死的是任务端点）。
// 运行实际：it1–68。横盘条款持续触发（dmg it45–68 全平）；it35 双检点：承伤 −7.1%
//   （过）＋ pu 1.23 < 1.27（不升 ⇒ 按文件只配宽限）；宽限到 it45 恰好是全程最优点，
//   it50–68 阴跌回来 —— 宽限机制第二次验证有效（x4 it45 同构）。68 轮停，无二次宽限。
//
// ① **it0 vs 终点 it45**（池外固定段 seed0 405000，800 局单关，同种子配对）：
//   | | it0（bc） | it45 | Δ |
//   | pass | 450/800 = 56.25% | 513/800 = **64.12%** | McNemar b01=200/b10=137，pd=**+7.88pp**，exact p=0.0007 |
//   | dmg/局 | 163.2 [156.9,169.4] | **147.3 [141.0,153.5]** | 配对 Δ=**−15.9**（SE 4.1，z=−3.89），**−9.8%，CI 不重叠** ⇒ 中间带（判线 ↓≥20% 未到），不是达成 |
//   | 通关 ticks | 1923 | **1909** | −0.7% ⇒ 约束①（≤2114）守住 |
//   kills 4.32 → 4.70。it35（152.5 / 59.13%）与 it68（155.2 / 59.38%）互不可区分；
//   it45 对 it35（pass p=0.025 显著）＋ 对 it68（pass p=0.0497 显著）⇒
//   **终点 = it45**（三选一规则下少见的"显著胜出"，非并列取高者）。
// ② **锚点配对 pd**：+7.88pp（p=0.0007），超灰度带。日常 it65–68 的回落（60.5%）
//   在池外未复现（it68 59.38% vs bc 56.25%，pd 仍 +3.1pp）—— M2 第五次应验。
// ③ 掉队：单关；终点 pass +7.9pp，护栏远未触发。god 对照（44.0% / 186.6 / 1836）：
//   终点三项全超教师（+20pp / −21% / ticks 打平略慢，如实记录）。
// ④ **约束③专项（供给先行，第一次 feasible）**：pu/局 1.077 → **1.294**（+20.2%）；
//   约束线 = min（2×1.077 = 2.15，供给 2.50×0.9×0.8 = 1.80）= **1.80** ⇒
//   1.294 < 1.80 ⇒ **失败，封顶部分达成**。教训：feasible（72% 拾取率）≠ reachable ——
//   策略实际拾取率 0.52（1.294/2.50），离 72% 还差得远；供给打开了，意愿没跟上
//   （c06 意愿曲线没跑，下次补 —— x5 的 cliff 在 c06 是否还在未知，记为 open）。
// ⑤ **★ 回测门（第一次实跑，双双通过）**：
//   c04 段（401000）：x6-it45 pass 77.88% / dmg 105.3 vs x4-it45 80.37% / 101.1 ⇒
//   配对 pd=−2.50pp（p=0.22 n.s.）、dmg +4.2%（z=+1.08 n.s.）⇒ **通过**（≤5pp / ≤+15%）。
//   c05 段（402000；与 x5a-it155 非同种子，独立双样本比较）：x6-it45 pass 65.62% /
//   dmg 143.2 vs 直接前任 x5a-it155 68.75% / 141.1 ⇒ −3.1pp / +1.5% ⇒ **通过**
//   （≤5pp / ≤+10%）。另一 agent 用 x5-it35（63.12%/141.9）作参照得 +2.5pp/+0.9%，
//   同样通过 —— 双参照一致，门稳健（见 §4 对账注）。
//   ⇒ **无新债**：与 x5-approach 在 c04 的 −18.1pp 灾难形成对照。最简解释（非定论）：
//   本腿训练基本没搬动策略（dmg 全程 ±5%），没学会就没东西可忘 —— "横盘腿不欠债"。
//   若此解释成立，回测门真正的考验在"大跃进腿"上，不在本腿。阈值维持首版，不修订。
// ⑥ killerKinds＋exposure（池外终点）：死因分散（c06 全链一致：basic 居首是基数效应，
//   单兵 power 最致命 —— c06 零样本分析已立）；零伤占比 26.7% → 29.6%；
//   趋近/远离比日常口径未动（shaping 在 c06 也没推起来，见④）。
// ⑦ 横盘读法 C ＋ M2：dmg it45–68 全平触发；日常 it65 的 60.5% 回落在池外未复现。
// ⑧ 每轮样本量（collected vs 24000 浪费率）：24000 档，浪费个位数（略）；
//   entropy 0.58–0.68 平，KL ~0.02，无熔断（趋近 hack 熔断未触发）。
// ⚠ 单 run（L4）。终点经池外段四选一（M2）＋ 回测门。未达标措辞三选一（L3）。
// 终点已另存 `nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json`
//   （379KB、`nn-weights-json`、`schema_major 3`、42 键，已核验）。
//   判决语料：`tmp/x6-rebirth-judge/eval.jsonl`（bc/it35/it45/it68 × 800）＋ `judge.py`；
//   回测语料：`back-c04.jsonl` / `back-c05.jsonl`（x6-it45 × 800）＋ `back.py` / `back5.py`。
