// 课程 x3-step —— 步长/缰绳腿（x3-power 的「死段」变活段）
// 状态：**2026-09-15 HOLD / 注销待重设计（评审处理后）** —— **禁止开训**。
//   原「就绪待开训」版被 `nn-training/curricula/x3-step.review.md` 三项 P0 打回：
//   ① kl_cap 在本执行路径根本不接线（机制错述）；② 工厂第二段（kl_coef 0.03 + lr 5e-5）
//   已被 x3-start 自家 333 轮历史证伪「能把 KL 抬到 ≥0.005」；③ 预注册判据不可证伪。
//   本文件只作**修订草稿 + 注销账**，不是可开训课程。重设计须另过评审再改状态。
//
// ── 立项（plan/x3-power-followup.plan.md §T4）───────────────────────────────
// x3-power 全程紧缰（`kl_coef=0.2` + lr 1.5e-4），30 轮实测 per-iter KL 仅
// 0.000581→0.002133（max 0.00231@it25）；第二段日程因 `until_iter=40 > iters=30`
// 是死段。在 KL≈0.002、pd≈5–6% 下任何奖励重定价都测不出来（含 wChip）。
// ⇒ **本腿唯一目的 = 恢复可测性**（把 KL/pd 抬到能分辨奖励改动的量级），不是涨点。
// ⚠ T3 画像已判 T5（metrics v6）为后继主路径；本腿若重生，定位是「先证策略能动」，
//   不是与 T5 抢跑。T5 若仍用紧缰日程，会踩同一可测性坑。
//
// ── 评审裁定（2026-09-15，对 x3-step.review.md 逐条；证据以代码/课程文件为准）──
// **接受（必须改）**：
// - P0-1 `kl_cap` 在串行/远端路径不接线：`ppo/engine.py` `ppo_update` 无 `kl_cap` 形参；
//   `remote/worker.py` 只读 `kl_coef`；全仓消费者仅 `rl/stream.py`（stream 波次闸）与
//   manifest 打包。x3 线全部 `stream=0`（remote 强制，`rl/config.py`）⇒ **it1–30 无硬顶**。
//   本腿真正生效的 KL 治理只有 `kl_coef` 软惩罚 + F4 breaker（`KL_BREAK=0.075` 连 3 轮，
//   量级差 30 倍，本腿够不着）。`p4-fast.jsonc` 早已写对；本文件与 x3-power/§46/§19.1
//   的「kl_cap 咬合/回落」表述作废（见 DECISIONS §2026-09-15-goalnn-kl-cap-unwired）。
// - P0-2 工厂第二段不是「放缰治疗」：x3-start 第二段（同 kl_coef 0.03 / lr 5e-5）
//   **全 333 轮 max KL = 0.00353**（x3-power 校准段已引），≥0.005 = 0；it41 处无台阶。
//   且第二段同时把 lr 1.5e-4→5e-5（÷3.3 踩刹车）与 kl_coef ÷6.7 放缰 = **复合处理**。
//   要求 it21–30 这 10 轮达到 293 轮峰值的 1.4 倍，无先验支持。
// - P0-3 判据不可证伪：Δ>0 无阈值（800 局 pd≈6% 时噪声带 ≈±3.7pp）；缺中间带
//   （最可能落点 0.003–0.005）；「KL 放大」未预注册统计量；无 x3-power 同窗对照；
//   主端点应是策略位移（pd / 权重 KL），胜率 Δ 是二级观察。
// - §4-C transport：本文件原写「默认走 hub/pull」**错**。`rl-config.json` 全局
//   `nodes[0]=gpu1`（`gpu_push:true`、`enabled:true`）；CLI `--remote-transport` 默认
//   `auto` ⇒ `resolve_transport("auto")` 取全部 gpu_push 节点 = **推 gpu1 快隧道**。
//   控制台 local 预设钉 pull，**CLI 不钉**。x3-power Run A 五次 wait_result/530 是同形态。
// - P1 语料：新 traj ⇒ 无 jsonl 历史 ⇒ rotateSeed 重算 ⇒ rollout (stage,seed) 与
//   x3-power **不同键**。合法说法 =「同环境/奖励/起点/分布，**不同语料键**」；不得
//   由 `corpus_identity_fp` 相同推出「同数据」。
// - P1 熔断轴 / 双池模板 / target_transitions 声明 / it20→it21 台阶记账：照改（见下）。
//
// **部分接受 / 保留意见**：
// - 「假说已被先行证伪」作为**本处理的开腿否决**接受；不作为「任何放缰都无效」的
//   永久科学结论——工厂第二段是复合处理且窗口短，不能外推到「只动 kl_coef、lr 不降」
//   或「只升 lr」的单旋钮处理。
// - 评审 §4-A「用冻结 shard 离线判决」**概念正确、执行阻塞**：写评时假设
//   `tmp/x3-power/it28|29|30/dist` 在盘，**现已不在**（tmp 已清）。若仍要离线判决，
//   须先用 `x3-power.it30` 权重采一轮新 shard（≈一轮 rollout 成本），再扫
//   `(kl_coef, lr)`；比 30 轮训练腿便宜一个量级，但不是「30 分钟零成本」。
// - 「先修 T7 再开腿」：**已修**（2026-09-15）。默认 ABORT 不静默降级；opt-in 有栈。
//
// **拒绝**：
// - 把「单变量 = only until_iter」写成机制单变量。配置层单键改动成立，机制层是复合
//   处理；重设计必须方向一致、只动一个旋钮（见下）。
// - 现判据三支（≥0.005 / 放大Δ≈0 / 仍~0.002）原样保留。
//
// ── 重设计草稿（未批准、未开训；批准前本文件仍是 HOLD）──────────────────────
// treatment 二选一（**绝不让 lr 与放缰反向**）：
//   A. 纯放缰：第二段 `{ "kl_coef": 0.0, "lr": 0.00015 }`（kl_coef=0 为 p 系合法值；
//      lr 不降，只摘软惩罚）；
//   B. 升步长：第二段 `{ "kl_coef": 0.03, "lr": 0.0003 }`（s4b 用过 3e-4；保留弱正则）。
// 暴露：`until_iter: 10`（it11–30 = 20 轮放）或 `iters: 40`；10 轮对 KL 慢变量太少。
// 主端点：it21–30 的 KL **max 与 mean**（预注册统计量）相对 **x3-power 同 it 窗**
//   配对比值；并落盘 it0/it10/it21/it30 锚点 200 局同种子 **pd**。
// 判据示例（须再预注册后才可开训）：
//   · KL mean ≥ 2× x3-power 同窗 且 pd ≥ 15% ⇒ 可测性恢复，奖励腿解封；
//   · 部分放大（1.3–2×）⇒ 绳有作用但不是主因；
//   · KL ≤1.3× 且 pd ≤5% ⇒ 步长/缰绳无辜，问题在信号（奖励/信用）或执行器。
//   胜率 Δ 仅作二级观察（≥+5pp 且 McNemar 单侧 p<0.05 才写「涨点」）。
//
// ── 启动（HOLD，以下命令**当前无效**，仅重设计批准后参考）────────────────────
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x3-step \
//     --remote-transport pull
//   ⚠ 必须显式 `--remote-transport pull`（或先关掉/换掉 rl-config gpu1，把活 URL 写进
//   本文件）。裸 CLI 默认 auto = 推 gpu1 快隧道，与「hub/pull」声明冲突。
//   控制台 local 预设钉 pull，但仍建议命令行写死，防 preset 漂移。
//
// ── 开工前置（重设计批准后重核；原 ③ 已作废）──────────────────────────────
// ① bc 权重：`nn-training/weights/x3-start/x3-start.it333.20260915-052649.json` +
//    `nn-training/weights/in-use/x3-start.it333.json`（§379）。
// ② 关卡 `ladder-c03`（2000–2003）。③ transport 显式 pull 或写明活 push URL。
// ④ T7 **已修**（2026-09-15）：默认不再自动降级（`--remote-degrade-after 0` → ABORT）；
//    启动界面「降级本机」opt-in 时会懒加载本机栈。开腿前确认开关意图即可。
// ⑤ 若做离线判决：先确认新采 shard 或旧 shard 实际在盘（写路径进事件账）。
//
// ── 结课后必填（DoD；HOLD 期间不适用）──────────────────────────────────────
// 事件账 / 主判据池声明 / 口径声明 / KL 统计量与 x3-power 同窗对照 / pd 落盘 /
// it20→it21 台阶有无 / 预注册项执行或显式注销 / **不启用 target_transitions**
// （动态采集；缺席该键=默认关，与 x3-power 同款声明）。
{
  "version": 5,
  "name": "x3-step",
  "mode": "per-tick",
  "level": "ladder-c03",

  "seed_rotate": 60,
  "workers": 8,

  // 奖励与 x3-power 逐字同构（本腿不动奖励，只动日程）。
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0)",
    "params": {
      "wKill": 4.0,
      "wHit": 0.15,
      "wWin": 2.0,
    },
    "terminal": {
      "lives_exhausted": -1.0,
    },
    "scheme": "toy",
  },

  "bc": "nn-training/weights/x3-start/x3-start.it333.20260915-052649.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  "lr": 0.00015,
  "epochs": 4,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  // ★ HOLD：下列日程仍是**被否决的工厂第二段原样**（kl_coef 0.03 + lr 5e-5），
  //   仅作 diff 参照。批准重设计后按上文草稿 A 或 B 替换，且 until_iter 按暴露窗改。
  //   kl_cap 在 remote/serial 路径**不接线**（写进字段也不生效）；保留键只为与
  //   x3-power 配置层对齐，**不得**再声称它是护栏。
  "ppo_schedule": [
    { "until_iter": 20, "kl_coef": 0.2, "lr": 0.00015, "kl_cap": 0.006 },
    { "kl_coef": 0.03, "lr": 0.00005 },
  ],

  "iters": 30,
  "max_hours": 12,
  "out": "tmp/x3-step/weights.json",
  "traj": "tmp/x3-step",
  "backup_dir": "nn-training/weights/x3-step",
  "backup_prefix": "x3-step",

  // ent_break：胜率 > max_winrate(0.5) 时结构性不触发（设计如此）。
  "ent_break": 0.25,

  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5,
}

// ── 注销账（2026-09-15 评审处理；对应 plan T4）──────────────────────────────
// 注销理由：机制错述（kl_cap 不接线）+ 工厂第二段历史证伪 KL≥0.005 + 判据不可证伪
//   + transport 默认与声明冲突。不是否定「先证可测性」目标，是否定**本处理与本判据**。
// 离线判决：评审建议的冻结 shard 路径已阻塞（tmp 清空）；若重生，先采一轮新 shard。
// 后继关系：T3 已判 T5；T5 若沿用紧缰日程须一并改，否则同样测不动。
// 预注册中途探针：本腿未设（无注销项）。
//
// ── 终点结算（2026-09-16；详账 docs/nn.progress.md §50）──────────────────────
// ⚠ 课程文件状态为 HOLD，仍按**原工厂第二段**跑满 30 轮（非重设计 A/B）。本结算
//   只覆盖该复合处理，不外推单旋钮。
// - 权重：nn-training/weights/x3-step/x3-step.it30.20260916-074742.json（it1–30 齐）
// - 主端点 it21–30 KL：step mean 0.001940 / max 0.002665 vs x3-power 同窗
//   0.001508 / 0.002313 ⇒ **mean 1.286× / max 1.152×** ⇒ 落在 ≤1.3× 支：
//   **步长/缰绳无辜，问题在信号或执行器**。pd 已由日常 eval 离线配对落盘：
//   it0 vs it30 锚点 200 对 b01=7/b10=4，**pd=5.5%，Δ=+1.5pp**（分关 Δ：
//   abc +2.0 / abd +4.0 / acd −2.0 / bcd +2.0pp）⇒ `Δ≤pd` 封死 +5pp 可观测性。
// - 二级：训练 wr 三段 0.688/0.671/0.677 横盘；日常 eval 只读 anchor 口径
//   0.650→0.665（**+1.5pp**，噪声内；0.6725 是 400 局混轨数，与 200 局 it0 不可比）；
//   分关 s2002 全程最弱（0.50–0.64）；kickstart KL it20 0.025→it30 0.060
//   （离 BC 更快，单步仍细）。
// - 事件：it1 wait 超时重启；it20 hub 403 ip-blocked ×5 后异常退出（未走 T7 ABORT，
//   覆盖范围立缺陷单）；全程 push_nodes=0，走课程隧道 hub/pull（cloudflare×2 →
//   Tailscale 直连 100.124.208.62:8787），HOLD 的 auto→gpu1 担忧未命中；07:30 第三次
//   run 补完 it20–30，run_complete 07:49「正常收官」。无 KL/ent 熔断。
// - 800-verdict（2026-09-16，用户要求形式完备而补跑；产物
//   tmp/x3-step/verdict-it30-seed400000.jsonl，800 行，126.1s @6.3 games/s）：
//   终点 **562/800=70.25%** vs 冻结基线 576/800=72.00%，**Δ=−14/800=−1.75pp**，
//   判线（≥616/800 且 McNemar 单侧 p<0.05）**失败 ⇒ 判负**。配对 b01=12/b10=26，
//   pd=4.75%；单侧 p=0.9931（反向），双侧 p=0.0336（显著更差，只记判负+方向为负，
//   不做因果断言）。分关：abc 155/200=77.5%（+2.5pp）/ abd 150/200=75.0%（−2.5pp）/
//   acd 121/200=60.5%（−3.0pp）/ bcd 136/200=68.0%（−4.0pp）⇒ 目标 power 关全降，
//   无拆东墙补西墙。伴随量：kills 2.411→2.377（持平）、shots 14.55→13.44（−7.6% 但
//   命中率 45.8%→48.7% 反升，无开火崩；日常 eval 口径 −1.3% 互证）、dmg 持平。
//   结论与 KL/pd 支一致：策略几乎没动（pd<5%），+5pp 在本腿不可观测。
//   预注册三项均已由本处理复证/落地，无中途注销。
// - 判决：**复证评审 P0-2**（工厂第二段抬不动 KL）；**不**否定重设计 A/B。
//   后继主路径仍是 T5；T5 立项须单旋钮先证可测，或主端点改 pd/指纹/分敌种命中。
