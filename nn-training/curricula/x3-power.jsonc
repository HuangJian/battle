// 课程 x3-power —— power 关命中信用重定价腿
// 状态：**2026-09-15 07:15 核验就绪**（原 standby 待命项已全部落地：bc 已填、
//   kl_cap 已按自定公式校准、开工前校准已登记、死段已声明）。开工阻塞见文件末「开工阻塞」。
//
// 背景：跨 count 稳定结构——含 power 关差 ~15-20pp（x2 门 ac/bc 0.63-0.66；
// x3 it20 分关 acd 0.56/bcd 0.60 vs abc/abd 0.76-0.78；god 亦然）。败局画像
// （x2-gate3）：ac 55% 败局 0 杀死（接战了：中位 3 发/1.52 命中/37 40 打中过，
// ~293t 先倒），bc 91% 败局 1 杀死（残局 1v1 vs power 死）。共同机制：伤害摊薄
// 零代价——wHit 按次给分，打中两个各一枪与打死一个，在账上几乎没区别，而 power
// 必须集火秒掉（否则它的快弹先赢）。
//
// ※ 表达力边界（诚实声明）：真·按敌种/按伤害归一写不出来——metrics 只有总数
//   （kills/enemyHits，无分敌种、无伤害量；firstKillTick 是整局常量，toy diff
//   下恒零）。本腿是现有语言下的可表达代理：**杀/中信用比**（wKill↑/wHit↓），
//   把“打中就行”的 satisficing 地板抽掉，逼梯度指向终结。若证伪，下一步是
//   metrics v6（分敌种命中列，TS+Python 全链）——不是继续调剂量。
// 另注：当前 wKill:wHit 已是 10:1 仍摊薄，故瓶颈可能是执行（活着打完第二枪）
// 而非信用——本腿同时是这个二选一的判决实验：比值陡峭化后若仍摊薄，判执行瓶颈。
//
// 下一个可能尝试方向：wChip 扫描豁免（2026-09-15 立项备选，开工需用户明示批准）。
// 由头：roadmap N3 总规则禁 c01-c03 上 wChip（理由=承伤基数极小），但 x2 实测
// 局均 dmg 97（胜 81/败 188，c4 基线 65%），“极小”前提不成立，且死因正是挨打。
// 方案：wChip=k/97，k∈{0, 0.5, 1.0}→{0, 0.005, 0.010}（c4 有效 0.03 的零头起步；
// c6 0.03 保守化翻车案在前），单变量、每臂 30 轮、verdict 配对看 dmg/kill 降且
// kills/shots 不降（降即停）。程序：用户批准 + DECISIONS 立案（破总规则）后开腿，
// 范围仅早期关。排序在本腿之后（先判信用/执行，再问疼不疼）。
//
// 与 x3-start 单变量对照：环境/优化器/日程与 x3-start 一致；唯一**训练**变量 = 奖励
// params（wKill 3.0→4.0，wHit 0.3→0.15；wWin/terminal/scheme 不动）。
// ⚠ 例外一处（2026-09-15 核验补记，原文误写「护栏逐字同构」）：**kl_cap 0.2→0.006**
// 也是改动。它是**护栏校准**而非训练变量——依据见下方护栏段：旧值 0.2 比实测 KL
// 峰值大两个数量级，330 轮从未咬合 = 死护栏。按实测新值「几乎不触发」⇒ 不构成
// 有效混淆；准确说法是「**除 kl_cap 外**逐字同构」，不得再声称全部同构。
// 开工假说（§5.7）：信用比×2（10:1→27:1）后，acd/bcd pooled it20 前 +5pp；
// 30 轮跑满无爬升即证伪停腿（防 c6-bonus 式盲跑）。
// 批量附录：为何不先加 rollout 量（2026-09-15，x2/x3 联合复盘结论）。
// 量纲纠正：PPO 吃的是 transitions/轮，不是局数。x3：240 局×967t≈23 万 **ticks**；
// c4-dodge 先例（600 局×1100+t≈66 万+ **ticks**）——roadmap“600-1000 局”实为 transition
// parity 要求，直接抄局数是刻舟求剑。以后定量按 transitions 对齐 ~60 万+ 线。
// ※ P0 量纲勘误（2026-09-15 结课评审复算，同份 training_log 30 轮全量）：账本口径的
//   transitions = samples（rl/resume.py:96-108，自声明“nSamples 之和”），ticks 只是 clocks。
//   实测 samples/轮均值 **23343**（范围 22398–25166）≈ **2.33 万 transitions/轮**；
//   ticks/轮均值 231924；samples/ticks = 0.1007 ≈ 1/K（K=10，exporter 降采样）。
//   上文“23.5 万 transitions/轮”实为 ticks，**10× 误差**；c4-dodge 的“66 万+”同理实为
//   ticks（≈6.6 万 transitions）。本腿自身有效性不受影响（跑的是固定 60 局/关，单位无关）；
//   但下文一切“X 万 transitions”规划公式必须按 samples 重算，见 103–105 行修正。
// 四条反证（方差不是 binding 约束）：① value 0.2-0.3 早拟合好；② x2-acbc
// 3 倍密度 30 轮 pooled −3/400 精确零移动（方差受限系统会动但抖，不会纹丝不动）；
// ③ x3 67 eval 点带宽仅 0.65-0.72（mean 0.669），天花板式紧致横盘非噪声摆动；
// ④ c4-dodge 是单关长局不同技能，归因混杂。成本（按修正量纲重算）：60 万 transitions
// 线需 seed_rotate ≈ 600000÷4÷97 ≈ 1546/关 ≈ **6200 局/轮** ≈ wall ×26（旧“720-900 局”,
// 按 ticks 错算 10×），GPU 同比膨胀，期望 +0~3pp——费效比比原估计更倒挂。排序不变：
// 先结构（本腿），若抬升后收敛抖，再加量做收尾平滑器。
// verdict：终点 vs 暖启基线配对（acd+bcd 各 200 新种子）pooled Δ≥+5pp；
// 熔断轴（s-dodge 教训）：shots 崩（开火被吓没）或 kills 不升反降即停——
// wHit 降≠开火惩罚，但盯 fire/shots/kill 三数是铁律。
// 门统计口径（对账 reports/x-curricula-review-2026-09-14.md §3.2/P1.5）：
// 正式门除点估计≥80% 外，加 Wald 95% 下限 LB=p−1.645·SE≥77%（n=800 时
// SE≈1.4pp，LB≈点估计−2.3pp；该 n 下 Wald≈Wilson，二阶差可忽略），两轮都过。
// 量级宪法（DECISIONS §2026-09-15-goalnn-gate-pooled）：多关课按 200/关跑足量
// （诊断用），判据只读 pooled；分关数字永远只诊断（配哨兵报告，不进判据）。
// 新 gate kind 不用加代码——gate_check 的 pooled+conf_z 已是同一检验，
// 本腿（无 gates 块，走离线正式门）只采用数口径。
// 行标签口径（P0 事后）：8db1f88 起 in-loop eval 行 iter=真权重轮；verdict
// 探针一律用归档文件显式离线重跑，不读在途数字。
// 日常 eval（50/关）只做趋势，不做判决。
// ※※ 2026-09-15 起口径变更（plan/dual-track-eval-seeds 已合入 826c48c）：本课
//   `eval_games_per_stage: 50` 会**自动触发双轨**（should_dual_track(50)=True）⇒
//   每关从 50 局变 **100 局（锚点 50 + 轮转 50）**，四关合计 200 → **400 局/次**。
//   · 锚点轨 = EVAL_SEEDS[0:50]（860001-860050）= 与历史逐点可比的那条线；
//   · 轮转轨 = EVAL_SEEDS[50 + ((it-1)%3)*50 : +50]，周期 3，本腿 eval_every=5
//     ⇒ 6 个 eval 点的段序列 = 1,0,2,1,0,2（三段全覆盖、相邻不重复；每 3 轮滑窗
//     恰好覆盖全部三段）。
//   **读数纪律（改前必须遵守）**：
//     ① 趋势线读 summary 的 **`anchor_wr`**，不要读 `winRate`——后者已变成
//        400 局混轨口径，与 400 轮历史（200 局纯锚点口径）**不可比**；
//     ② 泛化线读 `rotor_wr`（只看 3 轮滑动均值，不看单轮点）；
//     ③ 过拟合报警：`anchor 近3轮均值 − rotor 近3轮均值 ≥ 5pp` 且持续 3 轮 ⇒ 日志
//        WARN，此后选点看轮转线。本腿 6 个点 ⇒ 最早在 it15 之后才可能形成满窗。
//     ④ 门侧 `gate_check.py` 已随本次改动更新：`_row_from_summary` 在 summary 带
//        `anchor_wr` 时**优先读它**（旧行无该字段回退 `winRate`）⇒ 门禁趋势线、
//        G1 / G10 / plateau / pool_window 全部自动跟到锚点轨，跨 2026-09-15 无口径跳变。
//        正式判决仍走离线 800 局（池外种子段），日常 400 局只做趋势与过拟合对照。
//   SE 随之变化：winRate(400 局，混轨) ≈ 2.35pp；anchor_wr / rotor_wr(各 200 局) ≈ 3.32pp
//   （旧注释的 4.6pp 是 **100 局**的 SE，被误记成 50 局；50 局是 6.65pp。
//     过拟合报警量是两轨各 3 轮均值之差 ⇒ 有效样本各 ≈250 局、SE ≈ 2.97pp、5pp ≈ 1.68σ）。
//
// ※ 动态采集（plan/dynamic-rollout-volume，a366ab3）**本腿不启用**：加
//   `target_transitions` 会引入第二个训练变量，破坏"唯一变量 = 奖励比值"的
//   单变量对照。缺席该键 = 老 `seed_rotate` 语义逐字节不变（已由回归单测钉死）。
//   量纲留待本腿结束后按「批量附录」的排序处理（先结构，后加量做平滑器）。
//   若日后启用：本课训练关段 = `--stages 2000-2003`（4 关），
//   `_volume_stages()` 走 parse_range(args.stages) 与账本键一致（已核对）。
// 读数口径三则（对账评审 §1.6 遗留）：
// ① rollout胜率 vs 贪心eval恒差 ~13pp——前者采样+新种子，后者贪心+固定种子，
//   两条线不可互换，只看各自趋势；② timeout_frac恒0（max_ticks 2700 从未咬合，
//   门局均tick仅980）——速度无代价，局均时长只看不动门；若 c04 起要速度偏好，
//   按 codex 用阈值式 -wLate·max(0,ticks−target)（禁线性 wTick），另立项；
// ③ 跨种子比较只认同一种子配对（评审 §1.4），日常 EVAL_SEEDS 数与门种子数永不混读。
//
// 环境：`"level": "ladder-c03"`（与 arena3 逐关同形，arena3 已退役删除；
//   探针表内关名仍写 abc…三元组（与 ladder-c03-abc…同内容），课程侧禁写环境键）。
// 暖启：已填 = x3-start.it333（见下 bc 行；x3 主腿已于 05:34 人工停，归档 it1–it333 完整）。
// 开工上下文（2026-09-15 05:30 实测）：x3 it333 正式门一（800 局）69.5%
// （abc 73.5/abd 76.0/acd 60.0/bcd 68.5），67 轮日常 eval 横盘 0.65-0.72——
// x3-start 按自设 60 轮证伪条款已触发判负；**x3 主腿已停（用户 2026-09-15 决断）**，
// 本腿由用户拍板接棒（同批 800 局对照：it180 69.375% / it280 67.75%，三点统计无差别）。
//
// 开工前校准（2026-09-15 07:15 **已登记**，取 it333 正式门一实测值；量纲 2026-09-15 修正）：
//   局均 tick = **980**（x3-gate1.log：avgTicks 980；旧登记 967 来自早期探针，已更新）
//   局均 samples ≈ **97**（本腿 30 轮实测 samples/轮均值 23343 ÷ 240 局；samples/ticks≈0.1）
//   ⇒ 本腿 transitions/轮 = seed_rotate × 关数 × 局均samples = 60 × 4 × 97 ≈ **2.33 万**
//   （原文“×局均tick ≈ 23.5 万”实为 ticks，10× 勘误；kl_cap 等无量纲校准不受影响）。
//   日后若起加量腿，同式反解 seed_rotate ＝ 目标量 ÷ 关数 ÷ 局均samples
//   （如 60 万 ÷4÷97 ≈ 1546/关 ≈ 6200 局/轮），不许拍脑袋定局数。
//   ※ 同错蔓延（未顺手改，立缺陷单）：nn-training/curricula/x3-start.jsonc:66、
//   _example-custom-stage.jsonc:77 同样把 ticks 写成 transitions；
//   plan/dynamic-rollout-volume.plan.md:26 同错；rl/volume_waves.py 配额数学分子 samples
//   配分母 est_ticks_per_game，差 10×（结课评审已用真函数跑出兑现 37% 触顶用例）。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
{
  "version": 5,
  "name": "x3-power",
  "mode": "per-tick",
  "level": "ladder-c03",

  // 双关还是四关？四关（与 x3-start 同分布，verdict 可比；power 三关天然占 3/4
  // 权重，abc 作对照关——若 abc 跌而 power 关涨，说明信用比拆东墙补西墙）。
  // 全轮 = seed_rotate × 关数 = 60 × 4 = 240 局/轮（与 x3-start 同成本）。
  "seed_rotate": 60,
  "workers": 8,

  // ── 奖励（公式/scheme/terminal 与 x3-start 同构；唯一变量 = params 比值）───
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0)",
    "params": {
      // 3.0→4.0：终结信用 +33%。
      "wKill": 4.0,
      // 0.3→0.15：命中地板减半。摊薄 3 枪 0.9→0.45，集火 2 枪+杀 3.6→4.3。
      "wHit": 0.15,
      "wWin": 2.0,
    },
    "terminal": {
      "lives_exhausted": -1.0,
    },
    "scheme": "toy",
  },

  // 已填（2026-09-15 07:05）：x3-start 正式门一 800 局三点对照——
  //   it333 556/800 = 69.5%（abc 73.5 / abd 76.0 / acd 60.0 / bcd 68.5）
  //   it180 555/800 = 69.375% ; it280 542/800 = 67.75%
  // it180 与 it333 差 1 局（800 局 SE≈1.6pp，统计上无差别）⇒ 选 it333：
  // 末点、最新、与开工上下文的「正式门一」口径同源，verdict 配对基线即它本身。
  // ※ it180 在日常 eval（50/关）显示 72.0% 是噪声峰，800 局口径下已证伪——
  //   再次坐实「日常 50/关只做趋势不判决」（SE≈6.65pp 分辨不了 +5pp）。
  "bc": "nn-training/weights/x3-start/x3-start.it333.20260915-052649.json",
  "kickstart_ref": true,
  // 必须为 0（config 硬校验；改 1 启动失败）。
  "warmup_iters": 0,

  "lr": 0.00015,
  "epochs": 4,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // ※ 死段声明（2026-09-15 核验）：本腿 iters=30，第二段 until_iter=40 **永不触发**
    // ⇒ 30 轮全程 lr 1.5e-4 + kl_coef 0.2 紧缰。日程与 x3-start **除 kl_cap 外**同构
    // （x3-start kl_cap=0.2 → 本腿 0.006；依据见下方护栏校准段）。kl_cap 是**护栏**
    // 不是训练变量，且按实测「几乎不触发」⇒ 不构成有效混淆，但措辞上**不得**再说
    // 「日程逐字同构」。死段有意保留，不是笔误；若用户选择放缰，改 40→20。
    { "until_iter": 40, "kl_coef": 0.2, "lr": 0.00015, "kl_cap": 0.006 },
    { "kl_coef": 0.03, "lr": 0.00005 },
  ],

  // 短腿 30 轮：跑满即 verdict，不过关账不续。
  "iters": 30,
  "max_hours": 12,
  "out": "tmp/x3-power/weights.json",
  // reward params 进语义身份（与 x3-start 不同 identity），必须新目录，禁止续跑。
  "traj": "tmp/x3-power",
  "backup_dir": "nn-training/weights/x3-power",
  "backup_prefix": "x3-power",

  // 护栏校准 —— **已实测落地**（2026-09-15 07:15，不再是 standby 空话）：
  // kl_cap：按本文件原定公式「x3 同分布前 5 轮实测 KL 最大值 × 5」定档。
  //   实测（tmp/x3-start/training_log.jsonl）：it1-5 KL =
  //   0.000505 / 0.000770 / 0.000769 / 0.001113 / 0.000936 ⇒ max = 0.001113
  //   ⇒ kl_cap = 0.001113 × 5 = **0.0056 ⇒ 取 0.006**。
  //   旧值 0.2 是 x2 沿用的工厂默认，比实测 KL 峰值（全 333 轮 max 0.00353）大
  //   两个数量级 ⇒ 330 轮从未咬合，等同死护栏（对账评审 §2.1.3 结论）。
  //   收紧到 0.006 后它仍是「几乎不触发」的量级（0.006 ≈ 实测峰值 0.0035 的 1.7 倍），
  //   但一旦出现真正的策略跳变（如信用比改写引发的分布迁移）能立刻截住。
  // ent_break 0.25：胜率 > max_winrate(0.5) 时按 breaker.py:123 结构性不触发——
  //   **这是设计如此的观察线，不是故障**；本腿起点 69.5% > 0.5，故它整腿不触发。
  "ent_break": 0.25,

  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5,
}

// ── 开工阻塞（2026-09-15 07:15 核验）────────────────────────────────────────
// ⚠ P0：唯一 enabled 的 GPU push 节点指向**已死 tunnel**。
//   nn-training/rl-config.json 节点表：
//     [0] https://dictionary-adrian-publishers-satisfactory.trycloudflare.com  enabled=true  ← 死
//     [1] http://127.0.0.1:8443      (self) concurrency 8  enabled=true  ← 采集可用
//     [2] http://192.168.0.88:8443   (mac)  concurrency 8  enabled=true  ← 采集可用
//     其余 6 个节点 enabled=false
//   节点[0] 自 05:27 起持续 HTTP 530（云机 worker 掉线），本机 cloudflared 进程
//   已于 07:15 关闭 ⇒ **它现在必然 100% 失败**。
//   开工前二选一（未做则必然重现 x3-start it334 的 wait_result 无限重试）：
//     (a) 修云机 worker + 重起 cloudflared（tunnel URL 会变，需同步改 rl-config 节点[0]）；
//     (b) 把节点[0] enabled 改 false，PPO 退回本地（ppo 段变慢，但 30 轮短腿可承受）。
//   采集侧 self+mac 共 16 并发 ≥ workers 8，不受影响。
//
// ── 已验证（2026-09-15 07:15）──────────────────────────────────────────────
//   探针：bun tools/sim/eval-course-ckpt.ts --course nn-training/levels/ladder-c03.jsonc
//         --weights <it333 归档> --games 8 --seed0 400000  （1.4s，仅验管线；**已改池外段**，
//         原记录用的 860001 是池内种子，会把"验管线"顺手变成"看过池内局"）
//   结论：ladder-c03 可加载；**stage 段确认为 2000=abc / 2001=abd / 2002=acd /
//   2003=bcd**（与 eval_stages "2000-2003" 一致，与 arena3 同段）；暖启权重可正常推理。
//   ⇒ verdict 的「acd+bcd 各 200 新种子」离线探针应指定 stages 2002,2003。
//   局均 tick：探针 8 局 1026（n 太小），以 it333 正式门一 n=800 的 **980** 为准。
//
// ── verdict 执行规格（2026-09-15 预注册，开工后照此执行，禁止临场改口径）──────
// **种子段（P1-b 已修）：`--seed0 400000`，800 局 ⇒ 种子 400000-400199，池外、全新。**
//   映射口径（`tools/sim/eval-course-ckpt.ts:256-264`）：`stage = g % nStages`、
//     `seed = seed0 + floor(g / nStages)` ⇒ games=800 / 4 关 = **每关 200 局、
//     每关独立种子**，占用 **400000-400199**（不是 400799；200 个种子各被 4 关各用一次）。
//   为何必须池外：x3-gate1（正式门一）用的是 `--seed0 860001, 800 局` ⇒ 种子
//     860001-860200 = **EVAL_SEEDS 全池**。双轨上线后日常 eval 每轮就在池内跑 400 局
//     （锚点 50 + 轮转 50，×4 关），**轮转段 3 轮滚过全池 150 个** ⇒ 基线权重对池内
//     每一个种子都已被反复评估。verdict 若继续用池内段，"新种子"名不副实——
//     200 个种子**全部**是选 it 时见过的 ⇒ 系统性高估 Δ，正是本 plan 立项要消灭的过拟合。
//   段取值纪律：与 EVAL_SEEDS（860001-860200，含历史 860001-860100 扩池前段）**不重叠**；
//     取 400000 而非 0/1000 是为了远离池子且远离 build_pairs 的 1..2^30 低段惯用值，
//     避免与 rollout 采样种子撞出「同种子不同用途」的台账混淆。**此值一经写入不得更改**
//     （改 seed 段 = 换判决条件，等于事后重开判决）。
//   基线 it333 必须**同批 400000-400199 重跑**（不得复用 x3-gate1 的 860001-860200：
//     两批种子不同 ⇒ 配对失效，McNemar 无从谈起）。
// 规模：**四关全跑 200/关 = 800 局 × 2（基线 it333 + 终点 it30）= 1600 局**，
//   同批新种子（不得复用 x3-gate1 的 800 局——那是四关混跑且基线已含在 verdict 内）。
//   成本 ≈ 5 分钟（实测 5.7 games/s），相对整腿 32 分钟可忽略。
//   为何不只跑 acd+bcd（400×2）：(a) 200/关符合量级宪法（DECISIONS §…-gate-pooled）；
//   (b) 能看 abc/abd 对照关是否「拆东墙补西墙」（本文件立项理由之一）；
//   (c) 功效更硬，见下。
// 执行命令（两批同段，把 <WEIGHTS> 换成对应归档）：
//   bun tools/sim/eval-course-ckpt.ts --course nn-training/levels/ladder-c03.jsonc \
//     --weights <WEIGHTS> --games 800 --seed0 400000 --workers 8 --out tmp/x3-power/<tag>.jsonl
//
// ── ★ 基线已冻结（2026-09-15 12:06，池外段 400000-400199 实测）────────────────
//   权重：nn-training/weights/x3-start/x3-start.it333.20260915-052649.json
//   产物：tmp/x3-power/baseline-it333-seed400000.jsonl（800 行）+ 同名 .log
//   **pooled 576/800 = 72.00%**（分关：abc 150/200=75.0% / abd 155/200=77.5% /
//     acd 127/200=**63.5%** / bcd 144/200=72.0%；acd 恒最弱，与 x3-start 画像一致）
//   ⇒ **判 Δ=+5pp 需要终点 ≥ 616/800 = 77.00%**（配对比 pooled 差严格，此处只作粗门槛）。
//   对照：x3-gate1（池内 860001-860200）同权重 556/800 = 69.5%。
//     池外比池内高 **+2.5pp**（≈1.1σ；两批各 800 局的差值 SE≈2.3pp，原文“≈1.4 SE
//     （SE≈1.77pp）”误用单批保守上界，勘误）⇒ 两批种子难度本就不同，
//     **不可跨批比较**——这正是「基线必须同批重跑」的实证理由（若非同批，2.5pp 的
//     种子差会直接污染 Δ）。注意方向：这里池外更高 ⟹ 若偷懒复用池内基线，会**低估**
//     终点相对增幅（而非高估），与「选 it 见过池内 ⇒ 高估终点」的偏差方向相反、部分抵消；
//     两者都不可接受，唯一干净做法 = 同批同段。
// 判据：pooled Δ ≥ +5pp **且** McNemar 单侧 p < 0.05（点估计 alone 不算——
//   x2-acbc 教训：+5pp 点估计在 n=100 下 p=0.125，是样本不足不是无效果）。
// 功效（配对设计，只取决于 discordant 比例 pd；脚本 tmp/x3-power/power-analysis.py）：
//     pd= 7%（x2-acbc 实测先例）⇒ power 98%，MDE(power80%) = +3.29pp
//     pd=15%                    ⇒ power 83%
//     pd=20%                    ⇒ power 72%
//     pd=37%                    ⇒ power 50%（临界；但 pd 这么大时 Δ 不会只有 5pp）
//   ⇒ Δ=+5pp 判据下功效充足（判据 5pp > MDE 3.29pp）。
// **数学硬约束：Δ ≤ pd。** 若 30 轮后策略几乎没动（复现 x3-start 的 KL≈0.002 病根），
//   pd < 5% ⇒ 根本观测不到 +5pp ⇒ 判负。此时结论**不得**写成「信用比无效」。
//
// 中途观察点（预注册，不用于提前判决）：it15 用同一批种子跑一次 800 局，
//   配对 vs 基线，只看形状与方向；判决权只在 it30。
//   目的：本腿日常 eval（50/关锚点轨，SE≈6.65pp）分辨不了 +5pp ⇒ 若无中途点，
//   整腿只有结尾一次判决机会、中途零纠偏。
// 必读伴随量（缺一不可）：
//   ① **KL**：it30 的 KL 若仍 ~0.002 ⇒ 判「梯度无方向 / 执行瓶颈」，指向 metrics v6；
//      只有 KL 显著放大（如 ≥0.01）而 Δ 仍 ≈0，才能说「信用比无效」。
//   ② shots/kills：熔断轴——shots 崩或 kills 不升反降即停（s-dodge 教训）。
//
// ── ★ 终点结算（2026-09-15，30/30 跑满；正式 800-verdict 已执行，判负）────
//   终点权重：nn-training/weights/x3-power/x3-power.it30.20260915-174103.json
//     （wver d06453793001；训练 17:41 落账归档，evalA 17:47/17:53 两次确认同一 wver）。
//   正式 verdict（800 局 seed400000-400199，同批配对；产物
//     tmp/x3-power/verdict-it30-seed400000.jsonl（800 行）+ 同名 .log，92.4s @8.7 games/s）：
//     终点 **553/800 = 69.125%** vs 基线 576/800 = 72.00%，**Δ = −23/800 = −2.875pp**，
//     判线（≥616/800 且 McNemar 单侧 p<0.05）**失败 ⇒ 判负**。
//   配对：b01=12 / b10=35，pd=5.9%；McNemar 单侧（新>基线）p=0.9998（要求 <0.05，反向）；
//     双侧 p=0.0011，净 −23/√47 = −3.35σ，即终点显著**差于**基线——但只记「判负 + 方向为负」，
//     不做「信用比有害」的因果断言（单腿 −2.9pp 显著仍可来自起点游走 + acd 噪声；
//     预注册纪律只允许判「梯度无方向 / 执行瓶颈」）。
//   分关（各 200，同种子配对）：abc 146/200=73.0%（−2.0pp，b01=2/b10=6）、
//     abd 151/200=75.5%（−2.0pp，4/8）、acd 118/200=59.0%（−4.5pp，3/12，目标关降得最多）、
//     bcd 138/200=69.0%（−3.0pp，3/9）⇒ 四关全降，无「拆东墙补西墙」（没有一关涨）。
//   预注册主判据池（acd+bcd，54 行）并列：b01=6 / b10=21，pd=6.75%，Δ=−3.75pp，
//     单侧 p=0.9992，双侧 p=0.0059（acd alone −4.50pp p₂=0.0352；bcd alone −3.00pp
//     p₂=0.146）⇒ 两池同判负、方向一致（差 0.9pp）。头条取 800 池（后写的执行规格
//     230 行起胜出），主判据池数字亦列，读者不必自算。
//   伴随量（verdict 800 口径）：kills 2.411→2.357（−0.05，~1.3σ 噪声内）、
//     shots 14.55→14.07（−3%，无崩塌）、dmg 124.2→124.8（持平）、ticks 995→974 ⇒
//     熔断轴干净（无开火崩；kills 微降在噪声内，不构成反降停腿证据）。
//   ※ 熔断口径声明：预注册“不升反降即停”是绝对阈值措辞，本腿按噪声带执行（kills
//     −1.1~1.3σ + shots 未崩 + zero_kill 不动 ⇒ 不停）；后腿沿用此解释，不得回溯为“停腿条件是软的”。
//   训练（tmp/x3-power/training_log.jsonl，30 个 iteration 事件齐）：rollout wr 全程
//     0.6292–0.7417 无趋势；KL it1 0.000581 → it30 0.002133，全程 max 0.00231（it25），
//     kl_cap=0.006 在本路径**不接线**（remote/serial 无消费者；打包≠生效，
//     DECISIONS §2026-09-15-goalnn-kl-cap-unwired）——旧「从未咬合」作废；
//     KL≈0.002 由 kl_coef 0.2 软惩罚 + 小步长决定，与「死护栏」叙事无关。
//   日常 anchor（只读 anchor_wr；it30 因 engine_epoch mismatch 回落 local，只跑了锚点 200 局，
//     rotor 缺席 ⇒ it30 的过拟合窗记不完整）：it0 0.65 → it5 0.655 → it10 0.68 →
//     it15 0.67 → it20 0.685 → it25 0.66 → it30 0.64（128/200），最大 Δ+3.5pp ≈0.75σ
//     （跨点差值 SE≈4.7pp＝√2×3.3pp；原文“<1σ（SE≈3.3pp）”分母误用单点 SE，勘误），
//     +5pp 连边没摸到。
//   rotor/gap：-8.0/-2.5/+2.5/-3.5/-4.0（it5–it25），从未触发 ≥5pp×3 轮 ⇒ 无过拟合。
//   分关终点 vs 起点（同 50/关锚点种子 860001-860050）：abc 33/50=66% vs 34/50=68%，
//     abd 36/50=72% vs 36/50=72%，acd 25/50=50% vs 25/50=50%（逐数相同），
//     bcd 34/50=68% vs 35/50=70% ⇒ power 关零抬升，对照关 abc 未跌（无拆东墙补西墙，但也没涨）。
//   配对硬约束兑现：it0 vs it30 同种子 200 对，b01=4 / b10=6，pd=5.0%，Δ=-1.00pp；
//     策略在锚点上几乎没动（仅 10/200 翻转）⇒ 800-verdict 的 −2.875pp 与此互证（pd<5% 即看不到 +5pp）。
//   熔断轴干净：kills 2.28→2.27，shots 13.71→13.58，eHits 6.29→6.23，
//     zero_kill 6.5%→6.5% ⇒ 无开火崩、无 kills 反降，是跑满的干净证伪，非熔断停腿。
//   **分支判决（预注册 284 行起原样执行）**：KL~0.002 ⇒ 「梯度无方向 / 执行瓶颈」，
//     指向 metrics v6（分敌种命中列，TS+Python 全链）；**不得**写成「信用比无效」
//     （后者须 KL≥0.01 而 Δ≈0 才成立，本腿不在此分支）。
//   后继排序（本文件既定，不另行论证）：下一步 = metrics v6，不是调 wKill/wHit 剂量；
//     wChip 扫描是备选，须用户明示批准 + DECISIONS 破总规则（N3）立案后才开腿，范围仅早期关；
//     不加 rollout 量（批量附录排序：结构先行，加量只做抬升后的收尾平滑器）。
//   it15 中途 800 探针（预注册“中途观察点”段）**未执行，现显式注销**：终点 verdict +
//     anchor 预览已覆盖判决需求，且下述 4h10m 事故挤占无窗口；预注册观察点要么执行
//     要么注销，此条注销（非静默蒸发）。
//   事件账（training_log.jsonl 全量；“30 轮跑满”之外的时间线）：run_start ×10
//    （13:31/13:36/13:50/13:54/14:15/14:29/16:09/16:11/16:23/17:29），iter_error ×5 +
//     remote_degrade ×1（15:00 wait_result 超时 1800s；15:02–15:03 HTTP 530 ×2 后降级本机
//     PPO；15:03–15:04 `load_episodes` AttributeError ×3，5/5 耗尽 ⇒ **R9 降级落点被打穿**，
//     fix 前任何腿远端连败都会踩同一坑，立缺陷单：先复现后修 §7）；轮内 resume ×2
//     （it1 resumed=240、it25 resumed=239）；进程停机 1h04m（15:04→16:08）；it1 独占 2h54m
//     才落账；it2–it30 用 1h16m（≈2.6min/轮）。wall clock 13:31→17:41 = **4h10m，其中训练
//     仅 1h16m**（预注册“整腿 32 分钟”实为纯训练时间，口径差异立此存照）。
//     KL/胜率曲线无断点 ⇒ 判决不受影响；但“环境/优化器唯一变量 = 奖励比值”须补限定语
//     （restart 不改变已落账 shard，resume 段 PPO 幂等续跑，见 WAL replay-check 行）。
//   开工阻塞 P0 闭合：实际走 (a) 变体——tunnel 换新 URL（先后
//     careful-florist-found-orleans… 与 initially-entities-food-bestsellers…，
//     后者即 rl-config 现节点[0]，enabled/conc=1；证据 training-loop.log push 行）。
//     本文件开工阻塞段节点表已过期（仍写旧 tunnel +“开工前二选一”），标注过期，
//     不改写历史，权威以 rl-config 现值为准。
//   待办：verdict 已落账；在 docs/nn.progress.md 顶部记 §46（含 pooled Δ=−2.875pp、
//     McNemar 单侧 p=0.9998、pd=5.9%、KL it30=0.00213、熔断干净、
//     八字判决「梯度无方向/执行瓶颈」）；下一腿 metrics v6 立项。
//   缺陷单（本腿发现、训练侧修，不属本文件）：① R9 降级 `load_episodes` None（§7 先复现后修）；
//     ② evalA it30 三写 eval_summary + 末条 dropped=400 脏行（settle 口径 `total−settled` 反推，
//     控制台 iters.ts 按 iter 归并会读到；外加 reuse 回填行缺 anchor/rotor 键）；
//     ③ P0-1 蔓延：x3-start.jsonc:66、_example-custom-stage.jsonc:77、
//     plan/dynamic-rollout-volume.plan.md:26 同错 + volume_waves.py 分子 samples 配分母
//     est_ticks 的 10×（先钉“600000/4/980 语义”单测，现有 e2e 合成小值抓不到）。
//   DECISIONS：本腿 params-only（formula/scheme/terminal 未动）+ 新 traj/out 目录，§15.5 属灰区；
//     但判负结论 + 量纲勘误会被后腿引用（“未来再犯”门过）⇒ 补一条（判负 + transitions=samples
//     口径），或附三问“不立”理由——此处先占位，记 §46 时一并定。
