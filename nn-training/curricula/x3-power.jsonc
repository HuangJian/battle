// 课程 x3-power —— power 关命中信用重定价腿
// 状态：**2026-09-15 07:15 核验就绪**（原 standby 待命项已全部落地：bc 已填、
//   kl_cap 已按自定公式校准、开工前校准已登记、死段已声明）。开工阻塞见文件末「开工阻塞」。
//
// 背景：跨 count 稳定结构——含 power 关差 ~15-20pp（x2 门 ac/bc 0.63-0.66；
// x3 it20 分关 acd 0.56/bcd 0.60 vs abc/abd 0.76-0.78；god 亦然）。败局画像
// （x2-gate3）：ac 55% 败局 0 杀死（接战了：中位 3 发/1.52 命中/37 40 打中过，
// ~293t 先倒），bc 91% 败局 1 杀死（残局 1v1 vs power 死）。共同机制：伤害摊薄
// 零代价——wHit 按次给分，打中两个各一枪与打死一个，在账上几乎没区别，而 power
// 必须集火秒掉（否则它的快弹先赢）。
//
// ※ 表达力边界（诚实声明）：真·按敌种/按伤害归一写不出来——metrics 只有总数
//   （kills/enemyHits，无分敌种、无伤害量；firstKillTick 是整局常量，toy diff
//   下恒零）。本腿是现有语言下的可表达代理：**杀/中信用比**（wKill↑/wHit↓），
//   把“打中就行”的 satisficing 地板抽掉，逼梯度指向终结。若证伪，下一步是
//   metrics v6（分敌种命中列，TS+Python 全链）——不是继续调剂量。
// 另注：当前 wKill:wHit 已是 10:1 仍摊薄，故瓶颈可能是执行（活着打完第二枪）
// 而非信用——本腿同时是这个二选一的判决实验：比值陡峭化后若仍摊薄，判执行瓶颈。
//
// 下一个可能尝试方向：wChip 扫描豁免（2026-09-15 立项备选，开工需用户明示批准）。
// 由头：roadmap N3 总规则禁 c01-c03 上 wChip（理由=承伤基数极小），但 x2 实测
// 局均 dmg 97（胜 81/败 188，c4 基线 65%），“极小”前提不成立，且死因正是挨打。
// 方案：wChip=k/97，k∈{0, 0.5, 1.0}→{0, 0.005, 0.010}（c4 有效 0.03 的零头起步；
// c6 0.03 保守化翻车案在前），单变量、每臂 30 轮、verdict 配对看 dmg/kill 降且
// kills/shots 不降（降即停）。程序：用户批准 + DECISIONS 立案（破总规则）后开腿，
// 范围仅早期关。排序在本腿之后（先判信用/执行，再问疼不疼）。
//
// 与 x3-start 单变量对照：环境/优化器/日程/护栏逐字同构；唯一训练变量 = 奖励
// params（wKill 3.0→4.0，wHit 0.3→0.15；wWin/terminal/scheme 不动）。
// 开工假说（§5.7）：信用比×2（10:1→27:1）后，acd/bcd pooled it20 前 +5pp；
// 30 轮跑满无爬升即证伪停腿（防 c6-bonus 式盲跑）。
// 批量附录：为何不先加 rollout 量（2026-09-15，x2/x3 联合复盘结论）。
// 量纲纠正：PPO 吃的是 transitions/轮，不是局数。x3：240 局×967t≈23 万；
// c4-dodge 先例（600 局×1100+t≈66 万+）——roadmap“600-1000 局”实为 transition
// parity 要求，直接抄局数是刻舟求剑。以后定量按 transitions 对齐 ~60 万+ 线。
// 四条反证（方差不是 binding 约束）：① value 0.2-0.3 早拟合好；② x2-acbc
// 3 倍密度 30 轮 pooled −3/400 精确零移动（方差受限系统会动但抖，不会纹丝不动）；
// ③ x3 67 eval 点带宽仅 0.65-0.72（mean 0.669），天花板式紧致横盘非噪声摆动；
// ④ c4-dodge 是单关长局不同技能，归因混杂。成本：720-900 局/轮≈整轮 70s→3.5min，
// GPU ×3，期望 +0~3pp——费效比倒挂。排序：先结构（本腿），若抬升后收敛抖，
// 再加量做收尾平滑器。
// verdict：终点 vs 暖启基线配对（acd+bcd 各 200 新种子）pooled Δ≥+5pp；
// 熔断轴（s-dodge 教训）：shots 崩（开火被吓没）或 kills 不升反降即停——
// wHit 降≠开火惩罚，但盯 fire/shots/kill 三数是铁律。
// 门统计口径（对账 reports/x-curricula-review-2026-09-14.md §3.2/P1.5）：
// 正式门除点估计≥80% 外，加 Wald 95% 下限 LB=p−1.645·SE≥77%（n=800 时
// SE≈1.4pp，LB≈点估计−2.3pp；该 n 下 Wald≈Wilson，二阶差可忽略），两轮都过。
// 量级宪法（DECISIONS §2026-09-15-goalnn-gate-pooled）：多关课按 200/关跑足量
// （诊断用），判据只读 pooled；分关数字永远只诊断（配哨兵报告，不进判据）。
// 新 gate kind 不用加代码——gate_check 的 pooled+conf_z 已是同一检验，
// 本腿（无 gates 块，走离线正式门）只采用数口径。
// 行标签口径（P0 事后）：8db1f88 起 in-loop eval 行 iter=真权重轮；verdict
// 探针一律用归档文件显式离线重跑，不读在途数字。
// 日常 eval（50/关）只做趋势，不做判决（SE≈4.6pp 分辨不了 +5pp）。
// 读数口径三则（对账评审 §1.6 遗留）：
// ① rollout胜率 vs 贪心eval恒差 ~13pp——前者采样+新种子，后者贪心+固定种子，
//   两条线不可互换，只看各自趋势；② timeout_frac恒0（max_ticks 2700 从未咬合，
//   门局均tick仅980）——速度无代价，局均时长只看不动门；若 c04 起要速度偏好，
//   按 codex 用阈值式 -wLate·max(0,ticks−target)（禁线性 wTick），另立项；
// ③ 跨种子比较只认同一种子配对（评审 §1.4），日常 EVAL_SEEDS 数与门种子数永不混读。
//
// 环境：`"level": "ladder-c03"`（与 arena3 逐关同形，arena3 已退役删除；
//   探针表内关名仍写 abc…三元组（与 ladder-c03-abc…同内容），课程侧禁写环境键）。
// 暖启：已填 = x3-start.it333（见下 bc 行；x3 主腿已于 05:34 人工停，归档 it1–it333 完整）。
// 开工上下文（2026-09-15 05:30 实测）：x3 it333 正式门一（800 局）69.5%
// （abc 73.5/abd 76.0/acd 60.0/bcd 68.5），67 轮日常 eval 横盘 0.65-0.72——
// x3-start 按自设 60 轮证伪条款已触发判负；**x3 主腿已停（用户 2026-09-15 决断）**，
// 本腿由用户拍板接棒（同批 800 局对照：it180 69.375% / it280 67.75%，三点统计无差别）。
//
// 开工前校准（2026-09-15 07:15 **已登记**，取 it333 正式门一实测值）：
//   局均 tick = **980**（x3-gate1.log：avgTicks 980；旧登记 967 来自早期探针，已更新）
//   ⇒ 本腿 transitions/轮 = seed_rotate × 关数 × 局均tick = 60 × 4 × 980 ≈ **23.5 万**
//   日后若起加量腿，同式反解 seed_rotate ＝ 目标量 ÷ 关数 ÷ 局均tick
//   （如 60 万 ÷4÷980 ≈ 153/关），不许拍脑袋定局数。
//
// 启动（§5.6：禁裸 python，一律走启动器；用户手工开训）：
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x3-power
{
  "version": 5,
  "name": "x3-power",
  "mode": "per-tick",
  "level": "ladder-c03",

  // 双关还是四关？四关（与 x3-start 同分布，verdict 可比；power 三关天然占 3/4
  // 权重，abc 作对照关——若 abc 跌而 power 关涨，说明信用比拆东墙补西墙）。
  // 全轮 = seed_rotate × 关数 = 60 × 4 = 240 局/轮（与 x3-start 同成本）。
  "seed_rotate": 60,
  "workers": 8,

  // ── 奖励（公式/scheme/terminal 与 x3-start 同构；唯一变量 = params 比值）───
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0)",
    "params": {
      // 3.0→4.0：终结信用 +33%。
      "wKill": 4.0,
      // 0.3→0.15：命中地板减半。摊薄 3 枪 0.9→0.45，集火 2 枪+杀 3.6→4.3。
      "wHit": 0.15,
      "wWin": 2.0,
    },
    "terminal": {
      "lives_exhausted": -1.0,
    },
    "scheme": "toy",
  },

  // 已填（2026-09-15 07:05）：x3-start 正式门一 800 局三点对照——
  //   it333 556/800 = 69.5%（abc 73.5 / abd 76.0 / acd 60.0 / bcd 68.5）
  //   it180 555/800 = 69.375% ; it280 542/800 = 67.75%
  // it180 与 it333 差 1 局（800 局 SE≈1.6pp，统计上无差别）⇒ 选 it333：
  // 末点、最新、与开工上下文的「正式门一」口径同源，verdict 配对基线即它本身。
  // ※ it180 在日常 eval（50/关）显示 72.0% 是噪声峰，800 局口径下已证伪——
  //   再次坐实「日常 50/关只做趋势不判决」（SE≈4.6pp 分辨不了 +5pp）。
  "bc": "nn-training/weights/x3-start/x3-start.it333.20260915-052649.json",
  "kickstart_ref": true,
  // 必须为 0（config 硬校验；改 1 启动失败）。
  "warmup_iters": 0,

  "lr": 0.00015,
  "epochs": 4,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // ※ 死段声明（2026-09-15 核验）：本腿 iters=30，第二段 until_iter=40 **永不触发**
    // ⇒ 30 轮全程 lr 1.5e-4 + kl_coef 0.2 紧缰。这是与 x3-start「日程逐字同构」
    // （单变量对照要求）的代价，有意保留，不是笔误。若用户选择放缰，改 40→20。
    { "until_iter": 40, "kl_coef": 0.2, "lr": 0.00015, "kl_cap": 0.006 },
    { "kl_coef": 0.03, "lr": 0.00005 },
  ],

  // 短腿 30 轮：跑满即 verdict，不过关账不续。
  "iters": 30,
  "max_hours": 12,
  "out": "tmp/x3-power/weights.json",
  // reward params 进语义身份（与 x3-start 不同 identity），必须新目录，禁止续跑。
  "traj": "tmp/x3-power",
  "backup_dir": "nn-training/weights/x3-power",
  "backup_prefix": "x3-power",

  // 护栏校准 —— **已实测落地**（2026-09-15 07:15，不再是 standby 空话）：
  // kl_cap：按本文件原定公式「x3 同分布前 5 轮实测 KL 最大值 × 5」定档。
  //   实测（tmp/x3-start/training_log.jsonl）：it1-5 KL =
  //   0.000505 / 0.000770 / 0.000769 / 0.001113 / 0.000936 ⇒ max = 0.001113
  //   ⇒ kl_cap = 0.001113 × 5 = **0.0056 ⇒ 取 0.006**。
  //   旧值 0.2 是 x2 沿用的工厂默认，比实测 KL 峰值（全 333 轮 max 0.00353）大
  //   两个数量级 ⇒ 330 轮从未咬合，等同死护栏（对账评审 §2.1.3 结论）。
  //   收紧到 0.006 后它仍是「几乎不触发」的量级（0.006 ≈ 实测峰值 0.0035 的 1.7 倍），
  //   但一旦出现真正的策略跳变（如信用比改写引发的分布迁移）能立刻截住。
  // ent_break 0.25：胜率 > max_winrate(0.5) 时按 breaker.py:123 结构性不触发——
  //   **这是设计如此的观察线，不是故障**；本腿起点 69.5% > 0.5，故它整腿不触发。
  "ent_break": 0.25,

  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5,
}

// ── 开工阻塞（2026-09-15 07:15 核验）────────────────────────────────────────
// ⚠ P0：唯一 enabled 的 GPU push 节点指向**已死 tunnel**。
//   nn-training/rl-config.json 节点表：
//     [0] https://dictionary-adrian-publishers-satisfactory.trycloudflare.com  enabled=true  ← 死
//     [1] http://127.0.0.1:8443      (self) concurrency 8  enabled=true  ← 采集可用
//     [2] http://192.168.0.88:8443   (mac)  concurrency 8  enabled=true  ← 采集可用
//     其余 6 个节点 enabled=false
//   节点[0] 自 05:27 起持续 HTTP 530（云机 worker 掉线），本机 cloudflared 进程
//   已于 07:15 关闭 ⇒ **它现在必然 100% 失败**。
//   开工前二选一（未做则必然重现 x3-start it334 的 wait_result 无限重试）：
//     (a) 修云机 worker + 重起 cloudflared（tunnel URL 会变，需同步改 rl-config 节点[0]）；
//     (b) 把节点[0] enabled 改 false，PPO 退回本地（ppo 段变慢，但 30 轮短腿可承受）。
//   采集侧 self+mac 共 16 并发 ≥ workers 8，不受影响。
//
// ── 已验证（2026-09-15 07:15）──────────────────────────────────────────────
//   探针：bun tools/sim/eval-course-ckpt.ts --course nn-training/levels/ladder-c03.jsonc
//         --weights <it333 归档> --games 8 --seed0 860001   （1.4s，6/8，仅验管线）
//   结论：ladder-c03 可加载；**stage 段确认为 2000=abc / 2001=abd / 2002=acd /
//   2003=bcd**（与 eval_stages "2000-2003" 一致，与 arena3 同段）；暖启权重可正常推理。
//   ⇒ verdict 的「acd+bcd 各 200 新种子」离线探针应指定 stages 2002,2003。
//   局均 tick：探针 8 局 1026（n 太小），以 it333 正式门一 n=800 的 **980** 为准。
//
// ── verdict 执行规格（2026-09-15 预注册，开工后照此执行，禁止临场改口径）──────
// 规模：**四关全跑 200/关 = 800 局 × 2（基线 it333 + 终点 it30）= 1600 局**，
//   同批新种子（不得复用 x3-gate1 的 800 局——那是四关混跑且基线已含在 verdict 内）。
//   成本 ≈ 5 分钟（实测 5.7 games/s），相对整腿 32 分钟可忽略。
//   为何不只跑 acd+bcd（400×2）：(a) 200/关符合量级宪法（DECISIONS §…-gate-pooled）；
//   (b) 能看 abc/abd 对照关是否「拆东墙补西墙」（本文件立项理由之一）；
//   (c) 功效更硬，见下。
// 判据：pooled Δ ≥ +5pp **且** McNemar 单侧 p < 0.05（点估计 alone 不算——
//   x2-acbc 教训：+5pp 点估计在 n=100 下 p=0.125，是样本不足不是无效果）。
// 功效（配对设计，只取决于 discordant 比例 pd；脚本 tmp/x3-power/power-analysis.py）：
//     pd= 7%（x2-acbc 实测先例）⇒ power 98%，MDE(power80%) = +3.29pp
//     pd=15%                    ⇒ power 83%
//     pd=20%                    ⇒ power 72%
//     pd=37%                    ⇒ power 50%（临界；但 pd 这么大时 Δ 不会只有 5pp）
//   ⇒ Δ=+5pp 判据下功效充足（判据 5pp > MDE 3.29pp）。
// **数学硬约束：Δ ≤ pd。** 若 30 轮后策略几乎没动（复现 x3-start 的 KL≈0.002 病根），
//   pd < 5% ⇒ 根本观测不到 +5pp ⇒ 判负。此时结论**不得**写成「信用比无效」。
//
// 中途观察点（预注册，不用于提前判决）：it15 用同一批种子跑一次 800 局，
//   配对 vs 基线，只看形状与方向；判决权只在 it30。
//   目的：本腿日常 eval（50/关，SE≈4.6pp）分辨不了 +5pp ⇒ 若无中途点，
//   整腿只有结尾一次判决机会、中途零纠偏。
// 必读伴随量（缺一不可）：
//   ① **KL**：it30 的 KL 若仍 ~0.002 ⇒ 判「梯度无方向 / 执行瓶颈」，指向 metrics v6；
//      只有 KL 显著放大（如 ≥0.01）而 Δ 仍 ≈0，才能说「信用比无效」。
//   ② shots/kills：熔断轴——shots 崩或 kills 不升反降即停（s-dodge 教训）。
