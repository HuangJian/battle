// 课程 x5-rebirth —— 5 敌关（ladder-c05）的**热启迁移**腿（rebirth 阶梯第五级）
//
// 状态：**已结课（2026-09-18，63 轮，用户人工停）—— 判决 = 部分达成**。
//   池外固定段（seed0 402000，800 局）：承伤 161.2 → 141.9（−12.0%，中间带，CI 不重叠），
//   ticks +2.0%（约束守住），pass 54.6% → 63.1%（+8.5pp）；但 pu 0.61 → 0.74，
//   约束③（≥1.22）失败 ⇒ 封顶部分达成（"拾取没学会，升 c06 必崩"）。
//   终点 = it35（与末 it63 互不可区分，取 pass 高者），
//   已另存 `nn-training/weights/x5-rebirth-best/x5-rebirth.it35.best.json`。
//   详见文件尾「结算节（2026-09-18 结课回填）」。agent 不得启动训练。
//
// 前置假设：x4-rebirth 已达成，终点 `-best` 已落盘（选取规则见 §4）。
// 本腿把它的终点权重热启到 ladder-c05（5 敌 / 单关），回答三个问题：
//   ① 迁移链（c01 → … → c05）能不能继续守住「承伤 + 耗时」？
//   ② **拾取是不是 c05 的任务级必需**（用户命题）：起点策略几乎不捡星（stars 0.07/局，
//      见 §2），火力升不上去；5 敌含 armor，基础火力吞吐是否够用是本腿第一悬念。
//   ③ 朝向信号问题（a2 open limitation #2）—— c05 若仍能用半解 + 拾取项拿下，
//      它就继续往后排。
// ⚠ **本腿是 rebirth 线第一次换配方**（环境升级 + 公式加项 = 双变量，归因到「包」，
//   用户拍板；a2「包 vs 包」先例）。it0-vs-终点对照仍合法（唯一差异 = 训练轮数）；
//   与 x4 的直接对照仍禁止（另一起点另一配方）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（levels/ladder-c05.jsonc 实读，勿信记忆）
// ══════════════════════════════════════════════════════════════════════════
//   **单关**：ladder-c05，forces `abcdabcdabcdabcdabcd`（20 长），`count 5` ⇒
//   队列 20 取前 5 = **a+b+c+d+a**（basic 双份 + fast/power/armor 各一）。
//   max_ticks **3900**、lives 1、hard、空场 13×13。
//   自定义关 ID **2000-2000**（单关沿用，`rl/ladder_factory.py:228`）。
//   任务侧一个字不动（AGENTS §0.2）—— 只动训练侧。
//   单关的结构后果与 x4 同（无分关掉队 / seed_rotate 600 / per-stage 配额 = 总量，见 x4 §0）。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. ★ 拾取证据链（本节每一条都是 c 线真金白银，逐条决定 §3 的形状）
// ══════════════════════════════════════════════════════════════════════════
// · **P1（c6-pickup.it35，正例）**：wPickup=3.0 leverage 下同种子配对 38% vs 起点 23%
//   = +15pp（McNemar p=0.025）⇒ **拾取杠杆早期有效**，不是安慰剂。
// · **P2（c6-pickup3，反例/天花板警告）**：it5 37.5% → it61 29.0%（净 −9pp，p=0.050 边界显著）
//   ⇒ 高拾取价**长期未守住**。但该腿是 8 变量混杂（wHit 0.3 / wTick / wStuck / wChip /
//   wDmg挂playerHits=1.0…），**不能归因到 wPickup** —— 教训是「别抄整条公式」，
//   不是「拾取无效」。⇒ 本腿只加拾取两项，其余一字不动（§3）。
// · **P3（DECISIONS 791，定价先例）**：p10 = p4 toy + wPickup×1.5 + wStar×1.0，
//   star 实得 2.5 ≈ 一杀 ⇒ **star ≡ 1 kill 不变量**是本仓定过价的。本腿沿用该不变量
//   （wPickup 2.5 + wStar 0.5 ⇒ star 实得 **3.0 = 恰好 1 kill**，见 §3）。
// · **P4（ladder_factory，扎根先例）**：`params_for` 里 `wPickup = 2.5 if count >= 5`
//   （"c05 起 wPickup 扎根"）⇒ 立项时以 2.5 为锚。但 §前置②b 的门在新语料上 fired
//   （2.5 只给 +0.95 < 门限 +1.5）⇒ **实测语料优先于工厂锚**（a2 方法论②），
//   最终剂量 3.0（P1 同向）。工厂锚保留作"第一猜"，不当 verdict。
// · **P5（c6-dmgfix，红线）**：删 `-wDmg` 项把 wPickup/wKill 激励一起挤出去
//   （pickups −0.27，z=−2.91）⇒ **承伤项一个字不许动**。拾取是**加法**，
//   任何"用拾取替代承伤"的重组都禁止。
// · **P6（本腿 it0，机制确认）**：star 是 PowerUpType 之一（`src/types.ts:21`），
//   拾取走同一 `powerup_collected` 事件 ⇒ `powerUpsCollected` **含 star**
//   （`rl-reward-toy.ts:98-99`），`starsCollected` 另计 ⇒ star 实得 = wPickup + wStar。
//   起点 rollout stars **0.07/局**（40 局里约 3 颗）⇒ 策略基本没升过火力，
//   用户命题"单命通关关键在拾取"在本腿是**可证伪的**：终点 stars/pu 若不动而承伤达标，
//   命题即被证伪（见 §5 约束③的写法）。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 实测（2026-09-18，bc = x4-rebirth.it45.best；x3 权重的临时探针已作废）
// ══════════════════════════════════════════════════════════════════════════
// x4-best 比 x3 权重强得多（迁移红利）：rollout dmg 315.4 → 192.3，贪心 pass 27.0% →
// 64.0%（已超 god 的 48.5%）。下面是**最终分母**（§前置②已执行）。
// 口径 R —— **rollout**（采样，40 局 seed 0–39；`tmp/x5-rebirth-probe/rollout-it45`）：
//     40/40 = 100% 通关 ｜ dmg **192.3/局** ｜ ticks 1743 ｜ steps **175.9**
//     ｜ kills 5.00 ｜ hits 12.05 ｜ shots 31.7 ｜ cells 77.6 ｜ 命中率 38.0%
//     ｜ pu **1.02/局** ｜ stars **0.12/局** ｜ 零捡局 8/40
//     ⇒ `est_samples_per_game` 取 **175**（175.9 向下取整；见 §7）。
// 口径 G —— **贪心**（200 局 seed0 860001；`it0-x4.jsonl`，判据分母）：
//     pass **128/200 = 64.0%** ｜ dmg **154.8** ｜ ticks_win **1781**
//     ｜ kills 3.85 ｜ shots 22.5 ｜ pu **0.71** ｜ 零伤 21.9% ｜ gameover 72
//     ｜ firstHitKind **basic 200**（power 恒 0，第五条腿）
//     ｜ killerKinds power 26 / basic 20 / armor 16 / fast 13（死因分散，
//        power 不再占优 —— x3 临时探针里 basic 60 居首，x4-best 已把它打散）。
// 参照臂 —— **god**（同种子 200 局；只作参照，x3 权重时已跑，权重无关可复用）：
//     pass 48.5% ｜ dmg 173.7 ｜ ticks 1650 ｜ kills 3.19 ｜ pu 0.91。
// ⇒ **四条读数（最终）**：
//   ① **边际翻回负号**：−0.56 dmg/格（sit 218.2 → wander 190.0；LSQ −0.52 确认）——
//      x4-best 在 c05 已是"会打"态（x3 权重的 +2.34 是弱起点假象，已作废）。
//      EV 走 +37.4 > 坐 +26.1（margin +11.3，回到 x4 宽度）⇒ a2 剂量健康。
//   ② **捡/不捡缺口**：捡组 EV +31.94 vs 不捡组 +34.19（Δ=−2.25，不捡优 —— 主因不捡组
//      cells 更高 85.2 vs 75.7）。⇒ wPickup 必须满足 wPickup×1.28 > 2.25 + 1.5（门限），
//      即 **wPickup ≥ 2.93 ⇒ 取 3.0**（见 §3；§前置②b 的门 fired，剂量已按门调整）。
//   ③ 起点已超教师 +15.5pp（64.0% vs 48.5%），dmg 已低 18.9（154.8 vs 173.7）⇒
//      本腿不是"追赶"，是"守住并继续压承伤 + 学会拾取"。判据分母见 §5。
//   ④ stars 0.12/局（40 局约 5 颗）：火力线仍基本没启动 ⇒ 约束③（§5）保留。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 奖励：a2-v4 **+ 拾取一项**（rebirth 线第一次加项；其余逐字不动）
// ══════════════════════════════════════════════════════════════════════════
//   `wKill 3.0 / wHit 0.6 / wWin 2.0 / wDmg 0.03 / wShot 0.05 / wExplore 0.2`
//   **`+ wPickup 3.0`** ＋ terminal `−5.0 / −2.0`，scheme `toy`。
//   公式：`... + wExplore*cellsVisited + wPickup*powerUpsCollected`
//   （拾取项缀在末尾；metric 名已核：idx7，`reward_validation` 范围 0–30）。
// 剂量（§前置②b 的门 fired 后的最终值；临时剂量 2.5/0.5 已作废）：
//   ① **wPickup = 3.0**：新语料上捡组 +3.0×1.28 = **+3.84** ⇒ 捡超出不捡
//      **+1.59 ≥ 门限 +1.5**（§前置②b）。2.5 只给 +0.95，门不过 ⇒ 按门调整，
//      不是拍脑袋。P1 先例（c6-pickup 3.0 早期 +15pp）同向支持。
//   ② **wStar 项删除**（临时设计里有 wStar 0.5，现删）：star 含在 powerUpsCollected 内
//      （P6 双计数已核）⇒ star 实得 = **3.0 ≡ 恰好 1 kill**，P3 不变量成立，
//      且满足天花板（star 实得 ≤3.0，P2）。单留一个拾取价 = 普通道具与 star 同价 3.0 ——
//      star 的溢价不在单价里，在它下游的火力→吞吐里（机制定价，不是单价定价）。
//      公式里不留死项（wStar: 0 的写法删了，干净）。
//   ③ **天花板**：拾取单价 ≤3.0（= 1 kill）。c6-pickup3 的 star 实得 4.0 长期退化
//      （P2，虽混杂但方向是警告）。**任何加价提议必须先过离线判决**（L5）。
// 拾取项均值 +3.1/局（1.02×3.0）vs kill+win 17.0 —— 明确偏好，不喧宾夺主；
//   spawn 上限卡着，刷不出来（P1 的 3.0 也没刷出来过）。
// 不动项（P5 红线 + x3 §2 纪律）：wDmg/wShot/wExplore/wKill/wHit/wWin/terminal **一个数不许动**；
//   不上 wChip/wTick/wStuck（c 线词干一个不抄）；不上分列 killsByKind（L7）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 起点与缰绳
// ══════════════════════════════════════════════════════════════════════════
// `bc = x4-rebirth 的择优终点` = `nn-training/weights/x4-rebirth-best/x4-rebirth.it45.best.json`
//   （已存在：379KB、`nn-weights-json`、`schema_major 3`、42 键，2026-09-18 已核；
//   x4 池外五选一的终点，见 x4-rebirth.jsonc 结算节）。
//   选取规则沿用唯一规则：按 §5 最优归档保护选出的 `-best`（**不是最大 it**）。
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求，全链一致；M3：预期贡献 ≈0，
//   热启 = 权重初始化，首轮 KL 暴涨先查 bc 载入）。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 判据（预注册；分母已填实 —— x4-best 实测，§前置②已执行）
// ══════════════════════════════════════════════════════════════════════════
// **主判据 = 承伤（主端点）+ 耗时（约束）+ 拾取（升级资格约束）**。
// 主端点：**`dmg/局`** —— 分母 = x4-best 在 c05 的贪心 it0 **154.8** ⇒
//   达标线 **≤123.8（−20%）且 CI 不与 it0 重叠**。
// 约束①（耗时）：通关 ticks 分母 **1781** ⇒ **≤1959（+10%）**，x3 CI 口径照抄。
// 约束②（掉队，单关版）：终点 pass 相对 it0（**64.0%**）退步 >5pp 即掉队（护栏）。
// 约束③（拾取升级资格 —— 用户命题的操作化）：终点贪心口径 **pu/局 ≥ 2×it0**
//   （分母 **0.71** ⇒ **≥1.42**）**且 stars/局显著 > it0**（stars 稀有，看方向 + CI 下界，
//   不卡倍数）⇒ 否则即使承伤达标，
//   最高只判**部分达成**（评语写"靠别的能力赢的，升 c06 必崩" —— x2/x3 power 故事的重演防范）。
//   ⚠ 为什么拾取是约束不是主端点：主端点必须是任务本身（承伤/通关）；拾取是机制，
//   机制只能决定"能不能升级"，不能决定"本腿成败" —— 与 L12（power 不作判据）同构。
// 判线（与 x3 同表，+ 约束③）：达成（↓≥20% + 三约束全守住）/ 部分达成（含"拾取没动"）/
//   判负（≤5% 或反向 / 耗时恶化）。统计量纪律不变（CI，否则不进结算）。
// **最优归档保护 + M2**：每 eval 点记 dmg，最优另存；终点经池外段三选一
//  （日常最优 / pass 最优 / 末 it；互不可区分取 pass 高者）。
// **统计口径（写死）**：
//   · 判决只在**池外固定段**：`--seed0 402000`（**800 局单关**，402000–402799）。
//     **seed 台账**（2026-09-18 查，402xxx 全空）：
//       | 400800–400999 | x3-rebirth（池外判决段）                     |
//       | 401000–401999 | x4-rebirth（预注册）                         |
//       | **402000–402999** | **空闲 ⇒ 本腿预注册**                  |
//     **中途不得改段**（L3）。复验同 x4。
//   · 配套锚点 200 局同种子配对（seed0 860001；日常 eval 即此口径）；pd 灰度带不变。
//   · **日常单关 200 局只做趋势，绝不判决**。
// **对照**：**it0 vs 终点**（同环境、同种子、同权重族）。god 参照：48.5% / 173.7 / 1650
//   （起点已超教师，无需再比；如实记录）。
// **诊断项**：零伤占比（分母 21.9%）、killerKinds 死因结构（分母：power 26 / basic 20 /
//   armor 16 / fast 13 —— 死因分散，诊断看"是否收敛到某一种"，不看 power 本身）、
//   entropy、a_move 熵。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 止损（与 x4 同格；+ 拾取熔断）
// ══════════════════════════════════════════════════════════════════════════
//   · **机械横盘**：连续 **20 轮（= 4 个 eval 点）**承伤无改善（|Δ|<5%）或漂移形态 ⇒ 停腿。
//   · **宽限一次 +10 轮**（§68 先例；到期必停）。
//   · **it35 检点**（第 7 个 eval 点）：承伤无改善 ⇒ 停腿，判「热启 + 位移包 + 拾取项在
//     5 敌关不足以改写行为」，下一手是朝向 metric（独立工程）或停在 c04。
//     ⚠ it35 的第二读数是拾取：pu/局相对 it0 不升反降 ⇒ 即使承伤动了也只给宽限不给过。
//   · **熔断轴**：shots/kills 熔断与 x4 同口径（分母换 it0）＋ **pu 熔断**：
//     日常 pu/局掉到 **< 0.5×it0** ⇒ 拾取被吓没了（通常是 wDmg 被误动或 KL 暴走）⇒ **立即停**。
//   · **分支二（换血）**：dmg↑ 而 wr 不降 ⇒ 学错了，改奖励结构，不加轮数。
//   · **分支四（自锁）**：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3）。
//   · ⚠ **优先级：横盘 / it35 > 拾取约束③ > 一切诊断**。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 预算与采样规模
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（总量不动，PPO batch 守恒；单关 per-stage = 24000）。
// · `est_samples_per_game` = **175**（§2 口径 R 实测 175.9，向下取整）。
//   首轮 ≈ 24000/175 ≈ **137 局**。宁大勿小（§x4 §7 同理；trailing 5 轮自动纠偏）。
// · `seed_rotate` = **600**（单关回退值；volume 不参与）。
// · `iters 0`（无限轮；**由用户拍板**，M1）＋ `max_hours 12`。
//   wall-clock 估算：111 局/轮 × 局均 2145 ticks ≈ x4 量级（x4：133 局 × 1790）⇒
//   单轮 ~2 分钟，12 小时 ≈ 300+ 轮。刹车靠 §6 + 人工停。
// · 不开双 run（单 run 声明，L4）。`workers 8` / `ent_break 0.25` 其余全链一致不动。
// · **位移包不动**（`lr 0.0005 / epochs 6 / kl 0.02→0.01 / kl_cap 0.2 双段 / until 25`；
//   有限轮 N 则 until 取 min(25, 40%×N））。
//
// ══════════════════════════════════════════════════════════════════════════
// 8. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. ~~bc 不存在~~（已解决：x4 已结课，bc 见 §4；本文件提前设计时是占位）。
// 2. ~~剂量 provisional~~（已解决：§前置②b 的门 fired，新语料上 wPickup 2.5 不过门，
//    已按门调整为 3.0 ＋ 删 wStar 项；**现为最终值**，开训后改剂量 = 新腿）。
// 3. **双变量**（环境 + 配方）—— 归因到包，用户拍板；与 x4 直接对照禁止。
// 4. **单 run**（<2pp 非效应）。
// 5. c-line 对拾取的长期证据是阴性的（P2）—— 本腿是 rebirth 线对拾取的第一次干净检验
//    （单变量插入 + 它自己的 it0 对照）；若判负，合法措辞含「梯度无方向」，不得写「拾取无效」（L3）。
// 6. `est` 只是首轮兜底（trailing 5 轮覆盖）。

{
  "version": 5,
  "name": "x5-rebirth",
  // 每一决策步一条样本（非 intent/goal 窗口），与当前 PPO 管线匹配。
  "mode": "per-tick",
  // 环境唯一来源 = levels/ladder-c05.jsonc（单关，5 敌 a+b+c+d+a、队列 20 取前 5、
  // max_ticks 3900、lives 1、hard）。课程侧禁止重写
  // stages/difficulty/max_ticks/player（任务侧不可动，AGENTS §0.2）。
  "level": "ladder-c05",

  // ── 奖励：a2-v4 + 拾取一项（见 §3；除 wPickup 外逐字不动）─────────────────
  "reward": {
    // ⚠ 用**标量 kills**（L7）。拾取项缀尾。wStar 项已删（§3②：star 实得 3.0 ≡ 1 kill，
    //   公式里不留死项；starsCollected 仍被记录，只是不进公式）。
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      // P5 红线：一个数不许动。
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      // 最终剂量（§前置②b 的门 fired 后按门调整：2.5 → 3.0；wStar 项删除）。
      // 拾取项均值 +3.1/局 vs kill+win 17.0（§2 新语料）。
      "wPickup": 3.0,
    },
    "terminal": {
      "lives_exhausted": -5.0,
      "timeout": -2.0,
    },
    "scheme": "toy",
  },
  "dodge": "",

  // ── 起点：x4-rebirth 的择优终点 it45（见 §4；42 键已核）──────────────────
  "bc": "nn-training/weights/x4-rebirth-best/x4-rebirth.it45.best.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §7；iters 由用户拍板，默认 0）────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：全链一致不动（见 §7）─────────────────────────────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段 it1–25（iters=0 时；有限轮 N 则 until 取 min(25, 40%×N）。
    // kl_cap 两段显式（x2 教训）。
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段 it26+：kl 放松 + lr 砍半。
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 },
  ],

  // ── 采集：总量 24000/轮；单关 per-stage = 24000 ───────────────────────────
  "target_transitions": 24000,
  // 175 = 口径 R 实测 175.9 向下取整（§2 最终语料；之后 trailing 5 轮覆盖）。
  "est_samples_per_game": 175,
  // 单关回退值；volume 模式不参与。
  "seed_rotate": 600,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x5-rebirth/weights.json",
  "traj": "tmp/x5-rebirth",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x5-rebirth",
  "backup_prefix": "x5-rebirth",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 单关 2000。日常单关 **200 局/次**（只做趋势）；判决走池外段（§5：402000，800 局）。
  "eval_stages": "2000-2000",
  "eval_games_per_stage": 200,
  "eval_every": 5,
}

// **启用 `target_transitions`（动态采集）** —— rebirth 线统一。

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① **bc 已填实** = `nn-training/weights/x4-rebirth-best/x4-rebirth.it45.best.json`
//    （x4 池外五选一终点；379KB、`schema_major 3`、42 键，2026-09-18 已核）。
// ② **it0 重跑（已执行，2026-09-18）**：
//    a) 三件套 = §2 最终数（rollout-it45 ＋ it0-x4.jsonl ＋ god.jsonl 复用；god 与权重无关）；
//    b) **剂量复核门 fired**：新语料上 wPickup=2.5 只给捡超出 +0.95（< 门限 +1.5），
//       ⇒ 剂量已按门调整为 **wPickup 3.0（超出 +1.59 ✓）＋ 删除 wStar 项**
//       （star 实得 3.0 ≡ 1 kill，天花板守住）。**门已过，剂量已是最终值**，
//       开训后任何剂量改动 = 新腿（§15.5），不许热改。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`（x3 前置⑤事故）。
// ⑥ 确认 a95/a97 在线。
//
// 启动：
//   启动走**控制台开课**：`bun run dashboard` → http://127.0.0.1:8900 → 选本课 → 开课。
//   （裸 CLI 启动已下线：绕过控制台 = 漏开课标记/暂停解禁/hub 模式三件套 —— plan/accident.plan.md §1；agent 不得启动训练）
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 起跑线 / 边际 / EV 表：`tmp/x5-rebirth-probe.py`（WEIGHTS 换 x4-best 即 §②a 的 rollout 部分）
// · 贪心 + god：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c05.jsonc \
//     --games 200 --seed0 860001`（+ `--policy god` 即参照臂）
// · 判决（池外固定段）：`... --games 800 --seed0 402000`
// · 走势 / 动作分布 / 时间账：`tmp/x1-trend.py` / `tmp/x1-acthist2.py` / `tmp/a2-timing.py`
//
// ── 结算节（2026-09-18 结课回填；63 轮，用户人工停）───────────────────────
// **判决 = 部分达成**（承伤中间带 + 约束①②守住，但**约束③失败 ⇒ 封顶**；单 run）。
// 运行实际：it1–63。it21 横盘黄灯（字面触发，但 a2 爬升窗未到＋机器健康 ⇒ 继续，
//   该判断事后被证伪：爬升窗从未来过 —— 复盘见⑦）；it35 检点通过（dmg −11.4% 日常口径）；
//   it45–60 横盘确认；63 轮停（无二次宽限问题，停得比条款还晚 3 轮，属惯性）。
//
// ① **it0 vs 终点 it35**（池外固定段 seed0 402000，800 局单关，同种子配对）：
//   | | it0（bc） | it35 | Δ |
//   | pass | 437/800 = 54.63% | 505/800 = **63.12%** | McNemar b01=201/b10=133，pd=**+8.50pp**，exact p=0.0002 |
//   | dmg/局 | 161.2 [155.4,167.0] | **141.9 [135.5,148.0]** | 配对 Δ=**−19.3**（SE 3.9，z=−5.00），**−12.0%，CI 不重叠** ⇒ 中间带（判线 ↓5–20%），不是达成 |
//   | 通关 ticks | 1833 [1791,1875] | **1869 [1827,1911]** | +2.0%，CI 大幅重叠 ⇒ 约束①守住 |
//   kills 3.61 → 3.90。末 it63（148.0 / 60.50%）与 it35 互不可区分
//   （dmg z=+1.53，pass p=0.25）⇒ 按三选一规则取 pass 高者 it35。
//   ⚠ 池外 bc（161.2）比日常 it0（154.8）高 6.4 —— 段间差异，判决以池外为准（预注册的意义）。
// ② **锚点配对 pd**：+8.50pp（b01=201/b10=133），超灰度带。日常 it60 的 −4.5pp 回落
//   在池外未复现（it63 +5.88pp，p=0.0097）—— M2 第四次应验（日常回落也是锚点幻象）。
// ③ 掉队：单关；终点 pass +8.5pp，护栏（−5pp）远未触发。
// ④ **约束③专项（用户命题的判决点）**：pu/局 0.61 → **0.74**（约束线 ≥1.22，**失败**）；
//   stars 稀有事件无方向性变化。⇒ **即使承伤达标也只能部分达成**，评语预写兑现：
//   "靠别的能力赢的（dmg −12% 主要来自走位/开火效率），升 c06 必崩"。
//   killerKinds（池外）：bc 死因分散（power 26/basic 20/armor 16/fast 13 是 200 局口径；
//   800 局池外段结构一致，分散未收敛）—— 死因从未收敛到某一种，M5 诊断维持"分散"结论。
//   零伤占比 24.9% → 32.0%。
// ⑤ 横盘：it45–60 日常窗 −4.2% 触发；池外验证 it35/it63 互不可区分 ⇒ 纯粹横盘。
//   形态史：it35 dmg 137.1（日常）→ it63 142.3 —— 28 轮零改善（"it35 见底"，x2/x4 翻版）。
// ⑥ 样本量：（训练日志 collected 均值 ≈24000 档，浪费个位数；略）entropy 全程 0.59–0.73
//   上升不坍缩，KL ~0.02 —— 机器健康，信号到顶（与 x4 it46 结论同构）。
// ⑦ **复盘（it21 判断的自我纠正）**：it21 不停的理由是"a2 爬升窗 it26–32 未到 + entropy 上升"。
//   事后看：爬升窗从未来过，entropy 上升了 40 轮行为没跟。⇒ 新纪律：
//   **entropy 升只能证伪"坍缩"，不能证明"在学"**（证"在学"要看 rollout wr / eval 的
//   单调分量，不是探索量）。it35 检点是最后刹车，本腿它恰好通过（−11.4%），
//   所以多花的 28 轮是"按条款该花"，不是 L2 式违规 —— 但 it21 报告里的推理有一半是错的，如实记。
//
// ── 离线分析（2026-09-18，结课后；零新训练，语料见 `tmp/x5-rebirth-probe/`）──
// ⑧ **排除项（别往这三处查了）**：① 管线通（`powerUpsCollected` 进 shard idx7；
//    训练启动晚于文件定稿 2 分钟 ⇒ wPickup 3.0 实跑 63 轮）；② 可观测通（obs 有专用
//    powerup 通道：位置 + 15 类型 + 剩余时间，`obs-encoder.ts:332`）；
//    ③ 不是全局梯度死亡（E1 同种子配对 12 局：it35 dmg −39% vs bc，pu +0.08 上下对称 ⇒
//    **承伤学会了，拾取零位移** —— 拾取特异性不动，纠正"整个策略冻住"的初判）。
// ⑨ **Star 是供给问题（实锤）**：it64 shard 上 star spawn **0.085/局** vs collected 0.07 ⇒
//    拾取率 ~82%。看见的星基本全捡，是根本没星可捡（star 占 spawn 7%）。
//    ⇒ 用户命题的"星"半句在 c05 无处着力；约束③的 stars 半句删除（供给撑不起任何判决）。
// ⑩ **E② 轨迹分析结论（it64 shard obs 全轨迹；脚本 `will*.py`，事件 `will.json`）**：
//   ① **意愿半径 cliff**：出生距离 0–4 格拾取率 23/31 = **74%**，5–9 格 1/15 = **7%**，
//      10 格+ 0/6 —— 5 格是断崖（长可见 tracks，n=66；短遭遇只会抬高近端率，不改变断崖）。
//   ② **走到脸上都不吃**：过期（未捡）tracks 生前最近距离中位 **2.5 格**（27/33 ≤4 格，
//      平均可见 46 步）⇒ 零绕路，连 1–2 格都不绕 —— 不是"太远去不了"（空场无墙），
//      是"朝道具走"这个行为维度不存在。
//   ③ **掉落机制解释一半**：掉落刷在击杀点（`SimulationCombat.ts:685`，战斗路径上）⇒
//      近拾取多为"回头顺手"，远拾取才需专程 —— 而专程发生率 ≈0。
//   ④ **截胡假说**（最可能，见⑮④干预实验判别）：撞见拾取的那些步本身就是最优战斗走位 ⇒
//      +3.0 加固的是战斗（dmg 同种子 −39%！），不是拾取 —— 钱发了，事（拾取行为）没办，
//      因为"朝道具走"在策略动作分布里不存在，稀疏单步奖励无处附着。
//   ⑤ 方法学备注：obs/metric 差一行（off-by-one；e4/e6 初版数字作废，以 will/e10 为准）；
//      **BONUS 窗口**（清场后 ~600 tick 无敌自由捡，`SimulationEffects.ts:246`）与战前拾取
//      是两种技能，metric 不分账 ⇒ 约束③又错一处；`puGot*` 全零是输出侧 bug，别引用。
// ⑪ **约束③是作者写错的不可能条款（认）**：供给上限 ~1.2–1.6/局（bonus/elite/10杀/
//    分数里程硬卡，`SimulationCombat.ts:685`），2×弱起点均值 = 1.42 要求近乎全清+全捡。
//    即使拾取率 100% 也大概率过不了**均值线**。⇒ 新纪律：**机制约束线必须先算供给上限**，
//    不能拿弱起点的倍数拍（与"权重必须用真实语料标定"同级）。
// ⑫ **Value 侧存疑（未定论）**：value loss 0.40（x3 是 0.22）偏高；V0 相关性探针因读错
//    value.npy 元素作废（头内方差 0.4–0.9，头没死）。不断言，等 E②/后续。
// ⑬ **用户 doctrine（2026-09-18 立项级）**：除 fence 外所有道具（bomb/freeze/tank/shield/star）
//    在保证安全前提下都是通关正收益 ⇒ **不按道具差别定价**（DECISIONS 791 早有同结论），
//    当前公式的 wPickup 统一定价正是该 doctrine 的形状；fence 是否计入 metric 见⑭。
// ⑭ **fence 核查（已查，结论）**：fence 是 PowerUpType 之一，走同一 `powerup_collected`
//   事件 ⇒ **会计入 `powerUpsCollected`，+3.0 照发**。E② 定型样本里 fence 占 4/27
//   （有偏样本，仅定性不断量）。⇒ reward hack 矢量存在但量级小（fence 只占 spawn 一小部分）；
//   修法 = 分列 collected-by-kind（新 metric + bump 版本 ⇒ 历史语料作废）= 独立工程，
//   **不开**（doctrine 的"fence 除外"在价格上无法低成本实现，接受残差；fence 本身也非致命）。
// ⑮ **用户 doctrine 落实（2026-09-18，课程设计级）**：除 fence 外所有道具
//   （bomb/freeze/tank/shield/star）在保证安全前提下都是通关正收益 ⇒
//   ① **不按道具差别定价**（现状 wPickup 统一定价正是该形状，保留；DECISIONS 791 同结论）；
//   ② 约束③重写（供给上限先行；stars 半句删除；战前/窗口不分账则约束只看 pu 总量）；
//   ③ "保证安全"由现有的 −wDmg 项承担（绕路挨打自动扣钱），不另设安全项 ——
//   若将来发现自杀式捡（dmg↑而 pu↑），按分支二处理，不动公式。
//   ④ 下一步实验（未做）：拾取可达性干预 —— 定制关把道具刷在远/近处，测绕路意愿，
//   直接回答"看不见规划 vs 看见了不去"（E② 的升级版，约半天工作量）。
