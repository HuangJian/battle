// 课程 x4-rebirth —— 4 敌关（ladder-c04）的**热启迁移**腿（rebirth 阶梯第四级）
//
// 状态：**已结课（2026-09-18，~60 轮，用户人工停；宽限一次用完）—— 判决 = 达成**。
//   池外固定段（seed0 401000，800 局）：承伤 158.3 → 101.1（**−36.1%，z=−14.8**），
//   通关 ticks 1798 → 1573（**−12.5%，约束守住**），pass 53.4% → 80.4%（+27pp，
//   起步落后教师 11.5pp → 终点反超 16pp）。终点 = it45（五选一：dmg 并列组中 pass 最高），
//   已另存 `nn-training/weights/x4-rebirth-best/x4-rebirth.it45.best.json`。
//   详见文件尾「结算节（2026-09-18 结课回填）」。agent 不得启动训练。
//
// 前置假设：x3-rebirth 已达成（承伤 −45.5%、耗时 −8.6%、无掉队，终点 it153）。
// 本腿把它的终点权重热启到 ladder-c04（4 敌 / 单关 abcd），回答三个问题：
//   ① 迁移链（c01 → c02 → c03 → c04）能不能继续守住「承伤 + 耗时」？
//      —— 本腿起步是**全链第一次在贪心口径落后教师**（52.5% vs 64.0%，见 §2），
//      起点比 x2（81% > 教师）/ x3（72% ≈ 教师）都弱，问题比前两腿更难。
//   ② 单关 abcd = basic+fast+power+armor **全套同时上场**（队列 20 取前 4，见 §0），
//      power 盲区（x2/x3 两腿都没修好）在四敌合围下会不会变成致命伤？
//   ③ 每条腿都要回答的：多敌关到底需不需要一个真正的「朝敌」信号（a2 open limitation #2）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（levels/ladder-c04.jsonc 实读，勿信记忆）
// ══════════════════════════════════════════════════════════════════════════
//   **单关**：ladder-c04-abcd，forces `abcdabcdabcdabcdabcd`（20 长），
//   `count 4` ⇒ 队列 20 取前 4 = **a+b+c+d 各一**（每局固定全套，无变体）。
//   max_ticks **3300**、lives 1、hard、空场 13×13。
//   自定义关 ID **2000-2000**（单关沿用，`rl/ladder_factory.py:228 eval_stages_for`）。
//   任务侧一个字不动（AGENTS §0.2）—— 只动训练侧。
//   ⚠ **单关的结构后果**（本腿与 c01–c03 三腿最大的不同）：
//     ① 没有「分关掉队」可探测 —— 掉队规则退化为单关相对 it0（见 §5）；
//     ② `seed_rotate` 回到单关回退值 600（600/1；volume 模式下不参与）；
//     ③ `target_transitions` 总量不变 ⇒ per-stage 配额 = 24000（见 §7）。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. ★ x1→x3 教训（本节只收**增量**；a2/x2/x3 的 L1–L13 见 x3-rebirth.jsonc §1，不复述）
// ══════════════════════════════════════════════════════════════════════════
// · **M1（x3-rebirth，走势形态）**：x2 是「it5 见底、后 20 轮浪费」，x3 是
//   「持续爬升 ~120 轮」（dmg 115→47、pass 0.72→0.94）⇒ 「有效窗口只有前几轮」
//   **不是定律**，是 x2 的个例。⇒ 本腿 iters 不预设 40 上限（沿用 x3 的 `iters 0`，
//   用户拍板），刹车只靠 §6 条款 + 人工停。
// · **M2（x3-rebirth，口径纪律）**：日常锚点 it135–150 dmg 斜率 +1.21%/轮，
//   但池外段 it153 ≈ it120（Δ=+1.0，SE 3.2，z=+0.32）⇒ 日常漂移**未复现**，
//   属锚点特异波动。⇒ 日常只做趋势（老纪律）+ **终点必须经池外段三选一**（新纪律，见 §5）。
// · **M3（x3-rebirth，缰绳实测）**：kickstart 日志值全程 ≈0（缰绳实际没勒上），
//   迁移靠的是权重初始化本身。⇒ 本腿 `kickstart_ref` 照开（先例一致），但**预期贡献 ≈0**；
//   若首轮 KL 暴涨，先查 bc 载入（§6 分支四），不要先怪缰绳。
// · **M4（x3-rebirth，节点）**：a97 全程在线 + mac ~110 局/轮 ⇒ 远端分担约 28%
//   （x2 的 3.8% ⇒ L13 部分落地）。⇒ 本腿开训前照样先确认远端节点（§前置⑥），
//   但不再是"零代码 2×"（已有 28%，再优化天花板只剩 ~3×）。
// · **M5（x2/x3，power 诊断的定论）**：两腿先打 power 恒 ≈0（x2 是 0.0%，x3 是 0.5%），
//   但 ac 照样 30%→96%、acd 照样 70%→87.5% ⇒ **「先打 power」与通关脱钩**。
//   且本腿 it0 实测 god 的先打 power 也是 **0**（103 basic / 95 fast，见 §2）——
//   **教师自己都不先打 power**。⇒ 本腿继续不把它当判据（诊断都不重点看，只留 killerKinds）。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 实测（2026-09-18，bc = x3-rebirth.it153.best；三口径，种子对齐处注明）
// ══════════════════════════════════════════════════════════════════════════
// 口径 R —— **训练内 rollout**（采样，40 局 seed 0–39；`tmp/x4-rebirth-probe/`）：
//     **40/40 = 100% 通关** ｜ dmg **191.8/局** ｜ ticks 1790 ｜ steps **180.6**
//     ｜ kills 4.00 ｜ hits 9.82 ｜ shots 33.7 ｜ cells 79.0 ｜ 命中率 **29.2%**
//     ⇒ `est_samples_per_game` 取 **180**（180.6 向下取整；见 §7）。
// 口径 G —— **贪心 argmax**（200 局，seed0 860001；`it0.jsonl`，判据分母）：
//     pass **105/200 = 52.5%** ｜ dmg **149.1/局** ｜ ticks_win **1803**
//     ｜ kills 2.73 ｜ shots 22.5 ｜ 零伤通关 **28.6%** ｜ gameover 95
//     ｜ firstHitKind basic 199 / fast 1 / **power 0**
//     ｜ killerKinds power 59 / fast 19 / armor 19 / basic 1（**死因 62% 是 power**）
// 参照臂 —— **god**（同种子 200 局；**只作参照，不作门** —— AGENTS §0.2）：
//     pass **128/200 = 64.0%** ｜ dmg **149.1/局**（与 bc 到小数都打平，巧合）
//     ｜ ticks_win **1395** ｜ kills 2.98 ｜ shots 14.4 ｜ 零伤 24.2%
//     ｜ firstHitKind basic 103 / fast 95 / **power 0** ｜ killerKinds power 28 / basic 26
// ⇒ **四条读数**：
//   ① **全链第一次起跑落后教师**（52.5% vs 64.0%，−11.5pp）—— x2 起步超教师 7pp，
//      x3 起步 72% 打平（教师口径不同，约 74%）。c04 更难是事实，不恐慌：口径 R 仍是
//      100% 通关 ⇒ 能力在分布内，是贪心 argmax 把一部分弄丢了（x2-ac 70pp 分歧的放大版）。
//   ② **承伤端点没输**：dmg 打平 149.1，零伤占比还略优（28.6% vs 24.2%）⇒ 主端点起跑线不亏。
//   ③ **耗时落后**（1803 vs 1395，+29%）：bc 在四敌关打得犹豫。但耗时只是约束不是主端点，
//      且约束的分母就是 it0 自己（§5）⇒ 不构成开训障碍。
//   ④ **开火多 56%**（22.5 vs 14.4）但命中率 35.7%（1602/4492；教师 57.9%）⇒
//      单发期望收益 0.357×0.6 = **0.214 > 成本 0.05** ⇒ wShot 不抑制有效开火（见 §3）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 奖励：与 a2 / x2 / x3 **逐字一致**（整条 rebirth 线第四次不换配方）
// ══════════════════════════════════════════════════════════════════════════
//   `wKill 3.0 / wHit 0.6 / wWin 2.0 / wDmg 0.03 / wShot 0.05 / wExplore 0.2`
//   terminal `lives_exhausted −5.0 / timeout −2.0`，scheme `toy`。
// 为什么一字不改（四条，全部由 §2 的 it0 实测支撑）：
//   ① **EV 排序正确且 margin 是全链最宽**：a2-v4 剂量下
//      走 **+34.21** > 坐 +22.73 > 莽 +22.37（走−坐 = **+11.5**；x3 是 +8.3，a2 是 +1.2）。
//      剂量网格（x2a–x2d 四臂同跑）里走始终最优 ⇒ 没有重标方向。
//   ② **边际比继续翻号**：「每多走一格」承伤 **−2.27 dmg/格**（sit 235.4 → wander 133.3；
//      LSQ −2.19 交叉确认）—— 起点分布已经是「会打」（走得越多挨得越少），比 x2 的 −0.95
//      还强 ⇒ a2 的剂量在这里**偏紧都不至于**，改它只会牺牲归因。
//   ③ **wShot 自适应**：当前命中率 35.7% 下开火净正（+0.214 vs −0.05）；若训练把命中率
//      打崩，同一公式自动转负开始抑制 ⇒ 不需要手动设开关。熔断轴（§6）看的是 shots 崩，
//      剂量本身不用动。
//   ④ **反面**：x3 线改配方零成功先例（x3-power 判负且方向为负、x3-credit 双 run 判负）；
//      且 c04 是单关新环境，换配方 = 「环境升级 + 配方」双变量，归因直接报废。
// ⚠ 承伤项 `-wDmg*playerDamageTaken` 仍是「0 伤」的载体（`combat.ts:599`）。
// ⚠ **不上 wChip / wPickup / wTick**（roadmap N3 + x3 L8 定案为阴，c04 照样不上）。
// ⚠ 用**标量 kills**（L7：bomb 缺 11.6%，分列会静默丢信用）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 起点与缰绳
// ══════════════════════════════════════════════════════════════════════════
// `bc = x3-rebirth 的择优终点` = `nn-training/weights/x3-rebirth-best/x3-rebirth.it153.best.json`
//   （已存在：379KB、`nn-weights-json`、`schema_major 3`、42 键与 scratch-init 同集，2026-09-18 已核）。
//   选取规则沿用唯一规则：按 §5 最优归档保护选出的 `-best`，不是最大 it
//   （x3 恰好是末 it，但那是三选一选出来的，不是取最大的）。
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求，与全链一致）。
//   ⚠ 预期管理（M3）：x3 实测缰绳贡献 ≈0，本腿预期相同 —— **热启 = 权重初始化**，
//   不是 KL 锚。it1 总锚的纸面值仍是 1.02（1.0×0.5^(it−1) + kl_coef 并列），但别指望它护住什么。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 判据（预注册；与 x3 同表，**分母换成 §2 的 c04-it0 数**）
// ══════════════════════════════════════════════════════════════════════════
// **主判据 = 承伤（主端点）+ 耗时（约束）**（x3 2026-09-18 裁定沿用）。
// 主端点：**`dmg/局`** —— 分母 **149.1**（贪心 it0）⇒ 达标线 **≤119.3（−20%）且 CI 不与 it0 重叠**。
// 约束项：通关局 `avgTicks` —— 分母 **1803** ⇒ **≤1983（+10%）**，口径与 x3 同
//   （点估计计，95% CI 下界超 +10% 提前判负；x3 §4 的写法照抄）。
// 判线（与 x3 逐字同）：达成（↓≥20% + 约束守住）/ 部分达成（↓5–20% 中间带）/
//   判负（≤5% 或反向）/ 耗时恶化 >10% 即判负。**统计量纪律不变**：连续量不带 CI 不进结算。
// **★ 最优归档保护 + M2 新纪律**：每个 eval 点记 dmg，最优立刻另存 `<课>-best/`；
//   **终点必须经池外段三选一**（日常最优、pass 最优、末 it 中选；互不可区分时取 pass 高者，
//   x3 §结算先例）。日常锚点的尾段漂移若池外未复现 ⇒ 判锚点特异波动，不触发宽限。
// **统计口径（写死）**：
//   · 判决只在**池外固定段**：`--seed0 401000`（**800 局单关**，401000–401799；
//     与 x3 同量级功效，dmg CI 半宽预期 ≈±5）。
//     **seed 台账**（增补 §68 的表；2026-09-18 查）：
//       | 400000–400199 | x3-power（含冻结基线）+ x3-chip-k0/k05/k10 |
//       | 400200–400399 | 已用（x3-credit-p6 文件头注明）             |
//       | 400600–400799 | x3-credit-p6 / -r2                          |
//       | 400800–400999 | x3-rebirth（池外判决段）                     |
//       | **401000–401999** | **空闲 ⇒ 本腿预注册**                  |
//     **中途不得改段**（L3）。复验：
//       `grep -rn "seed0 40[0-9][0-9][0-9][0-9]" nn-training/curricula/*.jsonc docs/*.md`
//   · 配套**锚点 200 局同种子配对**（seed0 860001，沿用 x3 体例；日常 eval 即此口径）
//     用于看位移；`pd` 5–10% 灰度带规则不变（L4）。
//   · **日常单关 200 局只做趋势，绝不判决**（单关 200 局 pass SE≈3.5pp，L3）。
// **对照**：**it0 vs 终点**（同环境、同种子、同权重族，唯一差异 = 训练轮数）。
//   不与 x3-start / BC 线对照（另一起点另一配方）；god 只作参照（§2 已跑：64.0% / 149.1 / 1395）。
// **掉队规则（单关版）**：单关无分关可比 ⇒ 退化为**终点 pass 相对 it0（52.5%）退步 >5pp
//   即判掉队**（≈ 一条几乎不可能触发的护栏；触发即说明学崩）。绝对值与 D4 门都不作判据。
// **诊断项（不作判据）**：零伤通关占比（it0 28.6%）、`killerKinds` 的 power 死因占比
//   （it0 62%，看它降不降）、entropy 轨迹、a_move 归一化熵。
//   ⚠ `firstHitKind` **连诊断都不重点看**（M5：god 自己都是 0，先打 power 与通关脱钩）。
//   ⚠ 不用 KL 区间判据（a2 定论）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 止损（自执行条款；窗口按单关 + eval_every 5 重算）
// ══════════════════════════════════════════════════════════════════════════
//   · **机械横盘检测**：连续 **20 轮（= 4 个 eval 点，`eval_every=5`）**承伤无改善
//     （回归斜率 |Δ|<5%），或「承伤回退 + ticks 变快」漂移 ⇒ **立即停腿**并写 verdict「配方到顶」。
//     （20 轮而非 15 轮：单关日常 200 局噪声比 4 关 400 局大，3 点回归太抖；4 点是下限。）
//   · **宽限只给一次，上限 +10 轮**（§68 先例；到期必须停，不得二次宽限）。
//     宽限判读口径与 x3 同（回到 ≤ 最优水位 +5% 且耗时守住 ⇒ 取该点，否则取历史最优）。
//   · **it35 检点**（= 第 7 个 eval 点；网格是 5/10/…/30/35，it20/21 都采不到数）：
//     承伤相对 it0 **无改善（|Δ|<5% 且 pd<5%）** ⇒ 停腿，判「热启 + 位移包在 4 敌关
//     不足以改写行为」，下一手是**加朝向/距离 metric**（独立工程，metrics_version 作废
//     全部历史语料）或停在 c03。朝向 metric 在 c04 的优先级比 c03 更高（四敌合围，
//     半解可能是第一次不够用的地方 —— 但**先跑出 it35 数据再说**，现在谈是早了，a2 教训）。
//   · **熔断轴**：日常贪心口径 `shots` 相对 it0（22.5）崩（< 0.9× 即 <20.2）或
//     `kills` 相对 it0（2.73）不升反降 ⇒ 奖励把开火吓没了 ⇒ **立即停**，回退剂量。
//   · **分支二（换血）**：dmg↑ 而 wr 不降 ⇒ 判「学错了」，改奖励结构，**不加轮数**。
//   · **分支四（自锁）**：entropy 一步坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3：别先怪缰绳）。
//   · ⚠ **优先级：横盘检测 / it35 检点 > 一切诊断项**（含 power 死因）。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 预算与采样规模（用户问项的正面回答）
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（**总量不动**）：PPO 每轮看到的 transition 数决定位移力度，
//   a2/x2/x3 四腿同量 ⇒ 本腿照抄。单关 ⇒ per-stage 配额 = **24000**（是 x3 的 6000/关的 4 倍，
//   但这是总量守恒的自然结果，不是加量；PPO 侧无感知）。
// · `est_samples_per_game` = **180**（§2 口径 R 实测 180.6，向下取整；L9 量纲 = samples）。
//   ⚠ 不要用 x3 的 130 或 a2 的 120 —— 4 敌关局更长（avgTicks 1790，samples ≈ ticks/10 ≈ 180）。
//   估小了首波超采被 trim（白烧 rollout），估大了首波欠采（trailing 5 轮自动纠偏，伤害小）。
//   ⇒ 有疑问时**宁大勿小**，180 已是向下取整（保守侧是 185；取 180 是因为 trailing 会跟上）。
//   首轮局数 ≈ 24000/180 ≈ **133 局**（单关，无需除关数）。
// · `seed_rotate` = **600**（单关回退值；volume 模式不参与，保留只为对齐与回退）。
// · `iters 0`（无限轮，沿用 x3；**由用户拍板**，M1）＋ `max_hours 12` 兜底。
//    wall-clock 估算：133 局/轮 × 局均 1790 ticks，样本量与 x3（~25000/轮）同级 ⇒
//   单轮 ~1.5–2 分钟，12 小时 ≈ 350+ 轮。刹车只靠 §6 + 人工停（M1：这不是推荐配置，
//   是「没人盯」时的妥协；有人盯时 iters 设 20–40）。
// · 不开双 run（GPU 配额）⇒ 结算写明「单 run」（L4）。
// · `workers 8` / `ent_break 0.25` / `mb 512` / `normalize_ret` / `gamma 0.998` / `lam 0.95`
//   —— 全链一致，一字不改。
// · **位移包不动**（`lr 0.0005 / epochs 6 / kl 0.02→0.01 / kl_cap 0.2 双段 / until 25`）：
//   x3 实测 KL max 0.0217 健康、entropy −34% 无坍缩 ⇒ 没有动它的证据。
//   `until_iter 25` 沿用 x3（iters=0 时早段 1–25；若用户改有限轮 N，until 取 min(25, 40%×N））。
//
// ══════════════════════════════════════════════════════════════════════════
// 8. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. **起跑第一次落后教师**（52.5% vs 64.0%）—— 若终点仍超不过教师，本腿仍可判达成
//    （判据是相对 it0 的承伤/耗时，不是教师；§0.2），但要在结算里如实写「没超过教师」。
// 2. **单 run** ⇒ <2pp 的移动不是效应（T6 本底）。
// 3. 单关 ⇒ 没有「acd 例外」式部分达成的写法；掉队规则只剩护栏意义。
// 4. `cellsVisited` 仍是半解 —— c04 四敌合围是它**第一次可能不够用的地方**
//    （密度越高半解越够用，但合围复杂度也最高，两股力反向）；it35 检点见分晓。
// 5. `est_samples_per_game` 只是首轮兜底，之后 trailing 5 轮均值覆盖。
// 6. 本腿**不回答**「从零在 4 敌关能不能学」（热启链）。

{
  "version": 5,
  "name": "x4-rebirth",
  // 每一决策步一条样本（非 intent/goal 窗口），与当前 PPO 管线匹配。
  "mode": "per-tick",
  // 环境唯一来源 = levels/ladder-c04.jsonc（单关 abcd，4 敌、队列 20 取前 4、
  // max_ticks 3300、lives 1、hard）。课程侧禁止重写
  // stages/difficulty/max_ticks/player（任务侧不可动，AGENTS §0.2）。
  "level": "ladder-c04",

  // ── 奖励：与 a2 / x2 / x3 逐字一致（见文件头 §3，四条实测理由）──────────────
  "reward": {
    // ⚠ 用**标量 kills**（L7：bomb 缺 11.6%，分列静默丢信用）。
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      // 不重标的理由见 §3②（起点分布边际 −2.27 dmg/格，走越多挨得越少）。
      "wDmg": 0.03,
      // 35.7% 贪心命中率下单发期望 +0.214 > 成本 0.05，净正（见 §3③）。
      "wShot": 0.05,
      // 半解但够用：EV 走 +34.2 > 坐 +22.7（见 §3①）；c04 是否首次不够用由 it35 检点回答。
      "wExplore": 0.2,
    },
    "terminal": {
      "lives_exhausted": -5.0,
      "timeout": -2.0,
    },
    "scheme": "toy",
  },
  "dodge": "",

  // ── 起点：x3-rebirth 的择优终点 it153（见 §4；42 键已核）──────────────────
  "bc": "nn-training/weights/x3-rebirth-best/x3-rebirth.it153.best.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §7；iters 由用户拍板，默认 0 沿用 x3）────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：与 a2 / x2 / x3 逐字一致（见 §7，不动的理由）──────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段 it1–25（沿用 x3 的修法：iters=0 时 until 写 40 会浪费大位移窗口；
    // 若用户改有限轮 N，until 取 min(25, 40%×N）。kl_cap 两段都显式写（x2 教训）。
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段 it26+：kl 放松 + lr 砍半精修。
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 },
  ],

  // ── 采集：总量 24000/轮（见 §7）；单关 per-stage 配额 = 24000 ─────────────
  "target_transitions": 24000,
  // 首轮兜底 180 = 口径 R 实测 180.6 向下取整（§2；之后 trailing 5 轮覆盖）。
  "est_samples_per_game": 180,
  // 单关回退值；volume 模式不参与（种子流由 (rotate_seed, it, stage, wave) 键控）。
  "seed_rotate": 600,

  // ── 路径：bc 语义变了 ⇒ 必须新目录、禁续跑（§15.5）──────────────────────
  "out": "tmp/x4-rebirth/weights.json",
  "traj": "tmp/x4-rebirth",
  // 熔断线：探索崩掉的护栏，不是正常下降的阈值。
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x4-rebirth",
  "backup_prefix": "x4-rebirth",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 单关 2000。日常单关 **200 局/次**（与 x3 的 400 局/次同量级开销，**只做趋势**）；
  // 判决走池外固定段（§5：seed0 401000，800 局）。
  "eval_stages": "2000-2000",
  "eval_games_per_stage": 200,
  "eval_every": 5,
}

// **启用 `target_transitions`（动态采集）** —— rebirth 线统一：局时长随训练剧烈变化，
//   固定局数会让每轮样本量漂移。

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc = `nn-training/weights/x3-rebirth-best/x3-rebirth.it153.best.json`（已存在，
//    2026-09-18 已核：379KB、`schema_major 3`、42 键）。缺失会响亮报错。
// ② **it0 探针（已跑，2026-09-18，结果见 §2 与 `tmp/x4-rebirth-probe/`）**：
//    a) 贪心 200 局（`it0.jsonl`）：pass **52.5%**、dmg **149.1**、ticks_win **1803**；
//    b) god 同种子 200 局（`god.jsonl`）：pass **64.0%**、dmg **149.1**、ticks_win **1395**；
//    c) rollout 40 局（`rollout-it153` / `report-it153.txt`）：全通关、dmg 191.8、
//       **est 180.6**（已写入配置）、边际 **−2.27 dmg/格**、EV 走>坐>莽。
//    → 判据分母 = a) 的贪心数；est 已按 c) 覆盖。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 若与其它课并行：确认 `rl-config.json` 的 `courses` 块容量（`checkCapacity`）+
//    登记互不重复的 `courses.<课>.slot`（静默挂 hub 事故，见 x3 前置⑤）。
// ⑥ rollout 节点：确认 a95/a97 在线（x3 已有 28% 远端分担，开训前看一眼即可）。
//
// 启动（**用户手工；agent 不得启动训练**）：
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x4-rebirth \
//     --remote-transport pull
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 起跑线 / 边际 / EV 表：`tmp/x4-rebirth-probe.py`（已跑；改 WEIGHTS 可复算任意 it）
// · 分关 + power 读数：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c04.jsonc`
//   （行内含 `firstHitKind` / `killerKinds` / `killsByKind`）
// · 判决（池外固定段）：`bun tools/sim/eval-course-ckpt.ts ... --games 800 --seed0 401000`
// · 走势 / 动作分布 / 时间账：`tmp/x1-trend.py` / `tmp/x1-acthist2.py` / `tmp/a2-timing.py`
//   （改路径；⚠ shard 每轮被清理，动作分布诊断要及时做）
//
// ── 结算节（2026-09-18 结课回填；~60 轮，用户人工停）───────────────────────
// **判决 = 达成**（承伤主端点达标 + 耗时约束守住 + 单关无掉队；单 run，幅度远超本底）。
// 运行实际：it1–60，横盘条款 it45 触发（it30–45 回归 −4.60%，边界压线），
//   **宽限一次 +10 轮**（§68 先例）→ it55 到期必停，无二次宽限。
//   宽限验证了横盘（it45/50/55 池外互不可区分），与 x2 先例同构（宽限救不回来）。
//
// ① **it0 vs 终点 it45**（池外固定段 seed0 401000，800 局单关，同种子配对）：
//   | | it0（bc） | it45 | Δ |
//   | pass | 427/800 = 53.37% | 643/800 = **80.37%** | McNemar b01=187/b10=37，pd=**+27.00pp**，exact p<0.0001 |
//   | dmg/局 | 158.3 [153.0,163.6] | **101.1 [95.1,107.3]** | 配对 Δ=**−57.3**（SE 3.9，z=−14.77；变好 495 / 变差 189 / 持平 116），**−36.1%，CI 不重叠** ⇒ 主端点达标（判线 ↓≥20%） |
//   | 通关 ticks | 1798 [1746,1850] | **1573 [1540,1605]** | **−12.5%，CI 不重叠且方向是改善** ⇒ 约束（≤+10%）守住 |
//   CI = bootstrap 95%（B=10000）。kills 2.77 → 3.55。
// ② **锚点配对 pd**：+27.00pp（b01=187/b10=37），远超 5–10% 灰度带 ⇒ 不在灰度内。
//   日常锚点（860001 段）只做趋势：它曾显示 it15 最优（110.9 vs it45 的 110.6），
//   池外段推翻（it45 显著优于 it15，Δ=−12.9，z=−3.72）—— M2 纪律第三次应验
//   （x3-it153≈it120，x4-it50 的日常 93.8 也是锚点幻象，池外 101.8）。
// ③ **掉队**：单关无分关可比；终点 pass 相对 it0 **+27pp**（护栏线是 −5pp），无掉队。
//   god 对照如实报（64.0% / 149.1 / 1395）：终点三项**全部反超教师**
//   （+16pp / −32% / +12.7%耗时仍慢 178 ticks —— 耗时只赢了自己，没赢教师，如实记录）。
// ④ **诊断项**：零伤通关占比 21.3% → **43.2%**；
//   `firstHitKind`：先打 power **恒 0**（bc/it15/it35/it45 全 800 局里 ≤1 次 —— 第四条腿）；
//   `killerKinds` 的 power 死因占比 57.3% → **66.2%**（绝对数 211 → 104 局：死得少了，
//   但剩下的死里 power 比例更高 —— 不是靠先打 power 赢的，与 x2/x3 同构）。
//   ⚠ it55 首次出现 fast 先打 46/800（5.75%，此前全程 ~2 次）—— 行为在动，但终点是 it45，
//   不展开，留给复盘（日常 shard 已清，无动作分布可查，教训：重要 it 的 acthist 要及时存）。
// ⑤ **横盘检测**：it30–45 回归斜率 −0.258/轮 ⇒ 20 轮累计 **−4.60%**（|Δ|<5% ⇒ 触发，边界）；
//   it25–40 窗是 −7.35%（未触发）—— 趋势单调走平，本次触发非噪声。
//   宽限期（it45–55）池外验证：it45/it50/it55 dmg 互比 |z|<0.8、pass 互比 p>0.6 ⇒
//   **纯粹横盘**，宽限条款正确关闭（无二次宽限）。
//   形态史：it15 dmg 110.9 → it45 110.6（30 轮零改善，"it15 见底"，x2 的"it5 见底"重演）。
// ⑥ **样本量**：`transitions_collected` 均值 **24411** vs 配额 24000 ⇒ **+1.71% 超采**。
//   entropy 0.416 → 0.590（max 0.669，热启展开后缓落，无坍缩）；KL max **0.0343**
//   （全链最高，但远离熔断）；全程无熔断（shots 最低 it40 的 20.2 踩线未破，it45 回到 20.4+）。
//
// **终点选取（五选一）**：池外 dmg：it15 114.0 / it35 115.4 / it45 **101.1** / it50 101.8 / it55 104.6；
//   it45/it50/it55 互不可区分，且三者都显著优于 it15/it35（z≈−3.5）⇒ it15/it35 出局；
//   余下三者取 pass 点估计最高者 ⇒ **it45（80.37%）**（x3 §结算先例；it55 的 80.13% 只差 0.24pp 纯噪声）。
//   已另存 `nn-training/weights/x4-rebirth-best/x4-rebirth.it45.best.json`
//   （379KB、`nn-weights-json`、`schema_major 3`、42 键，已核验）。
//   判决语料：`tmp/x4-rebirth-judge/eval.jsonl`（bc/it15/it35/it45 × 800）
//   ＋ `eval2.jsonl`（it50/it55 × 800）＋ `judge.py` / `judge2.py` / `diag.py`。
//
// **回答立项三问**：① 迁移链 c01→c04 **全线贯通**（起步落后教师 11.5pp → 终点反超 16pp；
//   承伤 −36%、耗时 −12.5%）；② power 盲区四敌合围下**仍没变成致命伤**
//   （pass 照样 +27pp；先打 power 恒 0，死因 power 占比反升 —— 靠别的能力赢的，第四次）；
//   ③ 朝向信号继续后排（半解 + cells 在 c04 照样拿下 −36%）。
// ⚠ 单 run（L4）。终点经池外五选一（M2），非末 it（末 it 是 it60 附近，灰度行为 it55 已现，不取）。
