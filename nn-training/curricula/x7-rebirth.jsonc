// 课程 x7-rebirth —— 7 敌关（ladder-c07）的**热启迁移**腿（rebirth 阶梯第七级）
//
// 状态：**已结课（2026-09-19，211 轮，用户人工停）—— 判决 = 部分达成**。
//   池外固定段（seed0 410000，4 关各 200）：承伤 192.9 → 180.5（−6.4%，CI 不重叠）；
//   pass 30.38% → 46.00%（+15.6pp，p<0.0001）；pu 0.767 → 1.124（约束线 1.58 没过 ⇒
//   封顶部分达成）。回测门三候选全超限（it150 −6.4pp → it180 −9.5pp → it211 −14pp，
//   遗忘随训练单调加重）⇒ 判语加"**旧能力丢失**"。终点 = it150（相对最不坏），
//   已另存 `nn-training/weights/x7-rebirth-best/x7-rebirth.it150.best.json`。
//   详见文件尾「结算节（2026-09-19 结课回填）」。agent 不得启动训练。
//
// 本腿把 x6-rebirth 的终点权重热启到 ladder-c07（7 敌 / **4 出场变体** / **1 命**），
//   回答三个问题：
//   ① 迁移链（…c05 → c06 → c07）能不能在 1 命关上继续？c07 pooled 零样本 34.5% 是
//      x2-start 形状（35.5% 起步可开腿的前例）—— 比 c04 形状难，但合法开局。
//   ② power 盲区在 power 最致命的关上有没有代价？killer power pooled 占 37%，
//      bcda 关（fast 先出）最难（24%）—— 注意：firstHit 已**不能**作盲区读数
//      （出场顺序决定先打谁，200/200 铁证，见 §1；读数换成 killer power 占比/关 +
//      cdab 关〈power 先出〉通关率）。若终点 killer power 不降而 pass 照涨，
//      盲区"无代价"实锤（记下来，比修掉它更重要）。
//   ③ 1 命 ban 换血：本腿是检验"战斗技巧 vs 换血"的干净考场（c20 争议的正面回答）。
//      判据分母全部是 1 命口径，无命数争议。
// ⚠ 本腿**不换配方**（环境是唯一变量；x5 双变量之后，x6 回到干净热启，本腿沿用）。
//   it0-vs-终点对照合法；与 x6 直接对照禁止（另一起点）。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（levels/ladder-c07.jsonc 实读，勿信记忆）
// ══════════════════════════════════════════════════════════════════════════
//   **4 关**：ladder-c07-{abcd,bcda,cdab,dabc}，forces 为 `abcd` 循环的 4 个轮转
//   （成分恒 5/5/5/5；`count 7` ⇒ 队列 20 取前 7，各关先出类型不同：
//   abcd→basic / bcda→fast / cdab→power / dabc→armor）。
//   出场点四角 + 玩家中央（位置对称）＋ 出场间隔 staggered ⇒ **先打谁 = 谁先出生**
//   （顺序决定论，200/200 铁证，见 §1 立项④）。
//   max_ticks **5100**、lives **1**、hard、空场 13×13。
//   自定义关 ID **2000-2003**（4 变体；工厂 `eval_stages_for` 只覆盖 count≤3，
//   c07 多变体是 c02/c03 体例的自定义推广 —— 与工厂不一致处以本文件为准）。
//   任务侧一个字不动（AGENTS §0.2）—— 只动训练侧（变体是"同一任务的不同出场顺序"，
//   不是新任务；预计 pooled 难度上升是诚实定价，不是任务改难）。
//   结构后果：**真·分关掉队**（相对 it0 同关退步 >5pp，见 §4；单关护栏退役）；
//   seed_rotate 150（600/4；volume 不参与）；per-stage 配额 = 24000/4 = 6000。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 立项依据（2026-09-18；c06 transfer probe + c20 争议）
// ══════════════════════════════════════════════════════════════════════════
// · x6-rebirth 结课：部分达成（dmg −9.8% 中间带 + 约束①②守住 + 回测门双过；
//   约束③ 1.294 < 1.80 失败封顶；终点 it45）；c20-3命 83% vs c20-1命 10% 撞出
//   §0.2（1 命任务定义）vs D1/D2（阶梯命数）的矛盾 —— 未裁决，c07 之前先记一笔。
// · **路线裁决（用户拍板）**：不直冲 1 命 c20（9.9% 起步 = x1 形状），爬 c07 先
//   （53.1% 起步 = c04 形状；1 命 ban 换血；power 考场）。c08（2 命 92% 零样本）跳过，
//   c07 结课后重测 c20-1命再定总攻还是续梯。
// · bc = x6-rebirth.it45.best（c07 零样本就是它打的 —— abcd 关 53% 无需二选一；
//   x5a155 跨两级不测，衰减只会更大）。
// · **④ 顺序决定论（用户假设证实，2026-09-18，同种子干预零训练）**：
//   出场队列按 forces 顺序＋出场点轮转＋间隔 staggered（`World.ts:527` /
//   `SimulationSpawn.ts`）⇒ basic 永远先出生单挑玩家 ⇒ 先打 basic 是"打先到的"。
//   干预：同种子把 forces 换成 cbad 开头（角落/间隔全不变）⇒ firstHit power 193/200
//   （对照组 basic 199/200）；且 power 先出更难（pass −10pp，dmg +11%）。
//   ⇒ 九条腿的"power 盲区"至少一半是**出场顺序伪影**，不是偏好。
//   ⇒ 本腿把 ladder-c07 扩成 4 轮转变体（§0；c02/c03 体例；配置级改动，零引擎改动；
//   ledger D11 只认关名 + teacher-probe 只认 max_ticks，加变体全绿）。
//   firstHit 从此**退出盲区读数**（分关先出类型恒定，读出来也是 50/50/50/45 的复读）。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 实测（2026-09-18，bc = x6-rebirth.it45.best；god 同种子 200 局参照）
//    —— 单变体（abcd）旧数已作废（`it0.jsonl`），以下全是 4 变体新数。
// ══════════════════════════════════════════════════════════════════════════
// 口径 R —— **rollout**（采样，4 关各 10 局 seed 0–9，共 160 局；`rollout-it45-v4`）：
//     154/160 = 96.2% 通关 ｜ dmg **296.4/局** ｜ ticks 2366 ｜ steps **238.2**
//     ｜ kills 6.86 ｜ hits 15.75 ｜ shots 44.1 ｜ cells 97.1 ｜ 命中率 35.7%
//     ｜ 捡率 153/160（96%）｜ 边际 **+0.54 dmg/格**（sit 300.6 → wander 331.1；
//       LSQ +0.43 交叉确认；单变体时是 −0.74 —— 变体混合后翻正，dabc 关拖的）
//     ｜ EV 走 +45.69 > 坐 +32.03 > 莽 +30.24（margin +13.7，全链最宽）
//     分关（胜/dmg）：abcd 100%/245.1 ｜ bcda 100%/252.1 ｜ cdab 100%/297.6 ｜
//       **dabc 85%/390.9**（armor 先出最难缠；贪心最难是 bcda，见下）。
//     ⇒ `est_samples_per_game` 取 **238**（238.2 向下取整；见 §6；单变体旧 201 作废）。
// 口径 G —— **贪心**（4 关各 50 局 seed0 860001，共 200 局；`it0-v4.jsonl`，判据分母）：
//     pass **69/200 = 34.5%** ｜ dmg **187.0** ｜ ticks_win **2137**
//     ｜ kills 3.78 ｜ shots 23.9 ｜ pu **0.79** ｜ 零伤 24.6% ｜ gameover 131
//     分关 pass/dmg：abcd **52%**/164.3 ｜ bcda **24%**/197.7 ｜ cdab 36%/179.0 ｜
//       dabc 26%/207.0（**bcda 最难** —— fast 先出；单变体 abcd 的 57% 是 easy 读数）。
//     ｜ firstHitKind basic 52 / fast 52 / power 51 / armor 45（**均匀** ——
//        顺序决定论的终极证据：先打谁 = 谁先出，200/200；分关恒定：各关先出类型 ≈50/50）。
//     ｜ killerKinds power 50 / armor 35 / basic 26 / fast 24（power 仍居首 37%）。
// 参照臂 —— **god**（同种子 200 局；只作参照）：pass **94/200 = 47.0%**
//     （52/48/42/46%）｜ dmg 182.8 ｜ firstHit fast 65/power 59/armor 41/basic 32
//     （教师也被顺序带着走 —— 盲区是机制性的，不是 NN 特有的）。
//     ⇒ 起步 pooled 34.5% vs 教师 47.0%（−12.5pp）；x2-start 前例（35.5% 起步可开腿）覆盖。
// 剂量复核（结论：**不动**）：
//   · Σwhere0(pickupDist)/局 = **747.2** ⇒ wApproach 上限 0.5×23.0/747.2 = **0.0154**，
//     当前 0.008 在限内（余 48%）⇒ shaping 不动。
//   · wPickup 3.0：40/40 局都捡（c07 富供给，边际定价法失效 —— x6 同款情况），
//     +3.0×1.98 = +5.9 vs kill+win 23.0（26%，< 50% 线）⇒ 不动。
//   · wShot：rollout 命中率 38.4% ⇒ 单发 +0.230 > 0.05 ✓ 不动。
//   · wDmg/wExplore：边际 −0.74，EV 走最优 ✓ 不动；wKill/wHit/wWin/terminal 照旧。
//   · 趋近/远离步比 0.98（与 c05/c06 it0 同档 —— 趋近行为尚未出现，符合预期）。
//   ⇒ **奖励逐字沿用 x6**（a2-v4 + wPickup 3.0 + shaping 0.008），
//   wDmg 等六项 + terminal 照旧；不上 wChip/wTick/wStuck/分列（P2/P5/L7/L8 全沿用）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 起点与缰绳
// ══════════════════════════════════════════════════════════════════════════
// `bc = x6-rebirth 的择优终点` = `nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json`
//   （已存在：379KB、`nn-weights-json`、`schema_major 3`、42 键，2026-09-18 已核；
//   c06 池外四选一的终点，见 x6-rebirth.jsonc 结算节）。
//   选取规则沿用唯一规则：最优归档，不是最大 it（§68 血泪）。
// `kickstart_ref = true` + `warmup_iters = 0`（硬要求；M3：预期贡献 ≈0，
//   首轮 KL 暴涨先查 bc 载入）。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 判据（预注册；分母 = §2 贪心 it0）
// ══════════════════════════════════════════════════════════════════════════
// **主判据 = 承伤（主端点）+ 耗时（约束）+ 拾取（升级资格约束）**。
// 主端点：**`dmg/局`**（pooled） —— 分母 **187.0** ⇒ 达标线 **≤149.6（−20%）且 CI 不重叠**。
// 约束①（耗时）：通关 ticks 分母 **2137** ⇒ **≤2351（+10%）**，x3 CI 口径。
// 约束②（**真·分关掉队**）：分关 pass 相对 it0 **同关**比较 ——
//   abcd 52% / bcda 24% / cdab 36% / dabc 26%，任一关退步 >5pp 即判「掉队」，
//   该 it 不得作为终点（L11；绝对值与"最弱关"不作判据，只看相对退步）。
//   ⚠ bcda（24%）是重点盯防关 —— 起步最低，掉队/**爬升**都先看它。
// 约束③（拾取升级资格，供给先行）：
//   终点贪心 pu/局 ≥ min（2×it0 = 1.58，供给可行 2.63×0.9×0.8 = **1.89**）
//   ⇒ **≥1.58**（需 ~60% 拾取率；比 c06 的 1.80 线更易 —— 供给 2.63 vs 2.50）。
//   若连续三腿（c05 不可能，c06 未接，c07 再败）不过线，约束设计本身重审。
//   stars 半句删除；战前/窗口不分账（接受并声明）。
// 判线（全链同表）：达成（↓≥20% + 三约束全守住）/ 部分达成 /
//   判负（≤5% 或反向 / 耗时恶化）。统计量纪律不变（CI，否则不进结算）。
// **最优归档保护 + M2**：每 eval 点记 dmg，最优另存；终点经池外段三选一
//   （日常最优 / pass 最优 / 末 it；互不可区分取 pass 高者）。
// **★ 回测门（配对段，干净）**：终点候选必须在旧段回测不退步 ——
//   c06 段（seed0 405000，800 局）：相对 x6-it45（64.12% / 147.3）pass 退步 ≤5pp
//   且 dmg 恶化 ≤10%（**同种子，可配对** —— x6 判决段复用，种子一致）；
//   c05 段（seed0 403000，800 局）：相对 x5a-it155（68.75% / 141.1）pass 退步 ≤5pp
//   且 dmg 恶化 ≤10%（**同种子，可配对**）。
//   任一超限 ⇒ 该候选**不得为终点**（往次优顺延）；全部超限 ⇒ 判语加"旧能力丢失"，
//   最高部分达成。回测只跑终点候选（日常不跑）。
// **统计口径（写死）**：
//   · 判决只在**池外固定段**：`--seed0 410000`（**800 局 = 4 关各 200**，
//     seeds 410000–410199；2026-09-18 查 410xxx 全空；409000 是 lives1 探针段，
//     禁复用为判决段）。
//     **seed 台账增补**：| 406000–406799 | c07 transfer probe（x6best 零样本，单变体旧段） |
//     | 407000–407799 | c08 transfer probe | | 408000–408799 | c20 transfer probe |
//     | 409000–409799 | c20-lives1 transfer probe | | **410000–410999** | **空闲 ⇒ 本腿预注册** |
//     **中途不得改段**（L3）。
//   · 配套锚点 4 关各 50 局同种子配对（seed0 860001；日常 eval 即此口径）；pd 灰度带不变。
//   · **日常 4×50 局只做趋势，绝不判决**。
// **对照**：**it0 vs 终点**（同环境、同种子、同权重族）。god 参照：47.0% / 182.8（分关见 §2）。
// **诊断项**：零伤占比（分母 24.6%）、killerKinds＋exposure 双列（**分关 power 死因占比必单列** ——
//   立项②的考场读数）、firstHitKind（**退出盲区读数**：分关先出类型恒定，均匀即正常；
//   若某关 firstHit 偏离先出类型 ⇒ 真偏好证据，反而要单列）、entropy、a_move 熵、趋近/远离比。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 止损（与 x6 同格；power 诊断升级）
// ══════════════════════════════════════════════════════════════════════════
//   · **机械横盘**：连续 20 轮（= 4 个 eval 点）承伤无改善（|Δ|<5%，读法 C 优先）
//     或漂移形态 ⇒ 停腿；宽限一次 +10 轮（到期必停）。
//     **纪律补丁（2026-09-19，x7 执行问责）**：连续 **3 个 eval 点**触发横盘
//     即自动停，不等人（L2 制度版 —— 横盘是状态不是事件，等人工就是 x3-start）。
//   · **it35 检点**（第 7 个 eval 点）：承伤无改善（|Δ|<5% 且 pd<5%）⇒ 停腿，
//     判「热启 + 位移包 + shaping 在 7 敌关不足以改写行为」；
//     **pu 检点独立**：pu 相对 it0（0.79）不升 ⇒ 即使承伤动了也只给宽限不给过。
//     下一手：朝向 metric（独立工程）/ 意图层（F3）/ 停在 c06。
//   · **熔断轴**：shots/kills/pu 熔断与 x6 同口径（分母换 it0：shots<0.9×23.9=21.5
//     或 kills<3.78 不升反降 ⇒ 立即停）＋ 趋近 hack 熔断（cells 暴涨而 kills 不升 /
//     zero-kill-frac 翻倍 ⇒ 立即停）。
//   · **分支二（换血）**：dmg↑ 而 wr 不降 ⇒ 学错了，改奖励结构，不加轮数。
//     （1 命关换血 = 死亡，分支二在此是同义反复 —— 写明以免误用。）
//   · **分支四（自锁）**：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3）。
//   · ⚠ **优先级：横盘 / it35 > 拾取约束 > 回测门（结算时） > 一切诊断**（含 power ——
//     power 诊断再重要也不作判据，L12）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 预算与采样规模
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（总量不动，PPO batch 守恒；4 关均分 = 6000 samples/关，
//   正好回到 a2/x3 的 per-stage 配额）。
// · `est_samples_per_game` = **238**（§2 口径 R 实测 238.2，向下取整；trailing 5 轮纠偏）。
//   首轮 ≈ 24000/238 ≈ **101 局**（≈25 局/关）。
// · `seed_rotate` = **150**（600/4，多关分摊；volume 不参与，老模式回退）。
// · `iters 0`（无限轮；**由用户拍板**，M1）＋ `max_hours 12`。
//   wall-clock 估算：101 局/轮 × 局均 2366 ticks ⇒ 单轮 ~2.5 分钟，12 小时 ≈ 280 轮。
//   刹车靠 §5 + 人工停。
// · 不开双 run（单 run 声明，L4）。`workers 8` / `ent_break 0.25` 其余全链一致不动。
// · **位移包不动**（`lr 0.0005 / epochs 6 / kl 0.02→0.01 / kl_cap 0.2 双段 / until 25`；
//   有限轮 N 则 until 取 min(25, 40%×N））。
//
// ══════════════════════════════════════════════════════════════════════════
// 7. 已知局限
// ══════════════════════════════════════════════════════════════════════════
// 1. 起步 pooled 34.5% —— x2-start 前例（35.5%）覆盖可开腿；分关 24–52%，
//    若终点 pooled 超不过 60%，别误读成"失败"（判据是相对 it0，§0.2；但 bcda 关
//    24% 起步，终点若还在 30% 下晃就是真没学会，掉队规则会说话）。
// 2. 单 run（<2pp 非效应）。
// 3. c08 跳过：2 命 92% 零样本；c07 结课后重测 c08/c20-1命再定（路线裁决已定）。
// 4. `est` 只是首轮兜底（trailing 5 轮覆盖）。
// 5. wPickup 边际定价法在富供给关失效（40/40 局都捡；x6 同款）—— 剂量依据是占比健康
//    （+5.9 vs kill+win 23.0，26% < 50% 线），已在 §2 写明。
// 6. §0.2 vs D1/D2 矛盾未裁决（c20 的 1 命/3 命）：c07 本身是 1 命，无此问题；
//    矛盾留到 c20 腿立项时裁（用户拍板，agent 不猜）。

{
  "version": 5,
  "name": "x7-rebirth",
  // 每一决策步一条样本（非 intent/goal 窗口），与当前 PPO 管线匹配。
  "mode": "per-tick",
  // 环境唯一来源 = levels/ladder-c07.jsonc（4 变体 abcd/bcda/cdab/dabc，7 敌、
  // 队列 20 取前 7、max_ticks 5100、lives 1、hard）。课程侧禁止重写
  // stages/difficulty/max_ticks/player（任务侧不可动，AGENTS §0.2）。
  "level": "ladder-c07",

  // ── 奖励：x6 终点配置逐字沿用（见 §2 剂量复核；唯一变量 = 环境）───────────
  "reward": {
    // ⚠ 用**标量 kills**（L7）。
    "formula": "wKill*kills + wHit*enemyHits + wWin*where(clearTick>=0, 1, 0) - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wWin": 2.0,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      // 上限 0.0154（Σwhere0 747.2），冻结 0.008（余 48%）；改剂量 = 新腿。
      "wApproach": 0.008
    },
    "terminal": {
      "lives_exhausted": -5.0,
      "timeout": -2.0
    },
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点：x6-rebirth 的择优终点 it45（42 键已核）───────────────────────────
  "bc": "nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §6；iters 由用户拍板，默认 0）────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：全链一致不动 ─────────────────────────────────────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    // 早段 it1–25（iters=0 时；有限轮 N 则 until 取 min(25, 40%×N)。kl_cap 两段显式。
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    // 尾段 it26+：kl 放松 + lr 砍半。
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 }
  ],

  // ── 采集：总量 24000/轮；4 关均分 = 6000 samples/关 ───────────────────────
  "target_transitions": 24000,
  // 238 = 口径 R 实测 238.2 向下取整（§2；之后 trailing 5 轮覆盖）。
  "est_samples_per_game": 238,
  // 600/4 多关分摊；volume 模式不参与，老模式回退。
  "seed_rotate": 150,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x7-rebirth/weights.json",
  "traj": "tmp/x7-rebirth",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x7-rebirth",
  "backup_prefix": "x7-rebirth",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 4 关：ID 2000-2003（abcd/bcda/cdab/dabc）。日常 50/关（4×50=200 局/次，**只做趋势**）；
  // 判决走池外固定段（§4：410000，4 关各 200）。
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① bc = `nn-training/weights/x6-rebirth-best/x6-rebirth.it45.best.json`（已存在，
//    2026-09-18 已核：379KB、`schema_major 3`、42 键）。缺失会响亮报错。
// ② **it0 探针（已跑，2026-09-18，结果见 §2 与 `tmp/x7-rebirth-probe/`）**：
//    rollout 4 关各 10 局（est 238.2 已写入配置、边际 +0.54、EV margin +13.7 全链最宽）＋
//    贪心/god 各 4 关各 50 局 seed0 860001（pooled 34.5%/187.0/2137 vs 47.0%/182.8 ——
//    起步落后教师 12.5pp，x2-start 前例覆盖；单变体 abcd 旧数 57% 作废）。
//    → 判据分母 = 贪心 pooled + 分关表；est 已覆盖；剂量复核通过（§2，EV 排序干净）。
// ③ transport：**必须显式 `--remote-transport pull`**（L6）。
// ④ 新 out/traj 目录（禁续跑，§15.5）。
// ⑤ 并行先查 `rl-config.json` 容量 + 登记 `slot`（x3 前置⑤事故）。
// ⑥ 确认 a95/a97 在线。
//
// 启动（**用户手工；agent 不得启动训练**）：
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x7-rebirth \
//     --remote-transport pull
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 起跑线 / 边际 / EV 表：`tmp/x7-rebirth-probe.py`（已跑）
// · 贪心 + god：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c07.jsonc \
//     --games 200 --seed0 860001`（+ `--policy god` 即参照臂）
// · 判决（池外固定段）：`... --games 800 --seed0 410000`（4 关各 200，seeds 410000–410199）
// · 回测（终点候选）：`... --course nn-training/levels/ladder-c06.jsonc --games 800 --seed0 405000` /
//   `... --course nn-training/levels/ladder-c05.jsonc --games 800 --seed0 403000`
// · 走势 / 动作分布 / 时间账：`tmp/x1-trend.py` / `tmp/x1-acthist2.py` / `tmp/a2-timing.py`
//
// ── 结算节（2026-09-19 结课回填；211 轮，用户人工停）───────────────────────
// **判决 = 部分达成**（承伤中间带 −6.5% 显著 + 约束①②守住，但**约束③失败 +
// 回测门三候选全超限 ⇒ 双封顶 + "旧能力丢失"**；单 run）。合法措辞：主端点侧 = 中间带；
// 旧能力侧 = 灾难性遗忘进行中（见⑤）；不得写「拾取无效」（pu +47% pooled，机制动了）。
// 运行实际：it1–211。it35 检点通过（dmg 配对 −5.5% 过线 + pu 显著 + kills/pu 全动）；
//   it35–60 横盘确认本可停，实际跑到 211 —— 执行问责：与 x5-approach 同款惯性
//   （无二次宽限问题，是"一次宽限都没触发就一直跑"）。**纪律补丁（本次补写，进 §5）**：
//   连续 3 个 eval 点触发横盘即自动停，不等人（L2 制度版）。
//
// ① **it0 vs 终点 it150**（池外固定段 seed0 410000，4 关各 200，同种子配对）：
//   | | it0（bc） | it150 | Δ |
//   | pass | 243/800 = 30.38% | 368/800 = **46.00%** | McNemar b01=246/b10=121，pd=**+15.62pp**，exact p<0.0001 |
//   | dmg/局 | 192.9 [188.1,197.7] | **180.5 [174.8,186.1]** | 配对 Δ=**−12.5**（SE 3.6，z=−3.43），**−6.5%，CI 不重叠** ⇒ 中间带（判线 ↓≥20% 未到） |
//   | 通关 ticks | 2244 | **2127** | −5.2% ⇒ 约束①（≤2351）守住 |
//   kills 3.52 → 4.70（+34%）。it180（−4.9%，判负档）/ it211（−6.4%，掉队见③）
//   互不可区分（it150 对两者 dmg |z|<0.8、pass p>0.2）⇒ 三选一取主端点最强者 **it150**。
//   ⚠ 池外 bc（30.38%/192.9）比日常 it0（34.5%/187.0）更难 —— 段间差异，判决以池外为准。
// ② **锚点配对 pd**：+15.62pp（p<0.0001），远超灰度带。日常 it210 的 43.2% 回落
//   在池外未复现 —— M2 第六次应验（日常涨跌全是幻象，本腿日常全程 40–48% 噪声带）。
// ③ **分关掉队表（真·分关，首考）**：终点 it150 分关 pass vs it0 同关 ——
//   abcd 43.5% vs 51.0%（**−7.5pp**）｜ bcda 43.5% vs 19.5%（+24.0pp）｜
//   cdab 47.0% vs 31.5%（+15.5pp）｜ dabc 50.0% vs 19.5%（+30.5pp）。
//   abcd −7.5pp 按字面超 5pp 线，但 SE≈5.0pp（z≈−1.5，CI 含 0 及正值）⇒ **不显著**。
//   ⇒ **掉队规则补显著性条款**（x3 CI 纪律的延伸；判据 precedent）：分关退步必须
//   **点估计超 5pp 且 95% CI 不含 0** 才判掉队 —— 否则 200 局/关的噪声会把"收敛"
//   （方差坍缩：32pp 极差 → 6.5pp）误判成"牺牲"。本例：不掉队（是收敛，不是拆东墙）。
//   （对照：it211 的 abcd −12.5pp，z≈−2.5 显著 ⇒ 真掉队；终点若取 it211 即触线。）
//   god 对照（pooled 47.0%，分关 52/48/42/46%）如实记录。
// ④ **约束③专项**：pu/局 0.767 → **1.124**（+46.6%）；约束线 1.58 ⇒ **失败，封顶**。
//   （连续三腿不过线：c05 不可能、c06 未接、c07 1.12 —— 约束设计重审动议维持，
//   见 x6 结算；本腿供给 2.63 下实际拾取率 43%，线要 60%。）
//   killerKinds＋exposure：power 仍居首（pooled 37%）；cdab 关（power 先出）
//   36%→47%（+11pp）—— power 先出关涨了，但 killer power 占比未单独核算，
//   L12 延续（firstHit 均匀即正常，无偏好证据）。
// ⑤ **★ 回测门（三候选全超限；遗忘随训练单调加重 —— 本腿最大发现）**：
//   | 候选 | c06（405000） | c05（403000） |
//   | it150 | pass −6.38pp（p=0.0077 S）/ dmg +4.3% | pass −6.38pp（p=0.0069 S）/ dmg +1.6% |
//   | it180 | pass −9.50pp（p=0.0001 S）/ dmg +7.8% | pass −9.38pp（p=0.0001 S）/ dmg +9.3% |
//   | it211 | pass −14.00pp（p<0.0001 S）/ dmg +15.7% | pass −9.88pp（p<0.0001 S）/ dmg +2.8% |
//   （c06/c05 均为同种子配对；对比 x6 回测门的双双通过。）
//   ⇒ **三候选全超限** ⇒ 按文件判语加"**旧能力丢失**"（两级旧关，双显著，双单调）。
//   形态：c07 pooled +16pp 的每一分，拿 c05/c06 各 −6~−14pp 换的 ——
//   **4 变体训练 = 用旧能力换新变体**，切换成本实测。x5-approach 在 c04 的 −18pp 不是孤例，
//   是链式热启无 replay 的通病（c06 没忘是因为它基本没动 —— "横盘腿不欠债"在本腿反面验证：
//   动了就忘，忘得单调）。
//   终点取相对最不坏的 it150（主端点最强＋回测损伤最小＋掉队不显著），判语已含旧能力丢失。
// ⑥ 零伤占比 21.0% → 20.1%（没动）；趋近/远离比日常口径未动（shaping 在 c07 也没推起来）。
// ⑦ 横盘读法 C：dmg it35–211 全程 −4~−7% 噪声带，最优点 it150 后 60 轮零改善 ⇒ 触发
//   （早该在 it60 触发，执行惯性到 211，见首段问责）。
// ⑧ 每轮样本量（collected vs 24000）：24000 档，per-stage 6000 配额吻合（浪费个位数）。
// ⚠ 单 run（L4）。终点经池外段三选一（M2）＋ 回测门（全超限→判语）。未达标措辞三选一（L3）。
// 终点已另存 `nn-training/weights/x7-rebirth-best/x7-rebirth.it150.best.json`
//   （379KB、`nn-weights-json`、`schema_major 3`、42 键，已核验）。
//   判决语料：`tmp/x7-rebirth-judge/eval.jsonl`（bc/it150/it180/it211 × 800）＋ `judge.py`；
//   回测语料：`back-c06/back-c05/back2-*/back3-*`（it150/it211/it180 × 双段 × 800）＋ `back*.py`。
