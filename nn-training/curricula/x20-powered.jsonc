// 课程 x20-powered —— 20 敌 1 命的**带电开局迁移腿**（snowball 的 fallback ①）
//
// 状态：**2026-09-19 已结课 —— 判负（带电不迁移）**。it1–it94（16:53–19:47，用户手动停）。
//   一句话：带电趋势 94 轮净零（19 个 eval 点全在噪声内摆动），白板迁移四档全 <7.0 门
//   （**起点 it30 自身就只有 6.34**，训练后 it5 6.08 / it50 5.86 / it94 5.77）。
//   判决依据、全部读数与三条执行事故见文件尾「结算节（2026-09-19 回填）」。
//
// 立项（2026-09-19）：snowball it40 判死（5.63 < 7.0，bonus 够不着=不存在）。
//   但带电零样本给出因果证据：同权重（it30）同种子段（860001–860050，配对），
//   仅开局 level 0→1，200 局：pass 12.5%→**20.0%**（+7.5pp，+2.1σ）、
//   kills 6.88→**8.76**（+1.88，远超 ±0.5 量程）、pu 1.67→2.09。
//   ⇒ 力（power possession）是滚雪球的因，拾取行为是果；'acquire power' 本身
//   才是第一缺口。单变量 thesis：**带电开局能否把雪球滚起来并迁移回白板开局**。
//   level 1 = 保守单档（通关组均 6 pu ≈ 1–2 级等效）；无效再开 level 2 腿。
//
// 核心诚实点：**练带电，判白板**。日常趋势看带电（与训练同分布），判决看
//   白板迁移（目标任务本身）。带电涨、白板不涨 = 没迁移 = 判负，不许用带电
//   成绩单充数。
//
// 刻意**不动**的：奖励 = snowball 逐字相同（含里程碑三项 —— 死变量留作对照，
//   删了就是第二变量；若本腿成，下一腿再 ablation）；PPO schedule/batch/gamma
//   全与 x20/snowball 逐字相同；容量不动（用户指令）；c06 回测删除延续。
//
// ══════════════════════════════════════════════════════════════════════════
// 0. 环境（nn-training/levels/ladder-c20-lives1-powered.jsonc；与 lives1 仅两处不同）
// ══════════════════════════════════════════════════════════════════════════
//   与 `ladder-c20-lives1` 的 diff = 6 行：4 关改名＋顶层名＋`player.level 0→1`
//   （grid/forces/count/spawns/difficulty/max_ticks/lives 全逐字相同）。
//   lives 仍 1（§0.2 任务定义不动）。课程侧**不得**声明 player 块（load_course
//   合并规则：关卡已有 player 时课程重复声明 = 配置冲突 raise；level 文件是正道）。
//   max_ticks 12900；seed_rotate 150；per-stage 6000。
//   关卡晋升合规（D11 类比四项）：① DECISIONS 立案（§2026-09-19-x20-powered）；
//   ② fresh out/traj（`tmp/x20-powered/`，§15.5）；③ reward 未变（snowball 逐字，
//   注记）；④ bc 零样本重测（§2 带电 200 局 ＋ 白板 pool 基线 it30 7.5%/6.10 沿用）。
//
// ══════════════════════════════════════════════════════════════════════════
// 1. 起点与配方
// ══════════════════════════════════════════════════════════════════════════
// bc = `nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json`
//   （白板 pool 7.5%/6.10；带电零样本 20.0%/8.76 —— 同一文件，双基线）。
//   `kickstart_ref = true` + `warmup_iters = 0`。
// 奖励 = snowball 逐字沿用（含 wMS8/12/16 = 6/9/12；带电下到达率更高，bonus 首次
//   有机会被领到 —— 之前是"够不着"，现在看看"够着了学不学得会"，见 §3 诊断）。
//
// ══════════════════════════════════════════════════════════════════════════
// 2. it0 基线（已跑，2026-09-19，`tmp/x20-powered-zeroshot.jsonl`）
// ══════════════════════════════════════════════════════════════════════════
// 口径 T —— **趋势分母**（带电日常，4 关各 50 局 seed0 860001，共 200 局）：
//   pass **40/200 = 20.0%** ｜ kills **8.76** ｜ pu 2.09（pu/千tick 1.01，
//   向通关组 1.22 靠拢中）｜ dmg 298.6 ｜ ticks 2071 ｜ timeout 0。
//   分关（胜/杀）：s2000 16%/8.62 ｜ s2001 26%/10.16 ｜ s2002 18%/8.14 ｜ s2003 20%/8.10。
//   ⇒ 里程碑 8 **零样本已过**（8.76），趋势里程碑 **12 → 16 → 20**。
//   ⚠ wired 证明：同权重同种子，白板 12.5%/6.88 vs 带电 20.0%/8.76 —— level:1 生效确凿。
// 口径 R（rollout 采样）：开训后重算 est（带电局长 2071 ticks ⇒ est 取 **200**，
//   首轮 ≈120 局 ≈30/关；trailing 5 轮纠偏）＋ EV 三档重验（配方未变，预测不变，实测为准）。
// 白板对照（沿用，不重跑）：it30 池外 412000：7.5%/6.10杀/dmg246.9。
//   ⇒ 迁移标尺：白板 pool kills **7.0**（snowball 没够到的线，一厘米不降）。
//
// ══════════════════════════════════════════════════════════════════════════
// 3. 判据（预注册；趋势看带电，判决看白板迁移 —— 防带电成绩单充数）
// ══════════════════════════════════════════════════════════════════════════
// **趋势读数 = kills/局**（带电日常）：分母 **8.76** ⇒ 里程碑 **12 → 16 → 20**。
// **判决 = 白板迁移**（标准 lives1，白板开局）：
//   · it40 迁移抽查：`--seed0 415000`（800 局 = 4 关各 200，一次性）kills **≥7.0**
//     ⇒ 过；<7.0 ⇒ **停腿**，判「带电不迁移」（§5）。
//   · 终局判决：`--seed0 414000`（800 局，一次性）＋ M2 改制（见下）。
// **门 = pass**：迁移 pass ≥30%（与 x20/snowball 同线）；趋势 pass 只记录。
// **约束**：① dmg 双轨：带电趋势分母 298.6（+30% = 388），白板迁移分母 246.9
//   （+30% = 321）—— 任一超线且 kills 不升 ⇒ 换血漂移；
//   ② 超时熔断：`timeout_frac > 5%` ⇒ 立即停（带电局更长，熔断值不变）；
//   ③ 掉队（带电分关，相对 §2 同关）：s2000 16% / s2001 26% / s2002 18% / s2003 20%，
//      任一关退步 >5pp 即掉队（kills ±0.5 内不算）。
// 判线：**达成** = 迁移 pass ≥30% 且 kills ≥14；**部分达成** = 迁移 kills 到 8+；
//   **判负** = it40 迁移抽查未过（<7.0）或超时熔断。
// **M2 改制（迁移腿特例）**：候选照常三选一（带电趋势 kills 最优 / pass 最优 /
//   末 it），但终局在 414000 白板段**三候选全跑**（2400 局，约 12 分钟），
//   取迁移 kills 高者为终点（互不可区分取 kills 高者）。理由：迁移才是目标函数，
//   用带电趋势选候选再判白板 = 用代理 metric 选型。标准 M2 在此会选错人。
// **★ 回测门**：终点候选 c07 段（seed0 410000，800 局）不退步（pass ≤5pp /
//   dmg ≤+10%，配对段；c06 按用户指令删除）。超限 ⇒ 顺延；全超限 ⇒ 旧能力丢失。
// **统计口径（写死）**：
//   · 迁移抽查段 `--seed0 415000`（800 局，一次性，seeds 415000–415799；
//     2026-09-19 起查全空）；终局段 `--seed0 414000`（800 局，一次性，
//     seeds 414000–414199；同日起查全空）。**seed 台账增补**：
//     | 414000–414999 | x20-powered 终局迁移段（预注册） |
//     | 415000–415999 | x20-powered it40 迁移抽查段（预注册，一次性） |
//     **中途不得改段**（L3）；抽查段与终局段互不复用。
//   · 配套锚点带电 200 局（seed0 860001，日常 eval 即此口径）；pd 灰度带不变。
//   · **日常 200 局只做趋势，绝不判决**。
// **对照**：**it0 vs 终点**（双轨：带电趋势轨＋白板迁移轨）。god 参照 15.0%/7.55 仅参考。
// **诊断项**：pu/千tick（**第一读数**：是否从 1.01 向 1.22 爬 —— 滚雪球行为学没学会）、
//   milestone 到达率（8/12/16 bonus 有没有被领到 —— snowball 的"够不着"治好了没）、
//   killerKinds＋exposure、命中率、timeout_frac。
//
// ══════════════════════════════════════════════════════════════════════════
// 4. 止损
// ══════════════════════════════════════════════════════════════════════════
//   · **it20 只记录读数，不判决**。
//   · **主检点 it40 = 白板迁移抽查**（415000，800 局）：kills <7.0 ⇒ 停腿，
//     判「带电不迁移」→ fallback：wPickup 剂量腿（直接给拾取行为加价）或 level 2
//     腿（带电量加倍）。到期必停，不宽限（迁移是目标函数，不行就认）。
//   · **超时熔断**：`timeout_frac > 5%` ⇒ 立即停。
//   · **换血熔断**：dmg↑30%+ 且 kills 不升（双轨任一）⇒ 改奖励结构，不加轮数。
//   · **机械横盘**（带电趋势口径）：kills 连续 4 个 eval 点无改善（|Δ|<10%）⇒
//     停腿；宽限一次 +10 轮。
//   · **分支四（自锁）**：entropy 坍缩 / KL 暴涨 ⇒ 先查 bc 载入（M3）。
//   · ⚠ **优先级：超时熔断 / it40 迁移抽查 > 横盘 > 一切诊断**。
//
// ══════════════════════════════════════════════════════════════════════════
// 5. 预算与采样规模
// ══════════════════════════════════════════════════════════════════════════
// · `target_transitions 24000/轮`（4 关均分 6000/关；与 x20/snowball 同量）。
// · `est_samples_per_game` = **200**（带电局长 2071 ticks ≈ 207 steps，取整；
//   trailing 5 轮纠偏；首轮 ≈120 局 ≈30/关）。
// · `seed_rotate` = **150**；`iters 0`（无限轮，由用户拍板，M1）＋ `max_hours 12`。
// · 单 run（L4）。`workers 8` / `ent_break 0.25` 全链一致。
// · **位移包不动**（与 x20/snowball 逐字相同）。
//
// ══════════════════════════════════════════════════════════════════════════
// 6. 已知局限 + 程序事项
// ══════════════════════════════════════════════════════════════════════════
// 1. 单变量纪律：本腿唯一训练变量 = 带电开局（level:1）；奖励/schedule/batch/
//    gamma/容量全是控制（奖励含 snowball 的死 bonus —— 留作对照，ablation 下一腿）。
// 2. 迁移风险前置声明：带电特化（只会打带电开局）是本腿第一死法；it40 抽查就是
//    为它设的。抽查过、终局挂 ⇒ 白板微调腿（frozen 主干＋白板少量轮数），另案。
// 3. snowball 账：it40 已判死（5.63<7.0）；其 it45 反弹 10.8% 是噪声带外的单点，
//    不改变判决（预注册规则：it40 即判，不追单点）。开本腿前停 snowball（占集群）。
// 4. god 不是天花板（§0.2）；带电零样本 8.76 已超教师 7.55 —— 第一里程碑从超教师起步。
// 5. 结算必填：带电趋势里程碑 ＋ 白板迁移双段（415000/414000）＋ c07 回测 ＋
//    pu 率/milestone 到达率 ＋ 横盘 ＋ M2 改制三候选全跑。

{
  "version": 5,
  "name": "x20-powered",
  "mode": "per-tick",
  "level": "ladder-c20-lives1-powered",

  // ── 奖励：snowball 逐字沿用（死 bonus 留作对照；ablation 下一腿）─────────
  "reward": {
    "formula": "wKill*kills + wHit*enemyHits - wDmg*playerDamageTaken - wShot*playerShots + wExplore*cellsVisited + wPickup*powerUpsCollected - wApproach * where(pickupDist < 0, 0, pickupDist) + wMS8*where(kills>=8, 1, 0) + wMS12*where(kills>=12, 1, 0) + wMS16*where(kills>=16, 1, 0)",
    "params": {
      "wKill": 3.0,
      "wHit": 0.6,
      "wDmg": 0.03,
      "wShot": 0.05,
      "wExplore": 0.2,
      "wPickup": 3.0,
      "wApproach": 0.008,
      "wMS8": 6.0,
      "wMS12": 9.0,
      "wMS16": 12.0
    },
    "terminal": {
    },
    "scheme": "toy"
  },
  "dodge": "",

  // ── 起点：it30（白板 pool 7.5%/6.10；带电零样本 20.0%/8.76；同一文件）───
  "bc": "nn-training/weights/x20-rebirth/x20-rebirth.it30.20260919-085629.json",
  "kickstart_ref": true,
  "warmup_iters": 0,

  // ── 预算（见 §5）────────────────────────────────────────────────────────
  "iters": 0,
  "max_hours": 12,
  "workers": 8,

  // ── 位移包：与 x20/snowball 逐字相同 ────────────────────────────────────
  "lr": 0.0005,
  "epochs": 6,
  "mb": 512,
  "normalize_ret": true,
  "gamma": 0.998,
  "lam": 0.95,

  "ppo_schedule": [
    { "until_iter": 25, "kl_coef": 0.02, "lr": 0.0005, "kl_cap": 0.2 },
    { "kl_coef": 0.01, "lr": 0.00025, "kl_cap": 0.2 }
  ],

  // ── 采集：总量 24000/轮；est 200（带电局长，trailing 纠偏；首轮 ≈120 局）──
  "target_transitions": 24000,
  "est_samples_per_game": 200,
  "seed_rotate": 150,

  // ── 路径：新目录、禁续跑（§15.5）────────────────────────────────────────
  "out": "tmp/x20-powered/weights.json",
  "traj": "tmp/x20-powered",
  "ent_break": 0.25,
  "backup_dir": "nn-training/weights/x20-powered",
  "backup_prefix": "x20-powered",

  // ── 评估 ───────────────────────────────────────────────────────────────
  // 带电日常 50/关（只做趋势）；迁移抽查 415000 / 终局 414000（白板，不可复用）。
  "eval_stages": "2000-2003",
  "eval_games_per_stage": 50,
  "eval_every": 5
}

// ── 开工前置 ─────────────────────────────────────────────────────────────
// ① **先停 x20-snowball**（控制台停止；it40 已判死，占集群会挤死本腿探针）。
// ② bc（已存在；缺失会响亮报错）。
// ③ **it0 带电基线（已跑，§2：20.0%/8.76杀）** ＋ R 口径 est（200）—— 开训即有分母。
// ④ transport：**必须显式 `--remote-transport pull`**（L6）。
// ⑤ 新 out/traj 目录（禁续跑，§15.5）。
// ⑥ 并行先查 `rl-config.json` 容量 + 登记 `slot`。
// ⑦ 确认 a95/a97 在线。
//
// 启动（**用户手工；agent 不得启动训练**）：
//   bun dashboard/src/launch/cli.ts --script run_rl.py --course x20-powered \
//     --remote-transport pull
//
// ── 诊断工具 ─────────────────────────────────────────────────────────────
// · 带电日常：`eval-course-ckpt.ts ... --course nn-training/levels/ladder-c20-lives1-powered.jsonc \
//     --games 200 --seed0 860001`（看 kills 第一，看 pu/千tick 第二）
// · it40 迁移抽查：`... --course nn-training/levels/ladder-c20-lives1.jsonc --games 800 --seed0 415000`
// · 终局迁移：`... --course nn-training/levels/ladder-c20-lives1.jsonc --games 800 --seed0 414000`（三候选全跑）
// · 回测：`... --course nn-training/levels/ladder-c07.jsonc --games 800 --seed0 410000`
// · 走势：`tmp/x1-trend.py`（改路径；带电 kills＋pu 率双轨）
//
// ── 结算节（2026-09-19 回填）──────────────────────────────────────────────
// **判决：判负（带电不迁移）** —— §4 的门（迁移 kills ≥7.0）四档全不过；且 94 轮后白板迁移
//   不升反降（it94 vs it30 配对 −0.57 杀，SE 0.27）。
//
// 白板迁移段（`--seed0 415000`，800 局 = 4 关各 200，seeds 415000–415199；同种子配对）：
//   | 候选 | pass% | kills | dmg | pu | ticks | 配对Δkills vs it30 |
//   | it30（起点）| 7.6 | **6.34** | 245.6 | 1.43 | 1816 | — |
//   | it5  | 8.6 | 6.08 | 236.4 | 1.31 | 1817 | −0.26 (SE 0.27) |
//   | it50 | 6.9 | 5.86 | 240.2 | 1.29 | 1713 | −0.48 (SE 0.27) |
//   | it94 | 8.2 | 5.77 | 242.4 | 1.33 | 1747 | **−0.57 (SE 0.27)** |
//   ⇒ 四档全 <7.0 门；里程碑 m8 25.9–31.1% / m12 13.5–16.1%（it94 双双最低）。
//   ⇒ **段交叉验证**：it30 在 415000 测得 7.6%/6.34 vs 412000 的 7.5%/6.10
//      ⇒ 两段同分布，判决段的选择无偏。
//   ⚠ 终局段 414000 **未跑**（415000 抽查已判负，按 §5「到期必停，不宽限」不再追加）；
//     c07 回测**未跑**（判负腿不做终点回测）；故 M2 改制（三候选全跑 414000）未执行。
//
// 带电趋势（训练同分布，4 关 × 50 局 × 双轨 = 400 局/点，19 个 eval 点）：
//   pass 19.9% → 19.2%、kills 8.71 → 8.63（400 局 CI ≈ ±3.9pp ⇒ **净零**）；
//   中途 it20–it35 显著退化到 9.7% / 6.70 后又回弹 ⇒ **先散后回**，有效训练量 ≈ 0。
//   里程碑（带电）：m8 45.8%→42.6%、m12 29.4%→30.9%、m16 22.9%→22.4%
//   ⇒ snowball 那个"够不着"的 bonus 在带电下**没被治好**。
//   横盘：kills 连续 4 个 eval 点 |Δ|<10% 在 it35 即满足（§5 条款可停）。
//
// 训练侧诊断：entropy **0.583 → 0.766（+31%，峰 0.846）** —— 与 a2 的成功形状
//   （−44.65%、出峰）**相反**，位移在把已收敛的 it30 推散；value 0.482→0.408、
//   score_mean 0.434→0.387（−11%）；kl 94 轮恒在 0.017–0.029（贴 kl_coef 0.02、方向随机）；
//   kickstart 的 KL 贡献 0.025→0.235（it30）后 it31 到期归零 ⇒ 单向远离 bc。无 iter halted。
//
// **第一诊断读数判负**：pu/千tick **1.01 → 1.08**（目标 1.22）⇒ 滚雪球行为学没学会；
//   firstHitKind 19 个点全均匀（4 关轮转决定，非"学会均衡"）；killerKinds power 恒首
//   （127–154/400）⇒ power 仍是第一死因；timeout 全程 0.0%（超时熔断从未接近）。
//
// 机器：94 轮 / ≈2.26M transitions。**崩 3 次、重启 4 次**（rotateSeed 未变 = 续跑），
//   根因 `rl/loop_steps.py:853` 的 `report['games']` KeyError —— resume 一条
//   「样本已采完、PPO 未跑」的迭代时 report 走 batches=0 路径无 'games' 键，重试 5 次
//   即整进程退出、控制台恢复后再踩，连撞 10 次 ⇒ **it15 整轮丢**（记录 samples=0/ticks=0）。
//   节点参与：self 2754 / mac 2002 / gcs 1060 / local 1278 / a95 237 / a97 39 / a96 11。
//
// 迁移段执行事故（三条，都影响读数可信度）：
//   ① **self（127.0.0.1:8443）agent 未运行** ⇒ ping 0.05s 秒败被排除（**非门禁误判**，
//      是连接被拒）；用户 20:30 上线后同规模 800 局墙钟 **8m9s → 2m1s**。
//   ② it94 首跑挂在 `settled=799/800`：`s2003/seed415141` 遇 `HTTP 503 busy` 被反复重排，
//      **背压重排与失败重排共用 `attempts` 计数器** ⇒ attempt 累到 **7**（> `EVAL_TASK_ATTEMPTS` 2）
//      后判「试满，本单元不再重排」⇒ **该局永久丢弃**；而收尾条件只有 `len(seen) >= total`，
//      **无缺局兜底** ⇒ 主线程空转到 deadline（用户手动停）。
//      同期 411 条 tail-race / 111 条 dup settle，其中同一节点对同一已结算任务
//      5 秒内重复竞速 **48 次**（dup 分支 `pop_inflight` 把 nid 移出 `inflight_nodes`
//      ⇒ `pick_race_target` 又把它选回来）⇒ **竞速风车**，纯烧算力。
//   ③ 误判「`--out` 不存在 = 数据全丢」而全量重跑 ⇒ 同 runDir 触发 `_reset_run_ledgers`
//      **覆盖首跑已落盘的 799 行**。正确姿势：逐局结果在 `tmp/<out>.jsonl.run/eval_log.jsonl`；
//      补单局用 `--games 4 --seed0 <seed>`（seed 分配 `seed0+floor(g/4)`、`stage=stages[g%4]`）。
//
// **结论**：带电开局（level:1）+7.5pp 的零样本因果证据成立，但它只是**抬高起点**，
//   没有造出梯度 —— bc 已是同分布最优点时，dense-only（Σr=Φ[N]−Φ[0]）+ 已收敛策略下
//   advantage ≈ 0，94 轮 × 24000 samples 没换来任何可测的行为改变。
//   **别沿"再抬起点"（level 2 / 加电量）走**；唯一没被碰过、也没被证伪的方向仍是
//   **power 处理**（firstHit power 不存在、killerKinds power 恒首）。
// 产物：`tmp/x20-powered-mig-{it30,it5,it50,it94}.jsonl` ＋ `tmp/x20-powered-mig-analysis.txt`；
//   带电趋势 `tmp/x20-powered-analysis.txt`；训练侧 `tmp/x20-powered-train-analysis*.txt`。
