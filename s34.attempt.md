# S34 Battlement 攻坚尝试日志（§192 之后 · 2026-08-13）

> 现状基线（§192 发货后）：S34 hard 20/60（33.3%），全关均值 72.86%。
> 剩余 40 败局取证（tmp/fx-s34-shipped.json）：39 base_destroyed + 1 lives_exhausted；
> killer = fast 22 / power 9 / armor 8；~15 局死时玩家近在咫尺（dist ≤ 11，常满血 315hp）
> 却挡不住——"站错列" + "墙吃弹"仍是主旋律。

## 四个待试方向（2026-08-13 记录）

### 方向 2 — 中线火力门（Center-Line Fire Gate）★ 先做
- **问题**：defenseIntercept / firingLane 系开火基于双偏线 OR 扫描（PERP_OFFSETS = ±8px），
  偏线看见敌人（enemy=true）而真实子弹中线被砖挡（wall=true）时照样开火 → 子弹砖内自爆，
  白费 800ms 冷却弹（seed 14 t2189 死因，占败局大半）。
- **做法**：加"炮口 → 目标格"真实中线 1 格级砖检查谓词，墙挡则不开火（保留冷却等下一窗口）。
- **风险**：纯火力侧、零导航交互。全 35 关通用。
- **判定**：S34 60-seed flip-scan ≥ +3 或全关净正 → 考虑发货。

### 方向 1 — 卫位导航（Station Nav）
- **问题**：哨兵只保证"站对了才开火"，不保证站对。S34 唯一能穿透口袋的射击列是
  (15,22)→(15,23)→(15,24)（(16,23)/(17,23) 有砖）；玩家被 midLaneDefense 流拖到 (16,21)
  浪费窗口。~22 fast 败局 + 近距组直接受益。
- **做法**：当 csb/canBreachRing 敌靠近环时，若玩家不在"清晰对齐格"，低权分支导航到
  最近可站位、弹道无砖的对齐格。§141 D1 静态锚 (12,23) 不可达而败——本方向锚点动态派生可达。
- **风险**：v1/v3 已证哨兵绝不能接管导航 → 独立低权分支 + 仅上位路径。
- **判定**：同上。

### 方向 3 — 横向目标前移（Predictive Lead）
- **问题**：口袋 fast 横向穿行只在 (15,24) 短暂驻留，弹飞到行 24 需 16-30 ticks，
  目标已移出 1 格 → 近距离横走目标瞄当前格常落空。
- **做法**：开火时若目标沿行进方向横向移动，瞄准其前方 1 格（预测命中）。
- **风险**：低。通用（任何横向移动目标）。
- **判定**：同上。

### 方向 4 — 环破回防警戒（Ring-Damaged Return）
- **问题**：远距组 ~10 败（死时 dist ≥ 16）：玩家中盘被拾取/追杀吸引，环破后无人回防。
- **做法**：baseWallIntact ≤ 4 且有 shooter 在 distToBase ≤ 5 时，高权导航回口袋口列
  （与方向 1 同一套站位机器，第二个触发器）。
- **风险**：中（导航接管须谨慎）。
- **判定**：同上。

## 尝试记录

（每方向：实现要点 → S34 60-seed flip-scan → 全关 35×60 → 结果 → 发货/阴性归档）

### 方向 2 — 中线火力门（2026-08-13 已回退，阴性）
- 实现：`centerLineFireGate` 旋钮（params §193-A）——双偏线扫描同时报 enemy+wall 时，用真实 6px 弹道中线做炮口→目标格砖检查，墙挡则不开火。
- S34 60-seed：full 版全局 +20 (122/102) 但 S34 -4（中线门 520-650 权干掉了 S34 的高权 firingLane 击杀，7 flips 全在 waist/ring 区）；迷你版（仅 counter-fire）S34 干净但全局 +8 不达"均净胜"。
- 结论：**回退**（默认 0 = OFF，代码保留为实验旋钮）。详见 §193 DECISIONS。

### 方向 1 — 卫位导航 Station Nav（2026-08-13 达标 ✅）
- 实现：`baseLaneSentryStation` 旋钮（params §193-B，默认 0 = OFF 字节不变）。哨兵对准 csb/cbr 目标且玩家非对齐/被挡时，低权分支走向目标列 ±1 的清晰站台列（垂直廊道无砖、站台格可站），到位后由对齐开火接管。
- 迭代轨迹（S34 hard 60-seed flip）：
  - v1（追踪敌列 dc±1）：净 -3 → 弃
  - v2（固定站台列 base±6 扫描）：净 -1 → 弃
  - v3（固定列 + 玩家列清晰即跳过）：S34 归零、全局 +5 → 误杀 17/42
  - v4（目标列±1 就近 + 四门槛）：**S34 净 +1 (21/60)，全局净 +6 (11/5)，classic 0，chaos +2** ✅
- 四门槛（由 25/32/47/51/53/56 六败局取证得出）：
  1. 敌人已入带（row ≥ 23）→ 禁止站台（须下行堵口，否则基地被打爆）
  2. 玩家须已在带内（row ≥ 21）→ 带外下行回防不得被拽横移
  3. 玩家列与目标列差 ≤ 1 → 已在拦截列，守株待兔
  4. 目标横移（left/right）且与玩家同行 → 目标将横穿本行，不换列
- 最后修复 laneCorridorBlocked 退化情形：r===tr 同格时循环 `rr !== tr` 永不命中 → 无限下走越界崩溃（g[-n] undefined）——加 `if (r === tr) return 0`。

### 方向 3 — 横向目标前移（2026-08-13 达标发货 ✅ §193-D）
- 实现：`predictiveFireGate` 旋钮（默认 1 发货；classic restore 0）。enemy 分支加时间窗门：目标以垂直方向横穿弹道线、且子弹到达（enemyDist×CELL/bulletSpeed ticks）时身体已滑出 ±(TANK+BULLET)/2 命中窗 → 抑制必 miss 开火，时机窗口交给 P2.4 predictEnemyCrossing（管「将上线」，本门管「在线但将滑出」，互补）。
- 60-seed A/B（ab-predict 全关）：hard 净 **+7**（16/9，最差 S10 -2 无崩盘关）；classic +1（4/3）；chaos +1（12/11，**S34 +1**）。三难度净正 → 判定「全关净正 → 发货」达成。
- 取证：S34 触发面小（60 runs 仅 6 blocks）——fast 1.2px/tick 太慢，2-3 格内子弹先到；门主要命中 waist/ring 区远距横走目标（全关 604 blocks/60 runs）。
- gate 重测：hard 528/700（75.4%，较 §193-C 又 +0.5pp）、chaos 494/700、classic 629/700 全绿。

### 方向 4 — 环破回防（2026-08-13 阴性归档 ❌，§193-E）
- 实现：哨兵第二触发器——基地危局（csb/cbr/inBand）成立 + 玩家 dist > 16 时，850 权重导航回防。两轮修复：① 初始目标 `getDefaultDefensePositionImpl`（(12,23) 环砖格不可达）→ navigateTowards 恒 null、分支每 tick 空转刷共享 nav 缓存/重算计时器 → **幻影翻局**（claims=0 仍改结果，seed 11 首分歧 t3582）；② 换 `computeBaseGuardAnchorImpl`（§137 守位格，可站可达，input 侧缓存）后分支真实生效。
- S34 60-seed：full 版净 **-2**（4 to-win：30/38/42/54；6 to-lose：6/11/16/32/44/50）。机制：远距玩家靠场内压制赢的局被跨图拉回拖垮（seed 11 拉回 27s 未到基地已丢；seed 16 到家仍 overwhelm；seed 44 半路丢）。to-win/to-lose 拉回耗时无干净分离（30~836 claims 两组重叠）。
- csb-only 变体（仅"下一发毁基地"拉人）：S34 净 -3（0 to-win——to-win 恰依赖非 csb 拉回）。
- 全关 hard：60-seed 净 **-54**（35 关仅 2 关 +2、20+ 关负）——「S34 ≥ +3 或全关净正」两条均不达标 → **归档，实验痕迹全量回退**（git 树字节不变）。若重试需换触发门（队列 < N / HP 阈值），而非纯距离门。

## 四方向收束结论
- 方向 2（中线火力门）→ **发货 §193-C**（hard 全关 +41、S34 +2；classic restore 0）
- 方向 3（预测前移门）→ **发货 §193-D**（hard +7、classic +1、chaos +1）
- 方向 1（卫位导航）→ **达标待发货 §193-B**（S34 +1、全关 +6，默认 0 = OFF 需用户确认）
- 方向 4（环破回防）→ **阴性归档 §193-E**（S34 -2、全关 -54）
- S34 现状：§193-C/D 发货后 hard 19/60 → gate 复测 75.4%（floor 484，全关线）——S34 单关 19/60 仍是硬伤，剩余 41 败局取证待下一轮（近距组 dist ≤ 11 满血挡不住 → "站错列/墙吃弹"主旋律未变）。
